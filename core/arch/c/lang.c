#include "luaot-generated-header.c"

// source = @../lua/lang.lua
// linedefined = 0
// lastlinedefined = 0
// what = main
static int zz_magic_function_0 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [22]	CLOSURE  	0 0	; 0x559f63792f20
  label_0: {
    Instruction i = 0x0000002c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [23]	NEWTABLE 	1 0 0
  label_1: {
    Instruction i = 0x0000004b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [24]	LOADNIL  	2 94
  label_2: {
    Instruction i = 0x2f000084;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [27]	CLOSURE  	97 1	; 0x559f63794160
  label_3: {
    Instruction i = 0x0000586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [27]	MOVE     	2 97
  label_4: {
    Instruction i = 0x30800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [32]	CLOSURE  	97 2	; 0x559f63794330
  label_5: {
    Instruction i = 0x0000986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [32]	MOVE     	3 97
  label_6: {
    Instruction i = 0x308000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [35]	CLOSURE  	97 3	; 0x559f637944f0
  label_7: {
    Instruction i = 0x0000d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [35]	MOVE     	13 97
  label_8: {
    Instruction i = 0x30800340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [38]	CLOSURE  	97 4	; 0x559f63794710
  label_9: {
    Instruction i = 0x0001186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [38]	MOVE     	14 97
  label_10: {
    Instruction i = 0x30800380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [41]	CLOSURE  	97 5	; 0x559f63796a00
  label_11: {
    Instruction i = 0x0001586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [41]	MOVE     	15 97
  label_12: {
    Instruction i = 0x308003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [44]	CLOSURE  	97 6	; 0x559f63796b30
  label_13: {
    Instruction i = 0x0001986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [44]	MOVE     	16 97
  label_14: {
    Instruction i = 0x30800400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [47]	CLOSURE  	97 7	; 0x559f63796cb0
  label_15: {
    Instruction i = 0x0001d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [47]	MOVE     	17 97
  label_16: {
    Instruction i = 0x30800440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [50]	CLOSURE  	97 8	; 0x559f63796f90
  label_17: {
    Instruction i = 0x0002186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [50]	MOVE     	18 97
  label_18: {
    Instruction i = 0x30800480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [53]	CLOSURE  	97 9	; 0x559f63797020
  label_19: {
    Instruction i = 0x0002586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [53]	MOVE     	20 97
  label_20: {
    Instruction i = 0x30800500;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [56]	CLOSURE  	97 10	; 0x559f63797190
  label_21: {
    Instruction i = 0x0002986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [56]	MOVE     	21 97
  label_22: {
    Instruction i = 0x30800540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [59]	CLOSURE  	97 11	; 0x559f637972c0
  label_23: {
    Instruction i = 0x0002d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [59]	MOVE     	22 97
  label_24: {
    Instruction i = 0x30800580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [62]	CLOSURE  	97 12	; 0x559f63797440
  label_25: {
    Instruction i = 0x0003186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [62]	MOVE     	23 97
  label_26: {
    Instruction i = 0x308005c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [65]	CLOSURE  	97 13	; 0x559f63797570
  label_27: {
    Instruction i = 0x0003586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [65]	MOVE     	24 97
  label_28: {
    Instruction i = 0x30800600;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [68]	CLOSURE  	97 14	; 0x559f637976c0
  label_29: {
    Instruction i = 0x0003986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [68]	MOVE     	25 97
  label_30: {
    Instruction i = 0x30800640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [71]	CLOSURE  	97 15	; 0x559f63797830
  label_31: {
    Instruction i = 0x0003d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [71]	MOVE     	26 97
  label_32: {
    Instruction i = 0x30800680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [74]	CLOSURE  	97 16	; 0x559f63796f00
  label_33: {
    Instruction i = 0x0004186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [74]	MOVE     	27 97
  label_34: {
    Instruction i = 0x308006c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [77]	CLOSURE  	97 17	; 0x559f63797c70
  label_35: {
    Instruction i = 0x0004586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [77]	MOVE     	28 97
  label_36: {
    Instruction i = 0x30800700;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [86]	CLOSURE  	97 18	; 0x559f63797dc0
  label_37: {
    Instruction i = 0x0004986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [86]	MOVE     	29 97
  label_38: {
    Instruction i = 0x30800740;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [89]	CLOSURE  	97 19	; 0x559f63798060
  label_39: {
    Instruction i = 0x0004d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [89]	MOVE     	30 97
  label_40: {
    Instruction i = 0x30800780;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [92]	CLOSURE  	97 20	; 0x559f637981e0
  label_41: {
    Instruction i = 0x0005186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [92]	MOVE     	31 97
  label_42: {
    Instruction i = 0x308007c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [95]	CLOSURE  	97 21	; 0x559f63798310
  label_43: {
    Instruction i = 0x0005586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [95]	MOVE     	32 97
  label_44: {
    Instruction i = 0x30800800;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [98]	CLOSURE  	97 22	; 0x559f63798440
  label_45: {
    Instruction i = 0x0005986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [98]	MOVE     	33 97
  label_46: {
    Instruction i = 0x30800840;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [101]	CLOSURE  	97 23	; 0x559f637985c0
  label_47: {
    Instruction i = 0x0005d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [101]	MOVE     	34 97
  label_48: {
    Instruction i = 0x30800880;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [104]	CLOSURE  	97 24	; 0x559f637986f0
  label_49: {
    Instruction i = 0x0006186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [104]	MOVE     	35 97
  label_50: {
    Instruction i = 0x308008c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [107]	CLOSURE  	97 25	; 0x559f63798820
  label_51: {
    Instruction i = 0x0006586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [107]	MOVE     	36 97
  label_52: {
    Instruction i = 0x30800900;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [110]	CLOSURE  	97 26	; 0x559f63798990
  label_53: {
    Instruction i = 0x0006986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [110]	MOVE     	37 97
  label_54: {
    Instruction i = 0x30800940;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [113]	CLOSURE  	97 27	; 0x559f63798b10
  label_55: {
    Instruction i = 0x0006d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [113]	MOVE     	38 97
  label_56: {
    Instruction i = 0x30800980;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [116]	CLOSURE  	97 28	; 0x559f63798c40
  label_57: {
    Instruction i = 0x0007186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [116]	MOVE     	39 97
  label_58: {
    Instruction i = 0x308009c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [119]	CLOSURE  	97 29	; 0x559f63798db0
  label_59: {
    Instruction i = 0x0007586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [119]	MOVE     	40 97
  label_60: {
    Instruction i = 0x30800a00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [122]	CLOSURE  	97 30	; 0x559f63798f20
  label_61: {
    Instruction i = 0x0007986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [122]	MOVE     	41 97
  label_62: {
    Instruction i = 0x30800a40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [125]	CLOSURE  	97 31	; 0x559f63799090
  label_63: {
    Instruction i = 0x0007d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [125]	MOVE     	42 97
  label_64: {
    Instruction i = 0x30800a80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [128]	CLOSURE  	97 32	; 0x559f63796de0
  label_65: {
    Instruction i = 0x0008186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [128]	MOVE     	43 97
  label_66: {
    Instruction i = 0x30800ac0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [131]	CLOSURE  	97 33	; 0x559f63799680
  label_67: {
    Instruction i = 0x0008586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [131]	MOVE     	44 97
  label_68: {
    Instruction i = 0x30800b00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [134]	CLOSURE  	97 34	; 0x559f637997b0
  label_69: {
    Instruction i = 0x0008986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [134]	MOVE     	45 97
  label_70: {
    Instruction i = 0x30800b40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [137]	CLOSURE  	97 35	; 0x559f63799900
  label_71: {
    Instruction i = 0x0008d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [137]	MOVE     	46 97
  label_72: {
    Instruction i = 0x30800b80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [140]	CLOSURE  	97 36	; 0x559f63799a80
  label_73: {
    Instruction i = 0x0009186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [140]	MOVE     	47 97
  label_74: {
    Instruction i = 0x30800bc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [143]	CLOSURE  	97 37	; 0x559f63799bb0
  label_75: {
    Instruction i = 0x0009586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [143]	MOVE     	48 97
  label_76: {
    Instruction i = 0x30800c00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [166]	CLOSURE  	97 38	; 0x559f63799d00
  label_77: {
    Instruction i = 0x0009986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [166]	MOVE     	49 97
  label_78: {
    Instruction i = 0x30800c40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [177]	CLOSURE  	97 39	; 0x559f6379a000
  label_79: {
    Instruction i = 0x0009d86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [177]	MOVE     	75 97
  label_80: {
    Instruction i = 0x308012c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [188]	CLOSURE  	97 40	; 0x559f6379a490
  label_81: {
    Instruction i = 0x000a186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [188]	MOVE     	76 97
  label_82: {
    Instruction i = 0x30801300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [192]	CLOSURE  	97 41	; 0x559f63794a90
  label_83: {
    Instruction i = 0x000a586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [192]	MOVE     	77 97
  label_84: {
    Instruction i = 0x30801340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [208]	CLOSURE  	97 42	; 0x559f63794bc0
  label_85: {
    Instruction i = 0x000a986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [208]	MOVE     	78 97
  label_86: {
    Instruction i = 0x30801380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [211]	CLOSURE  	97 43	; 0x559f63794f00
  label_87: {
    Instruction i = 0x000ad86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [211]	MOVE     	79 97
  label_88: {
    Instruction i = 0x308013c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [338]	CLOSURE  	97 44	; 0x559f637951e0
  label_89: {
    Instruction i = 0x000b186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [338]	MOVE     	80 97
  label_90: {
    Instruction i = 0x30801400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [357]	CLOSURE  	97 45	; 0x559f637956d0
  label_91: {
    Instruction i = 0x000b586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [357]	MOVE     	81 97
  label_92: {
    Instruction i = 0x30801440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [385]	CLOSURE  	97 46	; 0x559f63795760
  label_93: {
    Instruction i = 0x000b986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [385]	MOVE     	83 97
  label_94: {
    Instruction i = 0x308014c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [397]	CLOSURE  	97 47	; 0x559f6379c8f0
  label_95: {
    Instruction i = 0x000bd86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [397]	MOVE     	84 97
  label_96: {
    Instruction i = 0x30801500;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [409]	CLOSURE  	97 48	; 0x559f6379dfa0
  label_97: {
    Instruction i = 0x000c186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [409]	MOVE     	85 97
  label_98: {
    Instruction i = 0x30801540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [420]	CLOSURE  	97 49	; 0x559f6379e240
  label_99: {
    Instruction i = 0x000c586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [420]	MOVE     	86 97
  label_100: {
    Instruction i = 0x30801580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [429]	CLOSURE  	97 50	; 0x559f6379e570
  label_101: {
    Instruction i = 0x000c986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [429]	MOVE     	87 97
  label_102: {
    Instruction i = 0x308015c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [539]	CLOSURE  	97 51	; 0x559f6379e650
  label_103: {
    Instruction i = 0x000cd86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [539]	MOVE     	88 97
  label_104: {
    Instruction i = 0x30801600;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [542]	CLOSURE  	97 52	; 0x559f6379e730
  label_105: {
    Instruction i = 0x000d186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [542]	MOVE     	89 97
  label_106: {
    Instruction i = 0x30801640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [601]	CLOSURE  	97 53	; 0x559f6379fe50
  label_107: {
    Instruction i = 0x000d586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [601]	MOVE     	91 97
  label_108: {
    Instruction i = 0x308016c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [626]	CLOSURE  	97 54	; 0x559f6379eb00
  label_109: {
    Instruction i = 0x000d986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [626]	MOVE     	92 97
  label_110: {
    Instruction i = 0x30801700;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [641]	CLOSURE  	97 55	; 0x559f637a0950
  label_111: {
    Instruction i = 0x000dd86c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [641]	MOVE     	93 97
  label_112: {
    Instruction i = 0x30801740;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [708]	CLOSURE  	97 56	; 0x559f6379f480
  label_113: {
    Instruction i = 0x000e186c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [708]	MOVE     	94 97
  label_114: {
    Instruction i = 0x30801780;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [756]	CLOSURE  	97 57	; 0x559f637a1660
  label_115: {
    Instruction i = 0x000e586c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [756]	MOVE     	95 97
  label_116: {
    Instruction i = 0x308017c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [793]	CLOSURE  	97 58	; 0x559f637a1b80
  label_117: {
    Instruction i = 0x000e986c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [793]	MOVE     	96 97
  label_118: {
    Instruction i = 0x30801800;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [794]	LOADK    	97 -1	; 0
  label_119: {
    Instruction i = 0x00001841;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [795]	LOADK    	4 -2	; 1
  label_120: {
    Instruction i = 0x00004101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [796]	LOADK    	5 -3	; 2
  label_121: {
    Instruction i = 0x00008141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [797]	LOADK    	6 -4	; 3
  label_122: {
    Instruction i = 0x0000c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [798]	LOADK    	7 -5	; 4
  label_123: {
    Instruction i = 0x000101c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [799]	LOADK    	8 -6	; 5
  label_124: {
    Instruction i = 0x00014201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [800]	LOADK    	9 -7	; 6
  label_125: {
    Instruction i = 0x00018241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [801]	LOADK    	10 -8	; 7
  label_126: {
    Instruction i = 0x0001c281;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [802]	LOADK    	11 -9	; 8
  label_127: {
    Instruction i = 0x000202c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [803]	LOADK    	12 -10	; 9
  label_128: {
    Instruction i = 0x00024301;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [804]	LOADNIL  	98 0
  label_129: {
    Instruction i = 0x00001884;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [807]	CLOSURE  	99 59	; 0x559f637a12d0
  label_130: {
    Instruction i = 0x000ed8ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [807]	MOVE     	98 99
  label_131: {
    Instruction i = 0x31801880;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [808]	SETTABLE 	1 -11 98	; "new_symbol" -
  label_132: {
    Instruction i = 0x8518804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [809]	SETTABLE 	1 -12 13	; "symbol_p" -
  label_133: {
    Instruction i = 0x8583404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [810]	SETTABLE 	1 -13 14	; "un_symbol" -
  label_134: {
    Instruction i = 0x8603804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [811]	SETTABLE 	1 -14 15	; "new_construction" -
  label_135: {
    Instruction i = 0x8683c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [812]	SETTABLE 	1 -15 16	; "construction_p" -
  label_136: {
    Instruction i = 0x8704004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [813]	SETTABLE 	1 -16 17	; "construction_head" -
  label_137: {
    Instruction i = 0x8784404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [814]	SETTABLE 	1 -17 18	; "construction_tail" -
  label_138: {
    Instruction i = 0x8804804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [815]	NEWTABLE 	99 1 0
  label_139: {
    Instruction i = 0x008018cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [815]	MOVE     	100 5
  label_140: {
    Instruction i = 0x02801900;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [815]	SETLIST  	99 1 1	; 1
  label_141: {
    Instruction i = 0x008058eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [815]	MOVE     	19 99
  label_142: {
    Instruction i = 0x318004c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [816]	SETTABLE 	1 -18 19	; "null_v" -
  label_143: {
    Instruction i = 0x8884c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [817]	SETTABLE 	1 -19 20	; "null_p" -
  label_144: {
    Instruction i = 0x8905004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [818]	SETTABLE 	1 -20 21	; "new_data" -
  label_145: {
    Instruction i = 0x8985404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [819]	SETTABLE 	1 -21 22	; "data_p" -
  label_146: {
    Instruction i = 0x8a05804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [820]	SETTABLE 	1 -22 23	; "data_name" -
  label_147: {
    Instruction i = 0x8a85c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [821]	SETTABLE 	1 -23 24	; "data_list" -
  label_148: {
    Instruction i = 0x8b06004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [822]	SETTABLE 	1 -24 25	; "new_error" -
  label_149: {
    Instruction i = 0x8b86404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [823]	SETTABLE 	1 -25 26	; "error_p" -
  label_150: {
    Instruction i = 0x8c06804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [824]	SETTABLE 	1 -26 27	; "error_name" -
  label_151: {
    Instruction i = 0x8c86c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [825]	SETTABLE 	1 -27 28	; "error_list" -
  label_152: {
    Instruction i = 0x8d07004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [826]	SETTABLE 	1 -28 32	; "evaluate" -
  label_153: {
    Instruction i = 0x8d88004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [827]	SETTABLE 	1 -29 45	; "apply" -
  label_154: {
    Instruction i = 0x8e0b404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [828]	SETTABLE 	1 -30 49	; "force_all_rec" -
  label_155: {
    Instruction i = 0x8e8c404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [829]	MOVE     	99 98
  label_156: {
    Instruction i = 0x310018c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [829]	LOADK    	100 -31	; "\229\164\170\229\167\139\229\136\157\230\160\184"
  label_157: {
    Instruction i = 0x00079901;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [829]	CALL     	99 2 2
  label_158: {
    Instruction i = 0x010098e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [829]	MOVE     	50 99
  label_159: {
    Instruction i = 0x31800c80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [830]	MOVE     	99 98
  label_160: {
    Instruction i = 0x310018c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [830]	LOADK    	100 -32	; "\231\172\166\229\144\141"
  label_161: {
    Instruction i = 0x0007d901;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [830]	CALL     	99 2 2
  label_162: {
    Instruction i = 0x010098e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [831]	MOVE     	100 98
  label_163: {
    Instruction i = 0x31001900;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [831]	LOADK    	101 -33	; "\229\140\150\230\187\133"
  label_164: {
    Instruction i = 0x00081941;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [831]	CALL     	100 2 2
  label_165: {
    Instruction i = 0x01009924;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [831]	MOVE     	51 100
  label_166: {
    Instruction i = 0x32000cc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [832]	MOVE     	100 98
  label_167: {
    Instruction i = 0x31001900;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [832]	LOADK    	101 -34	; "\229\188\143\229\189\162"
  label_168: {
    Instruction i = 0x00085941;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [832]	CALL     	100 2 2
  label_169: {
    Instruction i = 0x01009924;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [832]	MOVE     	52 100
  label_170: {
    Instruction i = 0x32000d00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [833]	MOVE     	100 98
  label_171: {
    Instruction i = 0x31001900;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [833]	LOADK    	101 -35	; "\231\173\137\229\144\140"
  label_172: {
    Instruction i = 0x00089941;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [833]	CALL     	100 2 2
  label_173: {
    Instruction i = 0x01009924;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [834]	MOVE     	101 98
  label_174: {
    Instruction i = 0x31001940;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [834]	LOADK    	102 -36	; "\232\167\163\231\174\151"
  label_175: {
    Instruction i = 0x0008d981;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [834]	CALL     	101 2 2
  label_176: {
    Instruction i = 0x01009964;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [835]	MOVE     	102 98
  label_177: {
    Instruction i = 0x31001980;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [835]	LOADK    	103 -37	; "\231\137\185\229\174\154\229\133\182\231\137\169"
  label_178: {
    Instruction i = 0x000919c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [835]	CALL     	102 2 2
  label_179: {
    Instruction i = 0x010099a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [836]	MOVE     	103 98
  label_180: {
    Instruction i = 0x310019c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [836]	LOADK    	104 -38	; "\231\156\129\231\149\165\228\184\128\231\137\169"
  label_181: {
    Instruction i = 0x00095a01;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [836]	CALL     	103 2 2
  label_182: {
    Instruction i = 0x010099e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [837]	MOVE     	104 98
  label_183: {
    Instruction i = 0x31001a00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [837]	LOADK    	105 -39	; "\230\152\160\232\161\168"
  label_184: {
    Instruction i = 0x00099a41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [837]	CALL     	104 2 2
  label_185: {
    Instruction i = 0x01009a24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [837]	MOVE     	53 104
  label_186: {
    Instruction i = 0x34000d40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [838]	MOVE     	104 98
  label_187: {
    Instruction i = 0x31001a00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [838]	LOADK    	105 -40	; "\232\139\165"
  label_188: {
    Instruction i = 0x0009da41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [838]	CALL     	104 2 2
  label_189: {
    Instruction i = 0x01009a24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [839]	MOVE     	105 98
  label_190: {
    Instruction i = 0x31001a40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [839]	LOADK    	106 -41	; "\228\184\128\233\161\158\228\189\149\231\137\169"
  label_191: {
    Instruction i = 0x000a1a81;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [839]	CALL     	105 2 2
  label_192: {
    Instruction i = 0x01009a64;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [840]	MOVE     	106 98
  label_193: {
    Instruction i = 0x31001a80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [840]	LOADK    	107 -42	; "\230\152\175\233\157\158"
  label_194: {
    Instruction i = 0x000a5ac1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [840]	CALL     	106 2 2
  label_195: {
    Instruction i = 0x01009aa4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [841]	MOVE     	107 98
  label_196: {
    Instruction i = 0x31001ac0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [841]	LOADK    	108 -43	; "\229\133\182\229\173\144"
  label_197: {
    Instruction i = 0x000a9b01;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [841]	CALL     	107 2 2
  label_198: {
    Instruction i = 0x01009ae4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [842]	MOVE     	108 98
  label_199: {
    Instruction i = 0x31001b00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [842]	LOADK    	109 -44	; "\233\153\189"
  label_200: {
    Instruction i = 0x000adb41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [842]	CALL     	108 2 2
  label_201: {
    Instruction i = 0x01009b24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [843]	MOVE     	109 98
  label_202: {
    Instruction i = 0x31001b40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [843]	LOADK    	110 -45	; "\233\153\176"
  label_203: {
    Instruction i = 0x000b1b81;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [843]	CALL     	109 2 2
  label_204: {
    Instruction i = 0x01009b64;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [844]	MOVE     	110 98
  label_205: {
    Instruction i = 0x31001b80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [844]	LOADK    	111 -46	; "\229\188\149\231\148\168"
  label_206: {
    Instruction i = 0x000b5bc1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [844]	CALL     	110 2 2
  label_207: {
    Instruction i = 0x01009ba4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [845]	MOVE     	111 98
  label_208: {
    Instruction i = 0x31001bc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [845]	LOADK    	112 -47	; "\230\135\137\231\148\168"
  label_209: {
    Instruction i = 0x000b9c01;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [845]	CALL     	111 2 2
  label_210: {
    Instruction i = 0x01009be4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [846]	MOVE     	112 98
  label_211: {
    Instruction i = 0x31001c00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [846]	LOADK    	113 -48	; "\231\169\186"
  label_212: {
    Instruction i = 0x000bdc41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [846]	CALL     	112 2 2
  label_213: {
    Instruction i = 0x01009c24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [847]	MOVE     	113 98
  label_214: {
    Instruction i = 0x31001c40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [847]	LOADK    	114 -49	; "\233\128\163"
  label_215: {
    Instruction i = 0x000c1c81;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [847]	CALL     	113 2 2
  label_216: {
    Instruction i = 0x01009c64;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [848]	MOVE     	114 98
  label_217: {
    Instruction i = 0x31001c80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [848]	LOADK    	115 -50	; "\230\167\139"
  label_218: {
    Instruction i = 0x000c5cc1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [848]	CALL     	114 2 2
  label_219: {
    Instruction i = 0x01009ca4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [849]	MOVE     	115 98
  label_220: {
    Instruction i = 0x31001cc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [849]	LOADK    	116 -51	; "\232\170\164"
  label_221: {
    Instruction i = 0x000c9d01;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [849]	CALL     	115 2 2
  label_222: {
    Instruction i = 0x01009ce4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [850]	MOVE     	116 98
  label_223: {
    Instruction i = 0x31001d00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [850]	LOADK    	117 -52	; "\232\169\158\231\180\160"
  label_224: {
    Instruction i = 0x000cdd41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [850]	CALL     	116 2 2
  label_225: {
    Instruction i = 0x01009d24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [851]	MOVE     	117 98
  label_226: {
    Instruction i = 0x31001d40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [851]	LOADK    	118 -53	; "\229\136\151"
  label_227: {
    Instruction i = 0x000d1d81;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [851]	CALL     	117 2 2
  label_228: {
    Instruction i = 0x01009d64;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [852]	MOVE     	118 98
  label_229: {
    Instruction i = 0x31001d80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [852]	LOADK    	119 -54	; "\233\166\150"
  label_230: {
    Instruction i = 0x000d5dc1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [852]	CALL     	118 2 2
  label_231: {
    Instruction i = 0x01009da4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [853]	MOVE     	119 98
  label_232: {
    Instruction i = 0x31001dc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [853]	LOADK    	120 -55	; "\229\176\190"
  label_233: {
    Instruction i = 0x000d9e01;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [853]	CALL     	119 2 2
  label_234: {
    Instruction i = 0x01009de4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [854]	MOVE     	120 98
  label_235: {
    Instruction i = 0x31001e00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [854]	LOADK    	121 -56	; "\231\137\169"
  label_236: {
    Instruction i = 0x000dde41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [854]	CALL     	120 2 2
  label_237: {
    Instruction i = 0x01009e24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [855]	MOVE     	121 98
  label_238: {
    Instruction i = 0x31001e40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [855]	LOADK    	122 -57	; "\229\174\135\229\174\153\228\186\161\231\159\163"
  label_239: {
    Instruction i = 0x000e1e81;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [855]	CALL     	121 2 2
  label_240: {
    Instruction i = 0x01009e64;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [856]	MOVE     	122 98
  label_241: {
    Instruction i = 0x31001e80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [856]	LOADK    	123 -58	; "\230\149\136\230\135\137"
  label_242: {
    Instruction i = 0x000e5ec1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [856]	CALL     	122 2 2
  label_243: {
    Instruction i = 0x01009ea4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [857]	MOVE     	123 98
  label_244: {
    Instruction i = 0x31001ec0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [857]	LOADK    	124 -59	; "\231\130\186\231\172\166\229\144\141\233\128\163"
  label_245: {
    Instruction i = 0x000e9f01;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [857]	CALL     	123 2 2
  label_246: {
    Instruction i = 0x01009ee4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [858]	MOVE     	124 98
  label_247: {
    Instruction i = 0x31001f00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [858]	LOADK    	125 -60	; "\229\135\186\229\133\165\230\148\185\230\187\133"
  label_248: {
    Instruction i = 0x000edf41;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [858]	CALL     	124 2 2
  label_249: {
    Instruction i = 0x01009f24;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [859]	SETTABLE 	1 -61 50	; "system_symbol" -
  label_250: {
    Instruction i = 0x9e0c804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [860]	SETTABLE 	1 -62 99	; "name_symbol" -
  label_251: {
    Instruction i = 0x9e98c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [861]	SETTABLE 	1 -63 51	; "function_symbol" -
  label_252: {
    Instruction i = 0x9f0cc04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [862]	SETTABLE 	1 -64 52	; "form_symbol" -
  label_253: {
    Instruction i = 0x9f8d004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [863]	SETTABLE 	1 -65 100	; "equal_symbol" -
  label_254: {
    Instruction i = 0xa019004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [864]	SETTABLE 	1 -66 101	; "evaluate_sym" -
  label_255: {
    Instruction i = 0xa099404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [865]	SETTABLE 	1 -67 102	; "theThing_symbol" -
  label_256: {
    Instruction i = 0xa119804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [866]	SETTABLE 	1 -68 103	; "something_symbol" -
  label_257: {
    Instruction i = 0xa199c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [867]	SETTABLE 	1 -69 53	; "mapping_symbol" -
  label_258: {
    Instruction i = 0xa20d404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [868]	SETTABLE 	1 -70 104	; "if_symbol" -
  label_259: {
    Instruction i = 0xa29a004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [869]	SETTABLE 	1 -71 105	; "typeAnnotation_symbol" -
  label_260: {
    Instruction i = 0xa31a404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [870]	SETTABLE 	1 -72 106	; "isOrNot_symbol" -
  label_261: {
    Instruction i = 0xa39a804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [871]	SETTABLE 	1 -73 107	; "sub_symbol" -
  label_262: {
    Instruction i = 0xa41ac04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [872]	SETTABLE 	1 -74 108	; "true_symbol" -
  label_263: {
    Instruction i = 0xa49b004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [873]	SETTABLE 	1 -75 109	; "false_symbol" -
  label_264: {
    Instruction i = 0xa51b404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [874]	SETTABLE 	1 -76 110	; "quote_symbol" -
  label_265: {
    Instruction i = 0xa59b804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [875]	SETTABLE 	1 -77 111	; "apply_symbol" -
  label_266: {
    Instruction i = 0xa61bc04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [876]	SETTABLE 	1 -78 112	; "null_symbol" -
  label_267: {
    Instruction i = 0xa69c004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [877]	SETTABLE 	1 -79 113	; "construction_symbol" -
  label_268: {
    Instruction i = 0xa71c404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [878]	SETTABLE 	1 -80 114	; "data_symbol" -
  label_269: {
    Instruction i = 0xa79c804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [879]	SETTABLE 	1 -81 115	; "error_symbol" -
  label_270: {
    Instruction i = 0xa81cc04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [880]	SETTABLE 	1 -82 116	; "symbol_symbol" -
  label_271: {
    Instruction i = 0xa89d004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [881]	SETTABLE 	1 -83 117	; "list_symbol" -
  label_272: {
    Instruction i = 0xa91d404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [882]	SETTABLE 	1 -84 118	; "head_symbol" -
  label_273: {
    Instruction i = 0xa99d804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [883]	SETTABLE 	1 -85 119	; "tail_symbol" -
  label_274: {
    Instruction i = 0xaa1dc04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [884]	SETTABLE 	1 -86 120	; "thing_symbol" -
  label_275: {
    Instruction i = 0xaa9e004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [885]	SETTABLE 	1 -87 121	; "theWorldStopped_symbol" -
  label_276: {
    Instruction i = 0xab1e404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [886]	SETTABLE 	1 -88 122	; "effect_symbol" -
  label_277: {
    Instruction i = 0xab9e804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [887]	SETTABLE 	1 -89 123	; "sequentialWordFormation_symbol" -
  label_278: {
    Instruction i = 0xac1ec04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [888]	SETTABLE 	1 -90 124	; "inputOutput_symbol" -
  label_279: {
    Instruction i = 0xac9f004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [889]	MOVE     	125 25
  label_280: {
    Instruction i = 0x0c801f40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [889]	MOVE     	126 50
  label_281: {
    Instruction i = 0x19001f80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [889]	MOVE     	127 77
  label_282: {
    Instruction i = 0x26801fc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [889]	MOVE     	128 121
  label_283: {
    Instruction i = 0x3c802000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [889]	MOVE     	129 103
  label_284: {
    Instruction i = 0x33802040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [889]	CALL     	127 3 0
  label_285: {
    Instruction i = 0x01801fe4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [889]	CALL     	125 0 2
  label_286: {
    Instruction i = 0x00009f64;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [889]	MOVE     	54 125
  label_287: {
    Instruction i = 0x3e800d80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [890]	LOADNIL  	125 0
  label_288: {
    Instruction i = 0x00001f44;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [893]	CLOSURE  	126 60	; 0x559f637a2660
  label_289: {
    Instruction i = 0x000f1fac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [893]	MOVE     	125 126
  label_290: {
    Instruction i = 0x3f001f40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [894]	LOADNIL  	126 0
  label_291: {
    Instruction i = 0x00001f84;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [897]	CLOSURE  	127 61	; 0x559f637a28d0
  label_292: {
    Instruction i = 0x000f5fec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [897]	MOVE     	126 127
  label_293: {
    Instruction i = 0x3f801f80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [898]	LOADNIL  	127 0
  label_294: {
    Instruction i = 0x00001fc4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [901]	CLOSURE  	128 62	; 0x559f63796560
  label_295: {
    Instruction i = 0x000fa02c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [901]	MOVE     	127 128
  label_296: {
    Instruction i = 0x40001fc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [902]	LOADNIL  	128 0
  label_297: {
    Instruction i = 0x00002004;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [905]	CLOSURE  	129 63	; 0x559f637967b0
  label_298: {
    Instruction i = 0x000fe06c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [905]	MOVE     	128 129
  label_299: {
    Instruction i = 0x40802000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [906]	MOVE     	129 126
  label_300: {
    Instruction i = 0x3f002040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [906]	MOVE     	130 114
  label_301: {
    Instruction i = 0x39002080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [906]	CALL     	129 2 2
  label_302: {
    Instruction i = 0x0100a064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [907]	MOVE     	130 127
  label_303: {
    Instruction i = 0x3f802080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [907]	MOVE     	131 114
  label_304: {
    Instruction i = 0x390020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [907]	MOVE     	132 99
  label_305: {
    Instruction i = 0x31802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [907]	CALL     	130 3 2
  label_306: {
    Instruction i = 0x0180a0a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [907]	MOVE     	55 130
  label_307: {
    Instruction i = 0x41000dc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [908]	MOVE     	130 127
  label_308: {
    Instruction i = 0x3f802080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [908]	MOVE     	131 114
  label_309: {
    Instruction i = 0x390020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [908]	MOVE     	132 117
  label_310: {
    Instruction i = 0x3a802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [908]	CALL     	130 3 2
  label_311: {
    Instruction i = 0x0180a0a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [908]	MOVE     	56 130
  label_312: {
    Instruction i = 0x41000e00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [909]	MOVE     	130 128
  label_313: {
    Instruction i = 0x40002080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [909]	MOVE     	131 114
  label_314: {
    Instruction i = 0x390020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [909]	CALL     	130 2 2
  label_315: {
    Instruction i = 0x0100a0a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [909]	MOVE     	57 130
  label_316: {
    Instruction i = 0x41000e40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [910]	MOVE     	130 126
  label_317: {
    Instruction i = 0x3f002080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [910]	MOVE     	131 115
  label_318: {
    Instruction i = 0x398020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [910]	CALL     	130 2 2
  label_319: {
    Instruction i = 0x0100a0a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [911]	MOVE     	131 127
  label_320: {
    Instruction i = 0x3f8020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [911]	MOVE     	132 115
  label_321: {
    Instruction i = 0x39802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [911]	MOVE     	133 99
  label_322: {
    Instruction i = 0x31802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [911]	CALL     	131 3 2
  label_323: {
    Instruction i = 0x0180a0e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [911]	MOVE     	58 131
  label_324: {
    Instruction i = 0x41800e80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [912]	MOVE     	131 127
  label_325: {
    Instruction i = 0x3f8020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [912]	MOVE     	132 115
  label_326: {
    Instruction i = 0x39802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [912]	MOVE     	133 117
  label_327: {
    Instruction i = 0x3a802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [912]	CALL     	131 3 2
  label_328: {
    Instruction i = 0x0180a0e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [912]	MOVE     	59 131
  label_329: {
    Instruction i = 0x41800ec0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [913]	MOVE     	131 128
  label_330: {
    Instruction i = 0x400020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [913]	MOVE     	132 115
  label_331: {
    Instruction i = 0x39802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [913]	CALL     	131 2 2
  label_332: {
    Instruction i = 0x0100a0e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [913]	MOVE     	60 131
  label_333: {
    Instruction i = 0x41800f00;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [914]	MOVE     	131 126
  label_334: {
    Instruction i = 0x3f0020c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [914]	MOVE     	132 113
  label_335: {
    Instruction i = 0x38802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [914]	CALL     	131 2 2
  label_336: {
    Instruction i = 0x0100a0e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [915]	MOVE     	132 128
  label_337: {
    Instruction i = 0x40002100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [915]	MOVE     	133 113
  label_338: {
    Instruction i = 0x38802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [915]	CALL     	132 2 2
  label_339: {
    Instruction i = 0x0100a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [915]	MOVE     	61 132
  label_340: {
    Instruction i = 0x42000f40;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [916]	MOVE     	132 127
  label_341: {
    Instruction i = 0x3f802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [916]	MOVE     	133 113
  label_342: {
    Instruction i = 0x38802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [916]	MOVE     	134 118
  label_343: {
    Instruction i = 0x3b002180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [916]	CALL     	132 3 2
  label_344: {
    Instruction i = 0x0180a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [916]	MOVE     	62 132
  label_345: {
    Instruction i = 0x42000f80;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [917]	MOVE     	132 127
  label_346: {
    Instruction i = 0x3f802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [917]	MOVE     	133 113
  label_347: {
    Instruction i = 0x38802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [917]	MOVE     	134 119
  label_348: {
    Instruction i = 0x3b802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [917]	CALL     	132 3 2
  label_349: {
    Instruction i = 0x0180a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [917]	MOVE     	63 132
  label_350: {
    Instruction i = 0x42000fc0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [918]	MOVE     	132 128
  label_351: {
    Instruction i = 0x40002100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [918]	MOVE     	133 116
  label_352: {
    Instruction i = 0x3a002140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [918]	CALL     	132 2 2
  label_353: {
    Instruction i = 0x0100a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [918]	MOVE     	64 132
  label_354: {
    Instruction i = 0x42001000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [919]	MOVE     	132 128
  label_355: {
    Instruction i = 0x40002100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [919]	MOVE     	133 112
  label_356: {
    Instruction i = 0x38002140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [919]	CALL     	132 2 2
  label_357: {
    Instruction i = 0x0100a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [919]	MOVE     	65 132
  label_358: {
    Instruction i = 0x42001040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	132 125
  label_359: {
    Instruction i = 0x3e802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	133 77
  label_360: {
    Instruction i = 0x26802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	134 105
  label_361: {
    Instruction i = 0x34802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	135 51
  label_362: {
    Instruction i = 0x198021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	136 77
  label_363: {
    Instruction i = 0x26802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	137 106
  label_364: {
    Instruction i = 0x35002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	MOVE     	138 100
  label_365: {
    Instruction i = 0x32002280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [920]	CALL     	136 3 0
  label_366: {
    Instruction i = 0x01802224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [920]	CALL     	133 0 0
  label_367: {
    Instruction i = 0x00002164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [920]	CALL     	132 0 2
  label_368: {
    Instruction i = 0x0000a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [920]	MOVE     	66 132
  label_369: {
    Instruction i = 0x42001080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	132 125
  label_370: {
    Instruction i = 0x3e802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	133 77
  label_371: {
    Instruction i = 0x26802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	134 105
  label_372: {
    Instruction i = 0x34802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	135 77
  label_373: {
    Instruction i = 0x268021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	136 51
  label_374: {
    Instruction i = 0x19802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	137 15
  label_375: {
    Instruction i = 0x07802240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	138 51
  label_376: {
    Instruction i = 0x19802280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	MOVE     	139 103
  label_377: {
    Instruction i = 0x338022c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	CALL     	137 3 2
  label_378: {
    Instruction i = 0x0180a264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [921]	MOVE     	138 103
  label_379: {
    Instruction i = 0x33802280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	CALL     	135 4 2
  label_380: {
    Instruction i = 0x0200a1e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [921]	MOVE     	136 111
  label_381: {
    Instruction i = 0x37802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [921]	CALL     	133 4 0
  label_382: {
    Instruction i = 0x02002164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [921]	CALL     	132 0 2
  label_383: {
    Instruction i = 0x0000a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [921]	MOVE     	67 132
  label_384: {
    Instruction i = 0x420010c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [922]	MOVE     	132 125
  label_385: {
    Instruction i = 0x3e802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [922]	MOVE     	133 77
  label_386: {
    Instruction i = 0x26802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [922]	MOVE     	134 105
  label_387: {
    Instruction i = 0x34802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [922]	MOVE     	135 51
  label_388: {
    Instruction i = 0x198021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [922]	MOVE     	136 101
  label_389: {
    Instruction i = 0x32802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [922]	CALL     	133 4 0
  label_390: {
    Instruction i = 0x02002164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [922]	CALL     	132 0 2
  label_391: {
    Instruction i = 0x0000a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [922]	MOVE     	68 132
  label_392: {
    Instruction i = 0x42001100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	MOVE     	132 127
  label_393: {
    Instruction i = 0x3f802100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	MOVE     	133 117
  label_394: {
    Instruction i = 0x3a802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	MOVE     	134 77
  label_395: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	MOVE     	135 105
  label_396: {
    Instruction i = 0x348021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	MOVE     	136 120
  label_397: {
    Instruction i = 0x3c002200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	MOVE     	137 103
  label_398: {
    Instruction i = 0x33802240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [923]	CALL     	134 4 0
  label_399: {
    Instruction i = 0x020021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [923]	CALL     	132 0 2
  label_400: {
    Instruction i = 0x0000a124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [924]	MOVE     	133 125
  label_401: {
    Instruction i = 0x3e802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [924]	MOVE     	134 77
  label_402: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [924]	MOVE     	135 105
  label_403: {
    Instruction i = 0x348021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [924]	MOVE     	136 51
  label_404: {
    Instruction i = 0x19802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [924]	MOVE     	137 104
  label_405: {
    Instruction i = 0x34002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [924]	CALL     	134 4 0
  label_406: {
    Instruction i = 0x020021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [924]	CALL     	133 0 2
  label_407: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [924]	MOVE     	69 133
  label_408: {
    Instruction i = 0x42801140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [925]	MOVE     	133 125
  label_409: {
    Instruction i = 0x3e802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [925]	MOVE     	134 77
  label_410: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [925]	MOVE     	135 105
  label_411: {
    Instruction i = 0x348021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [925]	MOVE     	136 52
  label_412: {
    Instruction i = 0x1a002200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [925]	MOVE     	137 110
  label_413: {
    Instruction i = 0x37002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [925]	CALL     	134 4 0
  label_414: {
    Instruction i = 0x020021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [925]	CALL     	133 0 2
  label_415: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [925]	MOVE     	70 133
  label_416: {
    Instruction i = 0x42801180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	133 125
  label_417: {
    Instruction i = 0x3e802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	134 77
  label_418: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	135 105
  label_419: {
    Instruction i = 0x348021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	136 77
  label_420: {
    Instruction i = 0x26802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	137 52
  label_421: {
    Instruction i = 0x1a002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	138 77
  label_422: {
    Instruction i = 0x26802280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	139 51
  label_423: {
    Instruction i = 0x198022c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	140 103
  label_424: {
    Instruction i = 0x33802300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	MOVE     	141 51
  label_425: {
    Instruction i = 0x19802340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	CALL     	138 4 0
  label_426: {
    Instruction i = 0x020022a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [926]	CALL     	136 0 2
  label_427: {
    Instruction i = 0x0000a224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [926]	MOVE     	137 102
  label_428: {
    Instruction i = 0x33002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [926]	CALL     	134 4 0
  label_429: {
    Instruction i = 0x020021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [926]	CALL     	133 0 2
  label_430: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [926]	MOVE     	71 133
  label_431: {
    Instruction i = 0x428011c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	MOVE     	133 125
  label_432: {
    Instruction i = 0x3e802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	MOVE     	134 77
  label_433: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	MOVE     	135 52
  label_434: {
    Instruction i = 0x1a0021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	MOVE     	136 77
  label_435: {
    Instruction i = 0x26802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	MOVE     	137 50
  label_436: {
    Instruction i = 0x19002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	MOVE     	138 51
  label_437: {
    Instruction i = 0x19802280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [927]	CALL     	136 3 0
  label_438: {
    Instruction i = 0x01802224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [927]	CALL     	134 0 0
  label_439: {
    Instruction i = 0x000021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [927]	CALL     	133 0 2
  label_440: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [927]	MOVE     	72 133
  label_441: {
    Instruction i = 0x42801200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	MOVE     	133 125
  label_442: {
    Instruction i = 0x3e802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	MOVE     	134 77
  label_443: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	MOVE     	135 52
  label_444: {
    Instruction i = 0x1a0021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	MOVE     	136 77
  label_445: {
    Instruction i = 0x26802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	MOVE     	137 50
  label_446: {
    Instruction i = 0x19002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	MOVE     	138 52
  label_447: {
    Instruction i = 0x1a002280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [928]	CALL     	136 3 0
  label_448: {
    Instruction i = 0x01802224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [928]	CALL     	134 0 0
  label_449: {
    Instruction i = 0x000021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [928]	CALL     	133 0 2
  label_450: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [928]	MOVE     	73 133
  label_451: {
    Instruction i = 0x42801240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [929]	MOVE     	133 125
  label_452: {
    Instruction i = 0x3e802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [929]	MOVE     	134 77
  label_453: {
    Instruction i = 0x26802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [929]	MOVE     	135 52
  label_454: {
    Instruction i = 0x1a0021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [929]	MOVE     	136 52
  label_455: {
    Instruction i = 0x1a002200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [929]	CALL     	134 3 0
  label_456: {
    Instruction i = 0x018021a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [929]	CALL     	133 0 2
  label_457: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [929]	MOVE     	74 133
  label_458: {
    Instruction i = 0x42801280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [930]	SETTABLE 	1 -91 129	; "new_data_function_builtin_systemName" -
  label_459: {
    Instruction i = 0xad20404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [931]	SETTABLE 	1 -92 55	; "data_name_function_builtin_systemName" -
  label_460: {
    Instruction i = 0xad8dc04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [932]	SETTABLE 	1 -93 56	; "data_list_function_builtin_systemName" -
  label_461: {
    Instruction i = 0xae0e004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [933]	SETTABLE 	1 -94 57	; "data_p_function_builtin_systemName" -
  label_462: {
    Instruction i = 0xae8e404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [934]	SETTABLE 	1 -95 130	; "new_error_function_builtin_systemName" -
  label_463: {
    Instruction i = 0xaf20804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [935]	SETTABLE 	1 -96 58	; "error_name_function_builtin_systemName" -
  label_464: {
    Instruction i = 0xaf8e804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [936]	SETTABLE 	1 -97 59	; "error_list_function_builtin_systemName" -
  label_465: {
    Instruction i = 0xb00ec04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [937]	SETTABLE 	1 -98 60	; "error_p_function_builtin_systemName" -
  label_466: {
    Instruction i = 0xb08f004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [938]	SETTABLE 	1 -99 131	; "new_construction_function_builtin_systemName" -
  label_467: {
    Instruction i = 0xb120c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [939]	SETTABLE 	1 -100 61	; "construction_p_function_builtin_systemName" -
  label_468: {
    Instruction i = 0xb18f404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [940]	SETTABLE 	1 -101 62	; "construction_head_function_builtin_systemName" -
  label_469: {
    Instruction i = 0xb20f804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [941]	SETTABLE 	1 -102 63	; "construction_tail_function_builtin_systemName" -
  label_470: {
    Instruction i = 0xb28fc04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [942]	SETTABLE 	1 -103 64	; "symbol_p_function_builtin_systemName" -
  label_471: {
    Instruction i = 0xb310004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [943]	SETTABLE 	1 -104 65	; "null_p_function_builtin_systemName" -
  label_472: {
    Instruction i = 0xb390404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [944]	SETTABLE 	1 -105 66	; "equal_p_function_builtin_systemName" -
  label_473: {
    Instruction i = 0xb410804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [945]	SETTABLE 	1 -106 67	; "apply_function_builtin_systemName" -
  label_474: {
    Instruction i = 0xb490c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [946]	SETTABLE 	1 -107 68	; "evaluate_function_builtin_systemName" -
  label_475: {
    Instruction i = 0xb511004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [947]	SETTABLE 	1 -108 132	; "list_chooseOne_function_builtin_systemName" -
  label_476: {
    Instruction i = 0xb5a1004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [948]	SETTABLE 	1 -109 69	; "if_function_builtin_systemName" -
  label_477: {
    Instruction i = 0xb611404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [949]	SETTABLE 	1 -110 70	; "quote_form_builtin_systemName" -
  label_478: {
    Instruction i = 0xb691804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [950]	SETTABLE 	1 -111 71	; "lambda_form_builtin_systemName" -
  label_479: {
    Instruction i = 0xb711c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [951]	SETTABLE 	1 -112 72	; "function_builtin_use_systemName" -
  label_480: {
    Instruction i = 0xb792004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [952]	SETTABLE 	1 -113 73	; "form_builtin_use_systemName" -
  label_481: {
    Instruction i = 0xb812404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [953]	SETTABLE 	1 -114 74	; "form_use_systemName" -
  label_482: {
    Instruction i = 0xb892804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [954]	MOVE     	133 21
  label_483: {
    Instruction i = 0x0a802140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [954]	MOVE     	134 109
  label_484: {
    Instruction i = 0x36802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [954]	MOVE     	135 77
  label_485: {
    Instruction i = 0x268021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [954]	CALL     	135 1 0
  label_486: {
    Instruction i = 0x008021e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [954]	CALL     	133 0 2
  label_487: {
    Instruction i = 0x0000a164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [955]	MOVE     	134 21
  label_488: {
    Instruction i = 0x0a802180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [955]	MOVE     	135 108
  label_489: {
    Instruction i = 0x360021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [955]	MOVE     	136 77
  label_490: {
    Instruction i = 0x26802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [955]	CALL     	136 1 0
  label_491: {
    Instruction i = 0x00802224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [955]	CALL     	134 0 2
  label_492: {
    Instruction i = 0x0000a1a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [956]	LOADNIL  	135 0
  label_493: {
    Instruction i = 0x000021c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [967]	CLOSURE  	136 64	; 0x559f637979b0
  label_494: {
    Instruction i = 0x0010222c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [967]	MOVE     	135 136
  label_495: {
    Instruction i = 0x440021c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [968]	LOADNIL  	136 0
  label_496: {
    Instruction i = 0x00002204;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [975]	CLOSURE  	137 65	; 0x559f6379c450
  label_497: {
    Instruction i = 0x0010626c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [975]	MOVE     	136 137
  label_498: {
    Instruction i = 0x44802200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [976]	SETTABLE 	1 -115 76	; "jsArray_to_list" -
  label_499: {
    Instruction i = 0xb913004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [977]	SETTABLE 	1 -116 136	; "maybe_list_to_jsArray" -
  label_500: {
    Instruction i = 0xb9a2004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [978]	SETTABLE 	1 -117 77	; "new_list" -
  label_501: {
    Instruction i = 0xba13404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [979]	SETTABLE 	1 -118 79	; "delay_p" -
  label_502: {
    Instruction i = 0xba93c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [980]	SETTABLE 	1 -119 80	; "force_all" -
  label_503: {
    Instruction i = 0xbb14004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [981]	SETTABLE 	1 -120 81	; "force1" -
  label_504: {
    Instruction i = 0xbb94404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [982]	NEWTABLE 	137 0 0
  label_505: {
    Instruction i = 0x0000224b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [982]	MOVE     	82 137
  label_506: {
    Instruction i = 0x44801480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [983]	LOADNIL  	137 0
  label_507: {
    Instruction i = 0x00002244;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1041]	CLOSURE  	138 66	; 0x559f63795f60
  label_508: {
    Instruction i = 0x0010a2ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1041]	MOVE     	137 138
  label_509: {
    Instruction i = 0x45002240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1042]	SETTABLE 	1 -121 82	; "env_null_v" -
  label_510: {
    Instruction i = 0xbc14804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1043]	SETTABLE 	1 -122 83	; "env_set" -
  label_511: {
    Instruction i = 0xbc94c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1044]	SETTABLE 	1 -123 84	; "env_get" -
  label_512: {
    Instruction i = 0xbd15004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1045]	SETTABLE 	1 -124 86	; "env2val" -
  label_513: {
    Instruction i = 0xbd95804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1046]	SETTABLE 	1 -125 87	; "env_foreach" -
  label_514: {
    Instruction i = 0xbe15c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1047]	SETTABLE 	1 -126 137	; "val2env" -
  label_515: {
    Instruction i = 0xbea2404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1048]	LOADNIL  	138 0
  label_516: {
    Instruction i = 0x00002284;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1060]	CLOSURE  	139 67	; 0x559f637a9f50
  label_517: {
    Instruction i = 0x0010e2ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1060]	MOVE     	138 139
  label_518: {
    Instruction i = 0x45802280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1061]	LOADNIL  	139 0
  label_519: {
    Instruction i = 0x000022c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1073]	CLOSURE  	140 68	; 0x559f637a3ae0
  label_520: {
    Instruction i = 0x0011232c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1073]	MOVE     	139 140
  label_521: {
    Instruction i = 0x460022c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	NEWTABLE 	140 18 0
  label_522: {
    Instruction i = 0x0900230b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1074]	MOVE     	141 138
  label_523: {
    Instruction i = 0x45002340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	142 57
  label_524: {
    Instruction i = 0x1c802380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	143 22
  label_525: {
    Instruction i = 0x0b0023c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	141 3 2
  label_526: {
    Instruction i = 0x0180a364;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	NEWTABLE 	142 3 0
  label_527: {
    Instruction i = 0x0180238b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1074]	MOVE     	143 129
  label_528: {
    Instruction i = 0x408023c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	LOADK    	144 -3	; 2
  label_529: {
    Instruction i = 0x0000a401;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1074]	MOVE     	145 21
  label_530: {
    Instruction i = 0x0a802440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	SETLIST  	142 3 1	; 1
  label_531: {
    Instruction i = 0x018063ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1074]	MOVE     	143 139
  label_532: {
    Instruction i = 0x458023c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	144 55
  label_533: {
    Instruction i = 0x1b802400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	145 22
  label_534: {
    Instruction i = 0x0b002440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	146 23
  label_535: {
    Instruction i = 0x0b802480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	143 4 2
  label_536: {
    Instruction i = 0x0200a3e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	MOVE     	144 139
  label_537: {
    Instruction i = 0x45802400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	145 56
  label_538: {
    Instruction i = 0x1c002440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	146 22
  label_539: {
    Instruction i = 0x0b002480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	147 24
  label_540: {
    Instruction i = 0x0c0024c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	144 4 2
  label_541: {
    Instruction i = 0x0200a424;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	MOVE     	145 138
  label_542: {
    Instruction i = 0x45002440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	146 60
  label_543: {
    Instruction i = 0x1e002480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	147 26
  label_544: {
    Instruction i = 0x0d0024c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	145 3 2
  label_545: {
    Instruction i = 0x0180a464;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	NEWTABLE 	146 3 0
  label_546: {
    Instruction i = 0x0180248b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1074]	MOVE     	147 130
  label_547: {
    Instruction i = 0x410024c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	LOADK    	148 -3	; 2
  label_548: {
    Instruction i = 0x0000a501;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1074]	MOVE     	149 25
  label_549: {
    Instruction i = 0x0c802540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	SETLIST  	146 3 1	; 1
  label_550: {
    Instruction i = 0x018064ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1074]	MOVE     	147 139
  label_551: {
    Instruction i = 0x458024c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	148 58
  label_552: {
    Instruction i = 0x1d002500;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	149 26
  label_553: {
    Instruction i = 0x0d002540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	150 27
  label_554: {
    Instruction i = 0x0d802580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	147 4 2
  label_555: {
    Instruction i = 0x0200a4e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	MOVE     	148 139
  label_556: {
    Instruction i = 0x45802500;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	149 59
  label_557: {
    Instruction i = 0x1d802540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	150 26
  label_558: {
    Instruction i = 0x0d002580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	151 28
  label_559: {
    Instruction i = 0x0e0025c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	148 4 2
  label_560: {
    Instruction i = 0x0200a524;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	MOVE     	149 138
  label_561: {
    Instruction i = 0x45002540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	150 65
  label_562: {
    Instruction i = 0x20802580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	151 20
  label_563: {
    Instruction i = 0x0a0025c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	149 3 2
  label_564: {
    Instruction i = 0x0180a564;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	NEWTABLE 	150 3 0
  label_565: {
    Instruction i = 0x0180258b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1074]	MOVE     	151 131
  label_566: {
    Instruction i = 0x418025c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	LOADK    	152 -3	; 2
  label_567: {
    Instruction i = 0x0000a601;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1074]	MOVE     	153 15
  label_568: {
    Instruction i = 0x07802640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	SETLIST  	150 3 1	; 1
  label_569: {
    Instruction i = 0x018065ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1074]	MOVE     	151 138
  label_570: {
    Instruction i = 0x450025c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	152 61
  label_571: {
    Instruction i = 0x1e802600;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	153 16
  label_572: {
    Instruction i = 0x08002640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	151 3 2
  label_573: {
    Instruction i = 0x0180a5e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	MOVE     	152 139
  label_574: {
    Instruction i = 0x45802600;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	153 62
  label_575: {
    Instruction i = 0x1f002640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	154 16
  label_576: {
    Instruction i = 0x08002680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	155 17
  label_577: {
    Instruction i = 0x088026c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	152 4 2
  label_578: {
    Instruction i = 0x0200a624;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	MOVE     	153 139
  label_579: {
    Instruction i = 0x45802640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	154 63
  label_580: {
    Instruction i = 0x1f802680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	155 16
  label_581: {
    Instruction i = 0x080026c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	MOVE     	156 18
  label_582: {
    Instruction i = 0x09002700;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	CALL     	153 4 2
  label_583: {
    Instruction i = 0x0200a664;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1074]	NEWTABLE 	154 3 0
  label_584: {
    Instruction i = 0x0180268b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1074]	MOVE     	155 66
  label_585: {
    Instruction i = 0x210026c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1074]	LOADK    	156 -3	; 2
  label_586: {
    Instruction i = 0x0000a701;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1126]	CLOSURE  	157 69	; 0x559f6379b000
  label_587: {
    Instruction i = 0x0011676c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1126]	SETLIST  	154 3 1	; 1
  label_588: {
    Instruction i = 0x018066ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1126]	NEWTABLE 	155 3 0
  label_589: {
    Instruction i = 0x018026cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1126]	MOVE     	156 67
  label_590: {
    Instruction i = 0x21802700;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1126]	LOADK    	157 -3	; 2
  label_591: {
    Instruction i = 0x0000a741;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1137]	CLOSURE  	158 70	; 0x559f6379bc70
  label_592: {
    Instruction i = 0x0011a7ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1137]	SETLIST  	155 3 1	; 1
  label_593: {
    Instruction i = 0x018066eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1137]	NEWTABLE 	156 3 0
  label_594: {
    Instruction i = 0x0180270b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1137]	MOVE     	157 68
  label_595: {
    Instruction i = 0x22002740;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1137]	LOADK    	158 -3	; 2
  label_596: {
    Instruction i = 0x0000a781;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1143]	CLOSURE  	159 71	; 0x559f6379b320
  label_597: {
    Instruction i = 0x0011e7ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1143]	SETLIST  	156 3 1	; 1
  label_598: {
    Instruction i = 0x0180672b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1143]	MOVE     	157 138
  label_599: {
    Instruction i = 0x45002740;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1143]	MOVE     	158 64
  label_600: {
    Instruction i = 0x20002780;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1143]	MOVE     	159 13
  label_601: {
    Instruction i = 0x068027c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1143]	CALL     	157 3 2
  label_602: {
    Instruction i = 0x0180a764;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1143]	NEWTABLE 	158 3 0
  label_603: {
    Instruction i = 0x0180278b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1143]	MOVE     	159 132
  label_604: {
    Instruction i = 0x420027c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1143]	LOADK    	160 -127	; 1
  label_605: {
    Instruction i = 0x001fa801;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1152]	CLOSURE  	161 72	; 0x559f637ac490
  label_606: {
    Instruction i = 0x0012286c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1152]	SETLIST  	158 3 1	; 1
  label_607: {
    Instruction i = 0x018067ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1152]	NEWTABLE 	159 3 0
  label_608: {
    Instruction i = 0x018027cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1152]	MOVE     	160 69
  label_609: {
    Instruction i = 0x22802800;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1152]	LOADK    	161 -4	; 3
  label_610: {
    Instruction i = 0x0000e841;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1171]	CLOSURE  	162 73	; 0x559f637acf40
  label_611: {
    Instruction i = 0x001268ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1171]	SETLIST  	159 3 1	; 1
  label_612: {
    Instruction i = 0x018067eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1171]	SETLIST  	140 19 1	; 1
  label_613: {
    Instruction i = 0x0980632b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1171]	MOVE     	90 140
  label_614: {
    Instruction i = 0x46001680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1172]	SETTABLE 	1 -128 95	; "equal_p" -
  label_615: {
    Instruction i = 0xbf97c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1173]	LOADNIL  	140 0
  label_616: {
    Instruction i = 0x00002304;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1229]	CLOSURE  	141 74	; 0x559f637ad0f0
  label_617: {
    Instruction i = 0x0012a36c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1229]	MOVE     	140 141
  label_618: {
    Instruction i = 0x46802300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1230]	LOADNIL  	141 0
  label_619: {
    Instruction i = 0x00002344;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1233]	CLOSURE  	142 75	; 0x559f637ad5b0
  label_620: {
    Instruction i = 0x0012e3ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1233]	MOVE     	141 142
  label_621: {
    Instruction i = 0x47002340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1234]	SETTABLE 	1 -129 96	; "simple_print" -
  label_622: {
    Instruction i = 0xc018004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1235]	SETTABLE 	1 -130 141	; "simple_print_force_all_rec" -
  label_623: {
    Instruction i = 0xc0a3404a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1236]	LOADNIL  	142 0
  label_624: {
    Instruction i = 0x00002384;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1524]	CLOSURE  	143 76	; 0x559f637ad400
  label_625: {
    Instruction i = 0x001323ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1524]	MOVE     	142 143
  label_626: {
    Instruction i = 0x47802380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1525]	SETTABLE 	1 -131 142	; "simple_parse" -
  label_627: {
    Instruction i = 0xc123804a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1526]	LOADNIL  	143 0
  label_628: {
    Instruction i = 0x000023c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1953]	CLOSURE  	144 77	; 0x559f637ada30
  label_629: {
    Instruction i = 0x0013642c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1953]	MOVE     	143 144
  label_630: {
    Instruction i = 0x480023c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1954]	SETTABLE 	1 -132 143	; "complex_parse" -
  label_631: {
    Instruction i = 0xc1a3c04a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1955]	LOADNIL  	144 0
  label_632: {
    Instruction i = 0x00002404;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [2071]	CLOSURE  	145 78	; 0x559f637b95f0
  label_633: {
    Instruction i = 0x0013a46c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [2071]	MOVE     	144 145
  label_634: {
    Instruction i = 0x48802400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2072]	SETTABLE 	1 -133 144	; "complex_print" -
  label_635: {
    Instruction i = 0xc224004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [2073]	RETURN   	1 2
  label_636: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2073]	RETURN   	0 1
  label_637: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 19
// lastlinedefined = 22
// what = Lua
static int zz_magic_function_1 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [20]	LEN      	2 0
  label_0: {
    Instruction i = 0x0000009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [20]	ADD      	2 2 -1	; - 1
  label_1: {
    Instruction i = 0x0140008d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [20]	SETTABLE 	0 2 1
  label_2: {
    Instruction i = 0x0100400a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [21]	LEN      	2 0
  label_3: {
    Instruction i = 0x0000009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [21]	RETURN   	2 2
  label_4: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [22]	RETURN   	0 1
  label_5: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 25
// lastlinedefined = 27
// what = Lua
static int zz_magic_function_2 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [26]	GETTABUP 	0 0 -1	; _ENV "error"
  label_0: {
    Instruction i = 0x00400006;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [26]	LOADK    	1 -2	; "TheLanguage PANIC"
  label_1: {
    Instruction i = 0x00004041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [26]	CALL     	0 2 1
  label_2: {
    Instruction i = 0x01004024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [27]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 28
// lastlinedefined = 32
// what = Lua
static int zz_magic_function_3 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [29]	TEST     	0 1
  label_0: {
    Instruction i = 0x00004022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [29]	JMP      	0 3	; to 5
  label_1: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [30]	GETUPVAL 	1 0	; ERROR
  label_2: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [30]	TAILCALL 	1 1 0
  label_3: {
    Instruction i = 0x00800065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [30]	RETURN   	1 0
  label_4: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [32]	RETURN   	0 1
  label_5: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 33
// lastlinedefined = 35
// what = Lua
static int zz_magic_function_4 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [34]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [34]	EQ       	1 1 -2	; - 0
  label_1: {
    Instruction i = 0x00c0405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_3;
    }
  }
  
  // [34]	JMP      	0 1	; to 4
  label_2: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [34]	LOADBOOL 	1 0 1
  label_3: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_5;
    }
  }
  
  // [34]	LOADBOOL 	1 1 0
  label_4: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [34]	RETURN   	1 2
  label_5: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [35]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 36
// lastlinedefined = 38
// what = Lua
static int zz_magic_function_5 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [37]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [37]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [38]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 39
// lastlinedefined = 41
// what = Lua
static int zz_magic_function_6 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [40]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [40]	GETUPVAL 	3 0	; construction_t
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [40]	MOVE     	4 0
  label_2: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [40]	MOVE     	5 1
  label_3: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [40]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [40]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [41]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 42
// lastlinedefined = 44
// what = Lua
static int zz_magic_function_7 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [43]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [43]	GETUPVAL 	2 0	; construction_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [43]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [43]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [43]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [43]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [43]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [44]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 45
// lastlinedefined = 47
// what = Lua
static int zz_magic_function_8 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [46]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [46]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [47]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 48
// lastlinedefined = 50
// what = Lua
static int zz_magic_function_9 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [49]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [49]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [50]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 51
// lastlinedefined = 53
// what = Lua
static int zz_magic_function_10 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [52]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [52]	GETUPVAL 	2 0	; null_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [52]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [52]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [52]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [52]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [52]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [53]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 54
// lastlinedefined = 56
// what = Lua
static int zz_magic_function_11 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [55]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [55]	GETUPVAL 	3 0	; data_t
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [55]	MOVE     	4 0
  label_2: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [55]	MOVE     	5 1
  label_3: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [55]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [55]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [56]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 57
// lastlinedefined = 59
// what = Lua
static int zz_magic_function_12 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [58]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [58]	GETUPVAL 	2 0	; data_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [58]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [58]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [58]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [58]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [58]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [59]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 60
// lastlinedefined = 62
// what = Lua
static int zz_magic_function_13 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [61]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [61]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [62]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 63
// lastlinedefined = 65
// what = Lua
static int zz_magic_function_14 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [64]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [64]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [65]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 66
// lastlinedefined = 68
// what = Lua
static int zz_magic_function_15 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [67]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [67]	GETUPVAL 	3 0	; error_t
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [67]	MOVE     	4 0
  label_2: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [67]	MOVE     	5 1
  label_3: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [67]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [67]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [68]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 69
// lastlinedefined = 71
// what = Lua
static int zz_magic_function_16 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [70]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [70]	GETUPVAL 	2 0	; error_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [70]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [70]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [70]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [70]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [70]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [71]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 72
// lastlinedefined = 74
// what = Lua
static int zz_magic_function_17 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [73]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [73]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [74]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 75
// lastlinedefined = 77
// what = Lua
static int zz_magic_function_18 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [76]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [76]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [77]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 78
// lastlinedefined = 86
// what = Lua
static int zz_magic_function_19 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [79]	EQ       	0 0 1
  label_0: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [79]	JMP      	0 1	; to 3
  label_1: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_3;
  }
  
  // [80]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [82]	GETUPVAL 	2 0	; just_t
  label_3: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [82]	SETTABLE 	0 -1 2	; 1 -
  label_4: {
    Instruction i = 0x8000800a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [83]	SETTABLE 	0 -2 1	; 2 -
  label_5: {
    Instruction i = 0x8080400a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [84]	SETTABLE 	0 -3 -4	; 3 false
  label_6: {
    Instruction i = 0x8140c00a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [85]	SETTABLE 	0 -5 -4	; 4 false
  label_7: {
    Instruction i = 0x8240c00a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [86]	RETURN   	0 1
  label_8: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 87
// lastlinedefined = 89
// what = Lua
static int zz_magic_function_20 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [88]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [88]	GETUPVAL 	2 0	; just_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [88]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [88]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [88]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [88]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [88]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [89]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 90
// lastlinedefined = 92
// what = Lua
static int zz_magic_function_21 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [91]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [91]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [92]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 93
// lastlinedefined = 95
// what = Lua
static int zz_magic_function_22 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [94]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [94]	GETUPVAL 	3 0	; delay_evaluate_t
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [94]	MOVE     	4 0
  label_2: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [94]	MOVE     	5 1
  label_3: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [94]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [94]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [95]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 96
// lastlinedefined = 98
// what = Lua
static int zz_magic_function_23 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [97]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [97]	GETUPVAL 	2 0	; delay_evaluate_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [97]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [97]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [97]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [97]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [97]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [98]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 99
// lastlinedefined = 101
// what = Lua
static int zz_magic_function_24 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [100]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [100]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [101]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 102
// lastlinedefined = 104
// what = Lua
static int zz_magic_function_25 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [103]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [103]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [104]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 105
// lastlinedefined = 107
// what = Lua
static int zz_magic_function_26 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [106]	NEWTABLE 	3 4 0
  label_0: {
    Instruction i = 0x020000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [106]	GETUPVAL 	4 0	; delay_builtin_form_t
  label_1: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [106]	MOVE     	5 0
  label_2: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [106]	MOVE     	6 1
  label_3: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [106]	MOVE     	7 2
  label_4: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [106]	SETLIST  	3 4 1	; 1
  label_5: {
    Instruction i = 0x020040eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [106]	RETURN   	3 2
  label_6: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [107]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 108
// lastlinedefined = 110
// what = Lua
static int zz_magic_function_27 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [109]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [109]	GETUPVAL 	2 0	; delay_builtin_form_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [109]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [109]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [109]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [109]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [109]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [110]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 111
// lastlinedefined = 113
// what = Lua
static int zz_magic_function_28 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [112]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [112]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [113]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 114
// lastlinedefined = 116
// what = Lua
static int zz_magic_function_29 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [115]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [115]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [116]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 117
// lastlinedefined = 119
// what = Lua
static int zz_magic_function_30 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [118]	GETTABLE 	1 0 -1	; 4
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [118]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [119]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 120
// lastlinedefined = 122
// what = Lua
static int zz_magic_function_31 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [121]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [121]	GETUPVAL 	3 0	; delay_builtin_func_t
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [121]	MOVE     	4 0
  label_2: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [121]	MOVE     	5 1
  label_3: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [121]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [121]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [122]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 123
// lastlinedefined = 125
// what = Lua
static int zz_magic_function_32 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [124]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [124]	GETUPVAL 	2 0	; delay_builtin_func_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [124]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [124]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [124]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [124]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [124]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [125]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 126
// lastlinedefined = 128
// what = Lua
static int zz_magic_function_33 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [127]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [127]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [128]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 129
// lastlinedefined = 131
// what = Lua
static int zz_magic_function_34 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [130]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [130]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [131]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 132
// lastlinedefined = 134
// what = Lua
static int zz_magic_function_35 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [133]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [133]	GETUPVAL 	3 0	; delay_apply_t
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [133]	MOVE     	4 0
  label_2: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [133]	MOVE     	5 1
  label_3: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [133]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [133]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [134]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 135
// lastlinedefined = 137
// what = Lua
static int zz_magic_function_36 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [136]	GETTABLE 	1 0 -1	; 1
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [136]	GETUPVAL 	2 0	; delay_apply_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [136]	EQ       	1 1 2
  label_2: {
    Instruction i = 0x0080805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [136]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [136]	LOADBOOL 	1 0 1
  label_4: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [136]	LOADBOOL 	1 1 0
  label_5: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [136]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [137]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 138
// lastlinedefined = 140
// what = Lua
static int zz_magic_function_37 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [139]	GETTABLE 	1 0 -1	; 2
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [139]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [140]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 141
// lastlinedefined = 143
// what = Lua
static int zz_magic_function_38 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [142]	GETTABLE 	1 0 -1	; 3
  label_0: {
    Instruction i = 0x00400047;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [142]	RETURN   	1 2
  label_1: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [143]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 144
// lastlinedefined = 166
// what = Lua
static int zz_magic_function_39 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [145]	GETUPVAL 	1 0	; force_all
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [145]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [145]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [146]	GETUPVAL 	2 1	; data_p
  label_3: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [146]	MOVE     	3 1
  label_4: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [146]	CALL     	2 2 2
  label_5: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [146]	TEST     	2 0
  label_6: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_8;
    }
  }
  
  // [146]	JMP      	0 12	; to 20
  label_7: {
    Instruction i = 0x8002c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_20;
  }
  
  // [147]	GETTABLE 	2 1 -1	; 2
  label_8: {
    Instruction i = 0x00c00087;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [148]	GETTABLE 	3 1 -2	; 3
  label_9: {
    Instruction i = 0x00c040c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [149]	GETUPVAL 	4 2	; force_all_rec
  label_10: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [149]	MOVE     	5 2
  label_11: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [149]	CALL     	4 2 2
  label_12: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [149]	SETTABLE 	1 -1 4	; 2 -
  label_13: {
    Instruction i = 0x8001004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [150]	GETUPVAL 	4 2	; force_all_rec
  label_14: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [150]	MOVE     	5 3
  label_15: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [150]	CALL     	4 2 2
  label_16: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [150]	SETTABLE 	1 -2 4	; 3 -
  label_17: {
    Instruction i = 0x8081004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [151]	RETURN   	1 2
  label_18: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [151]	JMP      	0 33	; to 53
  label_19: {
    Instruction i = 0x8008001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_53;
  }
  
  // [152]	GETUPVAL 	2 3	; error_p
  label_20: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [152]	MOVE     	3 1
  label_21: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [152]	CALL     	2 2 2
  label_22: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [152]	TEST     	2 0
  label_23: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_25;
    }
  }
  
  // [152]	JMP      	0 12	; to 37
  label_24: {
    Instruction i = 0x8002c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_37;
  }
  
  // [153]	GETTABLE 	2 1 -1	; 2
  label_25: {
    Instruction i = 0x00c00087;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [154]	GETTABLE 	3 1 -2	; 3
  label_26: {
    Instruction i = 0x00c040c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [155]	GETUPVAL 	4 2	; force_all_rec
  label_27: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [155]	MOVE     	5 2
  label_28: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [155]	CALL     	4 2 2
  label_29: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [155]	SETTABLE 	1 -1 4	; 2 -
  label_30: {
    Instruction i = 0x8001004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [156]	GETUPVAL 	4 2	; force_all_rec
  label_31: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [156]	MOVE     	5 3
  label_32: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [156]	CALL     	4 2 2
  label_33: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [156]	SETTABLE 	1 -2 4	; 3 -
  label_34: {
    Instruction i = 0x8081004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [157]	RETURN   	1 2
  label_35: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [157]	JMP      	0 16	; to 53
  label_36: {
    Instruction i = 0x8003c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_53;
  }
  
  // [158]	GETUPVAL 	2 4	; construction_p
  label_37: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [158]	MOVE     	3 1
  label_38: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [158]	CALL     	2 2 2
  label_39: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [158]	TEST     	2 0
  label_40: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_42;
    }
  }
  
  // [158]	JMP      	0 11	; to 53
  label_41: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_53;
  }
  
  // [159]	GETTABLE 	2 1 -1	; 2
  label_42: {
    Instruction i = 0x00c00087;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [160]	GETTABLE 	3 1 -2	; 3
  label_43: {
    Instruction i = 0x00c040c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [161]	GETUPVAL 	4 2	; force_all_rec
  label_44: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [161]	MOVE     	5 2
  label_45: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [161]	CALL     	4 2 2
  label_46: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [161]	SETTABLE 	1 -1 4	; 2 -
  label_47: {
    Instruction i = 0x8001004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [162]	GETUPVAL 	4 2	; force_all_rec
  label_48: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [162]	MOVE     	5 3
  label_49: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [162]	CALL     	4 2 2
  label_50: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [162]	SETTABLE 	1 -2 4	; 3 -
  label_51: {
    Instruction i = 0x8081004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [163]	RETURN   	1 2
  label_52: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [165]	RETURN   	1 2
  label_53: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [166]	RETURN   	0 1
  label_54: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 167
// lastlinedefined = 177
// what = Lua
static int zz_magic_function_40 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [168]	EQ       	0 0 1
  label_0: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [168]	JMP      	0 2	; to 4
  label_1: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [169]	LOADBOOL 	2 1 0
  label_2: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_4;
    }
  }
  
  // [169]	RETURN   	2 2
  label_3: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [171]	GETUPVAL 	2 0	; un_symbol
  label_4: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [171]	MOVE     	3 0
  label_5: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [171]	CALL     	2 2 2
  label_6: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [171]	GETUPVAL 	3 0	; un_symbol
  label_7: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [171]	MOVE     	4 1
  label_8: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [171]	CALL     	3 2 2
  label_9: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [171]	EQ       	0 2 3
  label_10: {
    Instruction i = 0x0100c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [171]	JMP      	0 7	; to 19
  label_11: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_19;
  }
  
  // [172]	GETUPVAL 	2 1	; lang_set_do
  label_12: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [172]	MOVE     	3 0
  label_13: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [172]	MOVE     	4 1
  label_14: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [172]	CALL     	2 3 1
  label_15: {
    Instruction i = 0x018040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [173]	LOADBOOL 	2 1 0
  label_16: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_18;
    }
  }
  
  // [173]	RETURN   	2 2
  label_17: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [173]	JMP      	0 2	; to 21
  label_18: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_21;
  }
  
  // [175]	LOADBOOL 	2 0 0
  label_19: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_21;
    }
  }
  
  // [175]	RETURN   	2 2
  label_20: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [177]	RETURN   	0 1
  label_21: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 178
// lastlinedefined = 188
// what = Lua
static int zz_magic_function_41 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [179]	GETUPVAL 	1 0	; null_v
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [181]	LEN      	2 0
  label_1: {
    Instruction i = 0x0000009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [181]	SUB      	2 2 -1	; - 1
  label_2: {
    Instruction i = 0x0140008e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [182]	LE       	0 -2 2	; 0 -
  label_3: {
    Instruction i = 0x80808021;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessequal(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [182]	JMP      	0 8	; to 13
  label_4: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [183]	GETUPVAL 	3 1	; new_construction
  label_5: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [183]	ADD      	4 2 -1	; - 1
  label_6: {
    Instruction i = 0x0140010d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [183]	GETTABLE 	4 0 4
  label_7: {
    Instruction i = 0x00010107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [183]	MOVE     	5 1
  label_8: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [183]	CALL     	3 3 2
  label_9: {
    Instruction i = 0x018080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [183]	MOVE     	1 3
  label_10: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [184]	SUB      	2 2 -1	; - 1
  label_11: {
    Instruction i = 0x0140008e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [184]	JMP      	0 -10	; to 3
  label_12: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_3;
  }
  
  // [187]	RETURN   	1 2
  label_13: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [188]	RETURN   	0 1
  label_14: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 189
// lastlinedefined = 192
// what = Lua
static int zz_magic_function_42 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [190]	NEWTABLE 	0 0 0
  label_0: {
    Instruction i = 0x0000000b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [190]	VARARG   	1 0
  label_1: {
    Instruction i = 0x0000006d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i) - 1;  /* required results */
    int j;
    int n = cast_int(base - ci->func) - cl->p->numparams - 1;
    if (n < 0)  /* less arguments than parameters? */
      n = 0;  /* no vararg arguments */
    if (b < 0) {  /* B == 0? */
      b = n;  /* get all var. arguments */
      Protect(luaD_checkstack(L, n));
      ra = RA(i);  /* previous call may change the stack */
      L->top = ra + n;
    }
    for (j = 0; j < b && j < n; j++)
      setobjs2s(L, ra + j, base - n + j);
    for (; j < b; j++)  /* complete required results with nil */
      setnilvalue(ra + j);
  }
  
  // [190]	SETLIST  	0 0 1	; 1
  label_2: {
    Instruction i = 0x0000402b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [191]	GETUPVAL 	1 0	; jsArray_to_list
  label_3: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [191]	MOVE     	2 0
  label_4: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [191]	TAILCALL 	1 2 0
  label_5: {
    Instruction i = 0x01000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [191]	RETURN   	1 0
  label_6: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [192]	RETURN   	0 1
  label_7: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 193
// lastlinedefined = 208
// what = Lua
static int zz_magic_function_43 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [194]	MOVE     	1 0
  label_0: {
    Instruction i = 0x00000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [195]	NEWTABLE 	2 0 0
  label_1: {
    Instruction i = 0x0000008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [196]	GETUPVAL 	3 0	; just_p
  label_2: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [196]	MOVE     	4 1
  label_3: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [196]	CALL     	3 2 2
  label_4: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [196]	TEST     	3 0
  label_5: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_7;
    }
  }
  
  // [196]	JMP      	0 9	; to 16
  label_6: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [197]	GETUPVAL 	3 1	; __TS__ArrayPush
  label_7: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [197]	MOVE     	4 2
  label_8: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [197]	MOVE     	5 1
  label_9: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [197]	CALL     	3 3 1
  label_10: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [198]	GETUPVAL 	3 2	; un_just
  label_11: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [198]	MOVE     	4 1
  label_12: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [198]	CALL     	3 2 2
  label_13: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [198]	MOVE     	1 3
  label_14: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [198]	JMP      	0 -14	; to 2
  label_15: {
    Instruction i = 0x7ffc401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_2;
  }
  
  // [201]	LOADK    	3 -1	; 0
  label_16: {
    Instruction i = 0x000000c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [202]	LEN      	4 2
  label_17: {
    Instruction i = 0x0100011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [202]	LT       	0 3 4
  label_18: {
    Instruction i = 0x01810020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [202]	JMP      	0 7	; to 27
  label_19: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_27;
  }
  
  // [203]	GETUPVAL 	4 3	; lang_set_do
  label_20: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [203]	ADD      	5 3 -2	; - 1
  label_21: {
    Instruction i = 0x01c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [203]	GETTABLE 	5 2 5
  label_22: {
    Instruction i = 0x01014147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [203]	MOVE     	6 1
  label_23: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [203]	CALL     	4 3 1
  label_24: {
    Instruction i = 0x01804124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [204]	ADD      	3 3 -2	; - 1
  label_25: {
    Instruction i = 0x01c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [204]	JMP      	0 -10	; to 17
  label_26: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_17;
  }
  
  // [207]	RETURN   	1 2
  label_27: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [208]	RETURN   	0 1
  label_28: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 209
// lastlinedefined = 211
// what = Lua
static int zz_magic_function_44 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [210]	GETUPVAL 	1 0	; just_p
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [210]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [210]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [210]	TEST     	1 1
  label_3: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [210]	JMP      	0 18	; to 23
  label_4: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [210]	GETUPVAL 	1 1	; delay_evaluate_p
  label_5: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [210]	MOVE     	2 0
  label_6: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [210]	CALL     	1 2 2
  label_7: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [210]	TEST     	1 1
  label_8: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [210]	JMP      	0 13	; to 23
  label_9: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [210]	GETUPVAL 	1 2	; delay_builtin_form_p
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [210]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [210]	CALL     	1 2 2
  label_12: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [210]	TEST     	1 1
  label_13: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_15;
    }
  }
  
  // [210]	JMP      	0 8	; to 23
  label_14: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [210]	GETUPVAL 	1 3	; delay_builtin_func_p
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [210]	MOVE     	2 0
  label_16: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [210]	CALL     	1 2 2
  label_17: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [210]	TEST     	1 1
  label_18: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [210]	JMP      	0 3	; to 23
  label_19: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [210]	GETUPVAL 	1 4	; delay_apply_p
  label_20: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [210]	MOVE     	2 0
  label_21: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [210]	CALL     	1 2 2
  label_22: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [210]	RETURN   	1 2
  label_23: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [211]	RETURN   	0 1
  label_24: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 212
// lastlinedefined = 338
// what = Lua
static int zz_magic_function_45 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [213]	EQ       	0 1 -1	; - nil
  label_0: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [213]	JMP      	0 2	; to 4
  label_1: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [214]	NEWTABLE 	3 0 0
  label_2: {
    Instruction i = 0x000000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [214]	MOVE     	1 3
  label_3: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [216]	EQ       	0 2 -1	; - nil
  label_4: {
    Instruction i = 0x0140001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_6;
    }
  }
  
  // [216]	JMP      	0 5	; to 11
  label_5: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_11;
  }
  
  // [217]	NEWTABLE 	3 2 0
  label_6: {
    Instruction i = 0x010000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [217]	LOADBOOL 	4 0 0
  label_7: {
    Instruction i = 0x00000103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_9;
    }
  }
  
  // [217]	LOADBOOL 	5 0 0
  label_8: {
    Instruction i = 0x00000143;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_10;
    }
  }
  
  // [217]	SETLIST  	3 2 1	; 1
  label_9: {
    Instruction i = 0x010040eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [217]	MOVE     	2 3
  label_10: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [219]	NEWTABLE 	3 0 0
  label_11: {
    Instruction i = 0x000000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [220]	MOVE     	4 0
  label_12: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [221]	NEWTABLE 	5 0 0
  label_13: {
    Instruction i = 0x0000014b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [222]	LOADNIL  	6 0
  label_14: {
    Instruction i = 0x00000184;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [234]	CLOSURE  	7 0	; 0x559f63795350
  label_15: {
    Instruction i = 0x000001ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [234]	MOVE     	6 7
  label_16: {
    Instruction i = 0x03800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [235]	LOADNIL  	7 0
  label_17: {
    Instruction i = 0x000001c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [257]	CLOSURE  	8 1	; 0x559f63795830
  label_18: {
    Instruction i = 0x0000422c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [257]	MOVE     	7 8
  label_19: {
    Instruction i = 0x040001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [258]	LOADNIL  	8 0
  label_20: {
    Instruction i = 0x00000204;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [268]	CLOSURE  	9 2	; 0x559f6379c530
  label_21: {
    Instruction i = 0x0000826c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [268]	MOVE     	8 9
  label_22: {
    Instruction i = 0x04800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [269]	GETUPVAL 	9 2	; any_delay_just_p
  label_23: {
    Instruction i = 0x01000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [269]	MOVE     	10 4
  label_24: {
    Instruction i = 0x02000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [269]	CALL     	9 2 2
  label_25: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [269]	TEST     	9 0
  label_26: {
    Instruction i = 0x00000262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [269]	JMP      	0 207	; to 235
  label_27: {
    Instruction i = 0x8033801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_235;
  }
  
  // [270]	GETUPVAL 	9 5	; simple_print
  label_28: {
    Instruction i = 0x02800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [270]	MOVE     	10 4
  label_29: {
    Instruction i = 0x02000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [270]	CALL     	9 2 2
  label_30: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [271]	GETTABLE 	10 1 9
  label_31: {
    Instruction i = 0x00824287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [271]	EQ       	0 10 -2	; - true
  label_32: {
    Instruction i = 0x0540401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_34;
    }
  }
  
  // [271]	JMP      	0 3	; to 37
  label_33: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_37;
  }
  
  // [272]	MOVE     	10 6
  label_34: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [272]	TAILCALL 	10 1 0
  label_35: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [272]	RETURN   	10 0
  label_36: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [274]	GETTABLE 	10 3 9
  label_37: {
    Instruction i = 0x01824287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [274]	EQ       	0 10 -2	; - true
  label_38: {
    Instruction i = 0x0540401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [274]	JMP      	0 185	; to 225
  label_39: {
    Instruction i = 0x802e001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_225;
  }
  
  // [275]	SETTABLE 	2 -3 -2	; 1 true
  label_40: {
    Instruction i = 0x8140408a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [276]	GETUPVAL 	10 6	; delay_evaluate_p
  label_41: {
    Instruction i = 0x03000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [276]	MOVE     	11 4
  label_42: {
    Instruction i = 0x020002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [276]	CALL     	10 2 2
  label_43: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [276]	TEST     	10 0
  label_44: {
    Instruction i = 0x000002a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_46;
    }
  }
  
  // [276]	JMP      	0 4	; to 50
  label_45: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_50;
  }
  
  // [277]	MOVE     	10 6
  label_46: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [277]	TAILCALL 	10 1 0
  label_47: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [277]	RETURN   	10 0
  label_48: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [277]	JMP      	0 172	; to 222
  label_49: {
    Instruction i = 0x802ac01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [278]	GETUPVAL 	10 7	; delay_builtin_func_p
  label_50: {
    Instruction i = 0x03800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [278]	MOVE     	11 4
  label_51: {
    Instruction i = 0x020002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [278]	CALL     	10 2 2
  label_52: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [278]	TEST     	10 0
  label_53: {
    Instruction i = 0x000002a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_55;
    }
  }
  
  // [278]	JMP      	0 150	; to 205
  label_54: {
    Instruction i = 0x8025401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_205;
  }
  
  // [279]	GETUPVAL 	10 8	; delay_builtin_func_f
  label_55: {
    Instruction i = 0x04000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [279]	MOVE     	11 4
  label_56: {
    Instruction i = 0x020002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [279]	CALL     	10 2 2
  label_57: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [280]	GETUPVAL 	11 9	; delay_builtin_func_xs
  label_58: {
    Instruction i = 0x048002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [280]	MOVE     	12 4
  label_59: {
    Instruction i = 0x02000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [280]	CALL     	11 2 2
  label_60: {
    Instruction i = 0x010082e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [281]	NEWTABLE 	12 11 0
  label_61: {
    Instruction i = 0x0580030b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [281]	GETUPVAL 	13 10	; data_name_function_builtin_systemName
  label_62: {
    Instruction i = 0x05000345;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	14 11	; data_list_function_builtin_systemName
  label_63: {
    Instruction i = 0x05800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	15 12	; data_p_function_builtin_systemName
  label_64: {
    Instruction i = 0x060003c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	16 13	; error_name_function_builtin_systemName
  label_65: {
    Instruction i = 0x06800405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	17 14	; error_list_function_builtin_systemName
  label_66: {
    Instruction i = 0x07000445;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	18 15	; error_p_function_builtin_systemName
  label_67: {
    Instruction i = 0x07800485;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	19 16	; construction_p_function_builtin_systemName
  label_68: {
    Instruction i = 0x080004c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	20 17	; construction_head_function_builtin_systemName
  label_69: {
    Instruction i = 0x08800505;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	21 18	; construction_tail_function_builtin_systemName
  label_70: {
    Instruction i = 0x09000545;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	22 19	; symbol_p_function_builtin_systemName
  label_71: {
    Instruction i = 0x09800585;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	GETUPVAL 	23 20	; null_p_function_builtin_systemName
  label_72: {
    Instruction i = 0x0a0005c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [281]	SETLIST  	12 11 1	; 1
  label_73: {
    Instruction i = 0x0580432b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [282]	LOADBOOL 	13 0 0
  label_74: {
    Instruction i = 0x00000343;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_76;
    }
  }
  
  // [284]	LOADK    	14 -4	; 0
  label_75: {
    Instruction i = 0x0000c381;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [285]	LEN      	15 12
  label_76: {
    Instruction i = 0x060003dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [285]	LT       	0 14 15
  label_77: {
    Instruction i = 0x0703c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_79;
    }
  }
  
  // [285]	JMP      	0 10	; to 89
  label_78: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_89;
  }
  
  // [286]	GETUPVAL 	15 21	; jsbool_equal_p
  label_79: {
    Instruction i = 0x0a8003c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [286]	ADD      	16 14 -3	; - 1
  label_80: {
    Instruction i = 0x0740840d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [286]	GETTABLE 	16 12 16
  label_81: {
    Instruction i = 0x06040407;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [286]	MOVE     	17 10
  label_82: {
    Instruction i = 0x05000440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [286]	CALL     	15 3 2
  label_83: {
    Instruction i = 0x018083e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [286]	TEST     	15 0
  label_84: {
    Instruction i = 0x000003e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_86;
    }
  }
  
  // [286]	JMP      	0 1	; to 87
  label_85: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_87;
  }
  
  // [287]	LOADBOOL 	13 1 0
  label_86: {
    Instruction i = 0x00800343;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_88;
    }
  }
  
  // [289]	ADD      	14 14 -3	; - 1
  label_87: {
    Instruction i = 0x0740838d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [289]	JMP      	0 -13	; to 76
  label_88: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_76;
  }
  
  // [292]	TEST     	13 0
  label_89: {
    Instruction i = 0x00000362;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_91;
    }
  }
  
  // [292]	JMP      	0 36	; to 127
  label_90: {
    Instruction i = 0x8008c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [293]	GETUPVAL 	14 22	; ASSERT
  label_91: {
    Instruction i = 0x0b000385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [293]	LEN      	15 11
  label_92: {
    Instruction i = 0x058003dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [293]	EQ       	1 15 -3	; - 1
  label_93: {
    Instruction i = 0x07c0805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_95;
    }
  }
  
  // [293]	JMP      	0 1	; to 96
  label_94: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_96;
  }
  
  // [293]	LOADBOOL 	15 0 1
  label_95: {
    Instruction i = 0x000043c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_97;
    }
  }
  
  // [293]	LOADBOOL 	15 1 0
  label_96: {
    Instruction i = 0x008003c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_98;
    }
  }
  
  // [293]	CALL     	14 2 1
  label_97: {
    Instruction i = 0x010043a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [294]	GETUPVAL 	14 22	; ASSERT
  label_98: {
    Instruction i = 0x0b000385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [294]	GETTABLE 	15 2 -5	; 2
  label_99: {
    Instruction i = 0x014103c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [294]	EQ       	1 15 -6	; - false
  label_100: {
    Instruction i = 0x07c1405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_102;
    }
  }
  
  // [294]	JMP      	0 1	; to 103
  label_101: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_103;
  }
  
  // [294]	LOADBOOL 	15 0 1
  label_102: {
    Instruction i = 0x000043c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_104;
    }
  }
  
  // [294]	LOADBOOL 	15 1 0
  label_103: {
    Instruction i = 0x008003c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_105;
    }
  }
  
  // [294]	CALL     	14 2 1
  label_104: {
    Instruction i = 0x010043a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [295]	GETUPVAL 	14 3	; force_all
  label_105: {
    Instruction i = 0x01800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [295]	GETTABLE 	15 11 -3	; 1
  label_106: {
    Instruction i = 0x05c083c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [295]	MOVE     	16 8
  label_107: {
    Instruction i = 0x04000400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [295]	CALL     	16 1 2
  label_108: {
    Instruction i = 0x00808424;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [295]	MOVE     	17 2
  label_109: {
    Instruction i = 0x01000440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [295]	CALL     	14 4 2
  label_110: {
    Instruction i = 0x020083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [296]	GETTABLE 	15 2 -5	; 2
  label_111: {
    Instruction i = 0x014103c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [296]	TEST     	15 0
  label_112: {
    Instruction i = 0x000003e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_114;
    }
  }
  
  // [296]	JMP      	0 10	; to 124
  label_113: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_124;
  }
  
  // [297]	MOVE     	15 7
  label_114: {
    Instruction i = 0x038003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [297]	GETUPVAL 	16 23	; builtin_func_apply
  label_115: {
    Instruction i = 0x0b800405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [297]	MOVE     	17 10
  label_116: {
    Instruction i = 0x05000440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [297]	NEWTABLE 	18 1 0
  label_117: {
    Instruction i = 0x0080048b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [297]	MOVE     	19 14
  label_118: {
    Instruction i = 0x070004c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [297]	SETLIST  	18 1 1	; 1
  label_119: {
    Instruction i = 0x008044ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [297]	CALL     	16 3 0
  label_120: {
    Instruction i = 0x01800424;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [297]	TAILCALL 	15 0 0
  label_121: {
    Instruction i = 0x000003e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [297]	RETURN   	15 0
  label_122: {
    Instruction i = 0x000003e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [297]	JMP      	0 3	; to 127
  label_123: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [299]	GETUPVAL 	15 24	; ERROR
  label_124: {
    Instruction i = 0x0c0003c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [299]	TAILCALL 	15 1 0
  label_125: {
    Instruction i = 0x008003e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [299]	RETURN   	15 0
  label_126: {
    Instruction i = 0x000003e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [302]	GETUPVAL 	14 21	; jsbool_equal_p
  label_127: {
    Instruction i = 0x0a800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [302]	MOVE     	15 10
  label_128: {
    Instruction i = 0x050003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [302]	GETUPVAL 	16 25	; equal_p_function_builtin_systemName
  label_129: {
    Instruction i = 0x0c800405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [302]	CALL     	14 3 2
  label_130: {
    Instruction i = 0x018083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [302]	TEST     	14 0
  label_131: {
    Instruction i = 0x000003a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_133;
    }
  }
  
  // [302]	JMP      	0 4	; to 137
  label_132: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_137;
  }
  
  // [303]	MOVE     	14 6
  label_133: {
    Instruction i = 0x03000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [303]	TAILCALL 	14 1 0
  label_134: {
    Instruction i = 0x008003a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [303]	RETURN   	14 0
  label_135: {
    Instruction i = 0x000003a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [303]	JMP      	0 64	; to 201
  label_136: {
    Instruction i = 0x800fc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_201;
  }
  
  // [304]	GETUPVAL 	14 21	; jsbool_equal_p
  label_137: {
    Instruction i = 0x0a800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [304]	MOVE     	15 10
  label_138: {
    Instruction i = 0x050003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [304]	GETUPVAL 	16 26	; apply_function_builtin_systemName
  label_139: {
    Instruction i = 0x0d000405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [304]	CALL     	14 3 2
  label_140: {
    Instruction i = 0x018083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [304]	TEST     	14 0
  label_141: {
    Instruction i = 0x000003a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_143;
    }
  }
  
  // [304]	JMP      	0 4	; to 147
  label_142: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_147;
  }
  
  // [305]	MOVE     	14 6
  label_143: {
    Instruction i = 0x03000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [305]	TAILCALL 	14 1 0
  label_144: {
    Instruction i = 0x008003a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [305]	RETURN   	14 0
  label_145: {
    Instruction i = 0x000003a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [305]	JMP      	0 54	; to 201
  label_146: {
    Instruction i = 0x800d401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_201;
  }
  
  // [306]	GETUPVAL 	14 21	; jsbool_equal_p
  label_147: {
    Instruction i = 0x0a800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [306]	MOVE     	15 10
  label_148: {
    Instruction i = 0x050003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [306]	GETUPVAL 	16 27	; evaluate_function_builtin_systemName
  label_149: {
    Instruction i = 0x0d800405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [306]	CALL     	14 3 2
  label_150: {
    Instruction i = 0x018083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [306]	TEST     	14 0
  label_151: {
    Instruction i = 0x000003a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_153;
    }
  }
  
  // [306]	JMP      	0 4	; to 157
  label_152: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_157;
  }
  
  // [307]	MOVE     	14 6
  label_153: {
    Instruction i = 0x03000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [307]	TAILCALL 	14 1 0
  label_154: {
    Instruction i = 0x008003a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [307]	RETURN   	14 0
  label_155: {
    Instruction i = 0x000003a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [307]	JMP      	0 44	; to 201
  label_156: {
    Instruction i = 0x800ac01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_201;
  }
  
  // [308]	GETUPVAL 	14 21	; jsbool_equal_p
  label_157: {
    Instruction i = 0x0a800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [308]	MOVE     	15 10
  label_158: {
    Instruction i = 0x050003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [308]	GETUPVAL 	16 28	; if_function_builtin_systemName
  label_159: {
    Instruction i = 0x0e000405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [308]	CALL     	14 3 2
  label_160: {
    Instruction i = 0x018083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [308]	TEST     	14 0
  label_161: {
    Instruction i = 0x000003a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_163;
    }
  }
  
  // [308]	JMP      	0 38	; to 201
  label_162: {
    Instruction i = 0x8009401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_201;
  }
  
  // [309]	GETUPVAL 	14 22	; ASSERT
  label_163: {
    Instruction i = 0x0b000385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [309]	LEN      	15 11
  label_164: {
    Instruction i = 0x058003dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [309]	EQ       	1 15 -7	; - 3
  label_165: {
    Instruction i = 0x07c1805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_167;
    }
  }
  
  // [309]	JMP      	0 1	; to 168
  label_166: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_168;
  }
  
  // [309]	LOADBOOL 	15 0 1
  label_167: {
    Instruction i = 0x000043c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_169;
    }
  }
  
  // [309]	LOADBOOL 	15 1 0
  label_168: {
    Instruction i = 0x008003c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_170;
    }
  }
  
  // [309]	CALL     	14 2 1
  label_169: {
    Instruction i = 0x010043a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [310]	GETUPVAL 	14 22	; ASSERT
  label_170: {
    Instruction i = 0x0b000385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [310]	GETTABLE 	15 2 -5	; 2
  label_171: {
    Instruction i = 0x014103c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [310]	EQ       	1 15 -6	; - false
  label_172: {
    Instruction i = 0x07c1405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_174;
    }
  }
  
  // [310]	JMP      	0 1	; to 175
  label_173: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_175;
  }
  
  // [310]	LOADBOOL 	15 0 1
  label_174: {
    Instruction i = 0x000043c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_176;
    }
  }
  
  // [310]	LOADBOOL 	15 1 0
  label_175: {
    Instruction i = 0x008003c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_177;
    }
  }
  
  // [310]	CALL     	14 2 1
  label_176: {
    Instruction i = 0x010043a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [311]	GETUPVAL 	14 3	; force_all
  label_177: {
    Instruction i = 0x01800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [311]	GETTABLE 	15 11 -3	; 1
  label_178: {
    Instruction i = 0x05c083c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [311]	MOVE     	16 8
  label_179: {
    Instruction i = 0x04000400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [311]	CALL     	16 1 2
  label_180: {
    Instruction i = 0x00808424;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [311]	MOVE     	17 2
  label_181: {
    Instruction i = 0x01000440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [311]	CALL     	14 4 2
  label_182: {
    Instruction i = 0x020083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [312]	GETTABLE 	15 2 -5	; 2
  label_183: {
    Instruction i = 0x014103c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [312]	TEST     	15 0
  label_184: {
    Instruction i = 0x000003e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_186;
    }
  }
  
  // [312]	JMP      	0 12	; to 198
  label_185: {
    Instruction i = 0x8002c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_198;
  }
  
  // [313]	MOVE     	15 7
  label_186: {
    Instruction i = 0x038003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [313]	GETUPVAL 	16 23	; builtin_func_apply
  label_187: {
    Instruction i = 0x0b800405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [313]	GETUPVAL 	17 28	; if_function_builtin_systemName
  label_188: {
    Instruction i = 0x0e000445;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [313]	NEWTABLE 	18 3 0
  label_189: {
    Instruction i = 0x0180048b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [313]	MOVE     	19 14
  label_190: {
    Instruction i = 0x070004c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [313]	GETTABLE 	20 11 -5	; 2
  label_191: {
    Instruction i = 0x05c10507;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [313]	GETTABLE 	21 11 -7	; 3
  label_192: {
    Instruction i = 0x05c18547;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [313]	SETLIST  	18 3 1	; 1
  label_193: {
    Instruction i = 0x018044ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [313]	CALL     	16 3 0
  label_194: {
    Instruction i = 0x01800424;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [313]	TAILCALL 	15 0 0
  label_195: {
    Instruction i = 0x000003e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [313]	RETURN   	15 0
  label_196: {
    Instruction i = 0x000003e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [313]	JMP      	0 3	; to 201
  label_197: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_201;
  }
  
  // [315]	GETUPVAL 	15 24	; ERROR
  label_198: {
    Instruction i = 0x0c0003c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [315]	TAILCALL 	15 1 0
  label_199: {
    Instruction i = 0x008003e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [315]	RETURN   	15 0
  label_200: {
    Instruction i = 0x000003e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [318]	GETUPVAL 	14 24	; ERROR
  label_201: {
    Instruction i = 0x0c000385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [318]	TAILCALL 	14 1 0
  label_202: {
    Instruction i = 0x008003a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [318]	RETURN   	14 0
  label_203: {
    Instruction i = 0x000003a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [318]	JMP      	0 17	; to 222
  label_204: {
    Instruction i = 0x8004001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [319]	GETUPVAL 	10 29	; delay_builtin_form_p
  label_205: {
    Instruction i = 0x0e800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [319]	MOVE     	11 4
  label_206: {
    Instruction i = 0x020002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [319]	CALL     	10 2 2
  label_207: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [319]	TEST     	10 0
  label_208: {
    Instruction i = 0x000002a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_210;
    }
  }
  
  // [319]	JMP      	0 4	; to 214
  label_209: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_214;
  }
  
  // [320]	MOVE     	10 6
  label_210: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [320]	TAILCALL 	10 1 0
  label_211: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [320]	RETURN   	10 0
  label_212: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [320]	JMP      	0 8	; to 222
  label_213: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [321]	GETUPVAL 	10 30	; delay_apply_p
  label_214: {
    Instruction i = 0x0f000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [321]	MOVE     	11 4
  label_215: {
    Instruction i = 0x020002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [321]	CALL     	10 2 2
  label_216: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [321]	TEST     	10 0
  label_217: {
    Instruction i = 0x000002a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_219;
    }
  }
  
  // [321]	JMP      	0 3	; to 222
  label_218: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [322]	MOVE     	10 6
  label_219: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [322]	TAILCALL 	10 1 0
  label_220: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [322]	RETURN   	10 0
  label_221: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [324]	GETUPVAL 	10 24	; ERROR
  label_222: {
    Instruction i = 0x0c000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [324]	TAILCALL 	10 1 0
  label_223: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [324]	RETURN   	10 0
  label_224: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [326]	SETTABLE 	3 9 -2	; - true
  label_225: {
    Instruction i = 0x04c040ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [327]	GETUPVAL 	10 31	; __TS__ArrayPush
  label_226: {
    Instruction i = 0x0f800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [327]	MOVE     	11 5
  label_227: {
    Instruction i = 0x028002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [327]	MOVE     	12 4
  label_228: {
    Instruction i = 0x02000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [327]	CALL     	10 3 1
  label_229: {
    Instruction i = 0x018042a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [328]	GETUPVAL 	10 32	; force1
  label_230: {
    Instruction i = 0x10000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [328]	MOVE     	11 4
  label_231: {
    Instruction i = 0x020002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [328]	CALL     	10 2 2
  label_232: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [328]	MOVE     	4 10
  label_233: {
    Instruction i = 0x05000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [328]	JMP      	0 -212	; to 23
  label_234: {
    Instruction i = 0x7fcac01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [331]	LOADK    	9 -4	; 0
  label_235: {
    Instruction i = 0x0000c241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [332]	LEN      	10 5
  label_236: {
    Instruction i = 0x0280029c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [332]	LT       	0 9 10
  label_237: {
    Instruction i = 0x04828020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_239;
    }
  }
  
  // [332]	JMP      	0 7	; to 246
  label_238: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_246;
  }
  
  // [333]	GETUPVAL 	10 0	; lang_set_do
  label_239: {
    Instruction i = 0x00000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [333]	ADD      	11 9 -3	; - 1
  label_240: {
    Instruction i = 0x04c082cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [333]	GETTABLE 	11 5 11
  label_241: {
    Instruction i = 0x0282c2c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [333]	MOVE     	12 4
  label_242: {
    Instruction i = 0x02000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [333]	CALL     	10 3 1
  label_243: {
    Instruction i = 0x018042a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [334]	ADD      	9 9 -3	; - 1
  label_244: {
    Instruction i = 0x04c0824d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [334]	JMP      	0 -10	; to 236
  label_245: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_236;
  }
  
  // [337]	RETURN   	4 2
  label_246: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [338]	RETURN   	0 1
  label_247: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 223
// lastlinedefined = 234
// what = Lua
static int zz_magic_function_46 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [224]	SETTABUP 	0 -1 -2	; ref_novalue_replace 2 true
  label_0: {
    Instruction i = 0x80404008;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *upval = cl->upvals[GETARG_A(i)]->v;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, upval, rb, rc);
  }
  
  // [225]	GETUPVAL 	0 1	; lang_set_do
  label_1: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [225]	GETUPVAL 	1 2	; x
  label_2: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [225]	GETUPVAL 	2 3	; the_world_stopped_v
  label_3: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [225]	CALL     	0 3 1
  label_4: {
    Instruction i = 0x01804024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [227]	LOADK    	0 -3	; 0
  label_5: {
    Instruction i = 0x00008001;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [228]	GETUPVAL 	1 4	; xs
  label_6: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [228]	LEN      	1 1
  label_7: {
    Instruction i = 0x0080005c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [228]	LT       	0 0 1
  label_8: {
    Instruction i = 0x00004020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [228]	JMP      	0 7	; to 17
  label_9: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_17;
  }
  
  // [229]	GETUPVAL 	1 1	; lang_set_do
  label_10: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [229]	ADD      	2 0 -4	; - 1
  label_11: {
    Instruction i = 0x0040c08d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [229]	GETTABUP 	2 4 2	; xs
  label_12: {
    Instruction i = 0x02008086;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [229]	GETUPVAL 	3 3	; the_world_stopped_v
  label_13: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [229]	CALL     	1 3 1
  label_14: {
    Instruction i = 0x01804064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [230]	ADD      	0 0 -4	; - 1
  label_15: {
    Instruction i = 0x0040c00d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [230]	JMP      	0 -11	; to 6
  label_16: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [233]	GETUPVAL 	0 3	; the_world_stopped_v
  label_17: {
    Instruction i = 0x01800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [233]	RETURN   	0 2
  label_18: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [234]	RETURN   	0 1
  label_19: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 236
// lastlinedefined = 257
// what = Lua
static int zz_magic_function_47 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [237]	GETUPVAL 	1 0	; lang_set_do
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [237]	GETUPVAL 	2 1	; x
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [237]	MOVE     	3 0
  label_2: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [237]	CALL     	1 3 1
  label_3: {
    Instruction i = 0x01804064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [239]	LOADK    	1 -1	; 0
  label_4: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [240]	GETUPVAL 	2 2	; xs
  label_5: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [240]	LEN      	2 2
  label_6: {
    Instruction i = 0x0100009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [240]	LT       	0 1 2
  label_7: {
    Instruction i = 0x00808020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [240]	JMP      	0 7	; to 16
  label_8: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [241]	GETUPVAL 	2 0	; lang_set_do
  label_9: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [241]	ADD      	3 1 -2	; - 1
  label_10: {
    Instruction i = 0x00c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [241]	GETTABUP 	3 2 3	; xs
  label_11: {
    Instruction i = 0x0100c0c6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [241]	MOVE     	4 0
  label_12: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [241]	CALL     	2 3 1
  label_13: {
    Instruction i = 0x018040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [242]	ADD      	1 1 -2	; - 1
  label_14: {
    Instruction i = 0x00c0404d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [242]	JMP      	0 -11	; to 5
  label_15: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [245]	GETUPVAL 	1 3	; any_delay_just_p
  label_16: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [245]	MOVE     	2 0
  label_17: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [245]	CALL     	1 2 2
  label_18: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [245]	TEST     	1 0
  label_19: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [245]	JMP      	0 20	; to 41
  label_20: {
    Instruction i = 0x8004c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_41;
  }
  
  // [246]	GETUPVAL 	1 4	; force_all
  label_21: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [246]	MOVE     	2 0
  label_22: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [246]	CALL     	1 2 2
  label_23: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [246]	MOVE     	0 1
  label_24: {
    Instruction i = 0x00800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [247]	GETUPVAL 	1 0	; lang_set_do
  label_25: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [247]	GETUPVAL 	2 1	; x
  label_26: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [247]	MOVE     	3 0
  label_27: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [247]	CALL     	1 3 1
  label_28: {
    Instruction i = 0x01804064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [249]	LOADK    	1 -1	; 0
  label_29: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [250]	GETUPVAL 	2 2	; xs
  label_30: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [250]	LEN      	2 2
  label_31: {
    Instruction i = 0x0100009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [250]	LT       	0 1 2
  label_32: {
    Instruction i = 0x00808020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_34;
    }
  }
  
  // [250]	JMP      	0 7	; to 41
  label_33: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_41;
  }
  
  // [251]	GETUPVAL 	2 0	; lang_set_do
  label_34: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [251]	ADD      	3 1 -2	; - 1
  label_35: {
    Instruction i = 0x00c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [251]	GETTABUP 	3 2 3	; xs
  label_36: {
    Instruction i = 0x0100c0c6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [251]	MOVE     	4 0
  label_37: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [251]	CALL     	2 3 1
  label_38: {
    Instruction i = 0x018040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [252]	ADD      	1 1 -2	; - 1
  label_39: {
    Instruction i = 0x00c0404d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [252]	JMP      	0 -11	; to 30
  label_40: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_30;
  }
  
  // [256]	RETURN   	0 2
  label_41: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [257]	RETURN   	0 1
  label_42: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 259
// lastlinedefined = 268
// what = Lua
static int zz_magic_function_48 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [260]	NEWTABLE 	0 0 0
  label_0: {
    Instruction i = 0x0000000b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [261]	GETTABUP 	1 0 -1	; _ENV "pairs"
  label_1: {
    Instruction i = 0x00400046;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [261]	GETUPVAL 	2 1	; history
  label_2: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [261]	CALL     	1 2 4
  label_3: {
    Instruction i = 0x01010064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [261]	JMP      	0 1	; to 6
  label_4: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [262]	SETTABLE 	0 4 -2	; - true
  label_5: {
    Instruction i = 0x0240400a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [261]	TFORCALL 	1 1
  label_6: {
    Instruction i = 0x00004069;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId cb = ra + 3;  /* call base */
    setobjs2s(L, cb+2, ra+2);
    setobjs2s(L, cb+1, ra+1);
    setobjs2s(L, cb, ra);
    L->top = cb + 3;  /* func. + 2 args (state and index) */
    Protect(luaD_call(L, cb, GETARG_C(i)));
    L->top = ci->top;
  }
  
  // [261]	TFORLOOP 	3 -3	; to 5
  label_7: {
    Instruction i = 0x7fff00ea;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (!ttisnil(ra + 1)) {  /* continue loop? */
      setobjs2s(L, ra, ra + 1);  /* save control variable */
      ci->u.l.savedpc += GETARG_sBx(i);  /* jump back */
      goto label_5; /* jump back */
    }
  }
  
  // [264]	GETTABUP 	1 0 -1	; _ENV "pairs"
  label_8: {
    Instruction i = 0x00400046;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [264]	GETUPVAL 	2 2	; parents_history
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [264]	CALL     	1 2 4
  label_10: {
    Instruction i = 0x01010064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [264]	JMP      	0 1	; to 13
  label_11: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [265]	SETTABLE 	0 4 -2	; - true
  label_12: {
    Instruction i = 0x0240400a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [264]	TFORCALL 	1 1
  label_13: {
    Instruction i = 0x00004069;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId cb = ra + 3;  /* call base */
    setobjs2s(L, cb+2, ra+2);
    setobjs2s(L, cb+1, ra+1);
    setobjs2s(L, cb, ra);
    L->top = cb + 3;  /* func. + 2 args (state and index) */
    Protect(luaD_call(L, cb, GETARG_C(i)));
    L->top = ci->top;
  }
  
  // [264]	TFORLOOP 	3 -3	; to 12
  label_14: {
    Instruction i = 0x7fff00ea;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (!ttisnil(ra + 1)) {  /* continue loop? */
      setobjs2s(L, ra, ra + 1);  /* save control variable */
      ci->u.l.savedpc += GETARG_sBx(i);  /* jump back */
      goto label_12; /* jump back */
    }
  }
  
  // [267]	RETURN   	0 2
  label_15: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [268]	RETURN   	0 1
  label_16: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 339
// lastlinedefined = 357
// what = Lua
static int zz_magic_function_49 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [340]	GETUPVAL 	1 0	; un_just_all
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [340]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [340]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [341]	LOADNIL  	2 0
  label_3: {
    Instruction i = 0x00000084;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [342]	GETUPVAL 	3 1	; ASSERT
  label_4: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [342]	GETUPVAL 	4 2	; just_p
  label_5: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [342]	MOVE     	5 1
  label_6: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [342]	CALL     	4 2 2
  label_7: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [342]	NOT      	4 4
  label_8: {
    Instruction i = 0x0200011b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RB(i);
    int res = l_isfalse(rb);  /* next assignment may change this value */
    setbvalue(ra, res);
  }
  
  // [342]	CALL     	3 2 1
  label_9: {
    Instruction i = 0x010040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [343]	GETUPVAL 	3 3	; delay_evaluate_p
  label_10: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [343]	MOVE     	4 1
  label_11: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [343]	CALL     	3 2 2
  label_12: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [343]	TEST     	3 0
  label_13: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_15;
    }
  }
  
  // [343]	JMP      	0 10	; to 25
  label_14: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [344]	GETUPVAL 	3 4	; real_evaluate
  label_15: {
    Instruction i = 0x020000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [344]	GETUPVAL 	4 5	; delay_evaluate_env
  label_16: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [344]	MOVE     	5 1
  label_17: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [344]	CALL     	4 2 2
  label_18: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [344]	GETUPVAL 	5 6	; delay_evaluate_x
  label_19: {
    Instruction i = 0x03000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [344]	MOVE     	6 1
  label_20: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [344]	CALL     	5 2 0
  label_21: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [344]	CALL     	3 0 2
  label_22: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [344]	MOVE     	2 3
  label_23: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [344]	JMP      	0 49	; to 74
  label_24: {
    Instruction i = 0x800c001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_74;
  }
  
  // [345]	GETUPVAL 	3 7	; delay_builtin_form_p
  label_25: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [345]	MOVE     	4 1
  label_26: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [345]	CALL     	3 2 2
  label_27: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [345]	TEST     	3 0
  label_28: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_30;
    }
  }
  
  // [345]	JMP      	0 13	; to 43
  label_29: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [346]	GETUPVAL 	3 8	; real_builtin_form_apply
  label_30: {
    Instruction i = 0x040000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [346]	GETUPVAL 	4 9	; delay_builtin_form_env
  label_31: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [346]	MOVE     	5 1
  label_32: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [346]	CALL     	4 2 2
  label_33: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [346]	GETUPVAL 	5 10	; delay_builtin_form_f
  label_34: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [346]	MOVE     	6 1
  label_35: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [346]	CALL     	5 2 2
  label_36: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [346]	GETUPVAL 	6 11	; delay_builtin_form_xs
  label_37: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [346]	MOVE     	7 1
  label_38: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [346]	CALL     	6 2 0
  label_39: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [346]	CALL     	3 0 2
  label_40: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [346]	MOVE     	2 3
  label_41: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [346]	JMP      	0 31	; to 74
  label_42: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_74;
  }
  
  // [347]	GETUPVAL 	3 12	; delay_builtin_func_p
  label_43: {
    Instruction i = 0x060000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [347]	MOVE     	4 1
  label_44: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [347]	CALL     	3 2 2
  label_45: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [347]	TEST     	3 0
  label_46: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_48;
    }
  }
  
  // [347]	JMP      	0 10	; to 58
  label_47: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_58;
  }
  
  // [348]	GETUPVAL 	3 13	; real_builtin_func_apply
  label_48: {
    Instruction i = 0x068000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [348]	GETUPVAL 	4 14	; delay_builtin_func_f
  label_49: {
    Instruction i = 0x07000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [348]	MOVE     	5 1
  label_50: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [348]	CALL     	4 2 2
  label_51: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [348]	GETUPVAL 	5 15	; delay_builtin_func_xs
  label_52: {
    Instruction i = 0x07800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [348]	MOVE     	6 1
  label_53: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [348]	CALL     	5 2 0
  label_54: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [348]	CALL     	3 0 2
  label_55: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [348]	MOVE     	2 3
  label_56: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [348]	JMP      	0 16	; to 74
  label_57: {
    Instruction i = 0x8003c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_74;
  }
  
  // [349]	GETUPVAL 	3 16	; delay_apply_p
  label_58: {
    Instruction i = 0x080000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [349]	MOVE     	4 1
  label_59: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [349]	CALL     	3 2 2
  label_60: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [349]	TEST     	3 0
  label_61: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_63;
    }
  }
  
  // [349]	JMP      	0 10	; to 73
  label_62: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_73;
  }
  
  // [350]	GETUPVAL 	3 17	; real_apply
  label_63: {
    Instruction i = 0x088000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [350]	GETUPVAL 	4 18	; delay_apply_f
  label_64: {
    Instruction i = 0x09000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [350]	MOVE     	5 1
  label_65: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [350]	CALL     	4 2 2
  label_66: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [350]	GETUPVAL 	5 19	; delay_apply_xs
  label_67: {
    Instruction i = 0x09800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [350]	MOVE     	6 1
  label_68: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [350]	CALL     	5 2 0
  label_69: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [350]	CALL     	3 0 2
  label_70: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [350]	MOVE     	2 3
  label_71: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [350]	JMP      	0 1	; to 74
  label_72: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_74;
  }
  
  // [352]	MOVE     	2 1
  label_73: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [354]	GETUPVAL 	3 0	; un_just_all
  label_74: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [354]	MOVE     	4 2
  label_75: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [354]	CALL     	3 2 2
  label_76: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [354]	MOVE     	2 3
  label_77: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [355]	GETUPVAL 	3 20	; lang_set_do
  label_78: {
    Instruction i = 0x0a0000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [355]	MOVE     	4 1
  label_79: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [355]	MOVE     	5 2
  label_80: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [355]	CALL     	3 3 1
  label_81: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [356]	RETURN   	2 2
  label_82: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [357]	RETURN   	0 1
  label_83: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 358
// lastlinedefined = 385
// what = Lua
static int zz_magic_function_50 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [359]	NEWTABLE 	3 0 0
  label_0: {
    Instruction i = 0x000000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [361]	LOADK    	4 -1	; 0
  label_1: {
    Instruction i = 0x00000101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [362]	LEN      	5 0
  label_2: {
    Instruction i = 0x0000015c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [362]	LT       	0 4 5
  label_3: {
    Instruction i = 0x02014020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [362]	JMP      	0 48	; to 53
  label_4: {
    Instruction i = 0x800bc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_53;
  }
  
  // [363]	GETUPVAL 	5 0	; jsbool_equal_p
  label_5: {
    Instruction i = 0x00000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [363]	ADD      	6 4 -1	; - 0
  label_6: {
    Instruction i = 0x0240018d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [363]	ADD      	6 6 -2	; - 1
  label_7: {
    Instruction i = 0x0340418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [363]	GETTABLE 	6 0 6
  label_8: {
    Instruction i = 0x00018187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [363]	MOVE     	7 1
  label_9: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [363]	CALL     	5 3 2
  label_10: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [363]	TEST     	5 0
  label_11: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_13;
    }
  }
  
  // [363]	JMP      	0 26	; to 39
  label_12: {
    Instruction i = 0x8006401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_39;
  }
  
  // [364]	ADD      	5 4 -1	; - 0
  label_13: {
    Instruction i = 0x0240014d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [364]	ADD      	5 5 -2	; - 1
  label_14: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [364]	SETTABLE 	3 5 1
  label_15: {
    Instruction i = 0x028040ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [365]	ADD      	5 4 -2	; - 1
  label_16: {
    Instruction i = 0x0240414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [365]	ADD      	5 5 -2	; - 1
  label_17: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [365]	SETTABLE 	3 5 2
  label_18: {
    Instruction i = 0x028080ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [367]	ADD      	4 4 -3	; - 2
  label_19: {
    Instruction i = 0x0240810d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [368]	LEN      	5 0
  label_20: {
    Instruction i = 0x0000015c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [368]	LT       	0 4 5
  label_21: {
    Instruction i = 0x02014020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_23;
    }
  }
  
  // [368]	JMP      	0 14	; to 37
  label_22: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_37;
  }
  
  // [369]	ADD      	5 4 -1	; - 0
  label_23: {
    Instruction i = 0x0240014d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [369]	ADD      	5 5 -2	; - 1
  label_24: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [369]	ADD      	6 4 -1	; - 0
  label_25: {
    Instruction i = 0x0240018d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [369]	ADD      	6 6 -2	; - 1
  label_26: {
    Instruction i = 0x0340418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [369]	GETTABLE 	6 0 6
  label_27: {
    Instruction i = 0x00018187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [369]	SETTABLE 	3 5 6
  label_28: {
    Instruction i = 0x028180ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [370]	ADD      	5 4 -2	; - 1
  label_29: {
    Instruction i = 0x0240414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [370]	ADD      	5 5 -2	; - 1
  label_30: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [370]	ADD      	6 4 -2	; - 1
  label_31: {
    Instruction i = 0x0240418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [370]	ADD      	6 6 -2	; - 1
  label_32: {
    Instruction i = 0x0340418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [370]	GETTABLE 	6 0 6
  label_33: {
    Instruction i = 0x00018187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [370]	SETTABLE 	3 5 6
  label_34: {
    Instruction i = 0x028180ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [371]	ADD      	4 4 -3	; - 2
  label_35: {
    Instruction i = 0x0240810d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [371]	JMP      	0 -17	; to 20
  label_36: {
    Instruction i = 0x7ffb801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_20;
  }
  
  // [374]	RETURN   	3 2
  label_37: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [374]	JMP      	0 12	; to 51
  label_38: {
    Instruction i = 0x8002c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_51;
  }
  
  // [376]	ADD      	5 4 -1	; - 0
  label_39: {
    Instruction i = 0x0240014d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [376]	ADD      	5 5 -2	; - 1
  label_40: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [376]	ADD      	6 4 -1	; - 0
  label_41: {
    Instruction i = 0x0240018d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [376]	ADD      	6 6 -2	; - 1
  label_42: {
    Instruction i = 0x0340418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [376]	GETTABLE 	6 0 6
  label_43: {
    Instruction i = 0x00018187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [376]	SETTABLE 	3 5 6
  label_44: {
    Instruction i = 0x028180ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [377]	ADD      	5 4 -2	; - 1
  label_45: {
    Instruction i = 0x0240414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [377]	ADD      	5 5 -2	; - 1
  label_46: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [377]	ADD      	6 4 -2	; - 1
  label_47: {
    Instruction i = 0x0240418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [377]	ADD      	6 6 -2	; - 1
  label_48: {
    Instruction i = 0x0340418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [377]	GETTABLE 	6 0 6
  label_49: {
    Instruction i = 0x00018187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [377]	SETTABLE 	3 5 6
  label_50: {
    Instruction i = 0x028180ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [379]	ADD      	4 4 -3	; - 2
  label_51: {
    Instruction i = 0x0240810d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [379]	JMP      	0 -51	; to 2
  label_52: {
    Instruction i = 0x7ff3001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_2;
  }
  
  // [382]	LEN      	4 0
  label_53: {
    Instruction i = 0x0000011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [382]	ADD      	4 4 -1	; - 0
  label_54: {
    Instruction i = 0x0240010d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [382]	ADD      	4 4 -2	; - 1
  label_55: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [382]	SETTABLE 	3 4 1
  label_56: {
    Instruction i = 0x020040ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [383]	LEN      	4 0
  label_57: {
    Instruction i = 0x0000011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [383]	ADD      	4 4 -2	; - 1
  label_58: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [383]	ADD      	4 4 -2	; - 1
  label_59: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [383]	SETTABLE 	3 4 2
  label_60: {
    Instruction i = 0x020080ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [384]	RETURN   	3 2
  label_61: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [385]	RETURN   	0 1
  label_62: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 386
// lastlinedefined = 397
// what = Lua
static int zz_magic_function_51 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [388]	LOADK    	3 -1	; 0
  label_0: {
    Instruction i = 0x000000c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [389]	LEN      	4 0
  label_1: {
    Instruction i = 0x0000011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [389]	LT       	0 3 4
  label_2: {
    Instruction i = 0x01810020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [389]	JMP      	0 14	; to 18
  label_3: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [390]	GETUPVAL 	4 0	; jsbool_equal_p
  label_4: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [390]	ADD      	5 3 -1	; - 0
  label_5: {
    Instruction i = 0x01c0014d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [390]	ADD      	5 5 -2	; - 1
  label_6: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [390]	GETTABLE 	5 0 5
  label_7: {
    Instruction i = 0x00014147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [390]	MOVE     	6 1
  label_8: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [390]	CALL     	4 3 2
  label_9: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [390]	TEST     	4 0
  label_10: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [390]	JMP      	0 4	; to 16
  label_11: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [391]	ADD      	4 3 -2	; - 1
  label_12: {
    Instruction i = 0x01c0410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [391]	ADD      	4 4 -2	; - 1
  label_13: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [391]	GETTABLE 	4 0 4
  label_14: {
    Instruction i = 0x00010107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [391]	RETURN   	4 2
  label_15: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [393]	ADD      	3 3 -3	; - 2
  label_16: {
    Instruction i = 0x01c080cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [393]	JMP      	0 -17	; to 1
  label_17: {
    Instruction i = 0x7ffb801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_1;
  }
  
  // [396]	RETURN   	2 2
  label_18: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [397]	RETURN   	0 1
  label_19: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 398
// lastlinedefined = 409
// what = Lua
static int zz_magic_function_52 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [400]	LOADK    	2 -1	; 0
  label_0: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [401]	LEN      	3 0
  label_1: {
    Instruction i = 0x000000dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [401]	LT       	0 2 3
  label_2: {
    Instruction i = 0x0100c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [401]	JMP      	0 14	; to 18
  label_3: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [402]	GETUPVAL 	3 0	; jsbool_equal_p
  label_4: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [402]	ADD      	4 2 -1	; - 0
  label_5: {
    Instruction i = 0x0140010d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [402]	ADD      	4 4 -2	; - 1
  label_6: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [402]	GETTABLE 	4 0 4
  label_7: {
    Instruction i = 0x00010107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [402]	MOVE     	5 1
  label_8: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [402]	CALL     	3 3 2
  label_9: {
    Instruction i = 0x018080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [402]	TEST     	3 0
  label_10: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [402]	JMP      	0 4	; to 16
  label_11: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [403]	ADD      	3 2 -2	; - 1
  label_12: {
    Instruction i = 0x014040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [403]	ADD      	3 3 -2	; - 1
  label_13: {
    Instruction i = 0x01c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [403]	GETTABLE 	3 0 3
  label_14: {
    Instruction i = 0x0000c0c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [403]	RETURN   	3 2
  label_15: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [405]	ADD      	2 2 -3	; - 2
  label_16: {
    Instruction i = 0x0140808d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [405]	JMP      	0 -17	; to 1
  label_17: {
    Instruction i = 0x7ffb801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_1;
  }
  
  // [408]	GETUPVAL 	2 1	; ERROR
  label_18: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [408]	TAILCALL 	2 1 0
  label_19: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [408]	RETURN   	2 0
  label_20: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [409]	RETURN   	0 1
  label_21: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 410
// lastlinedefined = 420
// what = Lua
static int zz_magic_function_53 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [411]	GETUPVAL 	1 0	; null_v
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [413]	LOADK    	2 -1	; 0
  label_1: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [414]	LEN      	3 0
  label_2: {
    Instruction i = 0x000000dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [414]	LT       	0 2 3
  label_3: {
    Instruction i = 0x0100c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [414]	JMP      	0 14	; to 19
  label_4: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_19;
  }
  
  // [415]	GETUPVAL 	3 1	; new_construction
  label_5: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [415]	GETUPVAL 	4 2	; new_list
  label_6: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [415]	ADD      	5 2 -1	; - 0
  label_7: {
    Instruction i = 0x0140014d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [415]	ADD      	5 5 -2	; - 1
  label_8: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [415]	GETTABLE 	5 0 5
  label_9: {
    Instruction i = 0x00014147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [415]	ADD      	6 2 -2	; - 1
  label_10: {
    Instruction i = 0x0140418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [415]	ADD      	6 6 -2	; - 1
  label_11: {
    Instruction i = 0x0340418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [415]	GETTABLE 	6 0 6
  label_12: {
    Instruction i = 0x00018187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [415]	CALL     	4 3 2
  label_13: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [415]	MOVE     	5 1
  label_14: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [415]	CALL     	3 3 2
  label_15: {
    Instruction i = 0x018080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [415]	MOVE     	1 3
  label_16: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [416]	ADD      	2 2 -3	; - 2
  label_17: {
    Instruction i = 0x0140808d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [416]	JMP      	0 -17	; to 2
  label_18: {
    Instruction i = 0x7ffb801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_2;
  }
  
  // [419]	GETUPVAL 	2 3	; new_data
  label_19: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [419]	GETUPVAL 	3 4	; mapping_symbol
  label_20: {
    Instruction i = 0x020000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [419]	GETUPVAL 	4 2	; new_list
  label_21: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [419]	MOVE     	5 1
  label_22: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [419]	CALL     	4 2 0
  label_23: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [419]	TAILCALL 	2 0 0
  label_24: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [419]	RETURN   	2 0
  label_25: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [420]	RETURN   	0 1
  label_26: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 421
// lastlinedefined = 429
// what = Lua
static int zz_magic_function_54 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [423]	LOADK    	2 -1	; 0
  label_0: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [424]	LEN      	3 0
  label_1: {
    Instruction i = 0x000000dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [424]	LT       	0 2 3
  label_2: {
    Instruction i = 0x0100c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [424]	JMP      	0 10	; to 14
  label_3: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_14;
  }
  
  // [425]	MOVE     	3 1
  label_4: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [425]	ADD      	4 2 -1	; - 0
  label_5: {
    Instruction i = 0x0140010d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [425]	ADD      	4 4 -2	; - 1
  label_6: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [425]	GETTABLE 	4 0 4
  label_7: {
    Instruction i = 0x00010107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [425]	ADD      	5 2 -2	; - 1
  label_8: {
    Instruction i = 0x0140414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [425]	ADD      	5 5 -2	; - 1
  label_9: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [425]	GETTABLE 	5 0 5
  label_10: {
    Instruction i = 0x00014147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [425]	CALL     	3 3 1
  label_11: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [426]	ADD      	2 2 -3	; - 2
  label_12: {
    Instruction i = 0x0140808d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [426]	JMP      	0 -13	; to 1
  label_13: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_1;
  }
  
  // [429]	RETURN   	0 1
  label_14: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 430
// lastlinedefined = 539
// what = Lua
static int zz_magic_function_55 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [431]	GETUPVAL 	2 0	; force1
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [431]	MOVE     	3 1
  label_1: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [431]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [432]	GETUPVAL 	3 1	; any_delay_just_p
  label_3: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [432]	MOVE     	4 2
  label_4: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [432]	CALL     	3 2 2
  label_5: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [432]	TEST     	3 0
  label_6: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_8;
    }
  }
  
  // [432]	JMP      	0 5	; to 13
  label_7: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [433]	GETUPVAL 	3 2	; evaluate
  label_8: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [433]	MOVE     	4 0
  label_9: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [433]	MOVE     	5 2
  label_10: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [433]	TAILCALL 	3 3 0
  label_11: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [433]	RETURN   	3 0
  label_12: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [435]	GETUPVAL 	3 3	; new_error
  label_13: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	4 4	; system_symbol
  label_14: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	5 5	; new_list
  label_15: {
    Instruction i = 0x02800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	6 6	; function_builtin_use_systemName
  label_16: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	7 5	; new_list
  label_17: {
    Instruction i = 0x028001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	8 7	; evaluate_function_builtin_systemName
  label_18: {
    Instruction i = 0x03800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	9 5	; new_list
  label_19: {
    Instruction i = 0x02800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	GETUPVAL 	10 8	; env2val
  label_20: {
    Instruction i = 0x04000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [435]	MOVE     	11 0
  label_21: {
    Instruction i = 0x000002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [435]	CALL     	10 2 2
  label_22: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [435]	MOVE     	11 2
  label_23: {
    Instruction i = 0x010002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [435]	CALL     	9 3 0
  label_24: {
    Instruction i = 0x01800264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [435]	CALL     	7 0 0
  label_25: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [435]	CALL     	5 0 0
  label_26: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [435]	CALL     	3 0 2
  label_27: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [436]	GETUPVAL 	4 9	; construction_p
  label_28: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [436]	MOVE     	5 2
  label_29: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [436]	CALL     	4 2 2
  label_30: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [436]	TEST     	4 0
  label_31: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_33;
    }
  }
  
  // [436]	JMP      	0 242	; to 275
  label_32: {
    Instruction i = 0x803c401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_275;
  }
  
  // [437]	NEWTABLE 	4 0 0
  label_33: {
    Instruction i = 0x0000010b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [438]	MOVE     	5 2
  label_34: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [439]	GETUPVAL 	6 10	; null_p
  label_35: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [439]	MOVE     	7 5
  label_36: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [439]	CALL     	6 2 2
  label_37: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [439]	TEST     	6 1
  label_38: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [439]	JMP      	0 31	; to 71
  label_39: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_71;
  }
  
  // [440]	GETUPVAL 	6 1	; any_delay_just_p
  label_40: {
    Instruction i = 0x00800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [440]	MOVE     	7 5
  label_41: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [440]	CALL     	6 2 2
  label_42: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [440]	TEST     	6 0
  label_43: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_45;
    }
  }
  
  // [440]	JMP      	0 6	; to 51
  label_44: {
    Instruction i = 0x8001401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_51;
  }
  
  // [441]	GETUPVAL 	6 2	; evaluate
  label_45: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [441]	MOVE     	7 0
  label_46: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [441]	MOVE     	8 2
  label_47: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [441]	TAILCALL 	6 3 0
  label_48: {
    Instruction i = 0x018001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [441]	RETURN   	6 0
  label_49: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [441]	JMP      	0 -16	; to 35
  label_50: {
    Instruction i = 0x7ffbc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [442]	GETUPVAL 	6 9	; construction_p
  label_51: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [442]	MOVE     	7 5
  label_52: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [442]	CALL     	6 2 2
  label_53: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [442]	TEST     	6 0
  label_54: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_56;
    }
  }
  
  // [442]	JMP      	0 13	; to 69
  label_55: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_69;
  }
  
  // [443]	GETUPVAL 	6 11	; __TS__ArrayPush
  label_56: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [443]	MOVE     	7 4
  label_57: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [443]	GETUPVAL 	8 12	; construction_head
  label_58: {
    Instruction i = 0x06000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [443]	MOVE     	9 5
  label_59: {
    Instruction i = 0x02800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [443]	CALL     	8 2 0
  label_60: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [443]	CALL     	6 0 1
  label_61: {
    Instruction i = 0x000041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [444]	GETUPVAL 	6 0	; force1
  label_62: {
    Instruction i = 0x00000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [444]	GETUPVAL 	7 13	; construction_tail
  label_63: {
    Instruction i = 0x068001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [444]	MOVE     	8 5
  label_64: {
    Instruction i = 0x02800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [444]	CALL     	7 2 0
  label_65: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [444]	CALL     	6 0 2
  label_66: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [444]	MOVE     	5 6
  label_67: {
    Instruction i = 0x03000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [444]	JMP      	0 -34	; to 35
  label_68: {
    Instruction i = 0x7ff7401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [446]	RETURN   	3 2
  label_69: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [447]	JMP      	0 -36	; to 35
  label_70: {
    Instruction i = 0x7ff6c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [449]	GETUPVAL 	6 14	; jsbool_equal_p
  label_71: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [449]	GETTABLE 	7 4 -1	; 1
  label_72: {
    Instruction i = 0x024001c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [449]	GETUPVAL 	8 15	; form_builtin_use_systemName
  label_73: {
    Instruction i = 0x07800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [449]	CALL     	6 3 2
  label_74: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [449]	TEST     	6 0
  label_75: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_77;
    }
  }
  
  // [449]	JMP      	0 24	; to 101
  label_76: {
    Instruction i = 0x8005c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_101;
  }
  
  // [450]	LEN      	6 4
  label_77: {
    Instruction i = 0x0200019c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [450]	EQ       	0 6 -1	; - 1
  label_78: {
    Instruction i = 0x0340001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_80;
    }
  }
  
  // [450]	JMP      	0 1	; to 81
  label_79: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_81;
  }
  
  // [451]	RETURN   	3 2
  label_80: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [453]	GETTABLE 	6 4 -2	; 2
  label_81: {
    Instruction i = 0x02404187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [454]	NEWTABLE 	7 0 0
  label_82: {
    Instruction i = 0x000001cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [456]	LOADK    	8 -2	; 2
  label_83: {
    Instruction i = 0x00004201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [457]	LEN      	9 4
  label_84: {
    Instruction i = 0x0200025c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [457]	LT       	0 8 9
  label_85: {
    Instruction i = 0x04024020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_87;
    }
  }
  
  // [457]	JMP      	0 7	; to 94
  label_86: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_94;
  }
  
  // [458]	SUB      	9 8 -2	; - 2
  label_87: {
    Instruction i = 0x0440424e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [458]	ADD      	9 9 -1	; - 1
  label_88: {
    Instruction i = 0x04c0024d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [458]	ADD      	10 8 -1	; - 1
  label_89: {
    Instruction i = 0x0440028d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [458]	GETTABLE 	10 4 10
  label_90: {
    Instruction i = 0x02028287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [458]	SETTABLE 	7 9 10
  label_91: {
    Instruction i = 0x048281ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [459]	ADD      	8 8 -1	; - 1
  label_92: {
    Instruction i = 0x0440020d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [459]	JMP      	0 -10	; to 84
  label_93: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_84;
  }
  
  // [462]	GETUPVAL 	8 16	; builtin_form_apply
  label_94: {
    Instruction i = 0x08000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [462]	MOVE     	9 0
  label_95: {
    Instruction i = 0x00000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [462]	MOVE     	10 6
  label_96: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [462]	MOVE     	11 7
  label_97: {
    Instruction i = 0x038002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [462]	TAILCALL 	8 4 0
  label_98: {
    Instruction i = 0x02000225;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [462]	RETURN   	8 0
  label_99: {
    Instruction i = 0x00000226;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [462]	JMP      	0 199	; to 300
  label_100: {
    Instruction i = 0x8031801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [463]	GETUPVAL 	6 14	; jsbool_equal_p
  label_101: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [463]	GETTABLE 	7 4 -1	; 1
  label_102: {
    Instruction i = 0x024001c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [463]	GETUPVAL 	8 17	; form_use_systemName
  label_103: {
    Instruction i = 0x08800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [463]	CALL     	6 3 2
  label_104: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [463]	TEST     	6 0
  label_105: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_107;
    }
  }
  
  // [463]	JMP      	0 111	; to 218
  label_106: {
    Instruction i = 0x801b801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_218;
  }
  
  // [464]	LEN      	6 4
  label_107: {
    Instruction i = 0x0200019c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [464]	EQ       	0 6 -1	; - 1
  label_108: {
    Instruction i = 0x0340001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_110;
    }
  }
  
  // [464]	JMP      	0 1	; to 111
  label_109: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_111;
  }
  
  // [465]	RETURN   	3 2
  label_110: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [467]	GETUPVAL 	6 18	; force_all
  label_111: {
    Instruction i = 0x09000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [467]	GETUPVAL 	7 2	; evaluate
  label_112: {
    Instruction i = 0x010001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [467]	MOVE     	8 0
  label_113: {
    Instruction i = 0x00000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [467]	GETTABLE 	9 4 -2	; 2
  label_114: {
    Instruction i = 0x02404247;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [467]	CALL     	7 3 0
  label_115: {
    Instruction i = 0x018001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [467]	CALL     	6 0 2
  label_116: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [468]	GETUPVAL 	7 19	; data_p
  label_117: {
    Instruction i = 0x098001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [468]	MOVE     	8 6
  label_118: {
    Instruction i = 0x03000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [468]	CALL     	7 2 2
  label_119: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [468]	TEST     	7 1
  label_120: {
    Instruction i = 0x000041e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_122;
    }
  }
  
  // [468]	JMP      	0 1	; to 123
  label_121: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_123;
  }
  
  // [469]	RETURN   	3 2
  label_122: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [471]	GETUPVAL 	7 0	; force1
  label_123: {
    Instruction i = 0x000001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [471]	GETUPVAL 	8 20	; data_name
  label_124: {
    Instruction i = 0x0a000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [471]	MOVE     	9 6
  label_125: {
    Instruction i = 0x03000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [471]	CALL     	8 2 0
  label_126: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [471]	CALL     	7 0 2
  label_127: {
    Instruction i = 0x000081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [472]	GETUPVAL 	8 1	; any_delay_just_p
  label_128: {
    Instruction i = 0x00800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [472]	MOVE     	9 7
  label_129: {
    Instruction i = 0x03800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [472]	CALL     	8 2 2
  label_130: {
    Instruction i = 0x01008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [472]	TEST     	8 0
  label_131: {
    Instruction i = 0x00000222;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_133;
    }
  }
  
  // [472]	JMP      	0 5	; to 138
  label_132: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_138;
  }
  
  // [473]	GETUPVAL 	8 2	; evaluate
  label_133: {
    Instruction i = 0x01000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [473]	MOVE     	9 0
  label_134: {
    Instruction i = 0x00000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [473]	MOVE     	10 2
  label_135: {
    Instruction i = 0x01000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [473]	TAILCALL 	8 3 0
  label_136: {
    Instruction i = 0x01800225;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [473]	RETURN   	8 0
  label_137: {
    Instruction i = 0x00000226;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [475]	GETUPVAL 	8 21	; symbol_p
  label_138: {
    Instruction i = 0x0a800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [475]	MOVE     	9 7
  label_139: {
    Instruction i = 0x03800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [475]	CALL     	8 2 2
  label_140: {
    Instruction i = 0x01008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [475]	TEST     	8 1
  label_141: {
    Instruction i = 0x00004222;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_143;
    }
  }
  
  // [475]	JMP      	0 1	; to 144
  label_142: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_144;
  }
  
  // [476]	RETURN   	3 2
  label_143: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [478]	GETUPVAL 	8 22	; symbol_equal_p
  label_144: {
    Instruction i = 0x0b000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [478]	MOVE     	9 7
  label_145: {
    Instruction i = 0x03800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [478]	GETUPVAL 	10 23	; form_symbol
  label_146: {
    Instruction i = 0x0b800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [478]	CALL     	8 3 2
  label_147: {
    Instruction i = 0x01808224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [478]	TEST     	8 1
  label_148: {
    Instruction i = 0x00004222;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_150;
    }
  }
  
  // [478]	JMP      	0 1	; to 151
  label_149: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [479]	RETURN   	3 2
  label_150: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [481]	GETUPVAL 	8 0	; force1
  label_151: {
    Instruction i = 0x00000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [481]	GETUPVAL 	9 24	; data_list
  label_152: {
    Instruction i = 0x0c000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [481]	MOVE     	10 6
  label_153: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [481]	CALL     	9 2 0
  label_154: {
    Instruction i = 0x01000264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [481]	CALL     	8 0 2
  label_155: {
    Instruction i = 0x00008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [482]	GETUPVAL 	9 1	; any_delay_just_p
  label_156: {
    Instruction i = 0x00800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [482]	MOVE     	10 8
  label_157: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [482]	CALL     	9 2 2
  label_158: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [482]	TEST     	9 0
  label_159: {
    Instruction i = 0x00000262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_161;
    }
  }
  
  // [482]	JMP      	0 5	; to 166
  label_160: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_166;
  }
  
  // [483]	GETUPVAL 	9 2	; evaluate
  label_161: {
    Instruction i = 0x01000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [483]	MOVE     	10 0
  label_162: {
    Instruction i = 0x00000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [483]	MOVE     	11 2
  label_163: {
    Instruction i = 0x010002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [483]	TAILCALL 	9 3 0
  label_164: {
    Instruction i = 0x01800265;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [483]	RETURN   	9 0
  label_165: {
    Instruction i = 0x00000266;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [485]	GETUPVAL 	9 9	; construction_p
  label_166: {
    Instruction i = 0x04800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [485]	MOVE     	10 8
  label_167: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [485]	CALL     	9 2 2
  label_168: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [485]	TEST     	9 1
  label_169: {
    Instruction i = 0x00004262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_171;
    }
  }
  
  // [485]	JMP      	0 1	; to 172
  label_170: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_172;
  }
  
  // [486]	RETURN   	3 2
  label_171: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [488]	GETUPVAL 	9 12	; construction_head
  label_172: {
    Instruction i = 0x06000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [488]	MOVE     	10 8
  label_173: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [488]	CALL     	9 2 2
  label_174: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [489]	GETUPVAL 	10 0	; force1
  label_175: {
    Instruction i = 0x00000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [489]	GETUPVAL 	11 13	; construction_tail
  label_176: {
    Instruction i = 0x068002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [489]	MOVE     	12 8
  label_177: {
    Instruction i = 0x04000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [489]	CALL     	11 2 0
  label_178: {
    Instruction i = 0x010002e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [489]	CALL     	10 0 2
  label_179: {
    Instruction i = 0x000082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [490]	GETUPVAL 	11 1	; any_delay_just_p
  label_180: {
    Instruction i = 0x008002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [490]	MOVE     	12 10
  label_181: {
    Instruction i = 0x05000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [490]	CALL     	11 2 2
  label_182: {
    Instruction i = 0x010082e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [490]	TEST     	11 0
  label_183: {
    Instruction i = 0x000002e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_185;
    }
  }
  
  // [490]	JMP      	0 5	; to 190
  label_184: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_190;
  }
  
  // [491]	GETUPVAL 	11 2	; evaluate
  label_185: {
    Instruction i = 0x010002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [491]	MOVE     	12 0
  label_186: {
    Instruction i = 0x00000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [491]	MOVE     	13 2
  label_187: {
    Instruction i = 0x01000340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [491]	TAILCALL 	11 3 0
  label_188: {
    Instruction i = 0x018002e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [491]	RETURN   	11 0
  label_189: {
    Instruction i = 0x000002e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [493]	GETUPVAL 	11 10	; null_p
  label_190: {
    Instruction i = 0x050002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [493]	MOVE     	12 10
  label_191: {
    Instruction i = 0x05000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [493]	CALL     	11 2 2
  label_192: {
    Instruction i = 0x010082e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [493]	TEST     	11 1
  label_193: {
    Instruction i = 0x000042e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_195;
    }
  }
  
  // [493]	JMP      	0 1	; to 196
  label_194: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_196;
  }
  
  // [494]	RETURN   	3 2
  label_195: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [496]	NEWTABLE 	11 0 0
  label_196: {
    Instruction i = 0x000002cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [496]	GETUPVAL 	12 8	; env2val
  label_197: {
    Instruction i = 0x04000305;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [496]	MOVE     	13 0
  label_198: {
    Instruction i = 0x00000340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [496]	CALL     	12 2 0
  label_199: {
    Instruction i = 0x01000324;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [496]	SETLIST  	11 0 1	; 1
  label_200: {
    Instruction i = 0x000042eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [498]	LOADK    	12 -2	; 2
  label_201: {
    Instruction i = 0x00004301;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [499]	LEN      	13 4
  label_202: {
    Instruction i = 0x0200035c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [499]	LT       	0 12 13
  label_203: {
    Instruction i = 0x06034020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_205;
    }
  }
  
  // [499]	JMP      	0 7	; to 212
  label_204: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_212;
  }
  
  // [500]	SUB      	13 12 -1	; - 1
  label_205: {
    Instruction i = 0x0640034e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [500]	ADD      	13 13 -1	; - 1
  label_206: {
    Instruction i = 0x06c0034d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [500]	ADD      	14 12 -1	; - 1
  label_207: {
    Instruction i = 0x0640038d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [500]	GETTABLE 	14 4 14
  label_208: {
    Instruction i = 0x02038387;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [500]	SETTABLE 	11 13 14
  label_209: {
    Instruction i = 0x068382ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [501]	ADD      	12 12 -1	; - 1
  label_210: {
    Instruction i = 0x0640030d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [501]	JMP      	0 -10	; to 202
  label_211: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [504]	GETUPVAL 	12 25	; apply
  label_212: {
    Instruction i = 0x0c800305;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [504]	MOVE     	13 9
  label_213: {
    Instruction i = 0x04800340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [504]	MOVE     	14 11
  label_214: {
    Instruction i = 0x05800380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [504]	TAILCALL 	12 3 0
  label_215: {
    Instruction i = 0x01800325;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [504]	RETURN   	12 0
  label_216: {
    Instruction i = 0x00000326;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [504]	JMP      	0 82	; to 300
  label_217: {
    Instruction i = 0x8014401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [505]	GETUPVAL 	6 14	; jsbool_equal_p
  label_218: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [505]	GETTABLE 	7 4 -1	; 1
  label_219: {
    Instruction i = 0x024001c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [505]	GETUPVAL 	8 6	; function_builtin_use_systemName
  label_220: {
    Instruction i = 0x03000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [505]	CALL     	6 3 2
  label_221: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [505]	TEST     	6 0
  label_222: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_224;
    }
  }
  
  // [505]	JMP      	0 26	; to 250
  label_223: {
    Instruction i = 0x8006401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_250;
  }
  
  // [506]	LEN      	6 4
  label_224: {
    Instruction i = 0x0200019c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [506]	EQ       	0 6 -1	; - 1
  label_225: {
    Instruction i = 0x0340001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_227;
    }
  }
  
  // [506]	JMP      	0 1	; to 228
  label_226: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_228;
  }
  
  // [507]	RETURN   	3 2
  label_227: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [509]	GETTABLE 	6 4 -2	; 2
  label_228: {
    Instruction i = 0x02404187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [510]	NEWTABLE 	7 0 0
  label_229: {
    Instruction i = 0x000001cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [512]	LOADK    	8 -2	; 2
  label_230: {
    Instruction i = 0x00004201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [513]	LEN      	9 4
  label_231: {
    Instruction i = 0x0200025c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [513]	LT       	0 8 9
  label_232: {
    Instruction i = 0x04024020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_234;
    }
  }
  
  // [513]	JMP      	0 10	; to 244
  label_233: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_244;
  }
  
  // [514]	SUB      	9 8 -2	; - 2
  label_234: {
    Instruction i = 0x0440424e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [514]	ADD      	9 9 -1	; - 1
  label_235: {
    Instruction i = 0x04c0024d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [514]	GETUPVAL 	10 2	; evaluate
  label_236: {
    Instruction i = 0x01000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [514]	MOVE     	11 0
  label_237: {
    Instruction i = 0x000002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [514]	ADD      	12 8 -1	; - 1
  label_238: {
    Instruction i = 0x0440030d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [514]	GETTABLE 	12 4 12
  label_239: {
    Instruction i = 0x02030307;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [514]	CALL     	10 3 2
  label_240: {
    Instruction i = 0x018082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [514]	SETTABLE 	7 9 10
  label_241: {
    Instruction i = 0x048281ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [515]	ADD      	8 8 -1	; - 1
  label_242: {
    Instruction i = 0x0440020d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [515]	JMP      	0 -13	; to 231
  label_243: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_231;
  }
  
  // [518]	GETUPVAL 	8 26	; builtin_func_apply
  label_244: {
    Instruction i = 0x0d000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [518]	MOVE     	9 6
  label_245: {
    Instruction i = 0x03000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [518]	MOVE     	10 7
  label_246: {
    Instruction i = 0x03800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [518]	TAILCALL 	8 3 0
  label_247: {
    Instruction i = 0x01800225;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [518]	RETURN   	8 0
  label_248: {
    Instruction i = 0x00000226;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [518]	JMP      	0 50	; to 300
  label_249: {
    Instruction i = 0x800c401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [520]	GETUPVAL 	6 2	; evaluate
  label_250: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [520]	MOVE     	7 0
  label_251: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [520]	GETTABLE 	8 4 -1	; 1
  label_252: {
    Instruction i = 0x02400207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [520]	CALL     	6 3 2
  label_253: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [521]	NEWTABLE 	7 0 0
  label_254: {
    Instruction i = 0x000001cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [523]	LOADK    	8 -1	; 1
  label_255: {
    Instruction i = 0x00000201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [524]	LEN      	9 4
  label_256: {
    Instruction i = 0x0200025c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [524]	LT       	0 8 9
  label_257: {
    Instruction i = 0x04024020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_259;
    }
  }
  
  // [524]	JMP      	0 10	; to 269
  label_258: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_269;
  }
  
  // [525]	SUB      	9 8 -1	; - 1
  label_259: {
    Instruction i = 0x0440024e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [525]	ADD      	9 9 -1	; - 1
  label_260: {
    Instruction i = 0x04c0024d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [525]	GETUPVAL 	10 2	; evaluate
  label_261: {
    Instruction i = 0x01000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [525]	MOVE     	11 0
  label_262: {
    Instruction i = 0x000002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [525]	ADD      	12 8 -1	; - 1
  label_263: {
    Instruction i = 0x0440030d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [525]	GETTABLE 	12 4 12
  label_264: {
    Instruction i = 0x02030307;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [525]	CALL     	10 3 2
  label_265: {
    Instruction i = 0x018082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [525]	SETTABLE 	7 9 10
  label_266: {
    Instruction i = 0x048281ca;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [526]	ADD      	8 8 -1	; - 1
  label_267: {
    Instruction i = 0x0440020d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [526]	JMP      	0 -13	; to 256
  label_268: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_256;
  }
  
  // [529]	GETUPVAL 	8 25	; apply
  label_269: {
    Instruction i = 0x0c800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [529]	MOVE     	9 6
  label_270: {
    Instruction i = 0x03000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [529]	MOVE     	10 7
  label_271: {
    Instruction i = 0x03800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [529]	TAILCALL 	8 3 0
  label_272: {
    Instruction i = 0x01800225;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [529]	RETURN   	8 0
  label_273: {
    Instruction i = 0x00000226;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [530]	JMP      	0 25	; to 300
  label_274: {
    Instruction i = 0x8006001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [531]	GETUPVAL 	4 10	; null_p
  label_275: {
    Instruction i = 0x05000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [531]	MOVE     	5 2
  label_276: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [531]	CALL     	4 2 2
  label_277: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [531]	TEST     	4 0
  label_278: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_280;
    }
  }
  
  // [531]	JMP      	0 2	; to 282
  label_279: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_282;
  }
  
  // [532]	RETURN   	2 2
  label_280: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [532]	JMP      	0 18	; to 300
  label_281: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [533]	GETUPVAL 	4 27	; name_p
  label_282: {
    Instruction i = 0x0d800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [533]	MOVE     	5 2
  label_283: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [533]	CALL     	4 2 2
  label_284: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [533]	TEST     	4 0
  label_285: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_287;
    }
  }
  
  // [533]	JMP      	0 7	; to 294
  label_286: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_294;
  }
  
  // [534]	GETUPVAL 	4 28	; env_get
  label_287: {
    Instruction i = 0x0e000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [534]	MOVE     	5 0
  label_288: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [534]	MOVE     	6 2
  label_289: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [534]	MOVE     	7 3
  label_290: {
    Instruction i = 0x018001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [534]	TAILCALL 	4 4 0
  label_291: {
    Instruction i = 0x02000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [534]	RETURN   	4 0
  label_292: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [534]	JMP      	0 6	; to 300
  label_293: {
    Instruction i = 0x8001401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [535]	GETUPVAL 	4 29	; error_p
  label_294: {
    Instruction i = 0x0e800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [535]	MOVE     	5 2
  label_295: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [535]	CALL     	4 2 2
  label_296: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [535]	TEST     	4 0
  label_297: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_299;
    }
  }
  
  // [535]	JMP      	0 1	; to 300
  label_298: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_300;
  }
  
  // [536]	RETURN   	3 2
  label_299: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [538]	GETUPVAL 	4 30	; ERROR
  label_300: {
    Instruction i = 0x0f000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [538]	TAILCALL 	4 1 0
  label_301: {
    Instruction i = 0x00800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [538]	RETURN   	4 0
  label_302: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [539]	RETURN   	0 1
  label_303: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 540
// lastlinedefined = 542
// what = Lua
static int zz_magic_function_56 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [541]	GETUPVAL 	1 0	; symbol_p
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [541]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [541]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [541]	TEST     	1 1
  label_3: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [541]	JMP      	0 3	; to 8
  label_4: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_8;
  }
  
  // [541]	GETUPVAL 	1 1	; data_p
  label_5: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [541]	MOVE     	2 0
  label_6: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [541]	CALL     	1 2 2
  label_7: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [541]	RETURN   	1 2
  label_8: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [542]	RETURN   	0 1
  label_9: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 543
// lastlinedefined = 601
// what = Lua
static int zz_magic_function_57 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [544]	LOADNIL  	2 0
  label_0: {
    Instruction i = 0x00000084;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [547]	CLOSURE  	3 0	; 0x559f6379ff30
  label_1: {
    Instruction i = 0x000000ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [547]	MOVE     	2 3
  label_2: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [548]	GETUPVAL 	3 6	; force1
  label_3: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [548]	MOVE     	4 0
  label_4: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [548]	CALL     	3 2 2
  label_5: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [548]	MOVE     	0 3
  label_6: {
    Instruction i = 0x01800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [549]	GETUPVAL 	3 7	; any_delay_just_p
  label_7: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [549]	MOVE     	4 0
  label_8: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [549]	CALL     	3 2 2
  label_9: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [549]	TEST     	3 0
  label_10: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [549]	JMP      	0 5	; to 17
  label_11: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_17;
  }
  
  // [550]	GETUPVAL 	3 8	; apply
  label_12: {
    Instruction i = 0x040000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [550]	MOVE     	4 0
  label_13: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [550]	MOVE     	5 1
  label_14: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [550]	TAILCALL 	3 3 0
  label_15: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [550]	RETURN   	3 0
  label_16: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [552]	GETUPVAL 	3 9	; data_p
  label_17: {
    Instruction i = 0x048000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [552]	MOVE     	4 0
  label_18: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [552]	CALL     	3 2 2
  label_19: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [552]	TEST     	3 1
  label_20: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_22;
    }
  }
  
  // [552]	JMP      	0 3	; to 25
  label_21: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [553]	MOVE     	3 2
  label_22: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [553]	TAILCALL 	3 1 0
  label_23: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [553]	RETURN   	3 0
  label_24: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [555]	GETUPVAL 	3 10	; force_all
  label_25: {
    Instruction i = 0x050000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [555]	GETUPVAL 	4 11	; data_name
  label_26: {
    Instruction i = 0x05800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [555]	MOVE     	5 0
  label_27: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [555]	CALL     	4 2 0
  label_28: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [555]	CALL     	3 0 2
  label_29: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [556]	GETUPVAL 	4 12	; symbol_p
  label_30: {
    Instruction i = 0x06000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [556]	MOVE     	5 3
  label_31: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [556]	CALL     	4 2 2
  label_32: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [556]	TEST     	4 0
  label_33: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_35;
    }
  }
  
  // [556]	JMP      	0 6	; to 41
  label_34: {
    Instruction i = 0x8001401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_41;
  }
  
  // [556]	GETUPVAL 	4 13	; symbol_equal_p
  label_35: {
    Instruction i = 0x06800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [556]	MOVE     	5 3
  label_36: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [556]	GETUPVAL 	6 14	; function_symbol
  label_37: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [556]	CALL     	4 3 2
  label_38: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [556]	TEST     	4 1
  label_39: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_41;
    }
  }
  
  // [556]	JMP      	0 3	; to 44
  label_40: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_44;
  }
  
  // [557]	MOVE     	4 2
  label_41: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [557]	TAILCALL 	4 1 0
  label_42: {
    Instruction i = 0x00800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [557]	RETURN   	4 0
  label_43: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [559]	GETUPVAL 	4 10	; force_all
  label_44: {
    Instruction i = 0x05000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [559]	GETUPVAL 	5 15	; data_list
  label_45: {
    Instruction i = 0x07800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [559]	MOVE     	6 0
  label_46: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [559]	CALL     	5 2 0
  label_47: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [559]	CALL     	4 0 2
  label_48: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [560]	GETUPVAL 	5 16	; construction_p
  label_49: {
    Instruction i = 0x08000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [560]	MOVE     	6 4
  label_50: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [560]	CALL     	5 2 2
  label_51: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [560]	TEST     	5 1
  label_52: {
    Instruction i = 0x00004162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_54;
    }
  }
  
  // [560]	JMP      	0 3	; to 57
  label_53: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_57;
  }
  
  // [561]	MOVE     	5 2
  label_54: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [561]	TAILCALL 	5 1 0
  label_55: {
    Instruction i = 0x00800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [561]	RETURN   	5 0
  label_56: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [563]	GETUPVAL 	5 17	; force_all_rec
  label_57: {
    Instruction i = 0x08800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [563]	GETUPVAL 	6 18	; construction_head
  label_58: {
    Instruction i = 0x09000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [563]	MOVE     	7 4
  label_59: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [563]	CALL     	6 2 0
  label_60: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [563]	CALL     	5 0 2
  label_61: {
    Instruction i = 0x00008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [564]	GETUPVAL 	6 10	; force_all
  label_62: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [564]	GETUPVAL 	7 19	; construction_tail
  label_63: {
    Instruction i = 0x098001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [564]	MOVE     	8 4
  label_64: {
    Instruction i = 0x02000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [564]	CALL     	7 2 0
  label_65: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [564]	CALL     	6 0 2
  label_66: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [565]	GETUPVAL 	7 16	; construction_p
  label_67: {
    Instruction i = 0x080001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [565]	MOVE     	8 6
  label_68: {
    Instruction i = 0x03000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [565]	CALL     	7 2 2
  label_69: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [565]	TEST     	7 0
  label_70: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_72;
    }
  }
  
  // [565]	JMP      	0 9	; to 81
  label_71: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_81;
  }
  
  // [565]	GETUPVAL 	7 20	; null_p
  label_72: {
    Instruction i = 0x0a0001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [565]	GETUPVAL 	8 10	; force_all
  label_73: {
    Instruction i = 0x05000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [565]	GETUPVAL 	9 19	; construction_tail
  label_74: {
    Instruction i = 0x09800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [565]	MOVE     	10 6
  label_75: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [565]	CALL     	9 2 0
  label_76: {
    Instruction i = 0x01000264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [565]	CALL     	8 0 0
  label_77: {
    Instruction i = 0x00000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [565]	CALL     	7 0 2
  label_78: {
    Instruction i = 0x000081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [565]	TEST     	7 1
  label_79: {
    Instruction i = 0x000041e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_81;
    }
  }
  
  // [565]	JMP      	0 3	; to 84
  label_80: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_84;
  }
  
  // [566]	MOVE     	7 2
  label_81: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [566]	TAILCALL 	7 1 0
  label_82: {
    Instruction i = 0x008001e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [566]	RETURN   	7 0
  label_83: {
    Instruction i = 0x000001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [568]	GETUPVAL 	7 18	; construction_head
  label_84: {
    Instruction i = 0x090001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [568]	MOVE     	8 6
  label_85: {
    Instruction i = 0x03000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [568]	CALL     	7 2 2
  label_86: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [569]	GETUPVAL 	8 21	; env_null_v
  label_87: {
    Instruction i = 0x0a800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [570]	LOADK    	9 -1	; 0
  label_88: {
    Instruction i = 0x00000241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [571]	GETUPVAL 	10 20	; null_p
  label_89: {
    Instruction i = 0x0a000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [571]	MOVE     	11 5
  label_90: {
    Instruction i = 0x028002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [571]	CALL     	10 2 2
  label_91: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [571]	TEST     	10 1
  label_92: {
    Instruction i = 0x000042a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_94;
    }
  }
  
  // [571]	JMP      	0 59	; to 153
  label_93: {
    Instruction i = 0x800e801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_153;
  }
  
  // [572]	GETUPVAL 	10 22	; name_p
  label_94: {
    Instruction i = 0x0b000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [572]	MOVE     	11 5
  label_95: {
    Instruction i = 0x028002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [572]	CALL     	10 2 2
  label_96: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [572]	TEST     	10 0
  label_97: {
    Instruction i = 0x000002a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_99;
    }
  }
  
  // [572]	JMP      	0 22	; to 121
  label_98: {
    Instruction i = 0x8005401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_121;
  }
  
  // [573]	GETUPVAL 	10 23	; null_v
  label_99: {
    Instruction i = 0x0b800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [575]	LEN      	11 1
  label_100: {
    Instruction i = 0x008002dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [575]	SUB      	11 11 -2	; - 1
  label_101: {
    Instruction i = 0x05c042ce;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [576]	LE       	0 9 11
  label_102: {
    Instruction i = 0x0482c021;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessequal(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_104;
    }
  }
  
  // [576]	JMP      	0 8	; to 112
  label_103: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_112;
  }
  
  // [577]	GETUPVAL 	12 24	; new_construction
  label_104: {
    Instruction i = 0x0c000305;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [577]	ADD      	13 11 -2	; - 1
  label_105: {
    Instruction i = 0x05c0434d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [577]	GETTABLE 	13 1 13
  label_106: {
    Instruction i = 0x00834347;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [577]	MOVE     	14 10
  label_107: {
    Instruction i = 0x05000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [577]	CALL     	12 3 2
  label_108: {
    Instruction i = 0x01808324;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [577]	MOVE     	10 12
  label_109: {
    Instruction i = 0x06000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [578]	SUB      	11 11 -2	; - 1
  label_110: {
    Instruction i = 0x05c042ce;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [578]	JMP      	0 -10	; to 102
  label_111: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_102;
  }
  
  // [581]	GETUPVAL 	11 25	; env_set
  label_112: {
    Instruction i = 0x0c8002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [581]	MOVE     	12 8
  label_113: {
    Instruction i = 0x04000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [581]	MOVE     	13 5
  label_114: {
    Instruction i = 0x02800340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [581]	MOVE     	14 10
  label_115: {
    Instruction i = 0x05000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [581]	CALL     	11 4 2
  label_116: {
    Instruction i = 0x020082e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [581]	MOVE     	8 11
  label_117: {
    Instruction i = 0x05800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [582]	LEN      	9 1
  label_118: {
    Instruction i = 0x0080025c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [583]	GETUPVAL 	5 23	; null_v
  label_119: {
    Instruction i = 0x0b800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [583]	JMP      	0 -32	; to 89
  label_120: {
    Instruction i = 0x7ff7c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_89;
  }
  
  // [584]	GETUPVAL 	10 16	; construction_p
  label_121: {
    Instruction i = 0x08000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [584]	MOVE     	11 5
  label_122: {
    Instruction i = 0x028002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [584]	CALL     	10 2 2
  label_123: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [584]	TEST     	10 0
  label_124: {
    Instruction i = 0x000002a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_126;
    }
  }
  
  // [584]	JMP      	0 23	; to 149
  label_125: {
    Instruction i = 0x8005801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_149;
  }
  
  // [585]	LEN      	10 1
  label_126: {
    Instruction i = 0x0080029c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [585]	LT       	0 9 10
  label_127: {
    Instruction i = 0x04828020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_129;
    }
  }
  
  // [585]	JMP      	0 16	; to 145
  label_128: {
    Instruction i = 0x8003c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_145;
  }
  
  // [586]	ADD      	10 9 -2	; - 1
  label_129: {
    Instruction i = 0x04c0428d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [586]	GETTABLE 	10 1 10
  label_130: {
    Instruction i = 0x00828287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [587]	ADD      	9 9 -2	; - 1
  label_131: {
    Instruction i = 0x04c0424d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [588]	GETUPVAL 	11 25	; env_set
  label_132: {
    Instruction i = 0x0c8002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [588]	MOVE     	12 8
  label_133: {
    Instruction i = 0x04000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [588]	GETUPVAL 	13 18	; construction_head
  label_134: {
    Instruction i = 0x09000345;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [588]	MOVE     	14 5
  label_135: {
    Instruction i = 0x02800380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [588]	CALL     	13 2 2
  label_136: {
    Instruction i = 0x01008364;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [588]	MOVE     	14 10
  label_137: {
    Instruction i = 0x05000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [588]	CALL     	11 4 2
  label_138: {
    Instruction i = 0x020082e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [588]	MOVE     	8 11
  label_139: {
    Instruction i = 0x05800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [589]	GETUPVAL 	11 19	; construction_tail
  label_140: {
    Instruction i = 0x098002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [589]	MOVE     	12 5
  label_141: {
    Instruction i = 0x02800300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [589]	CALL     	11 2 2
  label_142: {
    Instruction i = 0x010082e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [589]	MOVE     	5 11
  label_143: {
    Instruction i = 0x05800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [589]	JMP      	0 -56	; to 89
  label_144: {
    Instruction i = 0x7ff1c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_89;
  }
  
  // [591]	MOVE     	10 2
  label_145: {
    Instruction i = 0x01000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [591]	TAILCALL 	10 1 0
  label_146: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [591]	RETURN   	10 0
  label_147: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [592]	JMP      	0 -60	; to 89
  label_148: {
    Instruction i = 0x7ff0c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_89;
  }
  
  // [594]	MOVE     	10 2
  label_149: {
    Instruction i = 0x01000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [594]	TAILCALL 	10 1 0
  label_150: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [594]	RETURN   	10 0
  label_151: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [595]	JMP      	0 -64	; to 89
  label_152: {
    Instruction i = 0x7fefc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_89;
  }
  
  // [597]	LEN      	10 1
  label_153: {
    Instruction i = 0x0080029c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [597]	EQ       	1 10 9
  label_154: {
    Instruction i = 0x0502405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_156;
    }
  }
  
  // [597]	JMP      	0 3	; to 159
  label_155: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_159;
  }
  
  // [598]	MOVE     	10 2
  label_156: {
    Instruction i = 0x01000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [598]	TAILCALL 	10 1 0
  label_157: {
    Instruction i = 0x008002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [598]	RETURN   	10 0
  label_158: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [600]	GETUPVAL 	10 26	; evaluate
  label_159: {
    Instruction i = 0x0d000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [600]	MOVE     	11 8
  label_160: {
    Instruction i = 0x040002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [600]	MOVE     	12 7
  label_161: {
    Instruction i = 0x03800300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [600]	TAILCALL 	10 3 0
  label_162: {
    Instruction i = 0x018002a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [600]	RETURN   	10 0
  label_163: {
    Instruction i = 0x000002a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [601]	RETURN   	0 1
  label_164: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 545
// lastlinedefined = 547
// what = Lua
static int zz_magic_function_58 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [546]	GETUPVAL 	0 0	; new_error
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	1 1	; system_symbol
  label_1: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	2 2	; new_list
  label_2: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	3 3	; function_builtin_use_systemName
  label_3: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	4 2	; new_list
  label_4: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	5 4	; apply_function_builtin_systemName
  label_5: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	6 2	; new_list
  label_6: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	7 5	; f
  label_7: {
    Instruction i = 0x028001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	8 6	; jsArray_to_list
  label_8: {
    Instruction i = 0x03000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	GETUPVAL 	9 7	; xs
  label_9: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [546]	CALL     	8 2 0
  label_10: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [546]	CALL     	6 0 0
  label_11: {
    Instruction i = 0x000001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [546]	CALL     	4 0 0
  label_12: {
    Instruction i = 0x00000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [546]	CALL     	2 0 0
  label_13: {
    Instruction i = 0x000000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [546]	TAILCALL 	0 0 0
  label_14: {
    Instruction i = 0x00000025;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [546]	RETURN   	0 0
  label_15: {
    Instruction i = 0x00000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [547]	RETURN   	0 1
  label_16: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 602
// lastlinedefined = 626
// what = Lua
static int zz_magic_function_59 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [603]	GETUPVAL 	2 0	; new_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [603]	GETUPVAL 	3 1	; system_symbol
  label_1: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [603]	GETUPVAL 	4 2	; new_list
  label_2: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [603]	GETUPVAL 	5 3	; function_builtin_use_systemName
  label_3: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [603]	GETUPVAL 	6 2	; new_list
  label_4: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [603]	MOVE     	7 0
  label_5: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [603]	GETUPVAL 	8 4	; jsArray_to_list
  label_6: {
    Instruction i = 0x02000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [603]	MOVE     	9 1
  label_7: {
    Instruction i = 0x00800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [603]	CALL     	8 2 0
  label_8: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [603]	CALL     	6 0 0
  label_9: {
    Instruction i = 0x000001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [603]	CALL     	4 0 0
  label_10: {
    Instruction i = 0x00000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [603]	CALL     	2 0 2
  label_11: {
    Instruction i = 0x000080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [605]	LOADK    	3 -1	; 0
  label_12: {
    Instruction i = 0x000000c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [606]	GETUPVAL 	4 5	; real_builtin_func_apply_s
  label_13: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [606]	LEN      	4 4
  label_14: {
    Instruction i = 0x0200011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [606]	LT       	0 3 4
  label_15: {
    Instruction i = 0x01810020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_17;
    }
  }
  
  // [606]	JMP      	0 49	; to 66
  label_16: {
    Instruction i = 0x800c001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_66;
  }
  
  // [607]	GETUPVAL 	4 6	; jsbool_equal_p
  label_17: {
    Instruction i = 0x03000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [607]	MOVE     	5 0
  label_18: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [607]	ADD      	6 3 -2	; - 1
  label_19: {
    Instruction i = 0x01c0418d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [607]	GETTABUP 	6 5 6	; real_builtin_func_apply_s
  label_20: {
    Instruction i = 0x02818186;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [607]	GETTABLE 	6 6 -2	; 1
  label_21: {
    Instruction i = 0x03404187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [607]	CALL     	4 3 2
  label_22: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [607]	TEST     	4 0
  label_23: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_25;
    }
  }
  
  // [607]	JMP      	0 39	; to 64
  label_24: {
    Instruction i = 0x8009801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_64;
  }
  
  // [608]	ADD      	4 3 -2	; - 1
  label_25: {
    Instruction i = 0x01c0410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [608]	GETTABUP 	4 5 4	; real_builtin_func_apply_s
  label_26: {
    Instruction i = 0x02810106;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [608]	GETTABLE 	4 4 -3	; 2
  label_27: {
    Instruction i = 0x02408107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [609]	LEN      	5 1
  label_28: {
    Instruction i = 0x0080015c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [609]	EQ       	1 5 4
  label_29: {
    Instruction i = 0x0281005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_31;
    }
  }
  
  // [609]	JMP      	0 1	; to 32
  label_30: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_32;
  }
  
  // [610]	RETURN   	2 2
  label_31: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [612]	ADD      	5 3 -2	; - 1
  label_32: {
    Instruction i = 0x01c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [612]	GETTABUP 	5 5 5	; real_builtin_func_apply_s
  label_33: {
    Instruction i = 0x02814146;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [612]	GETTABLE 	5 5 -4	; 3
  label_34: {
    Instruction i = 0x02c0c147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [613]	EQ       	0 4 -2	; - 1
  label_35: {
    Instruction i = 0x0240401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_37;
    }
  }
  
  // [613]	JMP      	0 6	; to 43
  label_36: {
    Instruction i = 0x8001401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [614]	MOVE     	6 5
  label_37: {
    Instruction i = 0x02800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [614]	GETTABLE 	7 1 -2	; 1
  label_38: {
    Instruction i = 0x00c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [614]	MOVE     	8 2
  label_39: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [614]	TAILCALL 	6 3 0
  label_40: {
    Instruction i = 0x018001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [614]	RETURN   	6 0
  label_41: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [614]	JMP      	0 18	; to 61
  label_42: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [615]	EQ       	0 4 -3	; - 2
  label_43: {
    Instruction i = 0x0240801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_45;
    }
  }
  
  // [615]	JMP      	0 7	; to 52
  label_44: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_52;
  }
  
  // [616]	MOVE     	6 5
  label_45: {
    Instruction i = 0x02800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [616]	GETTABLE 	7 1 -2	; 1
  label_46: {
    Instruction i = 0x00c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [616]	GETTABLE 	8 1 -3	; 2
  label_47: {
    Instruction i = 0x00c08207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [616]	MOVE     	9 2
  label_48: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [616]	TAILCALL 	6 4 0
  label_49: {
    Instruction i = 0x020001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [616]	RETURN   	6 0
  label_50: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [616]	JMP      	0 9	; to 61
  label_51: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [617]	EQ       	0 4 -4	; - 3
  label_52: {
    Instruction i = 0x0240c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_54;
    }
  }
  
  // [617]	JMP      	0 7	; to 61
  label_53: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [618]	MOVE     	6 5
  label_54: {
    Instruction i = 0x02800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [618]	GETTABLE 	7 1 -2	; 1
  label_55: {
    Instruction i = 0x00c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [618]	GETTABLE 	8 1 -3	; 2
  label_56: {
    Instruction i = 0x00c08207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [618]	GETTABLE 	9 1 -4	; 3
  label_57: {
    Instruction i = 0x00c0c247;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [618]	MOVE     	10 2
  label_58: {
    Instruction i = 0x01000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [618]	TAILCALL 	6 5 0
  label_59: {
    Instruction i = 0x028001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [618]	RETURN   	6 0
  label_60: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [620]	GETUPVAL 	6 7	; ERROR
  label_61: {
    Instruction i = 0x03800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [620]	TAILCALL 	6 1 0
  label_62: {
    Instruction i = 0x008001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [620]	RETURN   	6 0
  label_63: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [622]	ADD      	3 3 -2	; - 1
  label_64: {
    Instruction i = 0x01c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [622]	JMP      	0 -53	; to 13
  label_65: {
    Instruction i = 0x7ff2801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [625]	RETURN   	2 2
  label_66: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [626]	RETURN   	0 1
  label_67: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 627
// lastlinedefined = 641
// what = Lua
static int zz_magic_function_60 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [628]	GETUPVAL 	3 0	; new_error
  label_0: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	GETUPVAL 	4 1	; system_symbol
  label_1: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	GETUPVAL 	5 2	; new_list
  label_2: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	GETUPVAL 	6 3	; form_builtin_use_systemName
  label_3: {
    Instruction i = 0x01800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	GETUPVAL 	7 2	; new_list
  label_4: {
    Instruction i = 0x010001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	GETUPVAL 	8 4	; env2val
  label_5: {
    Instruction i = 0x02000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	MOVE     	9 0
  label_6: {
    Instruction i = 0x00000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [628]	CALL     	8 2 2
  label_7: {
    Instruction i = 0x01008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [628]	MOVE     	9 1
  label_8: {
    Instruction i = 0x00800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [628]	GETUPVAL 	10 5	; jsArray_to_list
  label_9: {
    Instruction i = 0x02800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [628]	MOVE     	11 2
  label_10: {
    Instruction i = 0x010002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [628]	CALL     	10 2 0
  label_11: {
    Instruction i = 0x010002a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [628]	CALL     	7 0 0
  label_12: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [628]	CALL     	5 0 0
  label_13: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [628]	CALL     	3 0 2
  label_14: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [629]	GETUPVAL 	4 6	; jsbool_equal_p
  label_15: {
    Instruction i = 0x03000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [629]	MOVE     	5 1
  label_16: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [629]	GETUPVAL 	6 7	; quote_form_builtin_systemName
  label_17: {
    Instruction i = 0x03800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [629]	CALL     	4 3 2
  label_18: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [629]	TEST     	4 0
  label_19: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [629]	JMP      	0 7	; to 28
  label_20: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_28;
  }
  
  // [630]	LEN      	4 2
  label_21: {
    Instruction i = 0x0100011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [630]	EQ       	1 4 -1	; - 1
  label_22: {
    Instruction i = 0x0240005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_24;
    }
  }
  
  // [630]	JMP      	0 1	; to 25
  label_23: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [631]	RETURN   	3 2
  label_24: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [633]	GETTABLE 	4 2 -1	; 1
  label_25: {
    Instruction i = 0x01400107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [633]	RETURN   	4 2
  label_26: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [633]	JMP      	0 17	; to 45
  label_27: {
    Instruction i = 0x8004001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_45;
  }
  
  // [634]	GETUPVAL 	4 6	; jsbool_equal_p
  label_28: {
    Instruction i = 0x03000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [634]	MOVE     	5 1
  label_29: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [634]	GETUPVAL 	6 8	; lambda_form_builtin_systemName
  label_30: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [634]	CALL     	4 3 2
  label_31: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [634]	TEST     	4 0
  label_32: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_34;
    }
  }
  
  // [634]	JMP      	0 11	; to 45
  label_33: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_45;
  }
  
  // [635]	LEN      	4 2
  label_34: {
    Instruction i = 0x0100011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [635]	EQ       	1 4 -2	; - 2
  label_35: {
    Instruction i = 0x0240405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_37;
    }
  }
  
  // [635]	JMP      	0 1	; to 38
  label_36: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_38;
  }
  
  // [636]	RETURN   	3 2
  label_37: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [638]	GETUPVAL 	4 9	; new_lambda
  label_38: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [638]	MOVE     	5 0
  label_39: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [638]	GETTABLE 	6 2 -1	; 1
  label_40: {
    Instruction i = 0x01400187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [638]	GETTABLE 	7 2 -2	; 2
  label_41: {
    Instruction i = 0x014041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [638]	MOVE     	8 3
  label_42: {
    Instruction i = 0x01800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [638]	TAILCALL 	4 5 0
  label_43: {
    Instruction i = 0x02800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [638]	RETURN   	4 0
  label_44: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [640]	RETURN   	3 2
  label_45: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [641]	RETURN   	0 1
  label_46: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 642
// lastlinedefined = 708
// what = Lua
static int zz_magic_function_61 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [643]	EQ       	0 3 -1	; - nil
  label_0: {
    Instruction i = 0x01c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [643]	JMP      	0 1	; to 3
  label_1: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_3;
  }
  
  // [644]	LOADBOOL 	3 0 0
  label_2: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_4;
    }
  }
  
  // [646]	LOADNIL  	4 0
  label_3: {
    Instruction i = 0x00000104;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [653]	CLOSURE  	5 0	; 0x559f637a0060
  label_4: {
    Instruction i = 0x0000016c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [653]	MOVE     	4 5
  label_5: {
    Instruction i = 0x02800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [654]	LOADNIL  	5 0
  label_6: {
    Instruction i = 0x00000144;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [657]	CLOSURE  	6 1	; 0x559f637a0fd0
  label_7: {
    Instruction i = 0x000041ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [657]	MOVE     	5 6
  label_8: {
    Instruction i = 0x03000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [658]	GETUPVAL 	6 8	; force_all_rec
  label_9: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [658]	MOVE     	7 1
  label_10: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [658]	CALL     	6 2 2
  label_11: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [658]	MOVE     	1 6
  label_12: {
    Instruction i = 0x03000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [659]	NEWTABLE 	6 0 0
  label_13: {
    Instruction i = 0x0000018b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [660]	LOADBOOL 	7 0 0
  label_14: {
    Instruction i = 0x000001c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [661]	MOVE     	8 1
  label_15: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [662]	GETUPVAL 	9 9	; null_p
  label_16: {
    Instruction i = 0x04800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [662]	MOVE     	10 8
  label_17: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [662]	CALL     	9 2 2
  label_18: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [662]	TEST     	9 1
  label_19: {
    Instruction i = 0x00004262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [662]	JMP      	0 32	; to 53
  label_20: {
    Instruction i = 0x8007c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_53;
  }
  
  // [663]	GETUPVAL 	9 10	; name_p
  label_21: {
    Instruction i = 0x05000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [663]	MOVE     	10 8
  label_22: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [663]	CALL     	9 2 2
  label_23: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [663]	TEST     	9 0
  label_24: {
    Instruction i = 0x00000262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_26;
    }
  }
  
  // [663]	JMP      	0 7	; to 33
  label_25: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_33;
  }
  
  // [664]	GETUPVAL 	9 11	; __TS__ArrayPush
  label_26: {
    Instruction i = 0x05800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [664]	MOVE     	10 6
  label_27: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [664]	MOVE     	11 8
  label_28: {
    Instruction i = 0x040002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [664]	CALL     	9 3 1
  label_29: {
    Instruction i = 0x01804264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [665]	LOADBOOL 	7 1 0
  label_30: {
    Instruction i = 0x008001c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_32;
    }
  }
  
  // [666]	GETUPVAL 	8 12	; null_v
  label_31: {
    Instruction i = 0x06000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [666]	JMP      	0 -17	; to 16
  label_32: {
    Instruction i = 0x7ffb801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [667]	GETUPVAL 	9 13	; construction_p
  label_33: {
    Instruction i = 0x06800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [667]	MOVE     	10 8
  label_34: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [667]	CALL     	9 2 2
  label_35: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [667]	TEST     	9 0
  label_36: {
    Instruction i = 0x00000262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_38;
    }
  }
  
  // [667]	JMP      	0 11	; to 49
  label_37: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_49;
  }
  
  // [668]	GETUPVAL 	9 11	; __TS__ArrayPush
  label_38: {
    Instruction i = 0x05800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [668]	MOVE     	10 6
  label_39: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [668]	GETUPVAL 	11 14	; construction_head
  label_40: {
    Instruction i = 0x070002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [668]	MOVE     	12 8
  label_41: {
    Instruction i = 0x04000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [668]	CALL     	11 2 0
  label_42: {
    Instruction i = 0x010002e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [668]	CALL     	9 0 1
  label_43: {
    Instruction i = 0x00004264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [669]	GETUPVAL 	9 15	; construction_tail
  label_44: {
    Instruction i = 0x07800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [669]	MOVE     	10 8
  label_45: {
    Instruction i = 0x04000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [669]	CALL     	9 2 2
  label_46: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [669]	MOVE     	8 9
  label_47: {
    Instruction i = 0x04800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [669]	JMP      	0 -33	; to 16
  label_48: {
    Instruction i = 0x7ff7801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [671]	MOVE     	9 4
  label_49: {
    Instruction i = 0x02000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [671]	TAILCALL 	9 1 0
  label_50: {
    Instruction i = 0x00800265;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [671]	RETURN   	9 0
  label_51: {
    Instruction i = 0x00000266;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [672]	JMP      	0 -37	; to 16
  label_52: {
    Instruction i = 0x7ff6801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [674]	MOVE     	9 1
  label_53: {
    Instruction i = 0x00800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [675]	TEST     	7 0
  label_54: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_56;
    }
  }
  
  // [675]	JMP      	0 4	; to 60
  label_55: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_60;
  }
  
  // [676]	GETUPVAL 	10 6	; jsArray_to_list
  label_56: {
    Instruction i = 0x03000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [676]	MOVE     	11 6
  label_57: {
    Instruction i = 0x030002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [676]	CALL     	10 2 2
  label_58: {
    Instruction i = 0x010082a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [676]	MOVE     	9 10
  label_59: {
    Instruction i = 0x05000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [678]	NEWTABLE 	10 0 0
  label_60: {
    Instruction i = 0x0000028b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [679]	GETUPVAL 	11 16	; env_foreach
  label_61: {
    Instruction i = 0x080002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [679]	MOVE     	12 0
  label_62: {
    Instruction i = 0x00000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [690]	CLOSURE  	13 2	; 0x559f637a0d50
  label_63: {
    Instruction i = 0x0000836c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [679]	CALL     	11 3 1
  label_64: {
    Instruction i = 0x018042e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [691]	MOVE     	11 9
  label_65: {
    Instruction i = 0x048002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [693]	LEN      	12 10
  label_66: {
    Instruction i = 0x0500031c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [693]	SUB      	12 12 -2	; - 1
  label_67: {
    Instruction i = 0x0640430e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [694]	LE       	0 -3 12	; 0 -
  label_68: {
    Instruction i = 0x81030021;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessequal(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_70;
    }
  }
  
  // [694]	JMP      	0 8	; to 78
  label_69: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_78;
  }
  
  // [695]	GETUPVAL 	13 18	; new_construction
  label_70: {
    Instruction i = 0x09000345;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [695]	ADD      	14 12 -2	; - 1
  label_71: {
    Instruction i = 0x0640438d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [695]	GETTABLE 	14 10 14
  label_72: {
    Instruction i = 0x05038387;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [695]	MOVE     	15 11
  label_73: {
    Instruction i = 0x058003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [695]	CALL     	13 3 2
  label_74: {
    Instruction i = 0x01808364;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [695]	MOVE     	11 13
  label_75: {
    Instruction i = 0x068002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [696]	SUB      	12 12 -2	; - 1
  label_76: {
    Instruction i = 0x0640430e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [696]	JMP      	0 -10	; to 68
  label_77: {
    Instruction i = 0x7ffd401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_68;
  }
  
  // [699]	MOVE     	12 9
  label_78: {
    Instruction i = 0x04800300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [701]	LEN      	13 10
  label_79: {
    Instruction i = 0x0500035c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [701]	SUB      	13 13 -2	; - 1
  label_80: {
    Instruction i = 0x06c0434e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [702]	LE       	0 -3 13	; 0 -
  label_81: {
    Instruction i = 0x81034021;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessequal(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_83;
    }
  }
  
  // [702]	JMP      	0 13	; to 96
  label_82: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_96;
  }
  
  // [703]	GETUPVAL 	14 18	; new_construction
  label_83: {
    Instruction i = 0x09000385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [703]	MOVE     	15 5
  label_84: {
    Instruction i = 0x028003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [703]	GETUPVAL 	16 19	; must_env_get
  label_85: {
    Instruction i = 0x09800405;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [703]	MOVE     	17 0
  label_86: {
    Instruction i = 0x00000440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [703]	ADD      	18 13 -2	; - 1
  label_87: {
    Instruction i = 0x06c0448d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [703]	GETTABLE 	18 10 18
  label_88: {
    Instruction i = 0x05048487;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [703]	CALL     	16 3 0
  label_89: {
    Instruction i = 0x01800424;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [703]	CALL     	15 0 2
  label_90: {
    Instruction i = 0x000083e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [703]	MOVE     	16 12
  label_91: {
    Instruction i = 0x06000400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [703]	CALL     	14 3 2
  label_92: {
    Instruction i = 0x018083a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [703]	MOVE     	12 14
  label_93: {
    Instruction i = 0x07000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [704]	SUB      	13 13 -2	; - 1
  label_94: {
    Instruction i = 0x06c0434e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [704]	JMP      	0 -15	; to 81
  label_95: {
    Instruction i = 0x7ffc001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_81;
  }
  
  // [707]	GETUPVAL 	13 20	; new_data
  label_96: {
    Instruction i = 0x0a000345;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	GETUPVAL 	14 21	; function_symbol
  label_97: {
    Instruction i = 0x0a800385;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	GETUPVAL 	15 2	; new_list
  label_98: {
    Instruction i = 0x010003c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	MOVE     	16 1
  label_99: {
    Instruction i = 0x00800400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [707]	GETUPVAL 	17 18	; new_construction
  label_100: {
    Instruction i = 0x09000445;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	MOVE     	18 5
  label_101: {
    Instruction i = 0x02800480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [707]	GETUPVAL 	19 20	; new_data
  label_102: {
    Instruction i = 0x0a0004c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	GETUPVAL 	20 21	; function_symbol
  label_103: {
    Instruction i = 0x0a800505;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	GETUPVAL 	21 2	; new_list
  label_104: {
    Instruction i = 0x01000545;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [707]	MOVE     	22 11
  label_105: {
    Instruction i = 0x05800580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [707]	MOVE     	23 2
  label_106: {
    Instruction i = 0x010005c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [707]	CALL     	21 3 0
  label_107: {
    Instruction i = 0x01800564;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [707]	CALL     	19 0 0
  label_108: {
    Instruction i = 0x000004e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [707]	CALL     	18 0 2
  label_109: {
    Instruction i = 0x000084a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [707]	MOVE     	19 12
  label_110: {
    Instruction i = 0x060004c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [707]	CALL     	17 3 0
  label_111: {
    Instruction i = 0x01800464;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [707]	CALL     	15 0 0
  label_112: {
    Instruction i = 0x000003e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [707]	TAILCALL 	13 0 0
  label_113: {
    Instruction i = 0x00000365;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [707]	RETURN   	13 0
  label_114: {
    Instruction i = 0x00000366;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [708]	RETURN   	0 1
  label_115: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 647
// lastlinedefined = 653
// what = Lua
static int zz_magic_function_62 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [648]	GETUPVAL 	0 0	; error_v
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [648]	EQ       	0 0 -1	; - false
  label_1: {
    Instruction i = 0x0040001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_3;
    }
  }
  
  // [648]	JMP      	0 20	; to 23
  label_2: {
    Instruction i = 0x8004c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [649]	GETUPVAL 	0 1	; new_error
  label_3: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	1 2	; system_symbol
  label_4: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	2 3	; new_list
  label_5: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	3 4	; form_builtin_use_systemName
  label_6: {
    Instruction i = 0x020000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	4 3	; new_list
  label_7: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	5 5	; env2val
  label_8: {
    Instruction i = 0x02800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	6 6	; env
  label_9: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	CALL     	5 2 2
  label_10: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [649]	GETUPVAL 	6 7	; lambda_form_builtin_systemName
  label_11: {
    Instruction i = 0x03800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	7 8	; jsArray_to_list
  label_12: {
    Instruction i = 0x040001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	NEWTABLE 	8 2 0
  label_13: {
    Instruction i = 0x0100020b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [649]	GETUPVAL 	9 9	; args_pat
  label_14: {
    Instruction i = 0x04800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	GETUPVAL 	10 10	; body
  label_15: {
    Instruction i = 0x05000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [649]	SETLIST  	8 2 1	; 1
  label_16: {
    Instruction i = 0x0100422b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [649]	CALL     	7 2 0
  label_17: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [649]	CALL     	4 0 0
  label_18: {
    Instruction i = 0x00000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [649]	CALL     	2 0 0
  label_19: {
    Instruction i = 0x000000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [649]	TAILCALL 	0 0 0
  label_20: {
    Instruction i = 0x00000025;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [649]	RETURN   	0 0
  label_21: {
    Instruction i = 0x00000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [649]	JMP      	0 2	; to 25
  label_22: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [651]	GETUPVAL 	0 0	; error_v
  label_23: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [651]	RETURN   	0 2
  label_24: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [653]	RETURN   	0 1
  label_25: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 655
// lastlinedefined = 657
// what = Lua
static int zz_magic_function_63 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [656]	GETUPVAL 	1 0	; new_list
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [656]	GETUPVAL 	2 1	; form_builtin_use_systemName
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [656]	GETUPVAL 	3 2	; quote_form_builtin_systemName
  label_2: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [656]	MOVE     	4 0
  label_3: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [656]	TAILCALL 	1 4 0
  label_4: {
    Instruction i = 0x02000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [656]	RETURN   	1 0
  label_5: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [657]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 679
// lastlinedefined = 690
// what = Lua
static int zz_magic_function_64 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [681]	LOADK    	2 -1	; 0
  label_0: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [682]	GETUPVAL 	3 0	; args_pat_vars
  label_1: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [682]	LEN      	3 3
  label_2: {
    Instruction i = 0x018000dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [682]	LT       	0 2 3
  label_3: {
    Instruction i = 0x0100c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [682]	JMP      	0 10	; to 15
  label_4: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [683]	GETUPVAL 	3 1	; jsbool_equal_p
  label_5: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [683]	ADD      	4 2 -2	; - 1
  label_6: {
    Instruction i = 0x0140410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [683]	GETTABUP 	4 0 4	; args_pat_vars
  label_7: {
    Instruction i = 0x00010106;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [683]	MOVE     	5 0
  label_8: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [683]	CALL     	3 3 2
  label_9: {
    Instruction i = 0x018080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [683]	TEST     	3 0
  label_10: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [683]	JMP      	0 1	; to 13
  label_11: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [684]	RETURN   	0 1
  label_12: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [686]	ADD      	2 2 -2	; - 1
  label_13: {
    Instruction i = 0x0140408d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [686]	JMP      	0 -14	; to 1
  label_14: {
    Instruction i = 0x7ffc401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_1;
  }
  
  // [689]	GETUPVAL 	2 2	; __TS__ArrayPush
  label_15: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [689]	GETUPVAL 	3 3	; env_vars
  label_16: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [689]	MOVE     	4 0
  label_17: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [689]	CALL     	2 3 1
  label_18: {
    Instruction i = 0x018040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [690]	RETURN   	0 1
  label_19: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 709
// lastlinedefined = 756
// what = Lua
static int zz_magic_function_65 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [710]	EQ       	0 0 1
  label_0: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [710]	JMP      	0 2	; to 4
  label_1: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [711]	LOADBOOL 	2 1 0
  label_2: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_4;
    }
  }
  
  // [711]	RETURN   	2 2
  label_3: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [713]	GETUPVAL 	2 0	; force_all
  label_4: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [713]	MOVE     	3 0
  label_5: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [713]	CALL     	2 2 2
  label_6: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [713]	MOVE     	0 2
  label_7: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [714]	GETUPVAL 	2 0	; force_all
  label_8: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [714]	MOVE     	3 1
  label_9: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [714]	CALL     	2 2 2
  label_10: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [714]	MOVE     	1 2
  label_11: {
    Instruction i = 0x01000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [715]	EQ       	0 0 1
  label_12: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_14;
    }
  }
  
  // [715]	JMP      	0 2	; to 16
  label_13: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [716]	LOADBOOL 	2 1 0
  label_14: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [716]	RETURN   	2 2
  label_15: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [718]	LOADNIL  	2 0
  label_16: {
    Instruction i = 0x00000084;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [726]	CLOSURE  	3 0	; 0x559f637a13b0
  label_17: {
    Instruction i = 0x000000ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [726]	MOVE     	2 3
  label_18: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [727]	GETUPVAL 	3 3	; null_p
  label_19: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [727]	MOVE     	4 0
  label_20: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [727]	CALL     	3 2 2
  label_21: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [727]	TEST     	3 0
  label_22: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_24;
    }
  }
  
  // [727]	JMP      	0 18	; to 42
  label_23: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_42;
  }
  
  // [728]	GETUPVAL 	3 3	; null_p
  label_24: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [728]	MOVE     	4 1
  label_25: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [728]	CALL     	3 2 2
  label_26: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [728]	TEST     	3 1
  label_27: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_29;
    }
  }
  
  // [728]	JMP      	0 2	; to 31
  label_28: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [729]	LOADBOOL 	3 0 0
  label_29: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_31;
    }
  }
  
  // [729]	RETURN   	3 2
  label_30: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [731]	GETUPVAL 	3 2	; lang_set_do
  label_31: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [731]	MOVE     	4 0
  label_32: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [731]	GETUPVAL 	5 4	; null_v
  label_33: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [731]	CALL     	3 3 1
  label_34: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [732]	GETUPVAL 	3 2	; lang_set_do
  label_35: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [732]	MOVE     	4 1
  label_36: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [732]	GETUPVAL 	5 4	; null_v
  label_37: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [732]	CALL     	3 3 1
  label_38: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [733]	LOADBOOL 	3 1 0
  label_39: {
    Instruction i = 0x008000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_41;
    }
  }
  
  // [733]	RETURN   	3 2
  label_40: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [733]	JMP      	0 77	; to 119
  label_41: {
    Instruction i = 0x8013001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_119;
  }
  
  // [734]	GETUPVAL 	3 5	; symbol_p
  label_42: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [734]	MOVE     	4 0
  label_43: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [734]	CALL     	3 2 2
  label_44: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [734]	TEST     	3 0
  label_45: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_47;
    }
  }
  
  // [734]	JMP      	0 13	; to 60
  label_46: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_60;
  }
  
  // [735]	GETUPVAL 	3 5	; symbol_p
  label_47: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [735]	MOVE     	4 1
  label_48: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [735]	CALL     	3 2 2
  label_49: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [735]	TEST     	3 1
  label_50: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_52;
    }
  }
  
  // [735]	JMP      	0 2	; to 54
  label_51: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_54;
  }
  
  // [736]	LOADBOOL 	3 0 0
  label_52: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_54;
    }
  }
  
  // [736]	RETURN   	3 2
  label_53: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [738]	GETUPVAL 	3 6	; symbol_equal_p
  label_54: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [738]	MOVE     	4 0
  label_55: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [738]	MOVE     	5 1
  label_56: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [738]	TAILCALL 	3 3 0
  label_57: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [738]	RETURN   	3 0
  label_58: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [738]	JMP      	0 59	; to 119
  label_59: {
    Instruction i = 0x800e801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_119;
  }
  
  // [739]	GETUPVAL 	3 7	; construction_p
  label_60: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [739]	MOVE     	4 0
  label_61: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [739]	CALL     	3 2 2
  label_62: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [739]	TEST     	3 0
  label_63: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_65;
    }
  }
  
  // [739]	JMP      	0 15	; to 80
  label_64: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_80;
  }
  
  // [740]	GETUPVAL 	3 7	; construction_p
  label_65: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [740]	MOVE     	4 1
  label_66: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [740]	CALL     	3 2 2
  label_67: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [740]	TEST     	3 1
  label_68: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_70;
    }
  }
  
  // [740]	JMP      	0 2	; to 72
  label_69: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_72;
  }
  
  // [741]	LOADBOOL 	3 0 0
  label_70: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_72;
    }
  }
  
  // [741]	RETURN   	3 2
  label_71: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [743]	MOVE     	3 2
  label_72: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [743]	MOVE     	4 0
  label_73: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [743]	MOVE     	5 1
  label_74: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [743]	GETUPVAL 	6 8	; construction_head
  label_75: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [743]	GETUPVAL 	7 9	; construction_tail
  label_76: {
    Instruction i = 0x048001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [743]	TAILCALL 	3 5 0
  label_77: {
    Instruction i = 0x028000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [743]	RETURN   	3 0
  label_78: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [743]	JMP      	0 39	; to 119
  label_79: {
    Instruction i = 0x8009801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_119;
  }
  
  // [744]	GETUPVAL 	3 10	; error_p
  label_80: {
    Instruction i = 0x050000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [744]	MOVE     	4 0
  label_81: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [744]	CALL     	3 2 2
  label_82: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [744]	TEST     	3 0
  label_83: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_85;
    }
  }
  
  // [744]	JMP      	0 15	; to 100
  label_84: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_100;
  }
  
  // [745]	GETUPVAL 	3 10	; error_p
  label_85: {
    Instruction i = 0x050000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [745]	MOVE     	4 1
  label_86: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [745]	CALL     	3 2 2
  label_87: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [745]	TEST     	3 1
  label_88: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_90;
    }
  }
  
  // [745]	JMP      	0 2	; to 92
  label_89: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_92;
  }
  
  // [746]	LOADBOOL 	3 0 0
  label_90: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_92;
    }
  }
  
  // [746]	RETURN   	3 2
  label_91: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [748]	MOVE     	3 2
  label_92: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [748]	MOVE     	4 0
  label_93: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [748]	MOVE     	5 1
  label_94: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [748]	GETUPVAL 	6 11	; error_name
  label_95: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [748]	GETUPVAL 	7 12	; error_list
  label_96: {
    Instruction i = 0x060001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [748]	TAILCALL 	3 5 0
  label_97: {
    Instruction i = 0x028000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [748]	RETURN   	3 0
  label_98: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [748]	JMP      	0 19	; to 119
  label_99: {
    Instruction i = 0x8004801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_119;
  }
  
  // [749]	GETUPVAL 	3 13	; data_p
  label_100: {
    Instruction i = 0x068000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [749]	MOVE     	4 0
  label_101: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [749]	CALL     	3 2 2
  label_102: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [749]	TEST     	3 0
  label_103: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_105;
    }
  }
  
  // [749]	JMP      	0 14	; to 119
  label_104: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_119;
  }
  
  // [750]	GETUPVAL 	3 13	; data_p
  label_105: {
    Instruction i = 0x068000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [750]	MOVE     	4 1
  label_106: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [750]	CALL     	3 2 2
  label_107: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [750]	TEST     	3 1
  label_108: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_110;
    }
  }
  
  // [750]	JMP      	0 2	; to 112
  label_109: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_112;
  }
  
  // [751]	LOADBOOL 	3 0 0
  label_110: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_112;
    }
  }
  
  // [751]	RETURN   	3 2
  label_111: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [753]	MOVE     	3 2
  label_112: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [753]	MOVE     	4 0
  label_113: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [753]	MOVE     	5 1
  label_114: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [753]	GETUPVAL 	6 14	; data_name
  label_115: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [753]	GETUPVAL 	7 15	; data_list
  label_116: {
    Instruction i = 0x078001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [753]	TAILCALL 	3 5 0
  label_117: {
    Instruction i = 0x028000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [753]	RETURN   	3 0
  label_118: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [755]	GETUPVAL 	3 16	; ERROR
  label_119: {
    Instruction i = 0x080000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [755]	TAILCALL 	3 1 0
  label_120: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [755]	RETURN   	3 0
  label_121: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [756]	RETURN   	0 1
  label_122: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 719
// lastlinedefined = 726
// what = Lua
static int zz_magic_function_66 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [720]	GETUPVAL 	4 0	; jsbool_equal_p
  label_0: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [720]	MOVE     	5 2
  label_1: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	MOVE     	6 0
  label_2: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	CALL     	5 2 2
  label_3: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [720]	MOVE     	6 2
  label_4: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	MOVE     	7 1
  label_5: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	CALL     	6 2 0
  label_6: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [720]	CALL     	4 0 2
  label_7: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [720]	TEST     	4 0
  label_8: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [720]	JMP      	0 17	; to 27
  label_9: {
    Instruction i = 0x8004001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_27;
  }
  
  // [720]	GETUPVAL 	4 0	; jsbool_equal_p
  label_10: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [720]	MOVE     	5 3
  label_11: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	MOVE     	6 0
  label_12: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	CALL     	5 2 2
  label_13: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [720]	MOVE     	6 3
  label_14: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	MOVE     	7 1
  label_15: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [720]	CALL     	6 2 0
  label_16: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [720]	CALL     	4 0 2
  label_17: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [720]	TEST     	4 0
  label_18: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [720]	JMP      	0 7	; to 27
  label_19: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_27;
  }
  
  // [721]	GETUPVAL 	4 1	; lang_set_do
  label_20: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [721]	MOVE     	5 0
  label_21: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [721]	MOVE     	6 1
  label_22: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [721]	CALL     	4 3 1
  label_23: {
    Instruction i = 0x01804124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [722]	LOADBOOL 	4 1 0
  label_24: {
    Instruction i = 0x00800103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_26;
    }
  }
  
  // [722]	RETURN   	4 2
  label_25: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [722]	JMP      	0 2	; to 29
  label_26: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_29;
  }
  
  // [724]	LOADBOOL 	4 0 0
  label_27: {
    Instruction i = 0x00000103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_29;
    }
  }
  
  // [724]	RETURN   	4 2
  label_28: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [726]	RETURN   	0 1
  label_29: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 757
// lastlinedefined = 793
// what = Lua
static int zz_magic_function_67 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [758]	GETUPVAL 	1 0	; un_just_all
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [758]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [758]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [758]	MOVE     	0 1
  label_3: {
    Instruction i = 0x00800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [759]	LOADK    	1 -1	; ""
  label_4: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [760]	LOADK    	2 -1	; ""
  label_5: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [761]	GETUPVAL 	3 1	; null_p
  label_6: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [761]	MOVE     	4 0
  label_7: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [761]	CALL     	3 2 2
  label_8: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [761]	TEST     	3 0
  label_9: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_11;
    }
  }
  
  // [761]	JMP      	0 3	; to 14
  label_10: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_14;
  }
  
  // [762]	LOADK    	3 -2	; "()"
  label_11: {
    Instruction i = 0x000040c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [762]	RETURN   	3 2
  label_12: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [762]	JMP      	0 208	; to 222
  label_13: {
    Instruction i = 0x8033c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [763]	GETUPVAL 	3 2	; construction_p
  label_14: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [763]	MOVE     	4 0
  label_15: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [763]	CALL     	3 2 2
  label_16: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [763]	TEST     	3 0
  label_17: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_19;
    }
  }
  
  // [763]	JMP      	0 42	; to 61
  label_18: {
    Instruction i = 0x800a401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [764]	LOADK    	1 -3	; "("
  label_19: {
    Instruction i = 0x00008041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [765]	LOADK    	2 -1	; ""
  label_20: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [766]	GETUPVAL 	3 2	; construction_p
  label_21: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [766]	MOVE     	4 0
  label_22: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [766]	CALL     	3 2 2
  label_23: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [766]	TEST     	3 0
  label_24: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_26;
    }
  }
  
  // [766]	JMP      	0 16	; to 42
  label_25: {
    Instruction i = 0x8003c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_42;
  }
  
  // [767]	MOVE     	3 1
  label_26: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [767]	MOVE     	4 2
  label_27: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [767]	GETUPVAL 	5 3	; simple_print
  label_28: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [767]	GETUPVAL 	6 4	; construction_head
  label_29: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [767]	MOVE     	7 0
  label_30: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [767]	CALL     	6 2 0
  label_31: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [767]	CALL     	5 0 2
  label_32: {
    Instruction i = 0x00008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [767]	CONCAT   	1 3 5
  label_33: {
    Instruction i = 0x0181405d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [768]	LOADK    	2 -4	; " "
  label_34: {
    Instruction i = 0x0000c081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [769]	GETUPVAL 	3 0	; un_just_all
  label_35: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [769]	GETUPVAL 	4 5	; construction_tail
  label_36: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [769]	MOVE     	5 0
  label_37: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [769]	CALL     	4 2 0
  label_38: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [769]	CALL     	3 0 2
  label_39: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [769]	MOVE     	0 3
  label_40: {
    Instruction i = 0x01800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [769]	JMP      	0 -21	; to 21
  label_41: {
    Instruction i = 0x7ffa801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_21;
  }
  
  // [771]	GETUPVAL 	3 1	; null_p
  label_42: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [771]	MOVE     	4 0
  label_43: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [771]	CALL     	3 2 2
  label_44: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [771]	TEST     	3 0
  label_45: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_47;
    }
  }
  
  // [771]	JMP      	0 4	; to 51
  label_46: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_51;
  }
  
  // [772]	MOVE     	3 1
  label_47: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [772]	LOADK    	4 -5	; ")"
  label_48: {
    Instruction i = 0x00010101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [772]	CONCAT   	1 3 4
  label_49: {
    Instruction i = 0x0181005d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [772]	JMP      	0 8	; to 59
  label_50: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_59;
  }
  
  // [774]	MOVE     	3 1
  label_51: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [774]	LOADK    	4 -6	; " . "
  label_52: {
    Instruction i = 0x00014101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [774]	GETUPVAL 	5 3	; simple_print
  label_53: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [774]	MOVE     	6 0
  label_54: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [774]	CALL     	5 2 2
  label_55: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [774]	CONCAT   	4 4 5
  label_56: {
    Instruction i = 0x0201411d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [774]	LOADK    	5 -5	; ")"
  label_57: {
    Instruction i = 0x00010141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [774]	CONCAT   	1 3 5
  label_58: {
    Instruction i = 0x0181405d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [776]	RETURN   	1 2
  label_59: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [776]	JMP      	0 161	; to 222
  label_60: {
    Instruction i = 0x8028001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [777]	GETUPVAL 	3 6	; data_p
  label_61: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [777]	MOVE     	4 0
  label_62: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [777]	CALL     	3 2 2
  label_63: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [777]	TEST     	3 0
  label_64: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_66;
    }
  }
  
  // [777]	JMP      	0 14	; to 80
  label_65: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_80;
  }
  
  // [778]	LOADK    	3 -7	; "#"
  label_66: {
    Instruction i = 0x000180c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [778]	GETUPVAL 	4 3	; simple_print
  label_67: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [778]	GETUPVAL 	5 7	; new_construction
  label_68: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [778]	GETUPVAL 	6 8	; data_name
  label_69: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [778]	MOVE     	7 0
  label_70: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [778]	CALL     	6 2 2
  label_71: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [778]	GETUPVAL 	7 9	; data_list
  label_72: {
    Instruction i = 0x048001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [778]	MOVE     	8 0
  label_73: {
    Instruction i = 0x00000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [778]	CALL     	7 2 0
  label_74: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [778]	CALL     	5 0 0
  label_75: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [778]	CALL     	4 0 2
  label_76: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [778]	CONCAT   	3 3 4
  label_77: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [778]	RETURN   	3 2
  label_78: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [778]	JMP      	0 142	; to 222
  label_79: {
    Instruction i = 0x8023401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [779]	GETUPVAL 	3 10	; error_p
  label_80: {
    Instruction i = 0x050000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [779]	MOVE     	4 0
  label_81: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [779]	CALL     	3 2 2
  label_82: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [779]	TEST     	3 0
  label_83: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_85;
    }
  }
  
  // [779]	JMP      	0 14	; to 99
  label_84: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_99;
  }
  
  // [780]	LOADK    	3 -8	; "!"
  label_85: {
    Instruction i = 0x0001c0c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [780]	GETUPVAL 	4 3	; simple_print
  label_86: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [780]	GETUPVAL 	5 7	; new_construction
  label_87: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [780]	GETUPVAL 	6 11	; error_name
  label_88: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [780]	MOVE     	7 0
  label_89: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [780]	CALL     	6 2 2
  label_90: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [780]	GETUPVAL 	7 12	; error_list
  label_91: {
    Instruction i = 0x060001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [780]	MOVE     	8 0
  label_92: {
    Instruction i = 0x00000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [780]	CALL     	7 2 0
  label_93: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [780]	CALL     	5 0 0
  label_94: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [780]	CALL     	4 0 2
  label_95: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [780]	CONCAT   	3 3 4
  label_96: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [780]	RETURN   	3 2
  label_97: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [780]	JMP      	0 123	; to 222
  label_98: {
    Instruction i = 0x801e801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [781]	GETUPVAL 	3 13	; symbol_p
  label_99: {
    Instruction i = 0x068000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [781]	MOVE     	4 0
  label_100: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [781]	CALL     	3 2 2
  label_101: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [781]	TEST     	3 0
  label_102: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_104;
    }
  }
  
  // [781]	JMP      	0 5	; to 109
  label_103: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_109;
  }
  
  // [782]	GETUPVAL 	3 14	; un_symbol
  label_104: {
    Instruction i = 0x070000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [782]	MOVE     	4 0
  label_105: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [782]	TAILCALL 	3 2 0
  label_106: {
    Instruction i = 0x010000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [782]	RETURN   	3 0
  label_107: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [782]	JMP      	0 113	; to 222
  label_108: {
    Instruction i = 0x801c001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [783]	GETUPVAL 	3 15	; delay_evaluate_p
  label_109: {
    Instruction i = 0x078000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [783]	MOVE     	4 0
  label_110: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [783]	CALL     	3 2 2
  label_111: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [783]	TEST     	3 0
  label_112: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_114;
    }
  }
  
  // [783]	JMP      	0 21	; to 135
  label_113: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_135;
  }
  
  // [784]	LOADK    	3 -9	; "$("
  label_114: {
    Instruction i = 0x000200c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [784]	GETUPVAL 	4 3	; simple_print
  label_115: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [784]	GETUPVAL 	5 16	; env2val
  label_116: {
    Instruction i = 0x08000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [784]	GETUPVAL 	6 17	; delay_evaluate_env
  label_117: {
    Instruction i = 0x08800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [784]	MOVE     	7 0
  label_118: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [784]	CALL     	6 2 0
  label_119: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [784]	CALL     	5 0 0
  label_120: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [784]	CALL     	4 0 2
  label_121: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [784]	CONCAT   	3 3 4
  label_122: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [784]	LOADK    	4 -4	; " "
  label_123: {
    Instruction i = 0x0000c101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [784]	CONCAT   	3 3 4
  label_124: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [784]	GETUPVAL 	4 3	; simple_print
  label_125: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [784]	GETUPVAL 	5 18	; delay_evaluate_x
  label_126: {
    Instruction i = 0x09000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [784]	MOVE     	6 0
  label_127: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [784]	CALL     	5 2 0
  label_128: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [784]	CALL     	4 0 2
  label_129: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [784]	CONCAT   	3 3 4
  label_130: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [784]	LOADK    	4 -5	; ")"
  label_131: {
    Instruction i = 0x00010101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [784]	CONCAT   	3 3 4
  label_132: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [784]	RETURN   	3 2
  label_133: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [784]	JMP      	0 87	; to 222
  label_134: {
    Instruction i = 0x8015801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [785]	GETUPVAL 	3 19	; delay_builtin_func_p
  label_135: {
    Instruction i = 0x098000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [785]	MOVE     	4 0
  label_136: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [785]	CALL     	3 2 2
  label_137: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [785]	TEST     	3 0
  label_138: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_140;
    }
  }
  
  // [785]	JMP      	0 21	; to 161
  label_139: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_161;
  }
  
  // [786]	LOADK    	3 -10	; "%("
  label_140: {
    Instruction i = 0x000240c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [786]	GETUPVAL 	4 3	; simple_print
  label_141: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [786]	GETUPVAL 	5 20	; delay_builtin_func_f
  label_142: {
    Instruction i = 0x0a000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [786]	MOVE     	6 0
  label_143: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [786]	CALL     	5 2 0
  label_144: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [786]	CALL     	4 0 2
  label_145: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [786]	CONCAT   	3 3 4
  label_146: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [786]	LOADK    	4 -4	; " "
  label_147: {
    Instruction i = 0x0000c101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [786]	CONCAT   	3 3 4
  label_148: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [786]	GETUPVAL 	4 3	; simple_print
  label_149: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [786]	GETUPVAL 	5 21	; jsArray_to_list
  label_150: {
    Instruction i = 0x0a800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [786]	GETUPVAL 	6 22	; delay_builtin_func_xs
  label_151: {
    Instruction i = 0x0b000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [786]	MOVE     	7 0
  label_152: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [786]	CALL     	6 2 0
  label_153: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [786]	CALL     	5 0 0
  label_154: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [786]	CALL     	4 0 2
  label_155: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [786]	CONCAT   	3 3 4
  label_156: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [786]	LOADK    	4 -5	; ")"
  label_157: {
    Instruction i = 0x00010101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [786]	CONCAT   	3 3 4
  label_158: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [786]	RETURN   	3 2
  label_159: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [786]	JMP      	0 61	; to 222
  label_160: {
    Instruction i = 0x800f001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [787]	GETUPVAL 	3 23	; delay_builtin_form_p
  label_161: {
    Instruction i = 0x0b8000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [787]	MOVE     	4 0
  label_162: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [787]	CALL     	3 2 2
  label_163: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [787]	TEST     	3 0
  label_164: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_166;
    }
  }
  
  // [787]	JMP      	0 31	; to 197
  label_165: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_197;
  }
  
  // [788]	LOADK    	3 -11	; "@("
  label_166: {
    Instruction i = 0x000280c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [788]	GETUPVAL 	4 3	; simple_print
  label_167: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	GETUPVAL 	5 16	; env2val
  label_168: {
    Instruction i = 0x08000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	GETUPVAL 	6 24	; delay_builtin_form_env
  label_169: {
    Instruction i = 0x0c000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	MOVE     	7 0
  label_170: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [788]	CALL     	6 2 0
  label_171: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CALL     	5 0 0
  label_172: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CALL     	4 0 2
  label_173: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CONCAT   	3 3 4
  label_174: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [788]	LOADK    	4 -4	; " "
  label_175: {
    Instruction i = 0x0000c101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [788]	CONCAT   	3 3 4
  label_176: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [788]	GETUPVAL 	4 3	; simple_print
  label_177: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	GETUPVAL 	5 25	; delay_builtin_form_f
  label_178: {
    Instruction i = 0x0c800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	MOVE     	6 0
  label_179: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [788]	CALL     	5 2 0
  label_180: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CALL     	4 0 2
  label_181: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CONCAT   	3 3 4
  label_182: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [788]	LOADK    	4 -4	; " "
  label_183: {
    Instruction i = 0x0000c101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [788]	CONCAT   	3 3 4
  label_184: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [788]	GETUPVAL 	4 3	; simple_print
  label_185: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	GETUPVAL 	5 21	; jsArray_to_list
  label_186: {
    Instruction i = 0x0a800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	GETUPVAL 	6 26	; delay_builtin_form_xs
  label_187: {
    Instruction i = 0x0d000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [788]	MOVE     	7 0
  label_188: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [788]	CALL     	6 2 0
  label_189: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CALL     	5 0 0
  label_190: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CALL     	4 0 2
  label_191: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [788]	CONCAT   	3 3 4
  label_192: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [788]	LOADK    	4 -5	; ")"
  label_193: {
    Instruction i = 0x00010101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [788]	CONCAT   	3 3 4
  label_194: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [788]	RETURN   	3 2
  label_195: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [788]	JMP      	0 25	; to 222
  label_196: {
    Instruction i = 0x8006001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [789]	GETUPVAL 	3 27	; delay_apply_p
  label_197: {
    Instruction i = 0x0d8000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [789]	MOVE     	4 0
  label_198: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [789]	CALL     	3 2 2
  label_199: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [789]	TEST     	3 0
  label_200: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_202;
    }
  }
  
  // [789]	JMP      	0 20	; to 222
  label_201: {
    Instruction i = 0x8004c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_222;
  }
  
  // [790]	LOADK    	3 -12	; "^("
  label_202: {
    Instruction i = 0x0002c0c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [790]	GETUPVAL 	4 3	; simple_print
  label_203: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [790]	GETUPVAL 	5 28	; delay_apply_f
  label_204: {
    Instruction i = 0x0e000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [790]	MOVE     	6 0
  label_205: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [790]	CALL     	5 2 0
  label_206: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [790]	CALL     	4 0 2
  label_207: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [790]	CONCAT   	3 3 4
  label_208: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [790]	LOADK    	4 -4	; " "
  label_209: {
    Instruction i = 0x0000c101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [790]	CONCAT   	3 3 4
  label_210: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [790]	GETUPVAL 	4 3	; simple_print
  label_211: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [790]	GETUPVAL 	5 21	; jsArray_to_list
  label_212: {
    Instruction i = 0x0a800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [790]	GETUPVAL 	6 29	; delay_apply_xs
  label_213: {
    Instruction i = 0x0e800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [790]	MOVE     	7 0
  label_214: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [790]	CALL     	6 2 0
  label_215: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [790]	CALL     	5 0 0
  label_216: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [790]	CALL     	4 0 2
  label_217: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [790]	CONCAT   	3 3 4
  label_218: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [790]	LOADK    	4 -5	; ")"
  label_219: {
    Instruction i = 0x00010101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [790]	CONCAT   	3 3 4
  label_220: {
    Instruction i = 0x018100dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [790]	RETURN   	3 2
  label_221: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [792]	GETUPVAL 	3 30	; ERROR
  label_222: {
    Instruction i = 0x0f0000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [792]	TAILCALL 	3 1 0
  label_223: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [792]	RETURN   	3 0
  label_224: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [793]	RETURN   	0 1
  label_225: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 805
// lastlinedefined = 807
// what = Lua
static int zz_magic_function_68 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [806]	NEWTABLE 	1 2 0
  label_0: {
    Instruction i = 0x0100004b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [806]	GETUPVAL 	2 0	; symbol_t
  label_1: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [806]	MOVE     	3 0
  label_2: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [806]	SETLIST  	1 2 1	; 1
  label_3: {
    Instruction i = 0x0100406b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [806]	RETURN   	1 2
  label_4: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [807]	RETURN   	0 1
  label_5: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 891
// lastlinedefined = 893
// what = Lua
static int zz_magic_function_69 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [892]	GETUPVAL 	1 0	; new_data
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [892]	GETUPVAL 	2 1	; name_symbol
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [892]	GETUPVAL 	3 2	; new_list
  label_2: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [892]	GETUPVAL 	4 3	; system_symbol
  label_3: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [892]	MOVE     	5 0
  label_4: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [892]	CALL     	3 3 0
  label_5: {
    Instruction i = 0x018000e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [892]	TAILCALL 	1 0 0
  label_6: {
    Instruction i = 0x00000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [892]	RETURN   	1 0
  label_7: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [893]	RETURN   	0 1
  label_8: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 895
// lastlinedefined = 897
// what = Lua
static int zz_magic_function_70 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [896]	GETUPVAL 	1 0	; systemName_make
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	GETUPVAL 	2 1	; new_list
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	GETUPVAL 	3 2	; typeAnnotation_symbol
  label_2: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	GETUPVAL 	4 1	; new_list
  label_3: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	GETUPVAL 	5 3	; function_symbol
  label_4: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	GETUPVAL 	6 4	; something_symbol
  label_5: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	MOVE     	7 0
  label_6: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [896]	CALL     	4 4 2
  label_7: {
    Instruction i = 0x02008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [896]	GETUPVAL 	5 5	; theThing_symbol
  label_8: {
    Instruction i = 0x02800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [896]	CALL     	2 4 0
  label_9: {
    Instruction i = 0x020000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [896]	TAILCALL 	1 0 0
  label_10: {
    Instruction i = 0x00000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [896]	RETURN   	1 0
  label_11: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [897]	RETURN   	0 1
  label_12: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 899
// lastlinedefined = 901
// what = Lua
static int zz_magic_function_71 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [900]	GETUPVAL 	2 0	; systemName_make
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	GETUPVAL 	3 1	; new_list
  label_1: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	GETUPVAL 	4 2	; typeAnnotation_symbol
  label_2: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	GETUPVAL 	5 1	; new_list
  label_3: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	GETUPVAL 	6 3	; function_symbol
  label_4: {
    Instruction i = 0x01800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	GETUPVAL 	7 1	; new_list
  label_5: {
    Instruction i = 0x008001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	MOVE     	8 0
  label_6: {
    Instruction i = 0x00000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [900]	CALL     	7 2 2
  label_7: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [900]	GETUPVAL 	8 4	; something_symbol
  label_8: {
    Instruction i = 0x02000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [900]	CALL     	5 4 2
  label_9: {
    Instruction i = 0x02008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [900]	MOVE     	6 1
  label_10: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [900]	CALL     	3 4 0
  label_11: {
    Instruction i = 0x020000e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [900]	TAILCALL 	2 0 0
  label_12: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [900]	RETURN   	2 0
  label_13: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [901]	RETURN   	0 1
  label_14: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 903
// lastlinedefined = 905
// what = Lua
static int zz_magic_function_72 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [904]	GETUPVAL 	1 0	; systemName_make
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	2 1	; new_list
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	3 2	; typeAnnotation_symbol
  label_2: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	4 3	; function_symbol
  label_3: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	5 1	; new_list
  label_4: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	6 4	; isOrNot_symbol
  label_5: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	7 1	; new_list
  label_6: {
    Instruction i = 0x008001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	GETUPVAL 	8 2	; typeAnnotation_symbol
  label_7: {
    Instruction i = 0x01000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	MOVE     	9 0
  label_8: {
    Instruction i = 0x00000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [904]	GETUPVAL 	10 5	; something_symbol
  label_9: {
    Instruction i = 0x02800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [904]	CALL     	7 4 0
  label_10: {
    Instruction i = 0x020001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [904]	CALL     	5 0 0
  label_11: {
    Instruction i = 0x00000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [904]	CALL     	2 0 0
  label_12: {
    Instruction i = 0x000000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [904]	TAILCALL 	1 0 0
  label_13: {
    Instruction i = 0x00000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [904]	RETURN   	1 0
  label_14: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [905]	RETURN   	0 1
  label_15: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 957
// lastlinedefined = 967
// what = Lua
static int zz_magic_function_73 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [958]	NEWTABLE 	3 0 0
  label_0: {
    Instruction i = 0x000000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [959]	GETUPVAL 	4 0	; construction_p
  label_1: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [959]	MOVE     	5 0
  label_2: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [959]	CALL     	4 2 2
  label_3: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [959]	TEST     	4 0
  label_4: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_6;
    }
  }
  
  // [959]	JMP      	0 11	; to 17
  label_5: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_17;
  }
  
  // [960]	GETUPVAL 	4 1	; __TS__ArrayPush
  label_6: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [960]	MOVE     	5 3
  label_7: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [960]	GETUPVAL 	6 2	; construction_head
  label_8: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [960]	MOVE     	7 0
  label_9: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [960]	CALL     	6 2 0
  label_10: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [960]	CALL     	4 0 1
  label_11: {
    Instruction i = 0x00004124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [961]	GETUPVAL 	4 3	; construction_tail
  label_12: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [961]	MOVE     	5 0
  label_13: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [961]	CALL     	4 2 2
  label_14: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [961]	MOVE     	0 4
  label_15: {
    Instruction i = 0x02000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [961]	JMP      	0 -16	; to 1
  label_16: {
    Instruction i = 0x7ffbc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_1;
  }
  
  // [963]	GETUPVAL 	4 4	; null_p
  label_17: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [963]	MOVE     	5 0
  label_18: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [963]	CALL     	4 2 2
  label_19: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [963]	TEST     	4 0
  label_20: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_22;
    }
  }
  
  // [963]	JMP      	0 4	; to 26
  label_21: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_26;
  }
  
  // [964]	MOVE     	4 1
  label_22: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [964]	MOVE     	5 3
  label_23: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [964]	TAILCALL 	4 2 0
  label_24: {
    Instruction i = 0x01000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [964]	RETURN   	4 0
  label_25: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [966]	MOVE     	4 2
  label_26: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [966]	MOVE     	5 3
  label_27: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [966]	MOVE     	6 0
  label_28: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [966]	TAILCALL 	4 3 0
  label_29: {
    Instruction i = 0x01800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [966]	RETURN   	4 0
  label_30: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [967]	RETURN   	0 1
  label_31: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 969
// lastlinedefined = 975
// what = Lua
static int zz_magic_function_74 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [970]	GETUPVAL 	1 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [970]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [972]	CLOSURE  	3 0	; 0x559f63795da0
  label_2: {
    Instruction i = 0x000000ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [974]	CLOSURE  	4 1	; 0x559f63795e80
  label_3: {
    Instruction i = 0x0000412c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [970]	TAILCALL 	1 4 0
  label_4: {
    Instruction i = 0x02000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [974]	RETURN   	1 0
  label_5: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [975]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 970
// lastlinedefined = 972
// what = Lua
static int zz_magic_function_75 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [971]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [972]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 972
// lastlinedefined = 974
// what = Lua
static int zz_magic_function_76 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [973]	LOADBOOL 	2 0 0
  label_0: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_2;
    }
  }
  
  // [973]	RETURN   	2 2
  label_1: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [974]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 984
// lastlinedefined = 1041
// what = Lua
static int zz_magic_function_77 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [985]	GETUPVAL 	1 0	; force_all
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [985]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [985]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [985]	MOVE     	0 1
  label_3: {
    Instruction i = 0x00800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [986]	GETUPVAL 	1 1	; data_p
  label_4: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [986]	MOVE     	2 0
  label_5: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [986]	CALL     	1 2 2
  label_6: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [986]	TEST     	1 1
  label_7: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [986]	JMP      	0 2	; to 11
  label_8: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_11;
  }
  
  // [987]	LOADBOOL 	1 0 0
  label_9: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_11;
    }
  }
  
  // [987]	RETURN   	1 2
  label_10: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [989]	GETUPVAL 	1 0	; force_all
  label_11: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [989]	GETUPVAL 	2 2	; data_name
  label_12: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [989]	MOVE     	3 0
  label_13: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [989]	CALL     	2 2 0
  label_14: {
    Instruction i = 0x010000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [989]	CALL     	1 0 2
  label_15: {
    Instruction i = 0x00008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [990]	GETUPVAL 	2 3	; symbol_p
  label_16: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [990]	MOVE     	3 1
  label_17: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [990]	CALL     	2 2 2
  label_18: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [990]	TEST     	2 1
  label_19: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [990]	JMP      	0 2	; to 23
  label_20: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [991]	LOADBOOL 	2 0 0
  label_21: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_23;
    }
  }
  
  // [991]	RETURN   	2 2
  label_22: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [993]	GETUPVAL 	2 4	; symbol_equal_p
  label_23: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [993]	MOVE     	3 1
  label_24: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [993]	GETUPVAL 	4 5	; mapping_symbol
  label_25: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [993]	CALL     	2 3 2
  label_26: {
    Instruction i = 0x018080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [993]	TEST     	2 1
  label_27: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_29;
    }
  }
  
  // [993]	JMP      	0 2	; to 31
  label_28: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [994]	LOADBOOL 	2 0 0
  label_29: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_31;
    }
  }
  
  // [994]	RETURN   	2 2
  label_30: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [996]	GETUPVAL 	2 0	; force_all
  label_31: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [996]	GETUPVAL 	3 6	; data_list
  label_32: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [996]	MOVE     	4 0
  label_33: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [996]	CALL     	3 2 0
  label_34: {
    Instruction i = 0x010000e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [996]	CALL     	2 0 2
  label_35: {
    Instruction i = 0x000080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [996]	MOVE     	1 2
  label_36: {
    Instruction i = 0x01000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [997]	GETUPVAL 	2 7	; construction_p
  label_37: {
    Instruction i = 0x03800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [997]	MOVE     	3 1
  label_38: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [997]	CALL     	2 2 2
  label_39: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [997]	TEST     	2 1
  label_40: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_42;
    }
  }
  
  // [997]	JMP      	0 2	; to 44
  label_41: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_44;
  }
  
  // [998]	LOADBOOL 	2 0 0
  label_42: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_44;
    }
  }
  
  // [998]	RETURN   	2 2
  label_43: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1000]	GETUPVAL 	2 8	; null_p
  label_44: {
    Instruction i = 0x04000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1000]	GETUPVAL 	3 0	; force_all
  label_45: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1000]	GETUPVAL 	4 9	; construction_tail
  label_46: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1000]	MOVE     	5 1
  label_47: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1000]	CALL     	4 2 0
  label_48: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1000]	CALL     	3 0 0
  label_49: {
    Instruction i = 0x000000e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1000]	CALL     	2 0 2
  label_50: {
    Instruction i = 0x000080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1000]	TEST     	2 1
  label_51: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_53;
    }
  }
  
  // [1000]	JMP      	0 2	; to 55
  label_52: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_55;
  }
  
  // [1001]	LOADBOOL 	2 0 0
  label_53: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_55;
    }
  }
  
  // [1001]	RETURN   	2 2
  label_54: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1003]	NEWTABLE 	2 0 0
  label_55: {
    Instruction i = 0x0000008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1004]	GETUPVAL 	3 0	; force_all
  label_56: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1004]	GETUPVAL 	4 10	; construction_head
  label_57: {
    Instruction i = 0x05000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1004]	MOVE     	5 1
  label_58: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1004]	CALL     	4 2 0
  label_59: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1004]	CALL     	3 0 2
  label_60: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1005]	GETUPVAL 	4 8	; null_p
  label_61: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1005]	MOVE     	5 3
  label_62: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1005]	CALL     	4 2 2
  label_63: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1005]	TEST     	4 1
  label_64: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_66;
    }
  }
  
  // [1005]	JMP      	0 86	; to 152
  label_65: {
    Instruction i = 0x8015401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_152;
  }
  
  // [1006]	GETUPVAL 	4 7	; construction_p
  label_66: {
    Instruction i = 0x03800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1006]	MOVE     	5 3
  label_67: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1006]	CALL     	4 2 2
  label_68: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1006]	TEST     	4 1
  label_69: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_71;
    }
  }
  
  // [1006]	JMP      	0 2	; to 73
  label_70: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_73;
  }
  
  // [1007]	LOADBOOL 	4 0 0
  label_71: {
    Instruction i = 0x00000103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_73;
    }
  }
  
  // [1007]	RETURN   	4 2
  label_72: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1009]	GETUPVAL 	4 0	; force_all
  label_73: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1009]	GETUPVAL 	5 10	; construction_head
  label_74: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1009]	MOVE     	6 3
  label_75: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1009]	CALL     	5 2 0
  label_76: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1009]	CALL     	4 0 2
  label_77: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1010]	GETUPVAL 	5 0	; force_all
  label_78: {
    Instruction i = 0x00000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1010]	GETUPVAL 	6 9	; construction_tail
  label_79: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1010]	MOVE     	7 3
  label_80: {
    Instruction i = 0x018001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1010]	CALL     	6 2 0
  label_81: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1010]	CALL     	5 0 2
  label_82: {
    Instruction i = 0x00008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1010]	MOVE     	3 5
  label_83: {
    Instruction i = 0x028000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1011]	GETUPVAL 	5 7	; construction_p
  label_84: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1011]	MOVE     	6 4
  label_85: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1011]	CALL     	5 2 2
  label_86: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1011]	TEST     	5 1
  label_87: {
    Instruction i = 0x00004162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_89;
    }
  }
  
  // [1011]	JMP      	0 2	; to 91
  label_88: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_91;
  }
  
  // [1012]	LOADBOOL 	5 0 0
  label_89: {
    Instruction i = 0x00000143;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_91;
    }
  }
  
  // [1012]	RETURN   	5 2
  label_90: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1014]	GETUPVAL 	5 10	; construction_head
  label_91: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1014]	MOVE     	6 4
  label_92: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1014]	CALL     	5 2 2
  label_93: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1015]	GETUPVAL 	6 0	; force_all
  label_94: {
    Instruction i = 0x00000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1015]	GETUPVAL 	7 9	; construction_tail
  label_95: {
    Instruction i = 0x048001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1015]	MOVE     	8 4
  label_96: {
    Instruction i = 0x02000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1015]	CALL     	7 2 0
  label_97: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1015]	CALL     	6 0 2
  label_98: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1015]	MOVE     	4 6
  label_99: {
    Instruction i = 0x03000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1016]	GETUPVAL 	6 7	; construction_p
  label_100: {
    Instruction i = 0x03800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1016]	MOVE     	7 4
  label_101: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1016]	CALL     	6 2 2
  label_102: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1016]	TEST     	6 1
  label_103: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_105;
    }
  }
  
  // [1016]	JMP      	0 2	; to 107
  label_104: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_107;
  }
  
  // [1017]	LOADBOOL 	6 0 0
  label_105: {
    Instruction i = 0x00000183;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_107;
    }
  }
  
  // [1017]	RETURN   	6 2
  label_106: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1019]	GETUPVAL 	6 10	; construction_head
  label_107: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1019]	MOVE     	7 4
  label_108: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1019]	CALL     	6 2 2
  label_109: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1020]	GETUPVAL 	7 8	; null_p
  label_110: {
    Instruction i = 0x040001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1020]	GETUPVAL 	8 0	; force_all
  label_111: {
    Instruction i = 0x00000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1020]	GETUPVAL 	9 9	; construction_tail
  label_112: {
    Instruction i = 0x04800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1020]	MOVE     	10 4
  label_113: {
    Instruction i = 0x02000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1020]	CALL     	9 2 0
  label_114: {
    Instruction i = 0x01000264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1020]	CALL     	8 0 0
  label_115: {
    Instruction i = 0x00000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1020]	CALL     	7 0 2
  label_116: {
    Instruction i = 0x000081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1020]	TEST     	7 1
  label_117: {
    Instruction i = 0x000041e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_119;
    }
  }
  
  // [1020]	JMP      	0 2	; to 121
  label_118: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_121;
  }
  
  // [1021]	LOADBOOL 	7 0 0
  label_119: {
    Instruction i = 0x000001c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_121;
    }
  }
  
  // [1021]	RETURN   	7 2
  label_120: {
    Instruction i = 0x010001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1023]	LOADBOOL 	7 1 0
  label_121: {
    Instruction i = 0x008001c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_123;
    }
  }
  
  // [1025]	LOADK    	8 -1	; 0
  label_122: {
    Instruction i = 0x00000201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1026]	LEN      	9 2
  label_123: {
    Instruction i = 0x0100025c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1026]	LT       	0 8 9
  label_124: {
    Instruction i = 0x04024020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_126;
    }
  }
  
  // [1026]	JMP      	0 15	; to 141
  label_125: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_141;
  }
  
  // [1027]	GETUPVAL 	9 11	; jsbool_equal_p
  label_126: {
    Instruction i = 0x05800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1027]	ADD      	10 8 -1	; - 0
  label_127: {
    Instruction i = 0x0440028d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1027]	ADD      	10 10 -2	; - 1
  label_128: {
    Instruction i = 0x0540428d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1027]	GETTABLE 	10 2 10
  label_129: {
    Instruction i = 0x01028287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1027]	MOVE     	11 5
  label_130: {
    Instruction i = 0x028002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1027]	CALL     	9 3 2
  label_131: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1027]	TEST     	9 0
  label_132: {
    Instruction i = 0x00000262;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_134;
    }
  }
  
  // [1027]	JMP      	0 5	; to 139
  label_133: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_139;
  }
  
  // [1028]	ADD      	9 8 -2	; - 1
  label_134: {
    Instruction i = 0x0440424d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1028]	ADD      	9 9 -2	; - 1
  label_135: {
    Instruction i = 0x04c0424d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1028]	SETTABLE 	2 9 6
  label_136: {
    Instruction i = 0x0481808a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1029]	LOADBOOL 	7 0 0
  label_137: {
    Instruction i = 0x000001c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_139;
    }
  }
  
  // [1029]	JMP      	0 2	; to 141
  label_138: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_141;
  }
  
  // [1032]	ADD      	8 8 -3	; - 2
  label_139: {
    Instruction i = 0x0440820d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1032]	JMP      	0 -18	; to 123
  label_140: {
    Instruction i = 0x7ffb401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_123;
  }
  
  // [1035]	TEST     	7 0
  label_141: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_143;
    }
  }
  
  // [1035]	JMP      	0 -82	; to 61
  label_142: {
    Instruction i = 0x7feb401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [1036]	GETUPVAL 	8 12	; __TS__ArrayPush
  label_143: {
    Instruction i = 0x06000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1036]	MOVE     	9 2
  label_144: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1036]	MOVE     	10 5
  label_145: {
    Instruction i = 0x02800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1036]	CALL     	8 3 1
  label_146: {
    Instruction i = 0x01804224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1037]	GETUPVAL 	8 12	; __TS__ArrayPush
  label_147: {
    Instruction i = 0x06000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1037]	MOVE     	9 2
  label_148: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1037]	MOVE     	10 6
  label_149: {
    Instruction i = 0x03000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1037]	CALL     	8 3 1
  label_150: {
    Instruction i = 0x01804224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1038]	JMP      	0 -91	; to 61
  label_151: {
    Instruction i = 0x7fe9001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [1040]	RETURN   	2 2
  label_152: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1041]	RETURN   	0 1
  label_153: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1049
// lastlinedefined = 1060
// what = Lua
static int zz_magic_function_78 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1050]	NEWTABLE 	2 3 0
  label_0: {
    Instruction i = 0x0180008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1050]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1050]	LOADK    	4 -1	; 1
  label_2: {
    Instruction i = 0x00000101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1059]	CLOSURE  	5 0	; 0x559f637a3650
  label_3: {
    Instruction i = 0x0000016c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1059]	SETLIST  	2 3 1	; 1
  label_4: {
    Instruction i = 0x018040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1059]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1060]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1050
// lastlinedefined = 1059
// what = Lua
static int zz_magic_function_79 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1051]	GETUPVAL 	2 0	; force1
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1051]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1051]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1051]	MOVE     	0 2
  label_3: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1052]	GETUPVAL 	2 1	; any_delay_just_p
  label_4: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1052]	MOVE     	3 0
  label_5: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1052]	CALL     	2 2 2
  label_6: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1052]	TEST     	2 0
  label_7: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1052]	JMP      	0 7	; to 16
  label_8: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1053]	GETUPVAL 	2 2	; builtin_func_apply
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1053]	GETUPVAL 	3 3	; p_sym
  label_10: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1053]	NEWTABLE 	4 1 0
  label_11: {
    Instruction i = 0x0080010b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1053]	MOVE     	5 0
  label_12: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1053]	SETLIST  	4 1 1	; 1
  label_13: {
    Instruction i = 0x0080412b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1053]	TAILCALL 	2 3 0
  label_14: {
    Instruction i = 0x018000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1053]	RETURN   	2 0
  label_15: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1055]	GETUPVAL 	2 4	; p_jsfunc
  label_16: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1055]	MOVE     	3 0
  label_17: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1055]	CALL     	2 2 2
  label_18: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1055]	TEST     	2 0
  label_19: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1055]	JMP      	0 2	; to 23
  label_20: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [1056]	GETUPVAL 	2 5	; true_v
  label_21: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1056]	RETURN   	2 2
  label_22: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1058]	GETUPVAL 	2 6	; false_v
  label_23: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1058]	RETURN   	2 2
  label_24: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1059]	RETURN   	0 1
  label_25: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1062
// lastlinedefined = 1073
// what = Lua
static int zz_magic_function_80 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1063]	NEWTABLE 	3 3 0
  label_0: {
    Instruction i = 0x018000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1063]	MOVE     	4 0
  label_1: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1063]	LOADK    	5 -1	; 1
  label_2: {
    Instruction i = 0x00000141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1072]	CLOSURE  	6 0	; 0x559f637a3ba0
  label_3: {
    Instruction i = 0x000001ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1072]	SETLIST  	3 3 1	; 1
  label_4: {
    Instruction i = 0x018040eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1072]	RETURN   	3 2
  label_5: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1073]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1063
// lastlinedefined = 1072
// what = Lua
static int zz_magic_function_81 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1064]	GETUPVAL 	2 0	; force1
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1064]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1064]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1064]	MOVE     	0 2
  label_3: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1065]	GETUPVAL 	2 1	; any_delay_just_p
  label_4: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1065]	MOVE     	3 0
  label_5: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1065]	CALL     	2 2 2
  label_6: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1065]	TEST     	2 0
  label_7: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1065]	JMP      	0 7	; to 16
  label_8: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1066]	GETUPVAL 	2 2	; builtin_func_apply
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1066]	GETUPVAL 	3 3	; f_sym
  label_10: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1066]	NEWTABLE 	4 1 0
  label_11: {
    Instruction i = 0x0080010b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1066]	MOVE     	5 0
  label_12: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1066]	SETLIST  	4 1 1	; 1
  label_13: {
    Instruction i = 0x0080412b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1066]	TAILCALL 	2 3 0
  label_14: {
    Instruction i = 0x018000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1066]	RETURN   	2 0
  label_15: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1068]	GETUPVAL 	2 4	; p_jsfunc
  label_16: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1068]	MOVE     	3 0
  label_17: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1068]	CALL     	2 2 2
  label_18: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1068]	TEST     	2 0
  label_19: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1068]	JMP      	0 4	; to 25
  label_20: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [1069]	GETUPVAL 	2 5	; f_jsfunc
  label_21: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1069]	MOVE     	3 0
  label_22: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1069]	TAILCALL 	2 2 0
  label_23: {
    Instruction i = 0x010000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1069]	RETURN   	2 0
  label_24: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1071]	RETURN   	1 2
  label_25: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1072]	RETURN   	0 1
  label_26: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1074
// lastlinedefined = 1126
// what = Lua
static int zz_magic_function_82 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1075]	EQ       	0 0 1
  label_0: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [1075]	JMP      	0 2	; to 4
  label_1: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [1076]	GETUPVAL 	3 0	; true_v
  label_2: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1076]	RETURN   	3 2
  label_3: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1078]	GETUPVAL 	3 1	; force1
  label_4: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1078]	MOVE     	4 0
  label_5: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1078]	CALL     	3 2 2
  label_6: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1078]	MOVE     	0 3
  label_7: {
    Instruction i = 0x01800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1079]	GETUPVAL 	3 1	; force1
  label_8: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1079]	MOVE     	4 1
  label_9: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1079]	CALL     	3 2 2
  label_10: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1079]	MOVE     	1 3
  label_11: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1080]	GETUPVAL 	3 2	; any_delay_just_p
  label_12: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1080]	MOVE     	4 0
  label_13: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1080]	CALL     	3 2 2
  label_14: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1080]	TEST     	3 1
  label_15: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_17;
    }
  }
  
  // [1080]	JMP      	0 5	; to 22
  label_16: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1080]	GETUPVAL 	3 2	; any_delay_just_p
  label_17: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1080]	MOVE     	4 1
  label_18: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1080]	CALL     	3 2 2
  label_19: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1080]	TEST     	3 0
  label_20: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_22;
    }
  }
  
  // [1080]	JMP      	0 8	; to 30
  label_21: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_30;
  }
  
  // [1081]	GETUPVAL 	3 3	; builtin_func_apply
  label_22: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1081]	GETUPVAL 	4 4	; equal_p_function_builtin_systemName
  label_23: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1081]	NEWTABLE 	5 2 0
  label_24: {
    Instruction i = 0x0100014b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1081]	MOVE     	6 0
  label_25: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1081]	MOVE     	7 1
  label_26: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1081]	SETLIST  	5 2 1	; 1
  label_27: {
    Instruction i = 0x0100416b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1081]	TAILCALL 	3 3 0
  label_28: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1081]	RETURN   	3 0
  label_29: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1083]	EQ       	0 0 1
  label_30: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_32;
    }
  }
  
  // [1083]	JMP      	0 2	; to 34
  label_31: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_34;
  }
  
  // [1084]	GETUPVAL 	3 0	; true_v
  label_32: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1084]	RETURN   	3 2
  label_33: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1086]	LOADNIL  	3 0
  label_34: {
    Instruction i = 0x000000c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1089]	CLOSURE  	4 0	; 0x559f6379b200
  label_35: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1089]	MOVE     	3 4
  label_36: {
    Instruction i = 0x020000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1090]	LOADNIL  	4 0
  label_37: {
    Instruction i = 0x00000104;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1093]	CLOSURE  	5 1	; 0x559f6379b6b0
  label_38: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1093]	MOVE     	4 5
  label_39: {
    Instruction i = 0x02800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1094]	GETUPVAL 	5 7	; ASSERT
  label_40: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1094]	GETUPVAL 	6 2	; any_delay_just_p
  label_41: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1094]	MOVE     	7 0
  label_42: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1094]	CALL     	6 2 2
  label_43: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1094]	NOT      	6 6
  label_44: {
    Instruction i = 0x0300019b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RB(i);
    int res = l_isfalse(rb);  /* next assignment may change this value */
    setbvalue(ra, res);
  }
  
  // [1094]	CALL     	5 2 1
  label_45: {
    Instruction i = 0x01004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1095]	LOADNIL  	5 0
  label_46: {
    Instruction i = 0x00000144;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1098]	CLOSURE  	6 2	; 0x559f6379b7c0
  label_47: {
    Instruction i = 0x000081ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1098]	MOVE     	5 6
  label_48: {
    Instruction i = 0x03000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1099]	GETUPVAL 	6 8	; null_p
  label_49: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1099]	MOVE     	7 0
  label_50: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1099]	CALL     	6 2 2
  label_51: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1099]	TEST     	6 0
  label_52: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_54;
    }
  }
  
  // [1099]	JMP      	0 10	; to 64
  label_53: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_64;
  }
  
  // [1100]	GETUPVAL 	6 8	; null_p
  label_54: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1100]	MOVE     	7 0
  label_55: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1100]	CALL     	6 2 2
  label_56: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1100]	TEST     	6 1
  label_57: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_59;
    }
  }
  
  // [1100]	JMP      	0 2	; to 61
  label_58: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [1101]	GETUPVAL 	6 6	; false_v
  label_59: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1101]	RETURN   	6 2
  label_60: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1103]	GETUPVAL 	6 0	; true_v
  label_61: {
    Instruction i = 0x00000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1103]	RETURN   	6 2
  label_62: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1103]	JMP      	0 83	; to 147
  label_63: {
    Instruction i = 0x8014801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_147;
  }
  
  // [1104]	GETUPVAL 	6 9	; symbol_p
  label_64: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1104]	MOVE     	7 0
  label_65: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1104]	CALL     	6 2 2
  label_66: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1104]	TEST     	6 0
  label_67: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_69;
    }
  }
  
  // [1104]	JMP      	0 19	; to 88
  label_68: {
    Instruction i = 0x8004801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_88;
  }
  
  // [1105]	GETUPVAL 	6 9	; symbol_p
  label_69: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1105]	MOVE     	7 1
  label_70: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1105]	CALL     	6 2 2
  label_71: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1105]	TEST     	6 1
  label_72: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_74;
    }
  }
  
  // [1105]	JMP      	0 2	; to 76
  label_73: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_76;
  }
  
  // [1106]	GETUPVAL 	6 6	; false_v
  label_74: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1106]	RETURN   	6 2
  label_75: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1108]	GETUPVAL 	6 10	; symbol_equal_p
  label_76: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1108]	MOVE     	7 0
  label_77: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1108]	MOVE     	8 1
  label_78: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1108]	CALL     	6 3 2
  label_79: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1108]	TEST     	6 0
  label_80: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_82;
    }
  }
  
  // [1108]	JMP      	0 3	; to 85
  label_81: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_85;
  }
  
  // [1108]	GETUPVAL 	6 0	; true_v
  label_82: {
    Instruction i = 0x00000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1108]	TEST     	6 1
  label_83: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_85;
    }
  }
  
  // [1108]	JMP      	0 1	; to 86
  label_84: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_86;
  }
  
  // [1108]	GETUPVAL 	6 6	; false_v
  label_85: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1108]	RETURN   	6 2
  label_86: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1108]	JMP      	0 59	; to 147
  label_87: {
    Instruction i = 0x800e801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_147;
  }
  
  // [1109]	GETUPVAL 	6 11	; data_p
  label_88: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1109]	MOVE     	7 0
  label_89: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1109]	CALL     	6 2 2
  label_90: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1109]	TEST     	6 0
  label_91: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_93;
    }
  }
  
  // [1109]	JMP      	0 15	; to 108
  label_92: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_108;
  }
  
  // [1110]	GETUPVAL 	6 11	; data_p
  label_93: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1110]	MOVE     	7 1
  label_94: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1110]	CALL     	6 2 2
  label_95: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1110]	TEST     	6 1
  label_96: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_98;
    }
  }
  
  // [1110]	JMP      	0 2	; to 100
  label_97: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_100;
  }
  
  // [1111]	GETUPVAL 	6 6	; false_v
  label_98: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1111]	RETURN   	6 2
  label_99: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1113]	MOVE     	6 5
  label_100: {
    Instruction i = 0x02800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1113]	MOVE     	7 0
  label_101: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1113]	MOVE     	8 1
  label_102: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1113]	GETUPVAL 	9 12	; data_name
  label_103: {
    Instruction i = 0x06000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1113]	GETUPVAL 	10 13	; data_list
  label_104: {
    Instruction i = 0x06800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1113]	TAILCALL 	6 5 0
  label_105: {
    Instruction i = 0x028001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1113]	RETURN   	6 0
  label_106: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1113]	JMP      	0 39	; to 147
  label_107: {
    Instruction i = 0x8009801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_147;
  }
  
  // [1114]	GETUPVAL 	6 14	; construction_p
  label_108: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1114]	MOVE     	7 0
  label_109: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1114]	CALL     	6 2 2
  label_110: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1114]	TEST     	6 0
  label_111: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_113;
    }
  }
  
  // [1114]	JMP      	0 15	; to 128
  label_112: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_128;
  }
  
  // [1115]	GETUPVAL 	6 14	; construction_p
  label_113: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1115]	MOVE     	7 1
  label_114: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1115]	CALL     	6 2 2
  label_115: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1115]	TEST     	6 1
  label_116: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_118;
    }
  }
  
  // [1115]	JMP      	0 2	; to 120
  label_117: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_120;
  }
  
  // [1116]	GETUPVAL 	6 6	; false_v
  label_118: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1116]	RETURN   	6 2
  label_119: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1118]	MOVE     	6 5
  label_120: {
    Instruction i = 0x02800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1118]	MOVE     	7 0
  label_121: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1118]	MOVE     	8 1
  label_122: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1118]	GETUPVAL 	9 15	; construction_head
  label_123: {
    Instruction i = 0x07800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1118]	GETUPVAL 	10 16	; construction_tail
  label_124: {
    Instruction i = 0x08000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1118]	TAILCALL 	6 5 0
  label_125: {
    Instruction i = 0x028001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1118]	RETURN   	6 0
  label_126: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1118]	JMP      	0 19	; to 147
  label_127: {
    Instruction i = 0x8004801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_147;
  }
  
  // [1119]	GETUPVAL 	6 17	; error_p
  label_128: {
    Instruction i = 0x08800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1119]	MOVE     	7 0
  label_129: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1119]	CALL     	6 2 2
  label_130: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1119]	TEST     	6 0
  label_131: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_133;
    }
  }
  
  // [1119]	JMP      	0 14	; to 147
  label_132: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_147;
  }
  
  // [1120]	GETUPVAL 	6 17	; error_p
  label_133: {
    Instruction i = 0x08800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1120]	MOVE     	7 1
  label_134: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1120]	CALL     	6 2 2
  label_135: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1120]	TEST     	6 1
  label_136: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_138;
    }
  }
  
  // [1120]	JMP      	0 2	; to 140
  label_137: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_140;
  }
  
  // [1121]	GETUPVAL 	6 6	; false_v
  label_138: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1121]	RETURN   	6 2
  label_139: {
    Instruction i = 0x010001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1123]	MOVE     	6 5
  label_140: {
    Instruction i = 0x02800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1123]	MOVE     	7 0
  label_141: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1123]	MOVE     	8 1
  label_142: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1123]	GETUPVAL 	9 18	; error_name
  label_143: {
    Instruction i = 0x09000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1123]	GETUPVAL 	10 19	; error_list
  label_144: {
    Instruction i = 0x09800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1123]	TAILCALL 	6 5 0
  label_145: {
    Instruction i = 0x028001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1123]	RETURN   	6 0
  label_146: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1125]	GETUPVAL 	6 20	; ERROR
  label_147: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1125]	TAILCALL 	6 1 0
  label_148: {
    Instruction i = 0x008001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1125]	RETURN   	6 0
  label_149: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1126]	RETURN   	0 1
  label_150: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1087
// lastlinedefined = 1089
// what = Lua
static int zz_magic_function_83 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1088]	GETUPVAL 	3 0	; builtin_func_apply
  label_0: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1088]	GETUPVAL 	4 1	; if_function_builtin_systemName
  label_1: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1088]	NEWTABLE 	5 3 0
  label_2: {
    Instruction i = 0x0180014b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1088]	MOVE     	6 0
  label_3: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1088]	MOVE     	7 1
  label_4: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1088]	MOVE     	8 2
  label_5: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1088]	SETLIST  	5 3 1	; 1
  label_6: {
    Instruction i = 0x0180416b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1088]	TAILCALL 	3 3 0
  label_7: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1088]	RETURN   	3 0
  label_8: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1089]	RETURN   	0 1
  label_9: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1091
// lastlinedefined = 1093
// what = Lua
static int zz_magic_function_84 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1092]	GETUPVAL 	2 0	; H_if
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1092]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1092]	MOVE     	4 1
  label_2: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1092]	GETUPVAL 	5 1	; false_v
  label_3: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1092]	TAILCALL 	2 4 0
  label_4: {
    Instruction i = 0x020000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1092]	RETURN   	2 0
  label_5: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1093]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1096
// lastlinedefined = 1098
// what = Lua
static int zz_magic_function_85 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1097]	GETUPVAL 	4 0	; H_and
  label_0: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1097]	GETUPVAL 	5 1	; builtin_func_apply
  label_1: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1097]	GETUPVAL 	6 2	; equal_p_function_builtin_systemName
  label_2: {
    Instruction i = 0x01000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1097]	NEWTABLE 	7 1 0
  label_3: {
    Instruction i = 0x008001cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1097]	MOVE     	8 2
  label_4: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	MOVE     	9 0
  label_5: {
    Instruction i = 0x00000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	CALL     	8 2 2
  label_6: {
    Instruction i = 0x01008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	MOVE     	9 2
  label_7: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	MOVE     	10 1
  label_8: {
    Instruction i = 0x00800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	CALL     	9 2 0
  label_9: {
    Instruction i = 0x01000264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	SETLIST  	7 0 1	; 1
  label_10: {
    Instruction i = 0x000041eb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1097]	CALL     	5 3 2
  label_11: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	GETUPVAL 	6 1	; builtin_func_apply
  label_12: {
    Instruction i = 0x00800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1097]	GETUPVAL 	7 2	; equal_p_function_builtin_systemName
  label_13: {
    Instruction i = 0x010001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1097]	NEWTABLE 	8 1 0
  label_14: {
    Instruction i = 0x0080020b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1097]	MOVE     	9 3
  label_15: {
    Instruction i = 0x01800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	MOVE     	10 0
  label_16: {
    Instruction i = 0x00000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	CALL     	9 2 2
  label_17: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	MOVE     	10 3
  label_18: {
    Instruction i = 0x01800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	MOVE     	11 1
  label_19: {
    Instruction i = 0x008002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1097]	CALL     	10 2 0
  label_20: {
    Instruction i = 0x010002a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	SETLIST  	8 0 1	; 1
  label_21: {
    Instruction i = 0x0000422b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1097]	CALL     	6 3 0
  label_22: {
    Instruction i = 0x018001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	TAILCALL 	4 0 0
  label_23: {
    Instruction i = 0x00000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1097]	RETURN   	4 0
  label_24: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1098]	RETURN   	0 1
  label_25: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1126
// lastlinedefined = 1137
// what = Lua
static int zz_magic_function_86 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1127]	NEWTABLE 	3 0 0
  label_0: {
    Instruction i = 0x000000cb;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1128]	GETUPVAL 	4 0	; force_all
  label_1: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1128]	MOVE     	5 1
  label_2: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1128]	CALL     	4 2 2
  label_3: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1129]	GETUPVAL 	5 1	; construction_p
  label_4: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1129]	MOVE     	6 4
  label_5: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1129]	CALL     	5 2 2
  label_6: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1129]	TEST     	5 0
  label_7: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1129]	JMP      	0 13	; to 22
  label_8: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1130]	GETUPVAL 	5 2	; __TS__ArrayPush
  label_9: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1130]	MOVE     	6 3
  label_10: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1130]	GETUPVAL 	7 3	; construction_head
  label_11: {
    Instruction i = 0x018001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1130]	MOVE     	8 4
  label_12: {
    Instruction i = 0x02000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1130]	CALL     	7 2 0
  label_13: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1130]	CALL     	5 0 1
  label_14: {
    Instruction i = 0x00004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1131]	GETUPVAL 	5 0	; force_all
  label_15: {
    Instruction i = 0x00000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1131]	GETUPVAL 	6 4	; construction_tail
  label_16: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1131]	MOVE     	7 4
  label_17: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1131]	CALL     	6 2 0
  label_18: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1131]	CALL     	5 0 2
  label_19: {
    Instruction i = 0x00008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1131]	MOVE     	4 5
  label_20: {
    Instruction i = 0x02800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1131]	JMP      	0 -18	; to 4
  label_21: {
    Instruction i = 0x7ffb401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [1133]	GETUPVAL 	5 5	; null_p
  label_22: {
    Instruction i = 0x02800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1133]	MOVE     	6 4
  label_23: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1133]	CALL     	5 2 2
  label_24: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1133]	TEST     	5 1
  label_25: {
    Instruction i = 0x00004162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1133]	JMP      	0 1	; to 28
  label_26: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_28;
  }
  
  // [1134]	RETURN   	2 2
  label_27: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1136]	GETUPVAL 	5 6	; apply
  label_28: {
    Instruction i = 0x03000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1136]	MOVE     	6 0
  label_29: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1136]	MOVE     	7 3
  label_30: {
    Instruction i = 0x018001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1136]	TAILCALL 	5 3 0
  label_31: {
    Instruction i = 0x01800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1136]	RETURN   	5 0
  label_32: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1137]	RETURN   	0 1
  label_33: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1137
// lastlinedefined = 1143
// what = Lua
static int zz_magic_function_87 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1138]	GETUPVAL 	3 0	; val2env
  label_0: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1138]	MOVE     	4 0
  label_1: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1138]	CALL     	3 2 2
  label_2: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1139]	EQ       	0 3 -1	; - false
  label_3: {
    Instruction i = 0x01c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1139]	JMP      	0 1	; to 6
  label_4: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1140]	RETURN   	2 2
  label_5: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1142]	GETUPVAL 	4 1	; evaluate
  label_6: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1142]	MOVE     	5 3
  label_7: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1142]	MOVE     	6 1
  label_8: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1142]	TAILCALL 	4 3 0
  label_9: {
    Instruction i = 0x01800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1142]	RETURN   	4 0
  label_10: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1143]	RETURN   	0 1
  label_11: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1143
// lastlinedefined = 1152
// what = Lua
static int zz_magic_function_88 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1144]	GETUPVAL 	2 0	; force1
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1144]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1144]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1144]	MOVE     	0 2
  label_3: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1145]	GETUPVAL 	2 1	; any_delay_just_p
  label_4: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1145]	MOVE     	3 0
  label_5: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1145]	CALL     	2 2 2
  label_6: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1145]	TEST     	2 0
  label_7: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1145]	JMP      	0 7	; to 16
  label_8: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1146]	GETUPVAL 	2 2	; builtin_func_apply
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1146]	GETUPVAL 	3 3	; list_chooseOne_function_builtin_systemName
  label_10: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1146]	NEWTABLE 	4 1 0
  label_11: {
    Instruction i = 0x0080010b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1146]	MOVE     	5 0
  label_12: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1146]	SETLIST  	4 1 1	; 1
  label_13: {
    Instruction i = 0x0080412b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1146]	TAILCALL 	2 3 0
  label_14: {
    Instruction i = 0x018000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1146]	RETURN   	2 0
  label_15: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1148]	GETUPVAL 	2 4	; construction_p
  label_16: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1148]	MOVE     	3 0
  label_17: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1148]	CALL     	2 2 2
  label_18: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1148]	TEST     	2 1
  label_19: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1148]	JMP      	0 1	; to 22
  label_20: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1149]	RETURN   	1 2
  label_21: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1151]	GETUPVAL 	2 5	; construction_head
  label_22: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1151]	MOVE     	3 0
  label_23: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1151]	TAILCALL 	2 2 0
  label_24: {
    Instruction i = 0x010000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1151]	RETURN   	2 0
  label_25: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1152]	RETURN   	0 1
  label_26: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1152
// lastlinedefined = 1171
// what = Lua
static int zz_magic_function_89 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1153]	GETUPVAL 	4 0	; force1
  label_0: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1153]	MOVE     	5 0
  label_1: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1153]	CALL     	4 2 2
  label_2: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1153]	MOVE     	0 4
  label_3: {
    Instruction i = 0x02000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1154]	GETUPVAL 	4 1	; any_delay_just_p
  label_4: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1154]	MOVE     	5 0
  label_5: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1154]	CALL     	4 2 2
  label_6: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1154]	TEST     	4 0
  label_7: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1154]	JMP      	0 9	; to 18
  label_8: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [1155]	GETUPVAL 	4 2	; builtin_func_apply
  label_9: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1155]	GETUPVAL 	5 3	; if_function_builtin_systemName
  label_10: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1155]	NEWTABLE 	6 3 0
  label_11: {
    Instruction i = 0x0180018b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1155]	MOVE     	7 0
  label_12: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1155]	MOVE     	8 1
  label_13: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1155]	MOVE     	9 2
  label_14: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1155]	SETLIST  	6 3 1	; 1
  label_15: {
    Instruction i = 0x018041ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1155]	TAILCALL 	4 3 0
  label_16: {
    Instruction i = 0x01800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1155]	RETURN   	4 0
  label_17: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1157]	GETUPVAL 	4 4	; data_p
  label_18: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1157]	MOVE     	5 0
  label_19: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1157]	CALL     	4 2 2
  label_20: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1157]	TEST     	4 1
  label_21: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_23;
    }
  }
  
  // [1157]	JMP      	0 1	; to 24
  label_22: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_24;
  }
  
  // [1158]	RETURN   	3 2
  label_23: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1160]	GETUPVAL 	4 5	; force_all
  label_24: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1160]	GETUPVAL 	5 6	; data_name
  label_25: {
    Instruction i = 0x03000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1160]	MOVE     	6 0
  label_26: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1160]	CALL     	5 2 0
  label_27: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1160]	CALL     	4 0 2
  label_28: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1161]	GETUPVAL 	5 7	; symbol_p
  label_29: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1161]	MOVE     	6 4
  label_30: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1161]	CALL     	5 2 2
  label_31: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1161]	TEST     	5 1
  label_32: {
    Instruction i = 0x00004162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_34;
    }
  }
  
  // [1161]	JMP      	0 1	; to 35
  label_33: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [1162]	RETURN   	3 2
  label_34: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1164]	GETUPVAL 	5 8	; symbol_equal_p
  label_35: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1164]	MOVE     	6 4
  label_36: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1164]	GETUPVAL 	7 9	; true_symbol
  label_37: {
    Instruction i = 0x048001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1164]	CALL     	5 3 2
  label_38: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1164]	TEST     	5 0
  label_39: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_41;
    }
  }
  
  // [1164]	JMP      	0 1	; to 42
  label_40: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_42;
  }
  
  // [1165]	RETURN   	1 2
  label_41: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1167]	GETUPVAL 	5 8	; symbol_equal_p
  label_42: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1167]	MOVE     	6 4
  label_43: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1167]	GETUPVAL 	7 10	; false_symbol
  label_44: {
    Instruction i = 0x050001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1167]	CALL     	5 3 2
  label_45: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1167]	TEST     	5 0
  label_46: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_48;
    }
  }
  
  // [1167]	JMP      	0 1	; to 49
  label_47: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_49;
  }
  
  // [1168]	RETURN   	2 2
  label_48: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1170]	RETURN   	3 2
  label_49: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1171]	RETURN   	0 1
  label_50: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1174
// lastlinedefined = 1229
// what = Lua
static int zz_magic_function_90 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1175]	EQ       	0 0 1
  label_0: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [1175]	JMP      	0 2	; to 4
  label_1: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_4;
  }
  
  // [1176]	LOADBOOL 	2 1 0
  label_2: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_4;
    }
  }
  
  // [1176]	RETURN   	2 2
  label_3: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1178]	GETUPVAL 	2 0	; un_just_all
  label_4: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1178]	MOVE     	3 0
  label_5: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1178]	CALL     	2 2 2
  label_6: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1178]	MOVE     	0 2
  label_7: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1179]	GETUPVAL 	2 0	; un_just_all
  label_8: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1179]	MOVE     	3 1
  label_9: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1179]	CALL     	2 2 2
  label_10: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1179]	MOVE     	1 2
  label_11: {
    Instruction i = 0x01000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1180]	EQ       	0 0 1
  label_12: {
    Instruction i = 0x0000401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_14;
    }
  }
  
  // [1180]	JMP      	0 2	; to 16
  label_13: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1181]	LOADBOOL 	2 1 0
  label_14: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [1181]	RETURN   	2 2
  label_15: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1183]	LOADNIL  	2 0
  label_16: {
    Instruction i = 0x00000084;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1191]	CLOSURE  	3 0	; 0x559f637ad520
  label_17: {
    Instruction i = 0x000000ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1191]	MOVE     	2 3
  label_18: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1192]	GETUPVAL 	3 3	; null_p
  label_19: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1192]	MOVE     	4 0
  label_20: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1192]	CALL     	3 2 2
  label_21: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1192]	TEST     	3 0
  label_22: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_24;
    }
  }
  
  // [1192]	JMP      	0 18	; to 42
  label_23: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_42;
  }
  
  // [1193]	GETUPVAL 	3 3	; null_p
  label_24: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1193]	MOVE     	4 1
  label_25: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1193]	CALL     	3 2 2
  label_26: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1193]	TEST     	3 1
  label_27: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_29;
    }
  }
  
  // [1193]	JMP      	0 2	; to 31
  label_28: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1194]	LOADBOOL 	3 0 0
  label_29: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_31;
    }
  }
  
  // [1194]	RETURN   	3 2
  label_30: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1196]	GETUPVAL 	3 2	; lang_set_do
  label_31: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1196]	MOVE     	4 0
  label_32: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1196]	GETUPVAL 	5 4	; null_v
  label_33: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1196]	CALL     	3 3 1
  label_34: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1197]	GETUPVAL 	3 2	; lang_set_do
  label_35: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1197]	MOVE     	4 1
  label_36: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1197]	GETUPVAL 	5 4	; null_v
  label_37: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1197]	CALL     	3 3 1
  label_38: {
    Instruction i = 0x018040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1198]	LOADBOOL 	3 1 0
  label_39: {
    Instruction i = 0x008000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_41;
    }
  }
  
  // [1198]	RETURN   	3 2
  label_40: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1198]	JMP      	0 109	; to 151
  label_41: {
    Instruction i = 0x801b001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1199]	GETUPVAL 	3 5	; symbol_p
  label_42: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1199]	MOVE     	4 0
  label_43: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1199]	CALL     	3 2 2
  label_44: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1199]	TEST     	3 0
  label_45: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_47;
    }
  }
  
  // [1199]	JMP      	0 13	; to 60
  label_46: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_60;
  }
  
  // [1200]	GETUPVAL 	3 5	; symbol_p
  label_47: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1200]	MOVE     	4 1
  label_48: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1200]	CALL     	3 2 2
  label_49: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1200]	TEST     	3 1
  label_50: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_52;
    }
  }
  
  // [1200]	JMP      	0 2	; to 54
  label_51: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_54;
  }
  
  // [1201]	LOADBOOL 	3 0 0
  label_52: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_54;
    }
  }
  
  // [1201]	RETURN   	3 2
  label_53: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1203]	GETUPVAL 	3 6	; symbol_equal_p
  label_54: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1203]	MOVE     	4 0
  label_55: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1203]	MOVE     	5 1
  label_56: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1203]	TAILCALL 	3 3 0
  label_57: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1203]	RETURN   	3 0
  label_58: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1203]	JMP      	0 91	; to 151
  label_59: {
    Instruction i = 0x8016801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1204]	GETUPVAL 	3 7	; construction_p
  label_60: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1204]	MOVE     	4 0
  label_61: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1204]	CALL     	3 2 2
  label_62: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1204]	TEST     	3 0
  label_63: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_65;
    }
  }
  
  // [1204]	JMP      	0 15	; to 80
  label_64: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_80;
  }
  
  // [1205]	GETUPVAL 	3 7	; construction_p
  label_65: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1205]	MOVE     	4 1
  label_66: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1205]	CALL     	3 2 2
  label_67: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1205]	TEST     	3 1
  label_68: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_70;
    }
  }
  
  // [1205]	JMP      	0 2	; to 72
  label_69: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_72;
  }
  
  // [1206]	LOADBOOL 	3 0 0
  label_70: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_72;
    }
  }
  
  // [1206]	RETURN   	3 2
  label_71: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1208]	MOVE     	3 2
  label_72: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1208]	MOVE     	4 0
  label_73: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1208]	MOVE     	5 1
  label_74: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1208]	GETUPVAL 	6 8	; construction_head
  label_75: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1208]	GETUPVAL 	7 9	; construction_tail
  label_76: {
    Instruction i = 0x048001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1208]	TAILCALL 	3 5 0
  label_77: {
    Instruction i = 0x028000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1208]	RETURN   	3 0
  label_78: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1208]	JMP      	0 71	; to 151
  label_79: {
    Instruction i = 0x8011801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1209]	GETUPVAL 	3 10	; error_p
  label_80: {
    Instruction i = 0x050000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1209]	MOVE     	4 0
  label_81: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1209]	CALL     	3 2 2
  label_82: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1209]	TEST     	3 0
  label_83: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_85;
    }
  }
  
  // [1209]	JMP      	0 15	; to 100
  label_84: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_100;
  }
  
  // [1210]	GETUPVAL 	3 10	; error_p
  label_85: {
    Instruction i = 0x050000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1210]	MOVE     	4 1
  label_86: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1210]	CALL     	3 2 2
  label_87: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1210]	TEST     	3 1
  label_88: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_90;
    }
  }
  
  // [1210]	JMP      	0 2	; to 92
  label_89: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_92;
  }
  
  // [1211]	LOADBOOL 	3 0 0
  label_90: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_92;
    }
  }
  
  // [1211]	RETURN   	3 2
  label_91: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1213]	MOVE     	3 2
  label_92: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1213]	MOVE     	4 0
  label_93: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1213]	MOVE     	5 1
  label_94: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1213]	GETUPVAL 	6 11	; error_name
  label_95: {
    Instruction i = 0x05800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1213]	GETUPVAL 	7 12	; error_list
  label_96: {
    Instruction i = 0x060001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1213]	TAILCALL 	3 5 0
  label_97: {
    Instruction i = 0x028000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1213]	RETURN   	3 0
  label_98: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1213]	JMP      	0 51	; to 151
  label_99: {
    Instruction i = 0x800c801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1214]	GETUPVAL 	3 13	; data_p
  label_100: {
    Instruction i = 0x068000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1214]	MOVE     	4 0
  label_101: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1214]	CALL     	3 2 2
  label_102: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1214]	TEST     	3 0
  label_103: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_105;
    }
  }
  
  // [1214]	JMP      	0 15	; to 120
  label_104: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_120;
  }
  
  // [1215]	GETUPVAL 	3 13	; data_p
  label_105: {
    Instruction i = 0x068000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1215]	MOVE     	4 1
  label_106: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1215]	CALL     	3 2 2
  label_107: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1215]	TEST     	3 1
  label_108: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_110;
    }
  }
  
  // [1215]	JMP      	0 2	; to 112
  label_109: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_112;
  }
  
  // [1216]	LOADBOOL 	3 0 0
  label_110: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_112;
    }
  }
  
  // [1216]	RETURN   	3 2
  label_111: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1218]	MOVE     	3 2
  label_112: {
    Instruction i = 0x010000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1218]	MOVE     	4 0
  label_113: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1218]	MOVE     	5 1
  label_114: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1218]	GETUPVAL 	6 14	; data_name
  label_115: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1218]	GETUPVAL 	7 15	; data_list
  label_116: {
    Instruction i = 0x078001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1218]	TAILCALL 	3 5 0
  label_117: {
    Instruction i = 0x028000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1218]	RETURN   	3 0
  label_118: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1218]	JMP      	0 31	; to 151
  label_119: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1219]	GETUPVAL 	3 16	; delay_evaluate_p
  label_120: {
    Instruction i = 0x080000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1219]	MOVE     	4 0
  label_121: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1219]	CALL     	3 2 2
  label_122: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1219]	TEST     	3 0
  label_123: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_125;
    }
  }
  
  // [1219]	JMP      	0 3	; to 128
  label_124: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_128;
  }
  
  // [1220]	LOADBOOL 	3 0 0
  label_125: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_127;
    }
  }
  
  // [1220]	RETURN   	3 2
  label_126: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1220]	JMP      	0 23	; to 151
  label_127: {
    Instruction i = 0x8005801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1221]	GETUPVAL 	3 17	; delay_builtin_func_p
  label_128: {
    Instruction i = 0x088000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1221]	MOVE     	4 0
  label_129: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1221]	CALL     	3 2 2
  label_130: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1221]	TEST     	3 0
  label_131: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_133;
    }
  }
  
  // [1221]	JMP      	0 3	; to 136
  label_132: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_136;
  }
  
  // [1222]	LOADBOOL 	3 0 0
  label_133: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_135;
    }
  }
  
  // [1222]	RETURN   	3 2
  label_134: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1222]	JMP      	0 15	; to 151
  label_135: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1223]	GETUPVAL 	3 18	; delay_builtin_form_p
  label_136: {
    Instruction i = 0x090000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1223]	MOVE     	4 0
  label_137: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1223]	CALL     	3 2 2
  label_138: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1223]	TEST     	3 0
  label_139: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_141;
    }
  }
  
  // [1223]	JMP      	0 3	; to 144
  label_140: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_144;
  }
  
  // [1224]	LOADBOOL 	3 0 0
  label_141: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_143;
    }
  }
  
  // [1224]	RETURN   	3 2
  label_142: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1224]	JMP      	0 7	; to 151
  label_143: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1225]	GETUPVAL 	3 19	; delay_apply_p
  label_144: {
    Instruction i = 0x098000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1225]	MOVE     	4 0
  label_145: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1225]	CALL     	3 2 2
  label_146: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1225]	TEST     	3 0
  label_147: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_149;
    }
  }
  
  // [1225]	JMP      	0 2	; to 151
  label_148: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_151;
  }
  
  // [1226]	LOADBOOL 	3 0 0
  label_149: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_151;
    }
  }
  
  // [1226]	RETURN   	3 2
  label_150: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1228]	GETUPVAL 	3 20	; ERROR
  label_151: {
    Instruction i = 0x0a0000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1228]	TAILCALL 	3 1 0
  label_152: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1228]	RETURN   	3 0
  label_153: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1229]	RETURN   	0 1
  label_154: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1184
// lastlinedefined = 1191
// what = Lua
static int zz_magic_function_91 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1185]	GETUPVAL 	4 0	; jsbool_no_force_equal_p
  label_0: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1185]	MOVE     	5 2
  label_1: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	MOVE     	6 0
  label_2: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	CALL     	5 2 2
  label_3: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1185]	MOVE     	6 2
  label_4: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	MOVE     	7 1
  label_5: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	CALL     	6 2 0
  label_6: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1185]	CALL     	4 0 2
  label_7: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1185]	TEST     	4 0
  label_8: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1185]	JMP      	0 17	; to 27
  label_9: {
    Instruction i = 0x8004001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_27;
  }
  
  // [1185]	GETUPVAL 	4 0	; jsbool_no_force_equal_p
  label_10: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1185]	MOVE     	5 3
  label_11: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	MOVE     	6 0
  label_12: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	CALL     	5 2 2
  label_13: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1185]	MOVE     	6 3
  label_14: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	MOVE     	7 1
  label_15: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1185]	CALL     	6 2 0
  label_16: {
    Instruction i = 0x010001a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1185]	CALL     	4 0 2
  label_17: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1185]	TEST     	4 0
  label_18: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [1185]	JMP      	0 7	; to 27
  label_19: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_27;
  }
  
  // [1186]	GETUPVAL 	4 1	; lang_set_do
  label_20: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1186]	MOVE     	5 0
  label_21: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1186]	MOVE     	6 1
  label_22: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1186]	CALL     	4 3 1
  label_23: {
    Instruction i = 0x01804124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1187]	LOADBOOL 	4 1 0
  label_24: {
    Instruction i = 0x00800103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_26;
    }
  }
  
  // [1187]	RETURN   	4 2
  label_25: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1187]	JMP      	0 2	; to 29
  label_26: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_29;
  }
  
  // [1189]	LOADBOOL 	4 0 0
  label_27: {
    Instruction i = 0x00000103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_29;
    }
  }
  
  // [1189]	RETURN   	4 2
  label_28: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1191]	RETURN   	0 1
  label_29: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1231
// lastlinedefined = 1233
// what = Lua
static int zz_magic_function_92 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1232]	GETUPVAL 	1 0	; simple_print
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1232]	GETUPVAL 	2 1	; force_all_rec
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1232]	MOVE     	3 0
  label_2: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1232]	CALL     	2 2 0
  label_3: {
    Instruction i = 0x010000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1232]	TAILCALL 	1 0 0
  label_4: {
    Instruction i = 0x00000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1232]	RETURN   	1 0
  label_5: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1233]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1237
// lastlinedefined = 1524
// what = Lua
static int zz_magic_function_93 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1238]	LOADNIL  	1 17
  label_0: {
    Instruction i = 0x08800044;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1241]	CLOSURE  	19 0	; 0x559f637adb10
  label_1: {
    Instruction i = 0x000004ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1241]	MOVE     	3 19
  label_2: {
    Instruction i = 0x098000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1247]	CLOSURE  	19 1	; 0x559f637adc20
  label_3: {
    Instruction i = 0x000044ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1247]	MOVE     	4 19
  label_4: {
    Instruction i = 0x09800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1251]	CLOSURE  	19 2	; 0x559f6379a5b0
  label_5: {
    Instruction i = 0x000084ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1251]	MOVE     	5 19
  label_6: {
    Instruction i = 0x09800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1254]	CLOSURE  	19 3	; 0x559f6379a7b0
  label_7: {
    Instruction i = 0x0000c4ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1254]	MOVE     	6 19
  label_8: {
    Instruction i = 0x09800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1257]	CLOSURE  	19 4	; 0x559f6379a840
  label_9: {
    Instruction i = 0x000104ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1257]	MOVE     	7 19
  label_10: {
    Instruction i = 0x098001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1274]	CLOSURE  	19 5	; 0x559f6379a8d0
  label_11: {
    Instruction i = 0x000144ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1274]	MOVE     	8 19
  label_12: {
    Instruction i = 0x09800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1295]	CLOSURE  	19 6	; 0x559f6379aca0
  label_13: {
    Instruction i = 0x000184ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1295]	MOVE     	9 19
  label_14: {
    Instruction i = 0x09800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1364]	CLOSURE  	19 7	; 0x559f6379a9b0
  label_15: {
    Instruction i = 0x0001c4ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1364]	MOVE     	10 19
  label_16: {
    Instruction i = 0x09800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1382]	CLOSURE  	19 8	; 0x559f637af830
  label_17: {
    Instruction i = 0x000204ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1382]	MOVE     	11 19
  label_18: {
    Instruction i = 0x098002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1400]	CLOSURE  	19 9	; 0x559f637afa30
  label_19: {
    Instruction i = 0x000244ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1400]	MOVE     	12 19
  label_20: {
    Instruction i = 0x09800300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1416]	CLOSURE  	19 10	; 0x559f637b00c0
  label_21: {
    Instruction i = 0x000284ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1416]	MOVE     	17 19
  label_22: {
    Instruction i = 0x09800440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1431]	CLOSURE  	19 11	; 0x559f637b0cf0
  label_23: {
    Instruction i = 0x0002c4ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1431]	MOVE     	18 19
  label_24: {
    Instruction i = 0x09800480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1432]	MOVE     	1 0
  label_25: {
    Instruction i = 0x00000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1433]	LOADK    	2 -1	; 0
  label_26: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1434]	LOADNIL  	19 0
  label_27: {
    Instruction i = 0x000004c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1458]	CLOSURE  	20 12	; 0x559f637b1260
  label_28: {
    Instruction i = 0x0003052c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1458]	MOVE     	19 20
  label_29: {
    Instruction i = 0x0a0004c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1459]	LOADNIL  	20 0
  label_30: {
    Instruction i = 0x00000504;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1487]	CLOSURE  	21 13	; 0x559f637b15d0
  label_31: {
    Instruction i = 0x0003456c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1487]	MOVE     	20 21
  label_32: {
    Instruction i = 0x0a800500;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1488]	MOVE     	21 19
  label_33: {
    Instruction i = 0x09800540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1488]	LOADK    	22 -2	; "$"
  label_34: {
    Instruction i = 0x00004581;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1494]	CLOSURE  	23 14	; 0x559f637af950
  label_35: {
    Instruction i = 0x000385ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1488]	CALL     	21 3 2
  label_36: {
    Instruction i = 0x01808564;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1494]	MOVE     	13 21
  label_37: {
    Instruction i = 0x0a800340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1495]	MOVE     	21 19
  label_38: {
    Instruction i = 0x09800540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1495]	LOADK    	22 -3	; "%"
  label_39: {
    Instruction i = 0x00008581;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1502]	CLOSURE  	23 15	; 0x559f637b0310
  label_40: {
    Instruction i = 0x0003c5ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1495]	CALL     	21 3 2
  label_41: {
    Instruction i = 0x01808564;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1502]	MOVE     	14 21
  label_42: {
    Instruction i = 0x0a800380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1503]	MOVE     	21 20
  label_43: {
    Instruction i = 0x0a000540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1503]	LOADK    	22 -4	; "@"
  label_44: {
    Instruction i = 0x0000c581;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1514]	CLOSURE  	23 16	; 0x559f637af7a0
  label_45: {
    Instruction i = 0x000405ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1503]	CALL     	21 3 2
  label_46: {
    Instruction i = 0x01808564;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1514]	MOVE     	15 21
  label_47: {
    Instruction i = 0x0a8003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1515]	MOVE     	21 19
  label_48: {
    Instruction i = 0x09800540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1515]	LOADK    	22 -5	; "^"
  label_49: {
    Instruction i = 0x00010581;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1522]	CLOSURE  	23 17	; 0x559f637b2ba0
  label_50: {
    Instruction i = 0x000445ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1515]	CALL     	21 3 2
  label_51: {
    Instruction i = 0x01808564;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1522]	MOVE     	16 21
  label_52: {
    Instruction i = 0x0a800400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1523]	MOVE     	21 18
  label_53: {
    Instruction i = 0x09000540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1523]	TAILCALL 	21 1 0
  label_54: {
    Instruction i = 0x00800565;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1523]	RETURN   	21 0
  label_55: {
    Instruction i = 0x00000566;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1524]	RETURN   	0 1
  label_56: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1239
// lastlinedefined = 1241
// what = Lua
static int zz_magic_function_94 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1240]	GETUPVAL 	0 0	; state_const
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1240]	LEN      	0 0
  label_1: {
    Instruction i = 0x0000001c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1240]	GETUPVAL 	1 1	; state
  label_2: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1240]	EQ       	1 0 1
  label_3: {
    Instruction i = 0x0000405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1240]	JMP      	0 1	; to 6
  label_4: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1240]	LOADBOOL 	0 0 1
  label_5: {
    Instruction i = 0x00004003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [1240]	LOADBOOL 	0 1 0
  label_6: {
    Instruction i = 0x00800003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_8;
    }
  }
  
  // [1240]	RETURN   	0 2
  label_7: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1241]	RETURN   	0 1
  label_8: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1242
// lastlinedefined = 1247
// what = Lua
static int zz_magic_function_95 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1243]	GETUPVAL 	0 0	; ASSERT
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1243]	GETUPVAL 	1 1	; eof
  label_1: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1243]	CALL     	1 1 2
  label_2: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1243]	NOT      	1 1
  label_3: {
    Instruction i = 0x0080005b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RB(i);
    int res = l_isfalse(rb);  /* next assignment may change this value */
    setbvalue(ra, res);
  }
  
  // [1243]	CALL     	0 2 1
  label_4: {
    Instruction i = 0x01004024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1244]	GETUPVAL 	0 2	; state_const
  label_5: {
    Instruction i = 0x01000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1244]	SELF     	0 0 -1	; "sub"
  label_6: {
    Instruction i = 0x0040000c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    const TValue *aux;
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    TString *key = tsvalue(rc);  /* key must be a string */
    setobjs2s(L, ra + 1, rb);
    if (luaV_fastget(L, rb, key, aux, luaH_getstr)) {
      setobj2s(L, ra, aux);
    }
    else Protect(luaV_finishget(L, rb, rc, ra, aux));
  }
  
  // [1244]	GETUPVAL 	2 3	; state
  label_7: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1244]	ADD      	2 2 -2	; - 1
  label_8: {
    Instruction i = 0x0140408d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1244]	GETUPVAL 	3 3	; state
  label_9: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1244]	ADD      	3 3 -2	; - 1
  label_10: {
    Instruction i = 0x01c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1244]	CALL     	0 4 2
  label_11: {
    Instruction i = 0x02008024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1245]	GETUPVAL 	1 3	; state
  label_12: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1245]	ADD      	1 1 -2	; - 1
  label_13: {
    Instruction i = 0x00c0404d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1245]	SETUPVAL 	1 3	; state
  label_14: {
    Instruction i = 0x01800049;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    UpVal *uv = cl->upvals[GETARG_B(i)];
    setobj(L, uv->v, ra);
    luaC_upvalbarrier(L, uv);
  }
  
  // [1246]	RETURN   	0 2
  label_15: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1247]	RETURN   	0 1
  label_16: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1248
// lastlinedefined = 1251
// what = Lua
static int zz_magic_function_96 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1249]	GETUPVAL 	1 0	; ASSERT
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1249]	GETUPVAL 	2 1	; state_const
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1249]	SELF     	2 2 -1	; "sub"
  label_2: {
    Instruction i = 0x0140008c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    const TValue *aux;
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    TString *key = tsvalue(rc);  /* key must be a string */
    setobjs2s(L, ra + 1, rb);
    if (luaV_fastget(L, rb, key, aux, luaH_getstr)) {
      setobj2s(L, ra, aux);
    }
    else Protect(luaV_finishget(L, rb, rc, ra, aux));
  }
  
  // [1249]	GETUPVAL 	4 2	; state
  label_3: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1249]	SUB      	4 4 -2	; - 1
  label_4: {
    Instruction i = 0x0240410e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [1249]	ADD      	4 4 -2	; - 1
  label_5: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1249]	GETUPVAL 	5 2	; state
  label_6: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1249]	SUB      	5 5 -2	; - 1
  label_7: {
    Instruction i = 0x02c0414e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [1249]	ADD      	5 5 -2	; - 1
  label_8: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1249]	CALL     	2 4 2
  label_9: {
    Instruction i = 0x020080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1249]	EQ       	1 2 0
  label_10: {
    Instruction i = 0x0100005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [1249]	JMP      	0 1	; to 13
  label_11: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [1249]	LOADBOOL 	2 0 1
  label_12: {
    Instruction i = 0x00004083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_14;
    }
  }
  
  // [1249]	LOADBOOL 	2 1 0
  label_13: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1249]	CALL     	1 2 1
  label_14: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1250]	GETUPVAL 	1 2	; state
  label_15: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1250]	SUB      	1 1 -2	; - 1
  label_16: {
    Instruction i = 0x00c0404e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [1250]	SETUPVAL 	1 2	; state
  label_17: {
    Instruction i = 0x01000049;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    UpVal *uv = cl->upvals[GETARG_B(i)];
    setobj(L, uv->v, ra);
    luaC_upvalbarrier(L, uv);
  }
  
  // [1251]	RETURN   	0 1
  label_18: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1252
// lastlinedefined = 1254
// what = Lua
static int zz_magic_function_97 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1253]	GETTABUP 	0 0 -1	; _ENV "error"
  label_0: {
    Instruction i = 0x00400006;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [1253]	LOADK    	1 -2	; "TheLanguage parse ERROR!"
  label_1: {
    Instruction i = 0x00004041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1253]	CALL     	0 2 1
  label_2: {
    Instruction i = 0x01004024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1254]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1255
// lastlinedefined = 1257
// what = Lua
static int zz_magic_function_98 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1256]	EQ       	1 0 -1	; - " "
  label_0: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [1256]	JMP      	0 7	; to 9
  label_1: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1256]	EQ       	1 0 -2	; - "\n"
  label_2: {
    Instruction i = 0x0040405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1256]	JMP      	0 5	; to 9
  label_3: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1256]	EQ       	1 0 -3	; - "\t"
  label_4: {
    Instruction i = 0x0040805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_6;
    }
  }
  
  // [1256]	JMP      	0 3	; to 9
  label_5: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1256]	EQ       	1 0 -4	; - "\r"
  label_6: {
    Instruction i = 0x0040c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_8;
    }
  }
  
  // [1256]	JMP      	0 1	; to 9
  label_7: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1256]	LOADBOOL 	1 0 1
  label_8: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_10;
    }
  }
  
  // [1256]	LOADBOOL 	1 1 0
  label_9: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_11;
    }
  }
  
  // [1256]	RETURN   	1 2
  label_10: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1257]	RETURN   	0 1
  label_11: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1258
// lastlinedefined = 1274
// what = Lua
static int zz_magic_function_99 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1259]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1259]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1259]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1259]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1260]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1260]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1262]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1262]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1263]	GETUPVAL 	1 2	; a_space_p
  label_8: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1263]	MOVE     	2 0
  label_9: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1263]	CALL     	1 2 2
  label_10: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1263]	TEST     	1 1
  label_11: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_13;
    }
  }
  
  // [1263]	JMP      	0 5	; to 18
  label_12: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [1264]	GETUPVAL 	1 3	; put
  label_13: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1264]	MOVE     	2 0
  label_14: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1264]	CALL     	1 2 1
  label_15: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1265]	LOADBOOL 	1 0 0
  label_16: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_18;
    }
  }
  
  // [1265]	RETURN   	1 2
  label_17: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1267]	GETUPVAL 	1 2	; a_space_p
  label_18: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1267]	MOVE     	2 0
  label_19: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1267]	CALL     	1 2 2
  label_20: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1267]	TEST     	1 0
  label_21: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_23;
    }
  }
  
  // [1267]	JMP      	0 8	; to 31
  label_22: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1267]	GETUPVAL 	1 0	; eof
  label_23: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1267]	CALL     	1 1 2
  label_24: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1267]	TEST     	1 1
  label_25: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1267]	JMP      	0 4	; to 31
  label_26: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1268]	GETUPVAL 	1 1	; get
  label_27: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1268]	CALL     	1 1 2
  label_28: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1268]	MOVE     	0 1
  label_29: {
    Instruction i = 0x00800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1268]	JMP      	0 -13	; to 18
  label_30: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [1270]	GETUPVAL 	1 2	; a_space_p
  label_31: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1270]	MOVE     	2 0
  label_32: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1270]	CALL     	1 2 2
  label_33: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1270]	TEST     	1 1
  label_34: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_36;
    }
  }
  
  // [1270]	JMP      	0 3	; to 39
  label_35: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_39;
  }
  
  // [1271]	GETUPVAL 	1 3	; put
  label_36: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1271]	MOVE     	2 0
  label_37: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1271]	CALL     	1 2 1
  label_38: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1273]	LOADBOOL 	1 1 0
  label_39: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_41;
    }
  }
  
  // [1273]	RETURN   	1 2
  label_40: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1274]	RETURN   	0 1
  label_41: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1275
// lastlinedefined = 1295
// what = Lua
static int zz_magic_function_100 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1276]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1276]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1276]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1276]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1277]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1277]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1279]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1279]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1280]	LOADK    	1 -1	; ""
  label_8: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1281]	GETUPVAL 	2 2	; a_symbol_p
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1281]	MOVE     	3 0
  label_10: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1281]	CALL     	2 2 2
  label_11: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1281]	TEST     	2 1
  label_12: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_14;
    }
  }
  
  // [1281]	JMP      	0 5	; to 19
  label_13: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_19;
  }
  
  // [1282]	GETUPVAL 	2 3	; put
  label_14: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1282]	MOVE     	3 0
  label_15: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1282]	CALL     	2 2 1
  label_16: {
    Instruction i = 0x010040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1283]	LOADBOOL 	2 0 0
  label_17: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_19;
    }
  }
  
  // [1283]	RETURN   	2 2
  label_18: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1285]	GETUPVAL 	2 2	; a_symbol_p
  label_19: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1285]	MOVE     	3 0
  label_20: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1285]	CALL     	2 2 2
  label_21: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1285]	TEST     	2 0
  label_22: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_24;
    }
  }
  
  // [1285]	JMP      	0 11	; to 35
  label_23: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [1285]	GETUPVAL 	2 0	; eof
  label_24: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1285]	CALL     	2 1 2
  label_25: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1285]	TEST     	2 1
  label_26: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [1285]	JMP      	0 7	; to 35
  label_27: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [1286]	MOVE     	2 1
  label_28: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1286]	MOVE     	3 0
  label_29: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1286]	CONCAT   	1 2 3
  label_30: {
    Instruction i = 0x0100c05d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1287]	GETUPVAL 	2 1	; get
  label_31: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1287]	CALL     	2 1 2
  label_32: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1287]	MOVE     	0 2
  label_33: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1287]	JMP      	0 -16	; to 19
  label_34: {
    Instruction i = 0x7ffbc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_19;
  }
  
  // [1289]	GETUPVAL 	2 2	; a_symbol_p
  label_35: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1289]	MOVE     	3 0
  label_36: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1289]	CALL     	2 2 2
  label_37: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1289]	TEST     	2 0
  label_38: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [1289]	JMP      	0 4	; to 44
  label_39: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_44;
  }
  
  // [1290]	MOVE     	2 1
  label_40: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1290]	MOVE     	3 0
  label_41: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1290]	CONCAT   	1 2 3
  label_42: {
    Instruction i = 0x0100c05d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1290]	JMP      	0 3	; to 47
  label_43: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_47;
  }
  
  // [1292]	GETUPVAL 	2 3	; put
  label_44: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1292]	MOVE     	3 0
  label_45: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1292]	CALL     	2 2 1
  label_46: {
    Instruction i = 0x010040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1294]	GETUPVAL 	2 4	; new_symbol
  label_47: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1294]	MOVE     	3 1
  label_48: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1294]	TAILCALL 	2 2 0
  label_49: {
    Instruction i = 0x010000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1294]	RETURN   	2 0
  label_50: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1295]	RETURN   	0 1
  label_51: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1296
// lastlinedefined = 1364
// what = Lua
static int zz_magic_function_101 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1297]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1297]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1297]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1297]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1298]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1298]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1300]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1300]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1301]	EQ       	1 0 -1	; - "("
  label_8: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1301]	JMP      	0 5	; to 15
  label_9: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1302]	GETUPVAL 	1 2	; put
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1302]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1302]	CALL     	1 2 1
  label_12: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1303]	LOADBOOL 	1 0 0
  label_13: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1303]	RETURN   	1 2
  label_14: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1305]	GETUPVAL 	1 3	; new_symbol
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1305]	LOADK    	2 -2	; "!!@@READ||HOLE@@!!"
  label_16: {
    Instruction i = 0x00004081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1305]	CALL     	1 2 2
  label_17: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1306]	MOVE     	2 1
  label_18: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1307]	LOADNIL  	3 0
  label_19: {
    Instruction i = 0x000000c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1331]	CLOSURE  	4 0	; 0x559f637af8c0
  label_20: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1331]	MOVE     	3 4
  label_21: {
    Instruction i = 0x020000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1332]	LOADNIL  	4 0
  label_22: {
    Instruction i = 0x00000104;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1335]	CLOSURE  	5 1	; 0x559f637afbe0
  label_23: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1335]	MOVE     	4 5
  label_24: {
    Instruction i = 0x02800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1337]	GETUPVAL 	5 8	; space
  label_25: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1337]	CALL     	5 1 1
  label_26: {
    Instruction i = 0x00804164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1338]	GETUPVAL 	5 0	; eof
  label_27: {
    Instruction i = 0x00000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1338]	CALL     	5 1 2
  label_28: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1338]	TEST     	5 0
  label_29: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_31;
    }
  }
  
  // [1338]	JMP      	0 3	; to 34
  label_30: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_34;
  }
  
  // [1339]	GETUPVAL 	5 9	; parse_error
  label_31: {
    Instruction i = 0x04800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1339]	TAILCALL 	5 1 0
  label_32: {
    Instruction i = 0x00800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1339]	RETURN   	5 0
  label_33: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1341]	GETUPVAL 	5 1	; get
  label_34: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1341]	CALL     	5 1 2
  label_35: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1341]	MOVE     	0 5
  label_36: {
    Instruction i = 0x02800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1342]	EQ       	0 0 -3	; - ")"
  label_37: {
    Instruction i = 0x0040801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_39;
    }
  }
  
  // [1342]	JMP      	0 4	; to 43
  label_38: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [1343]	MOVE     	5 3
  label_39: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1343]	GETUPVAL 	6 10	; null_v
  label_40: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1343]	CALL     	5 2 1
  label_41: {
    Instruction i = 0x01004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1344]	RETURN   	2 2
  label_42: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1346]	EQ       	0 0 -4	; - "."
  label_43: {
    Instruction i = 0x0040c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_45;
    }
  }
  
  // [1346]	JMP      	0 25	; to 70
  label_44: {
    Instruction i = 0x8006001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_70;
  }
  
  // [1347]	GETUPVAL 	5 8	; space
  label_45: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1347]	CALL     	5 1 1
  label_46: {
    Instruction i = 0x00804164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1348]	GETUPVAL 	5 11	; val
  label_47: {
    Instruction i = 0x05800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1348]	CALL     	5 1 2
  label_48: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1349]	MOVE     	6 3
  label_49: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1349]	MOVE     	7 5
  label_50: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1349]	CALL     	6 2 1
  label_51: {
    Instruction i = 0x010041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1350]	GETUPVAL 	6 8	; space
  label_52: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1350]	CALL     	6 1 1
  label_53: {
    Instruction i = 0x008041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1351]	GETUPVAL 	6 0	; eof
  label_54: {
    Instruction i = 0x00000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1351]	CALL     	6 1 2
  label_55: {
    Instruction i = 0x008081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1351]	TEST     	6 0
  label_56: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_58;
    }
  }
  
  // [1351]	JMP      	0 3	; to 61
  label_57: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [1352]	GETUPVAL 	6 9	; parse_error
  label_58: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1352]	TAILCALL 	6 1 0
  label_59: {
    Instruction i = 0x008001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1352]	RETURN   	6 0
  label_60: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1354]	GETUPVAL 	6 1	; get
  label_61: {
    Instruction i = 0x00800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1354]	CALL     	6 1 2
  label_62: {
    Instruction i = 0x008081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1354]	MOVE     	0 6
  label_63: {
    Instruction i = 0x03000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1355]	EQ       	1 0 -3	; - ")"
  label_64: {
    Instruction i = 0x0040805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_66;
    }
  }
  
  // [1355]	JMP      	0 3	; to 69
  label_65: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_69;
  }
  
  // [1356]	GETUPVAL 	6 9	; parse_error
  label_66: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1356]	TAILCALL 	6 1 0
  label_67: {
    Instruction i = 0x008001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1356]	RETURN   	6 0
  label_68: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1358]	RETURN   	2 2
  label_69: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1360]	GETUPVAL 	5 2	; put
  label_70: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1360]	MOVE     	6 0
  label_71: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1360]	CALL     	5 2 1
  label_72: {
    Instruction i = 0x01004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1361]	GETUPVAL 	5 11	; val
  label_73: {
    Instruction i = 0x05800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1361]	CALL     	5 1 2
  label_74: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1362]	MOVE     	6 4
  label_75: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1362]	MOVE     	7 5
  label_76: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1362]	CALL     	6 2 1
  label_77: {
    Instruction i = 0x010041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1362]	JMP      	0 -54	; to 25
  label_78: {
    Instruction i = 0x7ff2401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [1364]	RETURN   	0 1
  label_79: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1308
// lastlinedefined = 1331
// what = Lua
static int zz_magic_function_102 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1309]	GETUPVAL 	1 0	; ret
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1309]	GETUPVAL 	2 1	; HOLE
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1309]	EQ       	0 1 2
  label_2: {
    Instruction i = 0x0080801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1309]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1310]	SETUPVAL 	0 0	; ret
  label_4: {
    Instruction i = 0x00000009;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    UpVal *uv = cl->upvals[GETARG_B(i)];
    setobj(L, uv->v, ra);
    luaC_upvalbarrier(L, uv);
  }
  
  // [1311]	RETURN   	0 1
  label_5: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1313]	GETUPVAL 	1 0	; ret
  label_6: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1315]	GETUPVAL 	2 2	; construction_p
  label_7: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1315]	MOVE     	3 1
  label_8: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1315]	CALL     	2 2 2
  label_9: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1315]	TEST     	2 1
  label_10: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [1315]	JMP      	0 3	; to 15
  label_11: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1316]	GETUPVAL 	2 3	; ERROR
  label_12: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1316]	TAILCALL 	2 1 0
  label_13: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1316]	RETURN   	2 0
  label_14: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1318]	GETUPVAL 	2 4	; construction_tail
  label_15: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1318]	MOVE     	3 1
  label_16: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1318]	CALL     	2 2 2
  label_17: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1319]	GETUPVAL 	3 1	; HOLE
  label_18: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1319]	EQ       	1 2 3
  label_19: {
    Instruction i = 0x0100c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1319]	JMP      	0 5	; to 26
  label_20: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_26;
  }
  
  // [1322]	GETUPVAL 	3 4	; construction_tail
  label_21: {
    Instruction i = 0x020000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1322]	MOVE     	4 1
  label_22: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1322]	CALL     	3 2 2
  label_23: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1322]	MOVE     	1 3
  label_24: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1322]	JMP      	0 -19	; to 7
  label_25: {
    Instruction i = 0x7ffb001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_7;
  }
  
  // [1324]	GETUPVAL 	2 2	; construction_p
  label_26: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1324]	MOVE     	3 1
  label_27: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1324]	CALL     	2 2 2
  label_28: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1324]	TEST     	2 1
  label_29: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_31;
    }
  }
  
  // [1324]	JMP      	0 3	; to 34
  label_30: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_34;
  }
  
  // [1325]	GETUPVAL 	2 3	; ERROR
  label_31: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1325]	TAILCALL 	2 1 0
  label_32: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1325]	RETURN   	2 0
  label_33: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1327]	GETUPVAL 	2 4	; construction_tail
  label_34: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1327]	MOVE     	3 1
  label_35: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1327]	CALL     	2 2 2
  label_36: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1327]	GETUPVAL 	3 1	; HOLE
  label_37: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1327]	EQ       	1 2 3
  label_38: {
    Instruction i = 0x0100c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [1327]	JMP      	0 3	; to 43
  label_39: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [1328]	GETUPVAL 	2 3	; ERROR
  label_40: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1328]	TAILCALL 	2 1 0
  label_41: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1328]	RETURN   	2 0
  label_42: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1330]	SETTABLE 	1 -1 0	; 3 -
  label_43: {
    Instruction i = 0x8000004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1331]	RETURN   	0 1
  label_44: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1333
// lastlinedefined = 1335
// what = Lua
static int zz_magic_function_103 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1334]	GETUPVAL 	1 0	; set_last
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1334]	GETUPVAL 	2 1	; new_construction
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1334]	MOVE     	3 0
  label_2: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1334]	GETUPVAL 	4 2	; HOLE
  label_3: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1334]	CALL     	2 3 0
  label_4: {
    Instruction i = 0x018000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1334]	CALL     	1 0 1
  label_5: {
    Instruction i = 0x00004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1335]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1365
// lastlinedefined = 1382
// what = Lua
static int zz_magic_function_104 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1366]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1366]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1366]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1366]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1367]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1367]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1369]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1369]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1370]	EQ       	1 0 -1	; - "#"
  label_8: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1370]	JMP      	0 5	; to 15
  label_9: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1371]	GETUPVAL 	1 2	; put
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1371]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1371]	CALL     	1 2 1
  label_12: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1372]	LOADBOOL 	1 0 0
  label_13: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1372]	RETURN   	1 2
  label_14: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1374]	GETUPVAL 	1 3	; list
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1374]	CALL     	1 1 2
  label_16: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1375]	EQ       	0 1 -2	; - false
  label_17: {
    Instruction i = 0x00c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_19;
    }
  }
  
  // [1375]	JMP      	0 3	; to 22
  label_18: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1376]	GETUPVAL 	2 4	; parse_error
  label_19: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1376]	TAILCALL 	2 1 0
  label_20: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1376]	RETURN   	2 0
  label_21: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1378]	GETUPVAL 	2 5	; construction_p
  label_22: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1378]	MOVE     	3 1
  label_23: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1378]	CALL     	2 2 2
  label_24: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1378]	TEST     	2 1
  label_25: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1378]	JMP      	0 3	; to 30
  label_26: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_30;
  }
  
  // [1379]	GETUPVAL 	2 4	; parse_error
  label_27: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1379]	TAILCALL 	2 1 0
  label_28: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1379]	RETURN   	2 0
  label_29: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1381]	GETUPVAL 	2 6	; new_data
  label_30: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1381]	GETUPVAL 	3 7	; construction_head
  label_31: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1381]	MOVE     	4 1
  label_32: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1381]	CALL     	3 2 2
  label_33: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1381]	GETUPVAL 	4 8	; construction_tail
  label_34: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1381]	MOVE     	5 1
  label_35: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1381]	CALL     	4 2 0
  label_36: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1381]	TAILCALL 	2 0 0
  label_37: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1381]	RETURN   	2 0
  label_38: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1382]	RETURN   	0 1
  label_39: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1383
// lastlinedefined = 1400
// what = Lua
static int zz_magic_function_105 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1384]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1384]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1384]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1384]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1385]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1385]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1387]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1387]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1388]	EQ       	1 0 -1	; - "!"
  label_8: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1388]	JMP      	0 5	; to 15
  label_9: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1389]	GETUPVAL 	1 2	; put
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1389]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1389]	CALL     	1 2 1
  label_12: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1390]	LOADBOOL 	1 0 0
  label_13: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1390]	RETURN   	1 2
  label_14: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1392]	GETUPVAL 	1 3	; list
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1392]	CALL     	1 1 2
  label_16: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1393]	EQ       	0 1 -2	; - false
  label_17: {
    Instruction i = 0x00c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_19;
    }
  }
  
  // [1393]	JMP      	0 3	; to 22
  label_18: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1394]	GETUPVAL 	2 4	; parse_error
  label_19: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1394]	TAILCALL 	2 1 0
  label_20: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1394]	RETURN   	2 0
  label_21: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1396]	GETUPVAL 	2 5	; construction_p
  label_22: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1396]	MOVE     	3 1
  label_23: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1396]	CALL     	2 2 2
  label_24: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1396]	TEST     	2 1
  label_25: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1396]	JMP      	0 3	; to 30
  label_26: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_30;
  }
  
  // [1397]	GETUPVAL 	2 4	; parse_error
  label_27: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1397]	TAILCALL 	2 1 0
  label_28: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1397]	RETURN   	2 0
  label_29: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1399]	GETUPVAL 	2 6	; new_error
  label_30: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1399]	GETUPVAL 	3 7	; construction_head
  label_31: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1399]	MOVE     	4 1
  label_32: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1399]	CALL     	3 2 2
  label_33: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1399]	GETUPVAL 	4 8	; construction_tail
  label_34: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1399]	MOVE     	5 1
  label_35: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1399]	CALL     	4 2 0
  label_36: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1399]	TAILCALL 	2 0 0
  label_37: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1399]	RETURN   	2 0
  label_38: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1400]	RETURN   	0 1
  label_39: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1401
// lastlinedefined = 1416
// what = Lua
static int zz_magic_function_106 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1402]	GETUPVAL 	1 0	; a_space_p
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1402]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1402]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1402]	TEST     	1 0
  label_3: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1402]	JMP      	0 2	; to 7
  label_4: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_7;
  }
  
  // [1403]	LOADBOOL 	1 0 0
  label_5: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [1403]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1405]	NEWTABLE 	1 18 0
  label_7: {
    Instruction i = 0x0900004b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1405]	LOADK    	2 -1	; "("
  label_8: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	3 -2	; ")"
  label_9: {
    Instruction i = 0x000040c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	4 -3	; "!"
  label_10: {
    Instruction i = 0x00008101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	5 -4	; "#"
  label_11: {
    Instruction i = 0x0000c141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	6 -5	; "."
  label_12: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	7 -6	; "$"
  label_13: {
    Instruction i = 0x000141c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	8 -7	; "%"
  label_14: {
    Instruction i = 0x00018201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	9 -8	; "^"
  label_15: {
    Instruction i = 0x0001c241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	10 -9	; "@"
  label_16: {
    Instruction i = 0x00020281;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	11 -10	; "~"
  label_17: {
    Instruction i = 0x000242c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	12 -11	; "/"
  label_18: {
    Instruction i = 0x00028301;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	13 -12	; "-"
  label_19: {
    Instruction i = 0x0002c341;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	14 -13	; ">"
  label_20: {
    Instruction i = 0x00030381;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	15 -14	; "_"
  label_21: {
    Instruction i = 0x000343c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	16 -15	; ":"
  label_22: {
    Instruction i = 0x00038401;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	17 -16	; "?"
  label_23: {
    Instruction i = 0x0003c441;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	18 -17	; "["
  label_24: {
    Instruction i = 0x00040481;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	19 -18	; "]"
  label_25: {
    Instruction i = 0x000444c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	LOADK    	20 -19	; "&"
  label_26: {
    Instruction i = 0x00048501;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1405]	SETLIST  	1 19 1	; 1
  label_27: {
    Instruction i = 0x0980406b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1407]	LOADK    	2 -20	; 0
  label_28: {
    Instruction i = 0x0004c081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1408]	LEN      	3 1
  label_29: {
    Instruction i = 0x008000dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1408]	LT       	0 2 3
  label_30: {
    Instruction i = 0x0100c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_32;
    }
  }
  
  // [1408]	JMP      	0 8	; to 40
  label_31: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_40;
  }
  
  // [1409]	ADD      	3 2 -21	; - 1
  label_32: {
    Instruction i = 0x014500cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1409]	GETTABLE 	3 1 3
  label_33: {
    Instruction i = 0x0080c0c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1409]	EQ       	0 0 3
  label_34: {
    Instruction i = 0x0000c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_36;
    }
  }
  
  // [1409]	JMP      	0 2	; to 38
  label_35: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_38;
  }
  
  // [1410]	LOADBOOL 	3 0 0
  label_36: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_38;
    }
  }
  
  // [1410]	RETURN   	3 2
  label_37: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1412]	ADD      	2 2 -21	; - 1
  label_38: {
    Instruction i = 0x0145008d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1412]	JMP      	0 -11	; to 29
  label_39: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_29;
  }
  
  // [1415]	LOADBOOL 	2 1 0
  label_40: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_42;
    }
  }
  
  // [1415]	RETURN   	2 2
  label_41: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1416]	RETURN   	0 1
  label_42: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1417
// lastlinedefined = 1431
// what = Lua
static int zz_magic_function_107 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1418]	GETUPVAL 	0 0	; space
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1418]	CALL     	0 1 1
  label_1: {
    Instruction i = 0x00804024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1419]	NEWTABLE 	0 8 0
  label_2: {
    Instruction i = 0x0400000b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1419]	GETUPVAL 	1 1	; list
  label_3: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	2 2	; symbol
  label_4: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	3 3	; data
  label_5: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	4 4	; readerror
  label_6: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	5 5	; readeval
  label_7: {
    Instruction i = 0x02800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	6 6	; readfuncapply
  label_8: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	7 7	; readformbuiltin
  label_9: {
    Instruction i = 0x038001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	GETUPVAL 	8 8	; readapply
  label_10: {
    Instruction i = 0x04000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1419]	SETLIST  	0 8 1	; 1
  label_11: {
    Instruction i = 0x0400402b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1421]	LOADK    	1 -1	; 0
  label_12: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1422]	LEN      	2 0
  label_13: {
    Instruction i = 0x0000009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1422]	LT       	0 1 2
  label_14: {
    Instruction i = 0x00808020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_16;
    }
  }
  
  // [1422]	JMP      	0 8	; to 24
  label_15: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_24;
  }
  
  // [1423]	ADD      	2 1 -2	; - 1
  label_16: {
    Instruction i = 0x00c0408d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1423]	GETTABLE 	2 0 2
  label_17: {
    Instruction i = 0x00008087;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1423]	CALL     	2 1 2
  label_18: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1424]	EQ       	1 2 -3	; - false
  label_19: {
    Instruction i = 0x0140805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1424]	JMP      	0 1	; to 22
  label_20: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1425]	RETURN   	2 2
  label_21: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1427]	ADD      	1 1 -2	; - 1
  label_22: {
    Instruction i = 0x00c0404d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1427]	JMP      	0 -11	; to 13
  label_23: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [1430]	GETUPVAL 	1 9	; parse_error
  label_24: {
    Instruction i = 0x04800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1430]	TAILCALL 	1 1 0
  label_25: {
    Instruction i = 0x00800065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1430]	RETURN   	1 0
  label_26: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1431]	RETURN   	0 1
  label_27: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1435
// lastlinedefined = 1458
// what = Lua
static int zz_magic_function_108 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1457]	CLOSURE  	2 0	; 0x559f637b12f0
  label_0: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1457]	RETURN   	2 2
  label_1: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1458]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1436
// lastlinedefined = 1457
// what = Lua
static int zz_magic_function_109 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1437]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1437]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1437]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1437]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1438]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1438]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1440]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1440]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1441]	GETUPVAL 	1 2	; prefix
  label_8: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1441]	EQ       	1 0 1
  label_9: {
    Instruction i = 0x0000405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_11;
    }
  }
  
  // [1441]	JMP      	0 5	; to 16
  label_10: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1442]	GETUPVAL 	1 3	; put
  label_11: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1442]	MOVE     	2 0
  label_12: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1442]	CALL     	1 2 1
  label_13: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1443]	LOADBOOL 	1 0 0
  label_14: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [1443]	RETURN   	1 2
  label_15: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1445]	GETUPVAL 	1 4	; list
  label_16: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1445]	CALL     	1 1 2
  label_17: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1446]	EQ       	0 1 -1	; - false
  label_18: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [1446]	JMP      	0 3	; to 23
  label_19: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [1447]	GETUPVAL 	2 5	; parse_error
  label_20: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1447]	TAILCALL 	2 1 0
  label_21: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1447]	RETURN   	2 0
  label_22: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1449]	GETUPVAL 	2 6	; construction_p
  label_23: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1449]	MOVE     	3 1
  label_24: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1449]	CALL     	2 2 2
  label_25: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1449]	TEST     	2 1
  label_26: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [1449]	JMP      	0 3	; to 31
  label_27: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1450]	GETUPVAL 	2 5	; parse_error
  label_28: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1450]	TAILCALL 	2 1 0
  label_29: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1450]	RETURN   	2 0
  label_30: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1452]	GETUPVAL 	2 7	; construction_tail
  label_31: {
    Instruction i = 0x03800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1452]	MOVE     	3 1
  label_32: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1452]	CALL     	2 2 2
  label_33: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1453]	GETUPVAL 	3 6	; construction_p
  label_34: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1453]	MOVE     	4 2
  label_35: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1453]	CALL     	3 2 2
  label_36: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1453]	TEST     	3 0
  label_37: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_39;
    }
  }
  
  // [1453]	JMP      	0 7	; to 46
  label_38: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_46;
  }
  
  // [1453]	GETUPVAL 	3 8	; null_p
  label_39: {
    Instruction i = 0x040000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1453]	GETUPVAL 	4 7	; construction_tail
  label_40: {
    Instruction i = 0x03800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1453]	MOVE     	5 2
  label_41: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1453]	CALL     	4 2 0
  label_42: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1453]	CALL     	3 0 2
  label_43: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1453]	TEST     	3 1
  label_44: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_46;
    }
  }
  
  // [1453]	JMP      	0 3	; to 49
  label_45: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_49;
  }
  
  // [1454]	GETUPVAL 	3 5	; parse_error
  label_46: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1454]	TAILCALL 	3 1 0
  label_47: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1454]	RETURN   	3 0
  label_48: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1456]	GETUPVAL 	3 9	; k
  label_49: {
    Instruction i = 0x048000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1456]	GETUPVAL 	4 10	; construction_head
  label_50: {
    Instruction i = 0x05000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1456]	MOVE     	5 1
  label_51: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1456]	CALL     	4 2 2
  label_52: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1456]	GETUPVAL 	5 10	; construction_head
  label_53: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1456]	MOVE     	6 2
  label_54: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1456]	CALL     	5 2 0
  label_55: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1456]	TAILCALL 	3 0 0
  label_56: {
    Instruction i = 0x000000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1456]	RETURN   	3 0
  label_57: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1457]	RETURN   	0 1
  label_58: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1460
// lastlinedefined = 1487
// what = Lua
static int zz_magic_function_110 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1486]	CLOSURE  	2 0	; 0x559f637b1660
  label_0: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1486]	RETURN   	2 2
  label_1: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1487]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1461
// lastlinedefined = 1486
// what = Lua
static int zz_magic_function_111 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1462]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1462]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1462]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1462]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1463]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1463]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1465]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1465]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1466]	GETUPVAL 	1 2	; prefix
  label_8: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1466]	EQ       	1 0 1
  label_9: {
    Instruction i = 0x0000405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_11;
    }
  }
  
  // [1466]	JMP      	0 5	; to 16
  label_10: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1467]	GETUPVAL 	1 3	; put
  label_11: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1467]	MOVE     	2 0
  label_12: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1467]	CALL     	1 2 1
  label_13: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1468]	LOADBOOL 	1 0 0
  label_14: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [1468]	RETURN   	1 2
  label_15: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1470]	GETUPVAL 	1 4	; list
  label_16: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1470]	CALL     	1 1 2
  label_17: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1471]	EQ       	0 1 -1	; - false
  label_18: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [1471]	JMP      	0 3	; to 23
  label_19: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [1472]	GETUPVAL 	2 5	; parse_error
  label_20: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1472]	TAILCALL 	2 1 0
  label_21: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1472]	RETURN   	2 0
  label_22: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1474]	GETUPVAL 	2 6	; construction_p
  label_23: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1474]	MOVE     	3 1
  label_24: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1474]	CALL     	2 2 2
  label_25: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1474]	TEST     	2 1
  label_26: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [1474]	JMP      	0 3	; to 31
  label_27: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1475]	GETUPVAL 	2 5	; parse_error
  label_28: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1475]	TAILCALL 	2 1 0
  label_29: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1475]	RETURN   	2 0
  label_30: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1477]	GETUPVAL 	2 7	; construction_tail
  label_31: {
    Instruction i = 0x03800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1477]	MOVE     	3 1
  label_32: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1477]	CALL     	2 2 2
  label_33: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1478]	GETUPVAL 	3 6	; construction_p
  label_34: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1478]	MOVE     	4 2
  label_35: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1478]	CALL     	3 2 2
  label_36: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1478]	TEST     	3 1
  label_37: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_39;
    }
  }
  
  // [1478]	JMP      	0 3	; to 42
  label_38: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_42;
  }
  
  // [1479]	GETUPVAL 	3 5	; parse_error
  label_39: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1479]	TAILCALL 	3 1 0
  label_40: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1479]	RETURN   	3 0
  label_41: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1481]	GETUPVAL 	3 7	; construction_tail
  label_42: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1481]	MOVE     	4 2
  label_43: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1481]	CALL     	3 2 2
  label_44: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1482]	GETUPVAL 	4 6	; construction_p
  label_45: {
    Instruction i = 0x03000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1482]	MOVE     	5 3
  label_46: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1482]	CALL     	4 2 2
  label_47: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1482]	TEST     	4 0
  label_48: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_50;
    }
  }
  
  // [1482]	JMP      	0 7	; to 57
  label_49: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_57;
  }
  
  // [1482]	GETUPVAL 	4 8	; null_p
  label_50: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1482]	GETUPVAL 	5 7	; construction_tail
  label_51: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1482]	MOVE     	6 3
  label_52: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1482]	CALL     	5 2 0
  label_53: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1482]	CALL     	4 0 2
  label_54: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1482]	TEST     	4 1
  label_55: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_57;
    }
  }
  
  // [1482]	JMP      	0 3	; to 60
  label_56: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_60;
  }
  
  // [1483]	GETUPVAL 	4 5	; parse_error
  label_57: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1483]	TAILCALL 	4 1 0
  label_58: {
    Instruction i = 0x00800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1483]	RETURN   	4 0
  label_59: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1485]	GETUPVAL 	4 9	; k
  label_60: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1485]	GETUPVAL 	5 10	; construction_head
  label_61: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1485]	MOVE     	6 1
  label_62: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1485]	CALL     	5 2 2
  label_63: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1485]	GETUPVAL 	6 10	; construction_head
  label_64: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1485]	MOVE     	7 2
  label_65: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1485]	CALL     	6 2 2
  label_66: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1485]	GETUPVAL 	7 10	; construction_head
  label_67: {
    Instruction i = 0x050001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1485]	MOVE     	8 3
  label_68: {
    Instruction i = 0x01800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1485]	CALL     	7 2 0
  label_69: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1485]	TAILCALL 	4 0 0
  label_70: {
    Instruction i = 0x00000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1485]	RETURN   	4 0
  label_71: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1486]	RETURN   	0 1
  label_72: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1488
// lastlinedefined = 1494
// what = Lua
static int zz_magic_function_112 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1489]	GETUPVAL 	2 0	; val2env
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1489]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1489]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1490]	EQ       	0 2 -1	; - false
  label_3: {
    Instruction i = 0x0140001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1490]	JMP      	0 3	; to 8
  label_4: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_8;
  }
  
  // [1491]	GETUPVAL 	3 1	; parse_error
  label_5: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1491]	TAILCALL 	3 1 0
  label_6: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1491]	RETURN   	3 0
  label_7: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1493]	GETUPVAL 	3 2	; evaluate
  label_8: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1493]	MOVE     	4 2
  label_9: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1493]	MOVE     	5 1
  label_10: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1493]	TAILCALL 	3 3 0
  label_11: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1493]	RETURN   	3 0
  label_12: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1494]	RETURN   	0 1
  label_13: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1495
// lastlinedefined = 1502
// what = Lua
static int zz_magic_function_113 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1496]	GETUPVAL 	2 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1496]	MOVE     	3 1
  label_1: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1498]	CLOSURE  	4 0	; 0x559f637b21a0
  label_2: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1500]	CLOSURE  	5 1	; 0x559f637b2230
  label_3: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1496]	CALL     	2 4 2
  label_4: {
    Instruction i = 0x020080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1501]	GETUPVAL 	3 2	; builtin_func_apply
  label_5: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1501]	MOVE     	4 0
  label_6: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1501]	MOVE     	5 2
  label_7: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1501]	TAILCALL 	3 3 0
  label_8: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1501]	RETURN   	3 0
  label_9: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1502]	RETURN   	0 1
  label_10: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1496
// lastlinedefined = 1498
// what = Lua
static int zz_magic_function_114 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1497]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1498]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1498
// lastlinedefined = 1500
// what = Lua
static int zz_magic_function_115 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1499]	GETUPVAL 	2 0	; parse_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1499]	TAILCALL 	2 1 0
  label_1: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1499]	RETURN   	2 0
  label_2: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1500]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1503
// lastlinedefined = 1514
// what = Lua
static int zz_magic_function_116 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1504]	GETUPVAL 	3 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1504]	MOVE     	4 2
  label_1: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1506]	CLOSURE  	5 0	; 0x559f637b2530
  label_2: {
    Instruction i = 0x0000016c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1508]	CLOSURE  	6 1	; 0x559f637b2610
  label_3: {
    Instruction i = 0x000041ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1504]	CALL     	3 4 2
  label_4: {
    Instruction i = 0x020080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1509]	GETUPVAL 	4 2	; val2env
  label_5: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1509]	MOVE     	5 0
  label_6: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1509]	CALL     	4 2 2
  label_7: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1510]	EQ       	0 4 -1	; - false
  label_8: {
    Instruction i = 0x0240001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1510]	JMP      	0 3	; to 13
  label_9: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [1511]	GETUPVAL 	5 1	; parse_error
  label_10: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1511]	TAILCALL 	5 1 0
  label_11: {
    Instruction i = 0x00800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1511]	RETURN   	5 0
  label_12: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1513]	GETUPVAL 	5 3	; builtin_form_apply
  label_13: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1513]	MOVE     	6 4
  label_14: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1513]	MOVE     	7 1
  label_15: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1513]	MOVE     	8 3
  label_16: {
    Instruction i = 0x01800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1513]	TAILCALL 	5 4 0
  label_17: {
    Instruction i = 0x02000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1513]	RETURN   	5 0
  label_18: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1514]	RETURN   	0 1
  label_19: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1504
// lastlinedefined = 1506
// what = Lua
static int zz_magic_function_117 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1505]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1506]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1506
// lastlinedefined = 1508
// what = Lua
static int zz_magic_function_118 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1507]	GETUPVAL 	2 0	; parse_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1507]	TAILCALL 	2 1 0
  label_1: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1507]	RETURN   	2 0
  label_2: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1508]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1515
// lastlinedefined = 1522
// what = Lua
static int zz_magic_function_119 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1516]	GETUPVAL 	2 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1516]	MOVE     	3 1
  label_1: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1518]	CLOSURE  	4 0	; 0x559f637b2c30
  label_2: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1520]	CLOSURE  	5 1	; 0x559f637b2cc0
  label_3: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1516]	CALL     	2 4 2
  label_4: {
    Instruction i = 0x020080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1521]	GETUPVAL 	3 2	; apply
  label_5: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1521]	MOVE     	4 0
  label_6: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1521]	MOVE     	5 2
  label_7: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1521]	TAILCALL 	3 3 0
  label_8: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1521]	RETURN   	3 0
  label_9: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1522]	RETURN   	0 1
  label_10: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1516
// lastlinedefined = 1518
// what = Lua
static int zz_magic_function_120 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1517]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1518]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1518
// lastlinedefined = 1520
// what = Lua
static int zz_magic_function_121 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1519]	GETUPVAL 	2 0	; parse_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1519]	TAILCALL 	2 1 0
  label_1: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1519]	RETURN   	2 0
  label_2: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1520]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1527
// lastlinedefined = 1953
// what = Lua
static int zz_magic_function_122 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1528]	LOADNIL  	1 22
  label_0: {
    Instruction i = 0x0b000044;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1531]	CLOSURE  	24 0	; 0x559f637b3020
  label_1: {
    Instruction i = 0x0000062c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1531]	MOVE     	3 24
  label_2: {
    Instruction i = 0x0c0000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1537]	CLOSURE  	24 1	; 0x559f637b3100
  label_3: {
    Instruction i = 0x0000462c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1537]	MOVE     	4 24
  label_4: {
    Instruction i = 0x0c000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1541]	CLOSURE  	24 2	; 0x559f637b3490
  label_5: {
    Instruction i = 0x0000862c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1541]	MOVE     	5 24
  label_6: {
    Instruction i = 0x0c000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1544]	CLOSURE  	24 3	; 0x559f637b3760
  label_7: {
    Instruction i = 0x0000c62c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1544]	MOVE     	6 24
  label_8: {
    Instruction i = 0x0c000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1547]	CLOSURE  	24 4	; 0x559f637b38e0
  label_9: {
    Instruction i = 0x0001062c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1547]	MOVE     	7 24
  label_10: {
    Instruction i = 0x0c0001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1564]	CLOSURE  	24 5	; 0x559f637b3ab0
  label_11: {
    Instruction i = 0x0001462c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1564]	MOVE     	8 24
  label_12: {
    Instruction i = 0x0c000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1585]	CLOSURE  	24 6	; 0x559f637b3c80
  label_13: {
    Instruction i = 0x0001862c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1585]	MOVE     	9 24
  label_14: {
    Instruction i = 0x0c000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1654]	CLOSURE  	24 7	; 0x559f637b40f0
  label_15: {
    Instruction i = 0x0001c62c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1654]	MOVE     	10 24
  label_16: {
    Instruction i = 0x0c000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1672]	CLOSURE  	24 8	; 0x559f637b4400
  label_17: {
    Instruction i = 0x0002062c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1672]	MOVE     	11 24
  label_18: {
    Instruction i = 0x0c0002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1690]	CLOSURE  	24 9	; 0x559f637b4680
  label_19: {
    Instruction i = 0x0002462c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1690]	MOVE     	12 24
  label_20: {
    Instruction i = 0x0c000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1706]	CLOSURE  	24 10	; 0x559f637b4d10
  label_21: {
    Instruction i = 0x0002862c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1706]	MOVE     	17 24
  label_22: {
    Instruction i = 0x0c000440;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1721]	CLOSURE  	24 11	; 0x559f637b5b70
  label_23: {
    Instruction i = 0x0002c62c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1721]	MOVE     	18 24
  label_24: {
    Instruction i = 0x0c000480;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1727]	CLOSURE  	24 12	; 0x559f637b5df0
  label_25: {
    Instruction i = 0x0003062c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1727]	MOVE     	19 24
  label_26: {
    Instruction i = 0x0c0004c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1730]	CLOSURE  	24 13	; 0x559f637b5e80
  label_27: {
    Instruction i = 0x0003462c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1730]	MOVE     	20 24
  label_28: {
    Instruction i = 0x0c000500;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1734]	CLOSURE  	24 14	; 0x559f637b5f60
  label_29: {
    Instruction i = 0x0003862c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1734]	MOVE     	21 24
  label_30: {
    Instruction i = 0x0c000540;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1850]	CLOSURE  	24 15	; 0x559f637b60e0
  label_31: {
    Instruction i = 0x0003c62c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1850]	MOVE     	22 24
  label_32: {
    Instruction i = 0x0c000580;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1860]	CLOSURE  	24 16	; 0x559f637b4550
  label_33: {
    Instruction i = 0x0004062c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1860]	MOVE     	23 24
  label_34: {
    Instruction i = 0x0c0005c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1861]	MOVE     	1 0
  label_35: {
    Instruction i = 0x00000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1862]	LOADK    	2 -1	; 0
  label_36: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1863]	LOADNIL  	24 0
  label_37: {
    Instruction i = 0x00000604;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1887]	CLOSURE  	25 17	; 0x559f637b6380
  label_38: {
    Instruction i = 0x0004466c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1887]	MOVE     	24 25
  label_39: {
    Instruction i = 0x0c800600;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1888]	LOADNIL  	25 0
  label_40: {
    Instruction i = 0x00000644;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1916]	CLOSURE  	26 18	; 0x559f637b78f0
  label_41: {
    Instruction i = 0x000486ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1916]	MOVE     	25 26
  label_42: {
    Instruction i = 0x0d000640;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1917]	MOVE     	26 24
  label_43: {
    Instruction i = 0x0c000680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1917]	LOADK    	27 -2	; "$"
  label_44: {
    Instruction i = 0x000046c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1923]	CLOSURE  	28 19	; 0x559f637b8310
  label_45: {
    Instruction i = 0x0004c72c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1917]	CALL     	26 3 2
  label_46: {
    Instruction i = 0x018086a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1923]	MOVE     	13 26
  label_47: {
    Instruction i = 0x0d000340;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1924]	MOVE     	26 24
  label_48: {
    Instruction i = 0x0c000680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1924]	LOADK    	27 -3	; "%"
  label_49: {
    Instruction i = 0x000086c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1931]	CLOSURE  	28 20	; 0x559f637b86b0
  label_50: {
    Instruction i = 0x0005072c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1924]	CALL     	26 3 2
  label_51: {
    Instruction i = 0x018086a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1931]	MOVE     	14 26
  label_52: {
    Instruction i = 0x0d000380;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1932]	MOVE     	26 25
  label_53: {
    Instruction i = 0x0c800680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1932]	LOADK    	27 -4	; "@"
  label_54: {
    Instruction i = 0x0000c6c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1943]	CLOSURE  	28 21	; 0x559f637b8ba0
  label_55: {
    Instruction i = 0x0005472c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1932]	CALL     	26 3 2
  label_56: {
    Instruction i = 0x018086a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1943]	MOVE     	15 26
  label_57: {
    Instruction i = 0x0d0003c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1944]	MOVE     	26 24
  label_58: {
    Instruction i = 0x0c000680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1944]	LOADK    	27 -5	; "^"
  label_59: {
    Instruction i = 0x000106c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1951]	CLOSURE  	28 22	; 0x559f637b91c0
  label_60: {
    Instruction i = 0x0005872c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1944]	CALL     	26 3 2
  label_61: {
    Instruction i = 0x018086a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1951]	MOVE     	16 26
  label_62: {
    Instruction i = 0x0d000400;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1952]	MOVE     	26 18
  label_63: {
    Instruction i = 0x09000680;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1952]	TAILCALL 	26 1 0
  label_64: {
    Instruction i = 0x008006a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1952]	RETURN   	26 0
  label_65: {
    Instruction i = 0x000006a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1953]	RETURN   	0 1
  label_66: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1529
// lastlinedefined = 1531
// what = Lua
static int zz_magic_function_123 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1530]	GETUPVAL 	0 0	; state_const
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1530]	LEN      	0 0
  label_1: {
    Instruction i = 0x0000001c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1530]	GETUPVAL 	1 1	; state
  label_2: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1530]	EQ       	1 0 1
  label_3: {
    Instruction i = 0x0000405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1530]	JMP      	0 1	; to 6
  label_4: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1530]	LOADBOOL 	0 0 1
  label_5: {
    Instruction i = 0x00004003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [1530]	LOADBOOL 	0 1 0
  label_6: {
    Instruction i = 0x00800003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_8;
    }
  }
  
  // [1530]	RETURN   	0 2
  label_7: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1531]	RETURN   	0 1
  label_8: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1532
// lastlinedefined = 1537
// what = Lua
static int zz_magic_function_124 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1533]	GETUPVAL 	0 0	; ASSERT
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1533]	GETUPVAL 	1 1	; eof
  label_1: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1533]	CALL     	1 1 2
  label_2: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1533]	NOT      	1 1
  label_3: {
    Instruction i = 0x0080005b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RB(i);
    int res = l_isfalse(rb);  /* next assignment may change this value */
    setbvalue(ra, res);
  }
  
  // [1533]	CALL     	0 2 1
  label_4: {
    Instruction i = 0x01004024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1534]	GETUPVAL 	0 2	; state_const
  label_5: {
    Instruction i = 0x01000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1534]	SELF     	0 0 -1	; "sub"
  label_6: {
    Instruction i = 0x0040000c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    const TValue *aux;
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    TString *key = tsvalue(rc);  /* key must be a string */
    setobjs2s(L, ra + 1, rb);
    if (luaV_fastget(L, rb, key, aux, luaH_getstr)) {
      setobj2s(L, ra, aux);
    }
    else Protect(luaV_finishget(L, rb, rc, ra, aux));
  }
  
  // [1534]	GETUPVAL 	2 3	; state
  label_7: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1534]	ADD      	2 2 -2	; - 1
  label_8: {
    Instruction i = 0x0140408d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1534]	GETUPVAL 	3 3	; state
  label_9: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1534]	ADD      	3 3 -2	; - 1
  label_10: {
    Instruction i = 0x01c040cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1534]	CALL     	0 4 2
  label_11: {
    Instruction i = 0x02008024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1535]	GETUPVAL 	1 3	; state
  label_12: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1535]	ADD      	1 1 -2	; - 1
  label_13: {
    Instruction i = 0x00c0404d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1535]	SETUPVAL 	1 3	; state
  label_14: {
    Instruction i = 0x01800049;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    UpVal *uv = cl->upvals[GETARG_B(i)];
    setobj(L, uv->v, ra);
    luaC_upvalbarrier(L, uv);
  }
  
  // [1536]	RETURN   	0 2
  label_15: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1537]	RETURN   	0 1
  label_16: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1538
// lastlinedefined = 1541
// what = Lua
static int zz_magic_function_125 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1539]	GETUPVAL 	1 0	; ASSERT
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1539]	GETUPVAL 	2 1	; state_const
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1539]	SELF     	2 2 -1	; "sub"
  label_2: {
    Instruction i = 0x0140008c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    const TValue *aux;
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    TString *key = tsvalue(rc);  /* key must be a string */
    setobjs2s(L, ra + 1, rb);
    if (luaV_fastget(L, rb, key, aux, luaH_getstr)) {
      setobj2s(L, ra, aux);
    }
    else Protect(luaV_finishget(L, rb, rc, ra, aux));
  }
  
  // [1539]	GETUPVAL 	4 2	; state
  label_3: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1539]	SUB      	4 4 -2	; - 1
  label_4: {
    Instruction i = 0x0240410e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [1539]	ADD      	4 4 -2	; - 1
  label_5: {
    Instruction i = 0x0240410d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1539]	GETUPVAL 	5 2	; state
  label_6: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1539]	SUB      	5 5 -2	; - 1
  label_7: {
    Instruction i = 0x02c0414e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [1539]	ADD      	5 5 -2	; - 1
  label_8: {
    Instruction i = 0x02c0414d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1539]	CALL     	2 4 2
  label_9: {
    Instruction i = 0x020080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1539]	EQ       	1 2 0
  label_10: {
    Instruction i = 0x0100005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [1539]	JMP      	0 1	; to 13
  label_11: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [1539]	LOADBOOL 	2 0 1
  label_12: {
    Instruction i = 0x00004083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_14;
    }
  }
  
  // [1539]	LOADBOOL 	2 1 0
  label_13: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1539]	CALL     	1 2 1
  label_14: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1540]	GETUPVAL 	1 2	; state
  label_15: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1540]	SUB      	1 1 -2	; - 1
  label_16: {
    Instruction i = 0x00c0404e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(-, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numsub(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_SUB)); }
  }
  
  // [1540]	SETUPVAL 	1 2	; state
  label_17: {
    Instruction i = 0x01000049;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    UpVal *uv = cl->upvals[GETARG_B(i)];
    setobj(L, uv->v, ra);
    luaC_upvalbarrier(L, uv);
  }
  
  // [1541]	RETURN   	0 1
  label_18: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1542
// lastlinedefined = 1544
// what = Lua
static int zz_magic_function_126 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1543]	GETTABUP 	0 0 -1	; _ENV "error"
  label_0: {
    Instruction i = 0x00400006;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *upval = cl->upvals[GETARG_B(i)]->v;
    TValue *rc = RKC(i);
    gettableProtected(L, upval, rc, ra);
  }
  
  // [1543]	LOADK    	1 -2	; "TheLanguage parse ERROR!"
  label_1: {
    Instruction i = 0x00004041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1543]	CALL     	0 2 1
  label_2: {
    Instruction i = 0x01004024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1544]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1545
// lastlinedefined = 1547
// what = Lua
static int zz_magic_function_127 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1546]	EQ       	1 0 -1	; - " "
  label_0: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [1546]	JMP      	0 7	; to 9
  label_1: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1546]	EQ       	1 0 -2	; - "\n"
  label_2: {
    Instruction i = 0x0040405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1546]	JMP      	0 5	; to 9
  label_3: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1546]	EQ       	1 0 -3	; - "\t"
  label_4: {
    Instruction i = 0x0040805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_6;
    }
  }
  
  // [1546]	JMP      	0 3	; to 9
  label_5: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1546]	EQ       	1 0 -4	; - "\r"
  label_6: {
    Instruction i = 0x0040c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_8;
    }
  }
  
  // [1546]	JMP      	0 1	; to 9
  label_7: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1546]	LOADBOOL 	1 0 1
  label_8: {
    Instruction i = 0x00004043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_10;
    }
  }
  
  // [1546]	LOADBOOL 	1 1 0
  label_9: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_11;
    }
  }
  
  // [1546]	RETURN   	1 2
  label_10: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1547]	RETURN   	0 1
  label_11: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1548
// lastlinedefined = 1564
// what = Lua
static int zz_magic_function_128 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1549]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1549]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1549]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1549]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1550]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1550]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1552]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1552]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1553]	GETUPVAL 	1 2	; a_space_p
  label_8: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1553]	MOVE     	2 0
  label_9: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1553]	CALL     	1 2 2
  label_10: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1553]	TEST     	1 1
  label_11: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_13;
    }
  }
  
  // [1553]	JMP      	0 5	; to 18
  label_12: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [1554]	GETUPVAL 	1 3	; put
  label_13: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1554]	MOVE     	2 0
  label_14: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1554]	CALL     	1 2 1
  label_15: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1555]	LOADBOOL 	1 0 0
  label_16: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_18;
    }
  }
  
  // [1555]	RETURN   	1 2
  label_17: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1557]	GETUPVAL 	1 2	; a_space_p
  label_18: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1557]	MOVE     	2 0
  label_19: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1557]	CALL     	1 2 2
  label_20: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1557]	TEST     	1 0
  label_21: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_23;
    }
  }
  
  // [1557]	JMP      	0 8	; to 31
  label_22: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1557]	GETUPVAL 	1 0	; eof
  label_23: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1557]	CALL     	1 1 2
  label_24: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1557]	TEST     	1 1
  label_25: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1557]	JMP      	0 4	; to 31
  label_26: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1558]	GETUPVAL 	1 1	; get
  label_27: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1558]	CALL     	1 1 2
  label_28: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1558]	MOVE     	0 1
  label_29: {
    Instruction i = 0x00800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1558]	JMP      	0 -13	; to 18
  label_30: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [1560]	GETUPVAL 	1 2	; a_space_p
  label_31: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1560]	MOVE     	2 0
  label_32: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1560]	CALL     	1 2 2
  label_33: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1560]	TEST     	1 1
  label_34: {
    Instruction i = 0x00004062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_36;
    }
  }
  
  // [1560]	JMP      	0 3	; to 39
  label_35: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_39;
  }
  
  // [1561]	GETUPVAL 	1 3	; put
  label_36: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1561]	MOVE     	2 0
  label_37: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1561]	CALL     	1 2 1
  label_38: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1563]	LOADBOOL 	1 1 0
  label_39: {
    Instruction i = 0x00800043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_41;
    }
  }
  
  // [1563]	RETURN   	1 2
  label_40: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1564]	RETURN   	0 1
  label_41: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1565
// lastlinedefined = 1585
// what = Lua
static int zz_magic_function_129 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1566]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1566]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1566]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1566]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1567]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1567]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1569]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1569]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1570]	LOADK    	1 -1	; ""
  label_8: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1571]	GETUPVAL 	2 2	; a_symbol_p
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1571]	MOVE     	3 0
  label_10: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1571]	CALL     	2 2 2
  label_11: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1571]	TEST     	2 1
  label_12: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_14;
    }
  }
  
  // [1571]	JMP      	0 5	; to 19
  label_13: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_19;
  }
  
  // [1572]	GETUPVAL 	2 3	; put
  label_14: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1572]	MOVE     	3 0
  label_15: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1572]	CALL     	2 2 1
  label_16: {
    Instruction i = 0x010040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1573]	LOADBOOL 	2 0 0
  label_17: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_19;
    }
  }
  
  // [1573]	RETURN   	2 2
  label_18: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1575]	GETUPVAL 	2 2	; a_symbol_p
  label_19: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1575]	MOVE     	3 0
  label_20: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1575]	CALL     	2 2 2
  label_21: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1575]	TEST     	2 0
  label_22: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_24;
    }
  }
  
  // [1575]	JMP      	0 11	; to 35
  label_23: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [1575]	GETUPVAL 	2 0	; eof
  label_24: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1575]	CALL     	2 1 2
  label_25: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1575]	TEST     	2 1
  label_26: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [1575]	JMP      	0 7	; to 35
  label_27: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [1576]	MOVE     	2 1
  label_28: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1576]	MOVE     	3 0
  label_29: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1576]	CONCAT   	1 2 3
  label_30: {
    Instruction i = 0x0100c05d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1577]	GETUPVAL 	2 1	; get
  label_31: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1577]	CALL     	2 1 2
  label_32: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1577]	MOVE     	0 2
  label_33: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1577]	JMP      	0 -16	; to 19
  label_34: {
    Instruction i = 0x7ffbc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_19;
  }
  
  // [1579]	GETUPVAL 	2 2	; a_symbol_p
  label_35: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1579]	MOVE     	3 0
  label_36: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1579]	CALL     	2 2 2
  label_37: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1579]	TEST     	2 0
  label_38: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [1579]	JMP      	0 4	; to 44
  label_39: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_44;
  }
  
  // [1580]	MOVE     	2 1
  label_40: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1580]	MOVE     	3 0
  label_41: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1580]	CONCAT   	1 2 3
  label_42: {
    Instruction i = 0x0100c05d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1580]	JMP      	0 3	; to 47
  label_43: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_47;
  }
  
  // [1582]	GETUPVAL 	2 3	; put
  label_44: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1582]	MOVE     	3 0
  label_45: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1582]	CALL     	2 2 1
  label_46: {
    Instruction i = 0x010040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1584]	GETUPVAL 	2 4	; new_symbol
  label_47: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1584]	MOVE     	3 1
  label_48: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1584]	TAILCALL 	2 2 0
  label_49: {
    Instruction i = 0x010000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1584]	RETURN   	2 0
  label_50: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1585]	RETURN   	0 1
  label_51: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1586
// lastlinedefined = 1654
// what = Lua
static int zz_magic_function_130 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1587]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1587]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1587]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1587]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1588]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1588]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1590]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1590]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1591]	EQ       	1 0 -1	; - "("
  label_8: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1591]	JMP      	0 5	; to 15
  label_9: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1592]	GETUPVAL 	1 2	; put
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1592]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1592]	CALL     	1 2 1
  label_12: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1593]	LOADBOOL 	1 0 0
  label_13: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1593]	RETURN   	1 2
  label_14: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1595]	GETUPVAL 	1 3	; new_symbol
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1595]	LOADK    	2 -2	; "!!@@READ||HOLE@@!!"
  label_16: {
    Instruction i = 0x00004081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1595]	CALL     	1 2 2
  label_17: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1596]	MOVE     	2 1
  label_18: {
    Instruction i = 0x00800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1597]	LOADNIL  	3 0
  label_19: {
    Instruction i = 0x000000c4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1621]	CLOSURE  	4 0	; 0x559f637b44c0
  label_20: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1621]	MOVE     	3 4
  label_21: {
    Instruction i = 0x020000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1622]	LOADNIL  	4 0
  label_22: {
    Instruction i = 0x00000104;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1625]	CLOSURE  	5 1	; 0x559f637b4830
  label_23: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1625]	MOVE     	4 5
  label_24: {
    Instruction i = 0x02800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1627]	GETUPVAL 	5 8	; space
  label_25: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1627]	CALL     	5 1 1
  label_26: {
    Instruction i = 0x00804164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1628]	GETUPVAL 	5 0	; eof
  label_27: {
    Instruction i = 0x00000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1628]	CALL     	5 1 2
  label_28: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1628]	TEST     	5 0
  label_29: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_31;
    }
  }
  
  // [1628]	JMP      	0 3	; to 34
  label_30: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_34;
  }
  
  // [1629]	GETUPVAL 	5 9	; parse_error
  label_31: {
    Instruction i = 0x04800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1629]	TAILCALL 	5 1 0
  label_32: {
    Instruction i = 0x00800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1629]	RETURN   	5 0
  label_33: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1631]	GETUPVAL 	5 1	; get
  label_34: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1631]	CALL     	5 1 2
  label_35: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1631]	MOVE     	0 5
  label_36: {
    Instruction i = 0x02800000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1632]	EQ       	0 0 -3	; - ")"
  label_37: {
    Instruction i = 0x0040801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_39;
    }
  }
  
  // [1632]	JMP      	0 4	; to 43
  label_38: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [1633]	MOVE     	5 3
  label_39: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1633]	GETUPVAL 	6 10	; null_v
  label_40: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1633]	CALL     	5 2 1
  label_41: {
    Instruction i = 0x01004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1634]	RETURN   	2 2
  label_42: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1636]	EQ       	0 0 -4	; - "."
  label_43: {
    Instruction i = 0x0040c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_45;
    }
  }
  
  // [1636]	JMP      	0 25	; to 70
  label_44: {
    Instruction i = 0x8006001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_70;
  }
  
  // [1637]	GETUPVAL 	5 8	; space
  label_45: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1637]	CALL     	5 1 1
  label_46: {
    Instruction i = 0x00804164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1638]	GETUPVAL 	5 11	; val
  label_47: {
    Instruction i = 0x05800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1638]	CALL     	5 1 2
  label_48: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1639]	MOVE     	6 3
  label_49: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1639]	MOVE     	7 5
  label_50: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1639]	CALL     	6 2 1
  label_51: {
    Instruction i = 0x010041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1640]	GETUPVAL 	6 8	; space
  label_52: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1640]	CALL     	6 1 1
  label_53: {
    Instruction i = 0x008041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1641]	GETUPVAL 	6 0	; eof
  label_54: {
    Instruction i = 0x00000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1641]	CALL     	6 1 2
  label_55: {
    Instruction i = 0x008081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1641]	TEST     	6 0
  label_56: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_58;
    }
  }
  
  // [1641]	JMP      	0 3	; to 61
  label_57: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [1642]	GETUPVAL 	6 9	; parse_error
  label_58: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1642]	TAILCALL 	6 1 0
  label_59: {
    Instruction i = 0x008001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1642]	RETURN   	6 0
  label_60: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1644]	GETUPVAL 	6 1	; get
  label_61: {
    Instruction i = 0x00800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1644]	CALL     	6 1 2
  label_62: {
    Instruction i = 0x008081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1644]	MOVE     	0 6
  label_63: {
    Instruction i = 0x03000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1645]	EQ       	1 0 -3	; - ")"
  label_64: {
    Instruction i = 0x0040805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_66;
    }
  }
  
  // [1645]	JMP      	0 3	; to 69
  label_65: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_69;
  }
  
  // [1646]	GETUPVAL 	6 9	; parse_error
  label_66: {
    Instruction i = 0x04800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1646]	TAILCALL 	6 1 0
  label_67: {
    Instruction i = 0x008001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1646]	RETURN   	6 0
  label_68: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1648]	RETURN   	2 2
  label_69: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1650]	GETUPVAL 	5 2	; put
  label_70: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1650]	MOVE     	6 0
  label_71: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1650]	CALL     	5 2 1
  label_72: {
    Instruction i = 0x01004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1651]	GETUPVAL 	5 11	; val
  label_73: {
    Instruction i = 0x05800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1651]	CALL     	5 1 2
  label_74: {
    Instruction i = 0x00808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1652]	MOVE     	6 4
  label_75: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1652]	MOVE     	7 5
  label_76: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1652]	CALL     	6 2 1
  label_77: {
    Instruction i = 0x010041a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1652]	JMP      	0 -54	; to 25
  label_78: {
    Instruction i = 0x7ff2401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [1654]	RETURN   	0 1
  label_79: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1598
// lastlinedefined = 1621
// what = Lua
static int zz_magic_function_131 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1599]	GETUPVAL 	1 0	; ret
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1599]	GETUPVAL 	2 1	; HOLE
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1599]	EQ       	0 1 2
  label_2: {
    Instruction i = 0x0080801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1599]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1600]	SETUPVAL 	0 0	; ret
  label_4: {
    Instruction i = 0x00000009;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    UpVal *uv = cl->upvals[GETARG_B(i)];
    setobj(L, uv->v, ra);
    luaC_upvalbarrier(L, uv);
  }
  
  // [1601]	RETURN   	0 1
  label_5: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1603]	GETUPVAL 	1 0	; ret
  label_6: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1605]	GETUPVAL 	2 2	; construction_p
  label_7: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1605]	MOVE     	3 1
  label_8: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1605]	CALL     	2 2 2
  label_9: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1605]	TEST     	2 1
  label_10: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_12;
    }
  }
  
  // [1605]	JMP      	0 3	; to 15
  label_11: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1606]	GETUPVAL 	2 3	; ERROR
  label_12: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1606]	TAILCALL 	2 1 0
  label_13: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1606]	RETURN   	2 0
  label_14: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1608]	GETUPVAL 	2 4	; construction_tail
  label_15: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1608]	MOVE     	3 1
  label_16: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1608]	CALL     	2 2 2
  label_17: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1609]	GETUPVAL 	3 1	; HOLE
  label_18: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1609]	EQ       	1 2 3
  label_19: {
    Instruction i = 0x0100c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1609]	JMP      	0 5	; to 26
  label_20: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_26;
  }
  
  // [1612]	GETUPVAL 	3 4	; construction_tail
  label_21: {
    Instruction i = 0x020000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1612]	MOVE     	4 1
  label_22: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1612]	CALL     	3 2 2
  label_23: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1612]	MOVE     	1 3
  label_24: {
    Instruction i = 0x01800040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1612]	JMP      	0 -19	; to 7
  label_25: {
    Instruction i = 0x7ffb001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_7;
  }
  
  // [1614]	GETUPVAL 	2 2	; construction_p
  label_26: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1614]	MOVE     	3 1
  label_27: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1614]	CALL     	2 2 2
  label_28: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1614]	TEST     	2 1
  label_29: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_31;
    }
  }
  
  // [1614]	JMP      	0 3	; to 34
  label_30: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_34;
  }
  
  // [1615]	GETUPVAL 	2 3	; ERROR
  label_31: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1615]	TAILCALL 	2 1 0
  label_32: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1615]	RETURN   	2 0
  label_33: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1617]	GETUPVAL 	2 4	; construction_tail
  label_34: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1617]	MOVE     	3 1
  label_35: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1617]	CALL     	2 2 2
  label_36: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1617]	GETUPVAL 	3 1	; HOLE
  label_37: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1617]	EQ       	1 2 3
  label_38: {
    Instruction i = 0x0100c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [1617]	JMP      	0 3	; to 43
  label_39: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [1618]	GETUPVAL 	2 3	; ERROR
  label_40: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1618]	TAILCALL 	2 1 0
  label_41: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1618]	RETURN   	2 0
  label_42: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1620]	SETTABLE 	1 -1 0	; 3 -
  label_43: {
    Instruction i = 0x8000004a;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    settableProtected(L, ra, rb, rc);
  }
  
  // [1621]	RETURN   	0 1
  label_44: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1623
// lastlinedefined = 1625
// what = Lua
static int zz_magic_function_132 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1624]	GETUPVAL 	1 0	; set_last
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1624]	GETUPVAL 	2 1	; new_construction
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1624]	MOVE     	3 0
  label_2: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1624]	GETUPVAL 	4 2	; HOLE
  label_3: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1624]	CALL     	2 3 0
  label_4: {
    Instruction i = 0x018000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1624]	CALL     	1 0 1
  label_5: {
    Instruction i = 0x00004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1625]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1655
// lastlinedefined = 1672
// what = Lua
static int zz_magic_function_133 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1656]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1656]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1656]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1656]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1657]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1657]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1659]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1659]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1660]	EQ       	1 0 -1	; - "#"
  label_8: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1660]	JMP      	0 5	; to 15
  label_9: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1661]	GETUPVAL 	1 2	; put
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1661]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1661]	CALL     	1 2 1
  label_12: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1662]	LOADBOOL 	1 0 0
  label_13: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1662]	RETURN   	1 2
  label_14: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1664]	GETUPVAL 	1 3	; list
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1664]	CALL     	1 1 2
  label_16: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1665]	EQ       	0 1 -2	; - false
  label_17: {
    Instruction i = 0x00c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_19;
    }
  }
  
  // [1665]	JMP      	0 3	; to 22
  label_18: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1666]	GETUPVAL 	2 4	; parse_error
  label_19: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1666]	TAILCALL 	2 1 0
  label_20: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1666]	RETURN   	2 0
  label_21: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1668]	GETUPVAL 	2 5	; construction_p
  label_22: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1668]	MOVE     	3 1
  label_23: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1668]	CALL     	2 2 2
  label_24: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1668]	TEST     	2 1
  label_25: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1668]	JMP      	0 3	; to 30
  label_26: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_30;
  }
  
  // [1669]	GETUPVAL 	2 4	; parse_error
  label_27: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1669]	TAILCALL 	2 1 0
  label_28: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1669]	RETURN   	2 0
  label_29: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1671]	GETUPVAL 	2 6	; new_data
  label_30: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1671]	GETUPVAL 	3 7	; construction_head
  label_31: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1671]	MOVE     	4 1
  label_32: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1671]	CALL     	3 2 2
  label_33: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1671]	GETUPVAL 	4 8	; construction_tail
  label_34: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1671]	MOVE     	5 1
  label_35: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1671]	CALL     	4 2 0
  label_36: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1671]	TAILCALL 	2 0 0
  label_37: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1671]	RETURN   	2 0
  label_38: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1672]	RETURN   	0 1
  label_39: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1673
// lastlinedefined = 1690
// what = Lua
static int zz_magic_function_134 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1674]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1674]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1674]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1674]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1675]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1675]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1677]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1677]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1678]	EQ       	1 0 -1	; - "!"
  label_8: {
    Instruction i = 0x0040005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1678]	JMP      	0 5	; to 15
  label_9: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_15;
  }
  
  // [1679]	GETUPVAL 	1 2	; put
  label_10: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1679]	MOVE     	2 0
  label_11: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1679]	CALL     	1 2 1
  label_12: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1680]	LOADBOOL 	1 0 0
  label_13: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_15;
    }
  }
  
  // [1680]	RETURN   	1 2
  label_14: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1682]	GETUPVAL 	1 3	; list
  label_15: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1682]	CALL     	1 1 2
  label_16: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1683]	EQ       	0 1 -2	; - false
  label_17: {
    Instruction i = 0x00c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_19;
    }
  }
  
  // [1683]	JMP      	0 3	; to 22
  label_18: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1684]	GETUPVAL 	2 4	; parse_error
  label_19: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1684]	TAILCALL 	2 1 0
  label_20: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1684]	RETURN   	2 0
  label_21: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1686]	GETUPVAL 	2 5	; construction_p
  label_22: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1686]	MOVE     	3 1
  label_23: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1686]	CALL     	2 2 2
  label_24: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1686]	TEST     	2 1
  label_25: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_27;
    }
  }
  
  // [1686]	JMP      	0 3	; to 30
  label_26: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_30;
  }
  
  // [1687]	GETUPVAL 	2 4	; parse_error
  label_27: {
    Instruction i = 0x02000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1687]	TAILCALL 	2 1 0
  label_28: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1687]	RETURN   	2 0
  label_29: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1689]	GETUPVAL 	2 6	; new_error
  label_30: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1689]	GETUPVAL 	3 7	; construction_head
  label_31: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1689]	MOVE     	4 1
  label_32: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1689]	CALL     	3 2 2
  label_33: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1689]	GETUPVAL 	4 8	; construction_tail
  label_34: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1689]	MOVE     	5 1
  label_35: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1689]	CALL     	4 2 0
  label_36: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1689]	TAILCALL 	2 0 0
  label_37: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1689]	RETURN   	2 0
  label_38: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1690]	RETURN   	0 1
  label_39: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1691
// lastlinedefined = 1706
// what = Lua
static int zz_magic_function_135 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1692]	GETUPVAL 	1 0	; a_space_p
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1692]	MOVE     	2 0
  label_1: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1692]	CALL     	1 2 2
  label_2: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1692]	TEST     	1 0
  label_3: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1692]	JMP      	0 2	; to 7
  label_4: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_7;
  }
  
  // [1693]	LOADBOOL 	1 0 0
  label_5: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_7;
    }
  }
  
  // [1693]	RETURN   	1 2
  label_6: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1695]	NEWTABLE 	1 18 0
  label_7: {
    Instruction i = 0x0900004b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1695]	LOADK    	2 -1	; "("
  label_8: {
    Instruction i = 0x00000081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	3 -2	; ")"
  label_9: {
    Instruction i = 0x000040c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	4 -3	; "!"
  label_10: {
    Instruction i = 0x00008101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	5 -4	; "#"
  label_11: {
    Instruction i = 0x0000c141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	6 -5	; "."
  label_12: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	7 -6	; "$"
  label_13: {
    Instruction i = 0x000141c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	8 -7	; "%"
  label_14: {
    Instruction i = 0x00018201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	9 -8	; "^"
  label_15: {
    Instruction i = 0x0001c241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	10 -9	; "@"
  label_16: {
    Instruction i = 0x00020281;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	11 -10	; "~"
  label_17: {
    Instruction i = 0x000242c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	12 -11	; "/"
  label_18: {
    Instruction i = 0x00028301;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	13 -12	; "-"
  label_19: {
    Instruction i = 0x0002c341;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	14 -13	; ">"
  label_20: {
    Instruction i = 0x00030381;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	15 -14	; "_"
  label_21: {
    Instruction i = 0x000343c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	16 -15	; ":"
  label_22: {
    Instruction i = 0x00038401;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	17 -16	; "?"
  label_23: {
    Instruction i = 0x0003c441;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	18 -17	; "["
  label_24: {
    Instruction i = 0x00040481;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	19 -18	; "]"
  label_25: {
    Instruction i = 0x000444c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	LOADK    	20 -19	; "&"
  label_26: {
    Instruction i = 0x00048501;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1695]	SETLIST  	1 19 1	; 1
  label_27: {
    Instruction i = 0x0980406b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1697]	LOADK    	2 -20	; 0
  label_28: {
    Instruction i = 0x0004c081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1698]	LEN      	3 1
  label_29: {
    Instruction i = 0x008000dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1698]	LT       	0 2 3
  label_30: {
    Instruction i = 0x0100c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_32;
    }
  }
  
  // [1698]	JMP      	0 8	; to 40
  label_31: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_40;
  }
  
  // [1699]	ADD      	3 2 -21	; - 1
  label_32: {
    Instruction i = 0x014500cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1699]	GETTABLE 	3 1 3
  label_33: {
    Instruction i = 0x0080c0c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1699]	EQ       	0 0 3
  label_34: {
    Instruction i = 0x0000c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_36;
    }
  }
  
  // [1699]	JMP      	0 2	; to 38
  label_35: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_38;
  }
  
  // [1700]	LOADBOOL 	3 0 0
  label_36: {
    Instruction i = 0x000000c3;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_38;
    }
  }
  
  // [1700]	RETURN   	3 2
  label_37: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1702]	ADD      	2 2 -21	; - 1
  label_38: {
    Instruction i = 0x0145008d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1702]	JMP      	0 -11	; to 29
  label_39: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_29;
  }
  
  // [1705]	LOADBOOL 	2 1 0
  label_40: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_42;
    }
  }
  
  // [1705]	RETURN   	2 2
  label_41: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1706]	RETURN   	0 1
  label_42: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1707
// lastlinedefined = 1721
// what = Lua
static int zz_magic_function_136 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1708]	GETUPVAL 	0 0	; space
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1708]	CALL     	0 1 1
  label_1: {
    Instruction i = 0x00804024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1709]	NEWTABLE 	0 8 0
  label_2: {
    Instruction i = 0x0400000b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1709]	GETUPVAL 	1 1	; list
  label_3: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	2 2	; readsysname
  label_4: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	3 3	; data
  label_5: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	4 4	; readerror
  label_6: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	5 5	; readeval
  label_7: {
    Instruction i = 0x02800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	6 6	; readfuncapply
  label_8: {
    Instruction i = 0x03000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	7 7	; readformbuiltin
  label_9: {
    Instruction i = 0x038001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	GETUPVAL 	8 8	; readapply
  label_10: {
    Instruction i = 0x04000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1709]	SETLIST  	0 8 1	; 1
  label_11: {
    Instruction i = 0x0400402b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1711]	LOADK    	1 -1	; 0
  label_12: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1712]	LEN      	2 0
  label_13: {
    Instruction i = 0x0000009c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1712]	LT       	0 1 2
  label_14: {
    Instruction i = 0x00808020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_16;
    }
  }
  
  // [1712]	JMP      	0 8	; to 24
  label_15: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_24;
  }
  
  // [1713]	ADD      	2 1 -2	; - 1
  label_16: {
    Instruction i = 0x00c0408d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1713]	GETTABLE 	2 0 2
  label_17: {
    Instruction i = 0x00008087;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1713]	CALL     	2 1 2
  label_18: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1714]	EQ       	1 2 -3	; - false
  label_19: {
    Instruction i = 0x0140805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1714]	JMP      	0 1	; to 22
  label_20: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_22;
  }
  
  // [1715]	RETURN   	2 2
  label_21: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1717]	ADD      	1 1 -2	; - 1
  label_22: {
    Instruction i = 0x00c0404d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1717]	JMP      	0 -11	; to 13
  label_23: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [1720]	GETUPVAL 	1 9	; parse_error
  label_24: {
    Instruction i = 0x04800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1720]	TAILCALL 	1 1 0
  label_25: {
    Instruction i = 0x00800065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1720]	RETURN   	1 0
  label_26: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1721]	RETURN   	0 1
  label_27: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1722
// lastlinedefined = 1727
// what = Lua
static int zz_magic_function_137 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1723]	EQ       	0 0 -1	; - false
  label_0: {
    Instruction i = 0x0040001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [1723]	JMP      	0 3	; to 5
  label_1: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [1724]	GETUPVAL 	1 0	; parse_error
  label_2: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1724]	TAILCALL 	1 1 0
  label_3: {
    Instruction i = 0x00800065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1724]	RETURN   	1 0
  label_4: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1726]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1727]	RETURN   	0 1
  label_6: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1728
// lastlinedefined = 1730
// what = Lua
static int zz_magic_function_138 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1729]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1729]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1729]	NOT      	0 0
  label_2: {
    Instruction i = 0x0000001b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RB(i);
    int res = l_isfalse(rb);  /* next assignment may change this value */
    setbvalue(ra, res);
  }
  
  // [1729]	RETURN   	0 2
  label_3: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1730]	RETURN   	0 1
  label_4: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1731
// lastlinedefined = 1734
// what = Lua
static int zz_magic_function_139 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1732]	GETUPVAL 	1 0	; un_maybe
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1732]	GETUPVAL 	2 1	; not_eof
  label_1: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1732]	CALL     	2 1 0
  label_2: {
    Instruction i = 0x008000a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1732]	CALL     	1 0 1
  label_3: {
    Instruction i = 0x00004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1733]	GETUPVAL 	1 0	; un_maybe
  label_4: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1733]	GETUPVAL 	2 2	; get
  label_5: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1733]	CALL     	2 1 2
  label_6: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1733]	EQ       	1 2 0
  label_7: {
    Instruction i = 0x0100005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1733]	JMP      	0 1	; to 10
  label_8: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_10;
  }
  
  // [1733]	LOADBOOL 	2 0 1
  label_9: {
    Instruction i = 0x00004083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_11;
    }
  }
  
  // [1733]	LOADBOOL 	2 1 0
  label_10: {
    Instruction i = 0x00800083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_12;
    }
  }
  
  // [1733]	CALL     	1 2 1
  label_11: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1734]	RETURN   	0 1
  label_12: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1735
// lastlinedefined = 1850
// what = Lua
static int zz_magic_function_140 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1736]	LOADNIL  	0 1
  label_0: {
    Instruction i = 0x00800004;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1760]	CLOSURE  	2 0	; 0x559f637b61c0
  label_1: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1760]	MOVE     	0 2
  label_2: {
    Instruction i = 0x01000000;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1799]	CLOSURE  	2 1	; 0x559f637b6b30
  label_3: {
    Instruction i = 0x000040ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1799]	MOVE     	1 2
  label_4: {
    Instruction i = 0x01000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1800]	GETUPVAL 	2 11	; eof
  label_5: {
    Instruction i = 0x05800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1800]	CALL     	2 1 2
  label_6: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1800]	TEST     	2 0
  label_7: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1800]	JMP      	0 2	; to 11
  label_8: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_11;
  }
  
  // [1801]	LOADBOOL 	2 0 0
  label_9: {
    Instruction i = 0x00000083;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_11;
    }
  }
  
  // [1801]	RETURN   	2 2
  label_10: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1803]	GETUPVAL 	2 12	; get
  label_11: {
    Instruction i = 0x06000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1803]	CALL     	2 1 2
  label_12: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1804]	EQ       	0 2 -1	; - "&"
  label_13: {
    Instruction i = 0x0140001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_15;
    }
  }
  
  // [1804]	JMP      	0 30	; to 45
  label_14: {
    Instruction i = 0x8007401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_45;
  }
  
  // [1805]	GETUPVAL 	3 24	; un_maybe
  label_15: {
    Instruction i = 0x0c0000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1805]	GETUPVAL 	4 25	; not_eof
  label_16: {
    Instruction i = 0x0c800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1805]	CALL     	4 1 0
  label_17: {
    Instruction i = 0x00800124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1805]	CALL     	3 0 1
  label_18: {
    Instruction i = 0x000040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1806]	GETUPVAL 	3 12	; get
  label_19: {
    Instruction i = 0x060000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1806]	CALL     	3 1 2
  label_20: {
    Instruction i = 0x008080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1807]	EQ       	0 3 -2	; - "+"
  label_21: {
    Instruction i = 0x01c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_23;
    }
  }
  
  // [1807]	JMP      	0 11	; to 34
  label_22: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_34;
  }
  
  // [1808]	MOVE     	4 0
  label_23: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1808]	CALL     	4 1 2
  label_24: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1809]	GETUPVAL 	5 13	; new_list
  label_25: {
    Instruction i = 0x06800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1809]	GETUPVAL 	6 26	; form_symbol
  label_26: {
    Instruction i = 0x0d000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1809]	GETUPVAL 	7 13	; new_list
  label_27: {
    Instruction i = 0x068001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1809]	GETUPVAL 	8 27	; system_symbol
  label_28: {
    Instruction i = 0x0d800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1809]	MOVE     	9 4
  label_29: {
    Instruction i = 0x02000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1809]	CALL     	7 3 0
  label_30: {
    Instruction i = 0x018001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1809]	TAILCALL 	5 0 0
  label_31: {
    Instruction i = 0x00000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1809]	RETURN   	5 0
  label_32: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1809]	JMP      	0 3	; to 37
  label_33: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_37;
  }
  
  // [1811]	GETUPVAL 	4 20	; put
  label_34: {
    Instruction i = 0x0a000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1811]	MOVE     	5 3
  label_35: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1811]	CALL     	4 2 1
  label_36: {
    Instruction i = 0x01004124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1813]	MOVE     	4 0
  label_37: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1813]	CALL     	4 1 2
  label_38: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1814]	GETUPVAL 	5 13	; new_list
  label_39: {
    Instruction i = 0x06800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1814]	GETUPVAL 	6 26	; form_symbol
  label_40: {
    Instruction i = 0x0d000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1814]	MOVE     	7 4
  label_41: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1814]	TAILCALL 	5 3 0
  label_42: {
    Instruction i = 0x01800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1814]	RETURN   	5 0
  label_43: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1814]	JMP      	0 105	; to 150
  label_44: {
    Instruction i = 0x801a001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_150;
  }
  
  // [1815]	EQ       	0 2 -3	; - ":"
  label_45: {
    Instruction i = 0x0140801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_47;
    }
  }
  
  // [1815]	JMP      	0 54	; to 101
  label_46: {
    Instruction i = 0x800d401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_101;
  }
  
  // [1816]	GETUPVAL 	3 24	; un_maybe
  label_47: {
    Instruction i = 0x0c0000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1816]	GETUPVAL 	4 25	; not_eof
  label_48: {
    Instruction i = 0x0c800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1816]	CALL     	4 1 0
  label_49: {
    Instruction i = 0x00800124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1816]	CALL     	3 0 1
  label_50: {
    Instruction i = 0x000040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1817]	GETUPVAL 	3 12	; get
  label_51: {
    Instruction i = 0x060000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1817]	CALL     	3 1 2
  label_52: {
    Instruction i = 0x008080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1818]	EQ       	0 3 -1	; - "&"
  label_53: {
    Instruction i = 0x01c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_55;
    }
  }
  
  // [1818]	JMP      	0 19	; to 74
  label_54: {
    Instruction i = 0x8004801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_74;
  }
  
  // [1819]	GETUPVAL 	4 0	; assert_get
  label_55: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1819]	LOADK    	5 -4	; ">"
  label_56: {
    Instruction i = 0x0000c141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1819]	CALL     	4 2 1
  label_57: {
    Instruction i = 0x01004124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1820]	MOVE     	4 0
  label_58: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1820]	CALL     	4 1 2
  label_59: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1821]	GETUPVAL 	5 13	; new_list
  label_60: {
    Instruction i = 0x06800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	GETUPVAL 	6 14	; typeAnnotation_symbol
  label_61: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	GETUPVAL 	7 13	; new_list
  label_62: {
    Instruction i = 0x068001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	GETUPVAL 	8 26	; form_symbol
  label_63: {
    Instruction i = 0x0d000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	GETUPVAL 	9 13	; new_list
  label_64: {
    Instruction i = 0x06800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	GETUPVAL 	10 15	; function_symbol
  label_65: {
    Instruction i = 0x07800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	GETUPVAL 	11 16	; something_symbol
  label_66: {
    Instruction i = 0x080002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	MOVE     	12 4
  label_67: {
    Instruction i = 0x02000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1821]	CALL     	9 4 0
  label_68: {
    Instruction i = 0x02000264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1821]	CALL     	7 0 2
  label_69: {
    Instruction i = 0x000081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1821]	GETUPVAL 	8 28	; theThing_symbol
  label_70: {
    Instruction i = 0x0e000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1821]	TAILCALL 	5 4 0
  label_71: {
    Instruction i = 0x02000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1821]	RETURN   	5 0
  label_72: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1821]	JMP      	0 18	; to 92
  label_73: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_92;
  }
  
  // [1822]	EQ       	0 3 -4	; - ">"
  label_74: {
    Instruction i = 0x01c0c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_76;
    }
  }
  
  // [1822]	JMP      	0 13	; to 89
  label_75: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_89;
  }
  
  // [1823]	MOVE     	4 0
  label_76: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1823]	CALL     	4 1 2
  label_77: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1824]	GETUPVAL 	5 13	; new_list
  label_78: {
    Instruction i = 0x06800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1824]	GETUPVAL 	6 14	; typeAnnotation_symbol
  label_79: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1824]	GETUPVAL 	7 13	; new_list
  label_80: {
    Instruction i = 0x068001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1824]	GETUPVAL 	8 15	; function_symbol
  label_81: {
    Instruction i = 0x07800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1824]	GETUPVAL 	9 16	; something_symbol
  label_82: {
    Instruction i = 0x08000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1824]	MOVE     	10 4
  label_83: {
    Instruction i = 0x02000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1824]	CALL     	7 4 2
  label_84: {
    Instruction i = 0x020081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1824]	GETUPVAL 	8 28	; theThing_symbol
  label_85: {
    Instruction i = 0x0e000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1824]	TAILCALL 	5 4 0
  label_86: {
    Instruction i = 0x02000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1824]	RETURN   	5 0
  label_87: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1824]	JMP      	0 3	; to 92
  label_88: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_92;
  }
  
  // [1826]	GETUPVAL 	4 20	; put
  label_89: {
    Instruction i = 0x0a000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1826]	MOVE     	5 3
  label_90: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1826]	CALL     	4 2 1
  label_91: {
    Instruction i = 0x01004124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1828]	MOVE     	4 0
  label_92: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1828]	CALL     	4 1 2
  label_93: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1829]	GETUPVAL 	5 13	; new_list
  label_94: {
    Instruction i = 0x06800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1829]	GETUPVAL 	6 14	; typeAnnotation_symbol
  label_95: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1829]	MOVE     	7 4
  label_96: {
    Instruction i = 0x020001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1829]	GETUPVAL 	8 28	; theThing_symbol
  label_97: {
    Instruction i = 0x0e000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1829]	TAILCALL 	5 4 0
  label_98: {
    Instruction i = 0x02000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1829]	RETURN   	5 0
  label_99: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1829]	JMP      	0 49	; to 150
  label_100: {
    Instruction i = 0x800c001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_150;
  }
  
  // [1830]	EQ       	0 2 -2	; - "+"
  label_101: {
    Instruction i = 0x0140401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_103;
    }
  }
  
  // [1830]	JMP      	0 8	; to 111
  label_102: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_111;
  }
  
  // [1831]	MOVE     	3 0
  label_103: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1831]	CALL     	3 1 2
  label_104: {
    Instruction i = 0x008080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1832]	GETUPVAL 	4 13	; new_list
  label_105: {
    Instruction i = 0x06800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1832]	GETUPVAL 	5 27	; system_symbol
  label_106: {
    Instruction i = 0x0d800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1832]	MOVE     	6 3
  label_107: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1832]	TAILCALL 	4 3 0
  label_108: {
    Instruction i = 0x01800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1832]	RETURN   	4 0
  label_109: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1832]	JMP      	0 39	; to 150
  label_110: {
    Instruction i = 0x8009801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_150;
  }
  
  // [1833]	EQ       	0 2 -5	; - "["
  label_111: {
    Instruction i = 0x0141001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_113;
    }
  }
  
  // [1833]	JMP      	0 10	; to 123
  label_112: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_123;
  }
  
  // [1834]	MOVE     	3 0
  label_113: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1834]	CALL     	3 1 2
  label_114: {
    Instruction i = 0x008080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1835]	GETUPVAL 	4 0	; assert_get
  label_115: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1835]	LOADK    	5 -6	; "]"
  label_116: {
    Instruction i = 0x00014141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1835]	CALL     	4 2 1
  label_117: {
    Instruction i = 0x01004124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1836]	MOVE     	4 1
  label_118: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1836]	MOVE     	5 3
  label_119: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1836]	TAILCALL 	4 2 0
  label_120: {
    Instruction i = 0x01000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1836]	RETURN   	4 0
  label_121: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1836]	JMP      	0 27	; to 150
  label_122: {
    Instruction i = 0x8006801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_150;
  }
  
  // [1837]	EQ       	0 2 -7	; - "_"
  label_123: {
    Instruction i = 0x0141801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_125;
    }
  }
  
  // [1837]	JMP      	0 12	; to 137
  label_124: {
    Instruction i = 0x8002c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_137;
  }
  
  // [1838]	GETUPVAL 	3 0	; assert_get
  label_125: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1838]	LOADK    	4 -3	; ":"
  label_126: {
    Instruction i = 0x00008101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1838]	CALL     	3 2 1
  label_127: {
    Instruction i = 0x010040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1839]	MOVE     	3 0
  label_128: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1839]	CALL     	3 1 2
  label_129: {
    Instruction i = 0x008080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1840]	GETUPVAL 	4 13	; new_list
  label_130: {
    Instruction i = 0x06800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1840]	GETUPVAL 	5 14	; typeAnnotation_symbol
  label_131: {
    Instruction i = 0x07000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1840]	MOVE     	6 3
  label_132: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1840]	GETUPVAL 	7 16	; something_symbol
  label_133: {
    Instruction i = 0x080001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1840]	TAILCALL 	4 4 0
  label_134: {
    Instruction i = 0x02000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1840]	RETURN   	4 0
  label_135: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1840]	JMP      	0 13	; to 150
  label_136: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_150;
  }
  
  // [1842]	GETUPVAL 	3 20	; put
  label_137: {
    Instruction i = 0x0a0000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1842]	MOVE     	4 2
  label_138: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1842]	CALL     	3 2 1
  label_139: {
    Instruction i = 0x010040e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1843]	GETUPVAL 	3 2	; symbol
  label_140: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1843]	CALL     	3 1 2
  label_141: {
    Instruction i = 0x008080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1844]	EQ       	0 3 -8	; - false
  label_142: {
    Instruction i = 0x01c1c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_144;
    }
  }
  
  // [1844]	JMP      	0 2	; to 146
  label_143: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_146;
  }
  
  // [1845]	LOADBOOL 	4 0 0
  label_144: {
    Instruction i = 0x00000103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_146;
    }
  }
  
  // [1845]	RETURN   	4 2
  label_145: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1847]	MOVE     	4 1
  label_146: {
    Instruction i = 0x00800100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1847]	MOVE     	5 3
  label_147: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1847]	TAILCALL 	4 2 0
  label_148: {
    Instruction i = 0x01000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1847]	RETURN   	4 0
  label_149: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1849]	GETUPVAL 	3 23	; ERROR
  label_150: {
    Instruction i = 0x0b8000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1849]	TAILCALL 	3 1 0
  label_151: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1849]	RETURN   	3 0
  label_152: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1850]	RETURN   	0 1
  label_153: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1737
// lastlinedefined = 1760
// what = Lua
static int zz_magic_function_141 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1738]	EQ       	0 0 -1	; - nil
  label_0: {
    Instruction i = 0x0040001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_2;
    }
  }
  
  // [1738]	JMP      	0 1	; to 3
  label_1: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_3;
  }
  
  // [1739]	LOADBOOL 	0 0 0
  label_2: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_4;
    }
  }
  
  // [1741]	LOADNIL  	1 0
  label_3: {
    Instruction i = 0x00000044;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1747]	CLOSURE  	2 0	; 0x559f637b62f0
  label_4: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1747]	MOVE     	1 2
  label_5: {
    Instruction i = 0x01000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1748]	TEST     	0 0
  label_6: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_8;
    }
  }
  
  // [1748]	JMP      	0 13	; to 21
  label_7: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_21;
  }
  
  // [1748]	NEWTABLE 	2 9 0
  label_8: {
    Instruction i = 0x0480008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1748]	GETUPVAL 	3 2	; list
  label_9: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	4 3	; symbol
  label_10: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	MOVE     	5 1
  label_11: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1748]	GETUPVAL 	6 4	; data
  label_12: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	7 5	; readerror
  label_13: {
    Instruction i = 0x028001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	8 6	; readeval
  label_14: {
    Instruction i = 0x03000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	9 7	; readfuncapply
  label_15: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	10 8	; readformbuiltin
  label_16: {
    Instruction i = 0x04000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	11 9	; readapply
  label_17: {
    Instruction i = 0x048002c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	SETLIST  	2 9 1	; 1
  label_18: {
    Instruction i = 0x048040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1748]	TEST     	2 1
  label_19: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_21;
    }
  }
  
  // [1748]	JMP      	0 10	; to 31
  label_20: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1748]	NEWTABLE 	2 8 0
  label_21: {
    Instruction i = 0x0400008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1748]	GETUPVAL 	3 2	; list
  label_22: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	4 10	; readsysname_no_pack
  label_23: {
    Instruction i = 0x05000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	5 4	; data
  label_24: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	6 5	; readerror
  label_25: {
    Instruction i = 0x02800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	7 6	; readeval
  label_26: {
    Instruction i = 0x030001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	8 7	; readfuncapply
  label_27: {
    Instruction i = 0x03800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	9 8	; readformbuiltin
  label_28: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	GETUPVAL 	10 9	; readapply
  label_29: {
    Instruction i = 0x04800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1748]	SETLIST  	2 8 1	; 1
  label_30: {
    Instruction i = 0x040040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1750]	LOADK    	3 -2	; 0
  label_31: {
    Instruction i = 0x000040c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1751]	LEN      	4 2
  label_32: {
    Instruction i = 0x0100011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1751]	LT       	0 3 4
  label_33: {
    Instruction i = 0x01810020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_35;
    }
  }
  
  // [1751]	JMP      	0 8	; to 43
  label_34: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [1752]	ADD      	4 3 -3	; - 1
  label_35: {
    Instruction i = 0x01c0810d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1752]	GETTABLE 	4 2 4
  label_36: {
    Instruction i = 0x01010107;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1752]	CALL     	4 1 2
  label_37: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1753]	EQ       	1 4 -4	; - false
  label_38: {
    Instruction i = 0x0240c05f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [1753]	JMP      	0 1	; to 41
  label_39: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_41;
  }
  
  // [1754]	RETURN   	4 2
  label_40: {
    Instruction i = 0x01000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1756]	ADD      	3 3 -3	; - 1
  label_41: {
    Instruction i = 0x01c080cd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [1756]	JMP      	0 -11	; to 32
  label_42: {
    Instruction i = 0x7ffd001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_32;
  }
  
  // [1759]	GETUPVAL 	3 11	; parse_error
  label_43: {
    Instruction i = 0x058000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1759]	TAILCALL 	3 1 0
  label_44: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1759]	RETURN   	3 0
  label_45: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1760]	RETURN   	0 1
  label_46: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1742
// lastlinedefined = 1747
// what = Lua
static int zz_magic_function_142 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1743]	GETUPVAL 	0 0	; assert_get
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1743]	LOADK    	1 -1	; "["
  label_1: {
    Instruction i = 0x00000041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1743]	CALL     	0 2 1
  label_2: {
    Instruction i = 0x01004024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1744]	GETUPVAL 	0 1	; readsysname_no_pack_inner_must
  label_3: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1744]	CALL     	0 1 2
  label_4: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1745]	GETUPVAL 	1 0	; assert_get
  label_5: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1745]	LOADK    	2 -2	; "]"
  label_6: {
    Instruction i = 0x00004081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1745]	CALL     	1 2 1
  label_7: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1746]	RETURN   	0 2
  label_8: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1747]	RETURN   	0 1
  label_9: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1761
// lastlinedefined = 1799
// what = Lua
static int zz_magic_function_143 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1762]	GETUPVAL 	1 0	; eof
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1762]	CALL     	1 1 2
  label_1: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1762]	TEST     	1 0
  label_2: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1762]	JMP      	0 1	; to 5
  label_3: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_5;
  }
  
  // [1763]	RETURN   	0 2
  label_4: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1765]	GETUPVAL 	1 1	; get
  label_5: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1765]	CALL     	1 1 2
  label_6: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1766]	EQ       	0 1 -1	; - "."
  label_7: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_9;
    }
  }
  
  // [1766]	JMP      	0 15	; to 24
  label_8: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_24;
  }
  
  // [1767]	GETUPVAL 	2 2	; readsysname_no_pack_inner_must
  label_9: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1767]	CALL     	2 1 2
  label_10: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1768]	GETUPVAL 	3 3	; new_list
  label_11: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1768]	GETUPVAL 	4 4	; typeAnnotation_symbol
  label_12: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1768]	GETUPVAL 	5 3	; new_list
  label_13: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1768]	GETUPVAL 	6 5	; function_symbol
  label_14: {
    Instruction i = 0x02800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1768]	GETUPVAL 	7 3	; new_list
  label_15: {
    Instruction i = 0x018001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1768]	MOVE     	8 0
  label_16: {
    Instruction i = 0x00000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1768]	CALL     	7 2 2
  label_17: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1768]	GETUPVAL 	8 6	; something_symbol
  label_18: {
    Instruction i = 0x03000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1768]	CALL     	5 4 2
  label_19: {
    Instruction i = 0x02008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1768]	MOVE     	6 2
  label_20: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1768]	TAILCALL 	3 4 0
  label_21: {
    Instruction i = 0x020000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1768]	RETURN   	3 0
  label_22: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1768]	JMP      	0 86	; to 110
  label_23: {
    Instruction i = 0x8015401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_110;
  }
  
  // [1769]	EQ       	0 1 -2	; - ":"
  label_24: {
    Instruction i = 0x00c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_26;
    }
  }
  
  // [1769]	JMP      	0 9	; to 35
  label_25: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_35;
  }
  
  // [1770]	GETUPVAL 	2 2	; readsysname_no_pack_inner_must
  label_26: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1770]	CALL     	2 1 2
  label_27: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1771]	GETUPVAL 	3 3	; new_list
  label_28: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1771]	GETUPVAL 	4 4	; typeAnnotation_symbol
  label_29: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1771]	MOVE     	5 2
  label_30: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1771]	MOVE     	6 0
  label_31: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1771]	TAILCALL 	3 4 0
  label_32: {
    Instruction i = 0x020000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1771]	RETURN   	3 0
  label_33: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1771]	JMP      	0 75	; to 110
  label_34: {
    Instruction i = 0x8012801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_110;
  }
  
  // [1772]	EQ       	0 1 -3	; - "~"
  label_35: {
    Instruction i = 0x00c0801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_37;
    }
  }
  
  // [1772]	JMP      	0 6	; to 43
  label_36: {
    Instruction i = 0x8001401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_43;
  }
  
  // [1773]	GETUPVAL 	2 3	; new_list
  label_37: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1773]	GETUPVAL 	3 7	; isOrNot_symbol
  label_38: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1773]	MOVE     	4 0
  label_39: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1773]	TAILCALL 	2 3 0
  label_40: {
    Instruction i = 0x018000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1773]	RETURN   	2 0
  label_41: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1773]	JMP      	0 67	; to 110
  label_42: {
    Instruction i = 0x8010801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_110;
  }
  
  // [1774]	EQ       	0 1 -4	; - "@"
  label_43: {
    Instruction i = 0x00c0c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_45;
    }
  }
  
  // [1774]	JMP      	0 16	; to 61
  label_44: {
    Instruction i = 0x8003c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [1775]	GETUPVAL 	2 2	; readsysname_no_pack_inner_must
  label_45: {
    Instruction i = 0x01000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1775]	CALL     	2 1 2
  label_46: {
    Instruction i = 0x008080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1776]	GETUPVAL 	3 3	; new_list
  label_47: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	GETUPVAL 	4 4	; typeAnnotation_symbol
  label_48: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	GETUPVAL 	5 3	; new_list
  label_49: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	GETUPVAL 	6 5	; function_symbol
  label_50: {
    Instruction i = 0x02800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	GETUPVAL 	7 8	; new_construction
  label_51: {
    Instruction i = 0x040001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	MOVE     	8 0
  label_52: {
    Instruction i = 0x00000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1776]	GETUPVAL 	9 6	; something_symbol
  label_53: {
    Instruction i = 0x03000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	CALL     	7 3 2
  label_54: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1776]	GETUPVAL 	8 6	; something_symbol
  label_55: {
    Instruction i = 0x03000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1776]	CALL     	5 4 2
  label_56: {
    Instruction i = 0x02008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1776]	MOVE     	6 2
  label_57: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1776]	TAILCALL 	3 4 0
  label_58: {
    Instruction i = 0x020000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1776]	RETURN   	3 0
  label_59: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1776]	JMP      	0 49	; to 110
  label_60: {
    Instruction i = 0x800c001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_110;
  }
  
  // [1777]	EQ       	0 1 -5	; - "?"
  label_61: {
    Instruction i = 0x00c1001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_63;
    }
  }
  
  // [1777]	JMP      	0 10	; to 73
  label_62: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_73;
  }
  
  // [1778]	GETUPVAL 	2 3	; new_list
  label_63: {
    Instruction i = 0x01800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1778]	GETUPVAL 	3 4	; typeAnnotation_symbol
  label_64: {
    Instruction i = 0x020000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1778]	GETUPVAL 	4 5	; function_symbol
  label_65: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1778]	GETUPVAL 	5 3	; new_list
  label_66: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1778]	GETUPVAL 	6 7	; isOrNot_symbol
  label_67: {
    Instruction i = 0x03800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1778]	MOVE     	7 0
  label_68: {
    Instruction i = 0x000001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1778]	CALL     	5 3 0
  label_69: {
    Instruction i = 0x01800164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1778]	TAILCALL 	2 0 0
  label_70: {
    Instruction i = 0x000000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1778]	RETURN   	2 0
  label_71: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1778]	JMP      	0 37	; to 110
  label_72: {
    Instruction i = 0x8009001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_110;
  }
  
  // [1779]	EQ       	0 1 -6	; - "/"
  label_73: {
    Instruction i = 0x00c1401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_75;
    }
  }
  
  // [1779]	JMP      	0 31	; to 106
  label_74: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_106;
  }
  
  // [1780]	NEWTABLE 	2 1 0
  label_75: {
    Instruction i = 0x0080008b;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    Table *t = luaH_new(L);
    sethvalue(L, ra, t);
    if (b != 0 || c != 0)
      luaH_resize(L, t, luaO_fb2int(b), luaO_fb2int(c));
    checkGC(L, ra + 1);
  }
  
  // [1780]	MOVE     	3 0
  label_76: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1780]	SETLIST  	2 1 1	; 1
  label_77: {
    Instruction i = 0x008040ab;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int n = GETARG_B(i);
    int c = GETARG_C(i);
    unsigned int last;
    Table *h;
    if (n == 0) n = cast_int(L->top - ra) - 1;
    if (c == 0) {
      lua_assert(GET_OPCODE(*ci->u.l.savedpc) == OP_EXTRAARG);
      c = GETARG_Ax(*ci->u.l.savedpc++);
    }
    h = hvalue(ra);
    last = ((c-1)*LFIELDS_PER_FLUSH) + n;
    if (last > h->sizearray)  /* needs more space? */
      luaH_resizearray(L, h, last);  /* preallocate it at once */
    for (; n > 0; n--) {
      TValue *val = ra+n;
      luaH_setint(L, h, last--, val);
      luaC_barrierback(L, h, val);
    }
    L->top = ci->top;  /* correct top (in case of previous open call) */
  }
  
  // [1782]	GETUPVAL 	3 2	; readsysname_no_pack_inner_must
  label_78: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1782]	LOADBOOL 	4 1 0
  label_79: {
    Instruction i = 0x00800103;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_81;
    }
  }
  
  // [1782]	CALL     	3 2 2
  label_80: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1783]	GETUPVAL 	4 9	; __TS__ArrayPush
  label_81: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1783]	MOVE     	5 2
  label_82: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1783]	MOVE     	6 3
  label_83: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1783]	CALL     	4 3 1
  label_84: {
    Instruction i = 0x01804124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1784]	GETUPVAL 	4 0	; eof
  label_85: {
    Instruction i = 0x00000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1784]	CALL     	4 1 2
  label_86: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1784]	TEST     	4 1
  label_87: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_89;
    }
  }
  
  // [1784]	JMP      	0 9	; to 98
  label_88: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_98;
  }
  
  // [1787]	GETUPVAL 	4 1	; get
  label_89: {
    Instruction i = 0x00800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1787]	CALL     	4 1 2
  label_90: {
    Instruction i = 0x00808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1788]	EQ       	1 4 -6	; - "/"
  label_91: {
    Instruction i = 0x0241405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_93;
    }
  }
  
  // [1788]	JMP      	0 -15	; to 78
  label_92: {
    Instruction i = 0x7ffc001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_78;
  }
  
  // [1789]	GETUPVAL 	5 10	; put
  label_93: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1789]	MOVE     	6 4
  label_94: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1789]	CALL     	5 2 1
  label_95: {
    Instruction i = 0x01004164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1789]	JMP      	0 1	; to 98
  label_96: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_98;
  }
  
  // [1791]	JMP      	0 -20	; to 78
  label_97: {
    Instruction i = 0x7ffac01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_78;
  }
  
  // [1793]	GETUPVAL 	3 3	; new_list
  label_98: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1793]	GETUPVAL 	4 11	; sub_symbol
  label_99: {
    Instruction i = 0x05800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1793]	GETUPVAL 	5 12	; jsArray_to_list
  label_100: {
    Instruction i = 0x06000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1793]	MOVE     	6 2
  label_101: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1793]	CALL     	5 2 0
  label_102: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1793]	TAILCALL 	3 0 0
  label_103: {
    Instruction i = 0x000000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1793]	RETURN   	3 0
  label_104: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1793]	JMP      	0 4	; to 110
  label_105: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_110;
  }
  
  // [1795]	GETUPVAL 	2 10	; put
  label_106: {
    Instruction i = 0x05000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1795]	MOVE     	3 1
  label_107: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1795]	CALL     	2 2 1
  label_108: {
    Instruction i = 0x010040a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1796]	RETURN   	0 2
  label_109: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1798]	GETUPVAL 	2 13	; ERROR
  label_110: {
    Instruction i = 0x06800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1798]	TAILCALL 	2 1 0
  label_111: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1798]	RETURN   	2 0
  label_112: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1799]	RETURN   	0 1
  label_113: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1851
// lastlinedefined = 1860
// what = Lua
static int zz_magic_function_144 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1852]	GETUPVAL 	0 0	; readsysname_no_pack
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1852]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1853]	EQ       	0 0 -1	; - false
  label_2: {
    Instruction i = 0x0040001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1853]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1854]	LOADBOOL 	1 0 0
  label_4: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1854]	RETURN   	1 2
  label_5: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1856]	GETUPVAL 	1 1	; symbol_p
  label_6: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1856]	MOVE     	2 0
  label_7: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1856]	CALL     	1 2 2
  label_8: {
    Instruction i = 0x01008064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1856]	TEST     	1 0
  label_9: {
    Instruction i = 0x00000062;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_11;
    }
  }
  
  // [1856]	JMP      	0 1	; to 12
  label_10: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_12;
  }
  
  // [1857]	RETURN   	0 2
  label_11: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1859]	GETUPVAL 	1 2	; systemName_make
  label_12: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1859]	MOVE     	2 0
  label_13: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1859]	TAILCALL 	1 2 0
  label_14: {
    Instruction i = 0x01000065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1859]	RETURN   	1 0
  label_15: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1860]	RETURN   	0 1
  label_16: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1864
// lastlinedefined = 1887
// what = Lua
static int zz_magic_function_145 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1886]	CLOSURE  	2 0	; 0x559f637b6590
  label_0: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1886]	RETURN   	2 2
  label_1: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1887]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1865
// lastlinedefined = 1886
// what = Lua
static int zz_magic_function_146 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1866]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1866]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1866]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1866]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1867]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1867]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1869]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1869]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1870]	GETUPVAL 	1 2	; prefix
  label_8: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1870]	EQ       	1 0 1
  label_9: {
    Instruction i = 0x0000405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_11;
    }
  }
  
  // [1870]	JMP      	0 5	; to 16
  label_10: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1871]	GETUPVAL 	1 3	; put
  label_11: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1871]	MOVE     	2 0
  label_12: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1871]	CALL     	1 2 1
  label_13: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1872]	LOADBOOL 	1 0 0
  label_14: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [1872]	RETURN   	1 2
  label_15: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1874]	GETUPVAL 	1 4	; list
  label_16: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1874]	CALL     	1 1 2
  label_17: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1875]	EQ       	0 1 -1	; - false
  label_18: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [1875]	JMP      	0 3	; to 23
  label_19: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [1876]	GETUPVAL 	2 5	; parse_error
  label_20: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1876]	TAILCALL 	2 1 0
  label_21: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1876]	RETURN   	2 0
  label_22: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1878]	GETUPVAL 	2 6	; construction_p
  label_23: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1878]	MOVE     	3 1
  label_24: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1878]	CALL     	2 2 2
  label_25: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1878]	TEST     	2 1
  label_26: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [1878]	JMP      	0 3	; to 31
  label_27: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1879]	GETUPVAL 	2 5	; parse_error
  label_28: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1879]	TAILCALL 	2 1 0
  label_29: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1879]	RETURN   	2 0
  label_30: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1881]	GETUPVAL 	2 7	; construction_tail
  label_31: {
    Instruction i = 0x03800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1881]	MOVE     	3 1
  label_32: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1881]	CALL     	2 2 2
  label_33: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1882]	GETUPVAL 	3 6	; construction_p
  label_34: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1882]	MOVE     	4 2
  label_35: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1882]	CALL     	3 2 2
  label_36: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1882]	TEST     	3 0
  label_37: {
    Instruction i = 0x000000e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_39;
    }
  }
  
  // [1882]	JMP      	0 7	; to 46
  label_38: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_46;
  }
  
  // [1882]	GETUPVAL 	3 8	; null_p
  label_39: {
    Instruction i = 0x040000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1882]	GETUPVAL 	4 7	; construction_tail
  label_40: {
    Instruction i = 0x03800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1882]	MOVE     	5 2
  label_41: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1882]	CALL     	4 2 0
  label_42: {
    Instruction i = 0x01000124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1882]	CALL     	3 0 2
  label_43: {
    Instruction i = 0x000080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1882]	TEST     	3 1
  label_44: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_46;
    }
  }
  
  // [1882]	JMP      	0 3	; to 49
  label_45: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_49;
  }
  
  // [1883]	GETUPVAL 	3 5	; parse_error
  label_46: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1883]	TAILCALL 	3 1 0
  label_47: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1883]	RETURN   	3 0
  label_48: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1885]	GETUPVAL 	3 9	; k
  label_49: {
    Instruction i = 0x048000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1885]	GETUPVAL 	4 10	; construction_head
  label_50: {
    Instruction i = 0x05000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1885]	MOVE     	5 1
  label_51: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1885]	CALL     	4 2 2
  label_52: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1885]	GETUPVAL 	5 10	; construction_head
  label_53: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1885]	MOVE     	6 2
  label_54: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1885]	CALL     	5 2 0
  label_55: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1885]	TAILCALL 	3 0 0
  label_56: {
    Instruction i = 0x000000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1885]	RETURN   	3 0
  label_57: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1886]	RETURN   	0 1
  label_58: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1889
// lastlinedefined = 1916
// what = Lua
static int zz_magic_function_147 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1915]	CLOSURE  	2 0	; 0x559f637b4f60
  label_0: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1915]	RETURN   	2 2
  label_1: {
    Instruction i = 0x010000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1916]	RETURN   	0 1
  label_2: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1890
// lastlinedefined = 1915
// what = Lua
static int zz_magic_function_148 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1891]	GETUPVAL 	0 0	; eof
  label_0: {
    Instruction i = 0x00000005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1891]	CALL     	0 1 2
  label_1: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1891]	TEST     	0 0
  label_2: {
    Instruction i = 0x00000022;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_4;
    }
  }
  
  // [1891]	JMP      	0 2	; to 6
  label_3: {
    Instruction i = 0x8000401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_6;
  }
  
  // [1892]	LOADBOOL 	0 0 0
  label_4: {
    Instruction i = 0x00000003;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_6;
    }
  }
  
  // [1892]	RETURN   	0 2
  label_5: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1894]	GETUPVAL 	0 1	; get
  label_6: {
    Instruction i = 0x00800005;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1894]	CALL     	0 1 2
  label_7: {
    Instruction i = 0x00808024;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1895]	GETUPVAL 	1 2	; prefix
  label_8: {
    Instruction i = 0x01000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1895]	EQ       	1 0 1
  label_9: {
    Instruction i = 0x0000405f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_11;
    }
  }
  
  // [1895]	JMP      	0 5	; to 16
  label_10: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_16;
  }
  
  // [1896]	GETUPVAL 	1 3	; put
  label_11: {
    Instruction i = 0x01800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1896]	MOVE     	2 0
  label_12: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1896]	CALL     	1 2 1
  label_13: {
    Instruction i = 0x01004064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1897]	LOADBOOL 	1 0 0
  label_14: {
    Instruction i = 0x00000043;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setbvalue(ra, GETARG_B(i));
    if (GETARG_C(i)) { /* skip next instruction (if C) */
      ci->u.l.savedpc++;
      goto label_16;
    }
  }
  
  // [1897]	RETURN   	1 2
  label_15: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1899]	GETUPVAL 	1 4	; list
  label_16: {
    Instruction i = 0x02000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1899]	CALL     	1 1 2
  label_17: {
    Instruction i = 0x00808064;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1900]	EQ       	0 1 -1	; - false
  label_18: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [1900]	JMP      	0 3	; to 23
  label_19: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_23;
  }
  
  // [1901]	GETUPVAL 	2 5	; parse_error
  label_20: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1901]	TAILCALL 	2 1 0
  label_21: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1901]	RETURN   	2 0
  label_22: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1903]	GETUPVAL 	2 6	; construction_p
  label_23: {
    Instruction i = 0x03000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1903]	MOVE     	3 1
  label_24: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1903]	CALL     	2 2 2
  label_25: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1903]	TEST     	2 1
  label_26: {
    Instruction i = 0x000040a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_28;
    }
  }
  
  // [1903]	JMP      	0 3	; to 31
  label_27: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_31;
  }
  
  // [1904]	GETUPVAL 	2 5	; parse_error
  label_28: {
    Instruction i = 0x02800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1904]	TAILCALL 	2 1 0
  label_29: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1904]	RETURN   	2 0
  label_30: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1906]	GETUPVAL 	2 7	; construction_tail
  label_31: {
    Instruction i = 0x03800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1906]	MOVE     	3 1
  label_32: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1906]	CALL     	2 2 2
  label_33: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1907]	GETUPVAL 	3 6	; construction_p
  label_34: {
    Instruction i = 0x030000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1907]	MOVE     	4 2
  label_35: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1907]	CALL     	3 2 2
  label_36: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1907]	TEST     	3 1
  label_37: {
    Instruction i = 0x000040e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_39;
    }
  }
  
  // [1907]	JMP      	0 3	; to 42
  label_38: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_42;
  }
  
  // [1908]	GETUPVAL 	3 5	; parse_error
  label_39: {
    Instruction i = 0x028000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1908]	TAILCALL 	3 1 0
  label_40: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1908]	RETURN   	3 0
  label_41: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1910]	GETUPVAL 	3 7	; construction_tail
  label_42: {
    Instruction i = 0x038000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1910]	MOVE     	4 2
  label_43: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1910]	CALL     	3 2 2
  label_44: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1911]	GETUPVAL 	4 6	; construction_p
  label_45: {
    Instruction i = 0x03000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1911]	MOVE     	5 3
  label_46: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1911]	CALL     	4 2 2
  label_47: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1911]	TEST     	4 0
  label_48: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_50;
    }
  }
  
  // [1911]	JMP      	0 7	; to 57
  label_49: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_57;
  }
  
  // [1911]	GETUPVAL 	4 8	; null_p
  label_50: {
    Instruction i = 0x04000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1911]	GETUPVAL 	5 7	; construction_tail
  label_51: {
    Instruction i = 0x03800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1911]	MOVE     	6 3
  label_52: {
    Instruction i = 0x01800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1911]	CALL     	5 2 0
  label_53: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1911]	CALL     	4 0 2
  label_54: {
    Instruction i = 0x00008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1911]	TEST     	4 1
  label_55: {
    Instruction i = 0x00004122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_57;
    }
  }
  
  // [1911]	JMP      	0 3	; to 60
  label_56: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_60;
  }
  
  // [1912]	GETUPVAL 	4 5	; parse_error
  label_57: {
    Instruction i = 0x02800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1912]	TAILCALL 	4 1 0
  label_58: {
    Instruction i = 0x00800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1912]	RETURN   	4 0
  label_59: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1914]	GETUPVAL 	4 9	; k
  label_60: {
    Instruction i = 0x04800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1914]	GETUPVAL 	5 10	; construction_head
  label_61: {
    Instruction i = 0x05000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1914]	MOVE     	6 1
  label_62: {
    Instruction i = 0x00800180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1914]	CALL     	5 2 2
  label_63: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1914]	GETUPVAL 	6 10	; construction_head
  label_64: {
    Instruction i = 0x05000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1914]	MOVE     	7 2
  label_65: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1914]	CALL     	6 2 2
  label_66: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1914]	GETUPVAL 	7 10	; construction_head
  label_67: {
    Instruction i = 0x050001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1914]	MOVE     	8 3
  label_68: {
    Instruction i = 0x01800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1914]	CALL     	7 2 0
  label_69: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1914]	TAILCALL 	4 0 0
  label_70: {
    Instruction i = 0x00000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1914]	RETURN   	4 0
  label_71: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1915]	RETURN   	0 1
  label_72: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1917
// lastlinedefined = 1923
// what = Lua
static int zz_magic_function_149 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1918]	GETUPVAL 	2 0	; val2env
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1918]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1918]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1919]	EQ       	0 2 -1	; - false
  label_3: {
    Instruction i = 0x0140001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1919]	JMP      	0 3	; to 8
  label_4: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_8;
  }
  
  // [1920]	GETUPVAL 	3 1	; parse_error
  label_5: {
    Instruction i = 0x008000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1920]	TAILCALL 	3 1 0
  label_6: {
    Instruction i = 0x008000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1920]	RETURN   	3 0
  label_7: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1922]	GETUPVAL 	3 2	; evaluate
  label_8: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1922]	MOVE     	4 2
  label_9: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1922]	MOVE     	5 1
  label_10: {
    Instruction i = 0x00800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1922]	TAILCALL 	3 3 0
  label_11: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1922]	RETURN   	3 0
  label_12: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1923]	RETURN   	0 1
  label_13: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1924
// lastlinedefined = 1931
// what = Lua
static int zz_magic_function_150 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1925]	GETUPVAL 	2 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1925]	MOVE     	3 1
  label_1: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1927]	CLOSURE  	4 0	; 0x559f637b87e0
  label_2: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1929]	CLOSURE  	5 1	; 0x559f637b8b10
  label_3: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1925]	CALL     	2 4 2
  label_4: {
    Instruction i = 0x020080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1930]	GETUPVAL 	3 2	; builtin_func_apply
  label_5: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1930]	MOVE     	4 0
  label_6: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1930]	MOVE     	5 2
  label_7: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1930]	TAILCALL 	3 3 0
  label_8: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1930]	RETURN   	3 0
  label_9: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1931]	RETURN   	0 1
  label_10: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1925
// lastlinedefined = 1927
// what = Lua
static int zz_magic_function_151 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1926]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1927]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1927
// lastlinedefined = 1929
// what = Lua
static int zz_magic_function_152 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1928]	GETUPVAL 	2 0	; parse_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1928]	TAILCALL 	2 1 0
  label_1: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1928]	RETURN   	2 0
  label_2: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1929]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1932
// lastlinedefined = 1943
// what = Lua
static int zz_magic_function_153 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1933]	GETUPVAL 	3 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x000000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1933]	MOVE     	4 2
  label_1: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1935]	CLOSURE  	5 0	; 0x559f637b8cd0
  label_2: {
    Instruction i = 0x0000016c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1937]	CLOSURE  	6 1	; 0x559f637b8db0
  label_3: {
    Instruction i = 0x000041ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1933]	CALL     	3 4 2
  label_4: {
    Instruction i = 0x020080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1938]	GETUPVAL 	4 2	; val2env
  label_5: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1938]	MOVE     	5 0
  label_6: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1938]	CALL     	4 2 2
  label_7: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1939]	EQ       	0 4 -1	; - false
  label_8: {
    Instruction i = 0x0240001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_10;
    }
  }
  
  // [1939]	JMP      	0 3	; to 13
  label_9: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_13;
  }
  
  // [1940]	GETUPVAL 	5 1	; parse_error
  label_10: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1940]	TAILCALL 	5 1 0
  label_11: {
    Instruction i = 0x00800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1940]	RETURN   	5 0
  label_12: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1942]	GETUPVAL 	5 3	; builtin_form_apply
  label_13: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1942]	MOVE     	6 4
  label_14: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1942]	MOVE     	7 1
  label_15: {
    Instruction i = 0x008001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1942]	MOVE     	8 3
  label_16: {
    Instruction i = 0x01800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1942]	TAILCALL 	5 4 0
  label_17: {
    Instruction i = 0x02000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1942]	RETURN   	5 0
  label_18: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1943]	RETURN   	0 1
  label_19: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1933
// lastlinedefined = 1935
// what = Lua
static int zz_magic_function_154 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1934]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1935]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1935
// lastlinedefined = 1937
// what = Lua
static int zz_magic_function_155 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1936]	GETUPVAL 	2 0	; parse_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1936]	TAILCALL 	2 1 0
  label_1: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1936]	RETURN   	2 0
  label_2: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1937]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1944
// lastlinedefined = 1951
// what = Lua
static int zz_magic_function_156 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1945]	GETUPVAL 	2 0	; list_to_jsArray
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1945]	MOVE     	3 1
  label_1: {
    Instruction i = 0x008000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1947]	CLOSURE  	4 0	; 0x559f637b9250
  label_2: {
    Instruction i = 0x0000012c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1949]	CLOSURE  	5 1	; 0x559f637b9330
  label_3: {
    Instruction i = 0x0000416c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1945]	CALL     	2 4 2
  label_4: {
    Instruction i = 0x020080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1950]	GETUPVAL 	3 2	; apply
  label_5: {
    Instruction i = 0x010000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1950]	MOVE     	4 0
  label_6: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1950]	MOVE     	5 2
  label_7: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1950]	TAILCALL 	3 3 0
  label_8: {
    Instruction i = 0x018000e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1950]	RETURN   	3 0
  label_9: {
    Instruction i = 0x000000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1951]	RETURN   	0 1
  label_10: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1945
// lastlinedefined = 1947
// what = Lua
static int zz_magic_function_157 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1946]	RETURN   	0 2
  label_0: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1947]	RETURN   	0 1
  label_1: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1947
// lastlinedefined = 1949
// what = Lua
static int zz_magic_function_158 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1948]	GETUPVAL 	2 0	; parse_error
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1948]	TAILCALL 	2 1 0
  label_1: {
    Instruction i = 0x008000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1948]	RETURN   	2 0
  label_2: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1949]	RETURN   	0 1
  label_3: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1956
// lastlinedefined = 2071
// what = Lua
static int zz_magic_function_159 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1957]	LOADNIL  	1 0
  label_0: {
    Instruction i = 0x00000044;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [2029]	CLOSURE  	2 0	; 0x559f637b5170
  label_1: {
    Instruction i = 0x000000ac;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [2029]	MOVE     	1 2
  label_2: {
    Instruction i = 0x01000040;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2030]	GETUPVAL 	2 18	; simple_parse
  label_3: {
    Instruction i = 0x09000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2030]	GETUPVAL 	3 16	; simple_print
  label_4: {
    Instruction i = 0x080000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2030]	MOVE     	4 0
  label_5: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2030]	CALL     	3 2 0
  label_6: {
    Instruction i = 0x010000e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2030]	CALL     	2 0 2
  label_7: {
    Instruction i = 0x000080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2031]	LOADK    	3 -1	; ""
  label_8: {
    Instruction i = 0x000000c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2032]	LOADK    	4 -1	; ""
  label_9: {
    Instruction i = 0x00000101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2033]	GETUPVAL 	5 19	; null_p
  label_10: {
    Instruction i = 0x09800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2033]	MOVE     	6 2
  label_11: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2033]	CALL     	5 2 2
  label_12: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2033]	TEST     	5 0
  label_13: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_15;
    }
  }
  
  // [2033]	JMP      	0 3	; to 18
  label_14: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_18;
  }
  
  // [2034]	LOADK    	5 -2	; "()"
  label_15: {
    Instruction i = 0x00004141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2034]	RETURN   	5 2
  label_16: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2034]	JMP      	0 233	; to 251
  label_17: {
    Instruction i = 0x803a001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2035]	GETUPVAL 	5 8	; construction_p
  label_18: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2035]	MOVE     	6 2
  label_19: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2035]	CALL     	5 2 2
  label_20: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2035]	TEST     	5 0
  label_21: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_23;
    }
  }
  
  // [2035]	JMP      	0 40	; to 63
  label_22: {
    Instruction i = 0x8009c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_63;
  }
  
  // [2036]	LOADK    	3 -3	; "("
  label_23: {
    Instruction i = 0x000080c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2037]	LOADK    	4 -1	; ""
  label_24: {
    Instruction i = 0x00000101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2038]	GETUPVAL 	5 8	; construction_p
  label_25: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2038]	MOVE     	6 2
  label_26: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2038]	CALL     	5 2 2
  label_27: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2038]	TEST     	5 0
  label_28: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_30;
    }
  }
  
  // [2038]	JMP      	0 14	; to 44
  label_29: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_44;
  }
  
  // [2039]	MOVE     	5 3
  label_30: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2039]	MOVE     	6 4
  label_31: {
    Instruction i = 0x02000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2039]	GETUPVAL 	7 20	; complex_print
  label_32: {
    Instruction i = 0x0a0001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2039]	GETUPVAL 	8 10	; construction_head
  label_33: {
    Instruction i = 0x05000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2039]	MOVE     	9 2
  label_34: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2039]	CALL     	8 2 0
  label_35: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2039]	CALL     	7 0 2
  label_36: {
    Instruction i = 0x000081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2039]	CONCAT   	3 5 7
  label_37: {
    Instruction i = 0x0281c0dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2040]	LOADK    	4 -4	; " "
  label_38: {
    Instruction i = 0x0000c101;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2041]	GETUPVAL 	5 9	; construction_tail
  label_39: {
    Instruction i = 0x04800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2041]	MOVE     	6 2
  label_40: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2041]	CALL     	5 2 2
  label_41: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2041]	MOVE     	2 5
  label_42: {
    Instruction i = 0x02800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2041]	JMP      	0 -19	; to 25
  label_43: {
    Instruction i = 0x7ffb001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_25;
  }
  
  // [2043]	GETUPVAL 	5 19	; null_p
  label_44: {
    Instruction i = 0x09800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2043]	MOVE     	6 2
  label_45: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2043]	CALL     	5 2 2
  label_46: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2043]	TEST     	5 0
  label_47: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_49;
    }
  }
  
  // [2043]	JMP      	0 4	; to 53
  label_48: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_53;
  }
  
  // [2044]	MOVE     	5 3
  label_49: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2044]	LOADK    	6 -5	; ")"
  label_50: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2044]	CONCAT   	3 5 6
  label_51: {
    Instruction i = 0x028180dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2044]	JMP      	0 8	; to 61
  label_52: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_61;
  }
  
  // [2046]	MOVE     	5 3
  label_53: {
    Instruction i = 0x01800140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2046]	LOADK    	6 -6	; " . "
  label_54: {
    Instruction i = 0x00014181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2046]	GETUPVAL 	7 20	; complex_print
  label_55: {
    Instruction i = 0x0a0001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2046]	MOVE     	8 2
  label_56: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2046]	CALL     	7 2 2
  label_57: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2046]	CONCAT   	6 6 7
  label_58: {
    Instruction i = 0x0301c19d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2046]	LOADK    	7 -5	; ")"
  label_59: {
    Instruction i = 0x000101c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2046]	CONCAT   	3 5 7
  label_60: {
    Instruction i = 0x0281c0dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2048]	RETURN   	3 2
  label_61: {
    Instruction i = 0x010000e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2048]	JMP      	0 188	; to 251
  label_62: {
    Instruction i = 0x802ec01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2049]	GETUPVAL 	5 21	; data_p
  label_63: {
    Instruction i = 0x0a800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2049]	MOVE     	6 2
  label_64: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2049]	CALL     	5 2 2
  label_65: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2049]	TEST     	5 0
  label_66: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_68;
    }
  }
  
  // [2049]	JMP      	0 41	; to 109
  label_67: {
    Instruction i = 0x800a001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_109;
  }
  
  // [2050]	GETUPVAL 	5 22	; data_name
  label_68: {
    Instruction i = 0x0b000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2050]	MOVE     	6 2
  label_69: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2050]	CALL     	5 2 2
  label_70: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2051]	GETUPVAL 	6 23	; data_list
  label_71: {
    Instruction i = 0x0b800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2051]	MOVE     	7 2
  label_72: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2051]	CALL     	6 2 2
  label_73: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2052]	GETUPVAL 	7 3	; maybe_list_to_jsArray
  label_74: {
    Instruction i = 0x018001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2052]	MOVE     	8 6
  label_75: {
    Instruction i = 0x03000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2052]	CALL     	7 2 2
  label_76: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2053]	EQ       	1 7 -7	; - false
  label_77: {
    Instruction i = 0x03c1805f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_79;
    }
  }
  
  // [2053]	JMP      	0 20	; to 99
  label_78: {
    Instruction i = 0x8004c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_99;
  }
  
  // [2053]	LEN      	8 7
  label_79: {
    Instruction i = 0x0380021c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [2053]	EQ       	0 8 -8	; - 2
  label_80: {
    Instruction i = 0x0441c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_82;
    }
  }
  
  // [2053]	JMP      	0 17	; to 99
  label_81: {
    Instruction i = 0x8004001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_99;
  }
  
  // [2053]	GETUPVAL 	8 4	; jsbool_no_force_equal_p
  label_82: {
    Instruction i = 0x02000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2053]	MOVE     	9 5
  label_83: {
    Instruction i = 0x02800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2053]	GETUPVAL 	10 24	; name_symbol
  label_84: {
    Instruction i = 0x0c000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2053]	CALL     	8 3 2
  label_85: {
    Instruction i = 0x01808224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2053]	TEST     	8 0
  label_86: {
    Instruction i = 0x00000222;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_88;
    }
  }
  
  // [2053]	JMP      	0 11	; to 99
  label_87: {
    Instruction i = 0x8002801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_99;
  }
  
  // [2053]	GETUPVAL 	8 4	; jsbool_no_force_equal_p
  label_88: {
    Instruction i = 0x02000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2053]	GETTABLE 	9 7 -9	; 1
  label_89: {
    Instruction i = 0x03c20247;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2053]	GETUPVAL 	10 14	; system_symbol
  label_90: {
    Instruction i = 0x07000285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2053]	CALL     	8 3 2
  label_91: {
    Instruction i = 0x01808224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2053]	TEST     	8 0
  label_92: {
    Instruction i = 0x00000222;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_94;
    }
  }
  
  // [2053]	JMP      	0 5	; to 99
  label_93: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_99;
  }
  
  // [2054]	MOVE     	8 1
  label_94: {
    Instruction i = 0x00800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2054]	GETTABLE 	9 7 -8	; 2
  label_95: {
    Instruction i = 0x03c1c247;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2054]	LOADK    	10 -10	; "top"
  label_96: {
    Instruction i = 0x00024281;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2054]	TAILCALL 	8 3 0
  label_97: {
    Instruction i = 0x01800225;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2054]	RETURN   	8 0
  label_98: {
    Instruction i = 0x00000226;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2056]	LOADK    	8 -11	; "#"
  label_99: {
    Instruction i = 0x00028201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2056]	GETUPVAL 	9 20	; complex_print
  label_100: {
    Instruction i = 0x0a000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2056]	GETUPVAL 	10 25	; new_construction
  label_101: {
    Instruction i = 0x0c800285;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2056]	MOVE     	11 5
  label_102: {
    Instruction i = 0x028002c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2056]	MOVE     	12 6
  label_103: {
    Instruction i = 0x03000300;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2056]	CALL     	10 3 0
  label_104: {
    Instruction i = 0x018002a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2056]	CALL     	9 0 2
  label_105: {
    Instruction i = 0x00008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2056]	CONCAT   	8 8 9
  label_106: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2056]	RETURN   	8 2
  label_107: {
    Instruction i = 0x01000226;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2056]	JMP      	0 142	; to 251
  label_108: {
    Instruction i = 0x8023401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2057]	GETUPVAL 	5 26	; error_p
  label_109: {
    Instruction i = 0x0d000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2057]	MOVE     	6 2
  label_110: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2057]	CALL     	5 2 2
  label_111: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2057]	TEST     	5 0
  label_112: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_114;
    }
  }
  
  // [2057]	JMP      	0 14	; to 128
  label_113: {
    Instruction i = 0x8003401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_128;
  }
  
  // [2058]	LOADK    	5 -12	; "!"
  label_114: {
    Instruction i = 0x0002c141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2058]	GETUPVAL 	6 20	; complex_print
  label_115: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2058]	GETUPVAL 	7 25	; new_construction
  label_116: {
    Instruction i = 0x0c8001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2058]	GETUPVAL 	8 27	; error_name
  label_117: {
    Instruction i = 0x0d800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2058]	MOVE     	9 2
  label_118: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2058]	CALL     	8 2 2
  label_119: {
    Instruction i = 0x01008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2058]	GETUPVAL 	9 28	; error_list
  label_120: {
    Instruction i = 0x0e000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2058]	MOVE     	10 2
  label_121: {
    Instruction i = 0x01000280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2058]	CALL     	9 2 0
  label_122: {
    Instruction i = 0x01000264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2058]	CALL     	7 0 0
  label_123: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2058]	CALL     	6 0 2
  label_124: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2058]	CONCAT   	5 5 6
  label_125: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2058]	RETURN   	5 2
  label_126: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2058]	JMP      	0 123	; to 251
  label_127: {
    Instruction i = 0x801e801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2059]	GETUPVAL 	5 0	; symbol_p
  label_128: {
    Instruction i = 0x00000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2059]	MOVE     	6 2
  label_129: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2059]	CALL     	5 2 2
  label_130: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2059]	TEST     	5 0
  label_131: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_133;
    }
  }
  
  // [2059]	JMP      	0 5	; to 138
  label_132: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_138;
  }
  
  // [2060]	GETUPVAL 	5 1	; un_symbol
  label_133: {
    Instruction i = 0x00800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2060]	MOVE     	6 2
  label_134: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2060]	TAILCALL 	5 2 0
  label_135: {
    Instruction i = 0x01000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2060]	RETURN   	5 0
  label_136: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2060]	JMP      	0 113	; to 251
  label_137: {
    Instruction i = 0x801c001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2061]	GETUPVAL 	5 29	; delay_evaluate_p
  label_138: {
    Instruction i = 0x0e800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2061]	MOVE     	6 2
  label_139: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2061]	CALL     	5 2 2
  label_140: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2061]	TEST     	5 0
  label_141: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_143;
    }
  }
  
  // [2061]	JMP      	0 21	; to 164
  label_142: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_164;
  }
  
  // [2062]	LOADK    	5 -13	; "$("
  label_143: {
    Instruction i = 0x00030141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2062]	GETUPVAL 	6 20	; complex_print
  label_144: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2062]	GETUPVAL 	7 30	; env2val
  label_145: {
    Instruction i = 0x0f0001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2062]	GETUPVAL 	8 31	; delay_evaluate_env
  label_146: {
    Instruction i = 0x0f800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2062]	MOVE     	9 2
  label_147: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2062]	CALL     	8 2 0
  label_148: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2062]	CALL     	7 0 0
  label_149: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2062]	CALL     	6 0 2
  label_150: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2062]	CONCAT   	5 5 6
  label_151: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2062]	LOADK    	6 -4	; " "
  label_152: {
    Instruction i = 0x0000c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2062]	CONCAT   	5 5 6
  label_153: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2062]	GETUPVAL 	6 20	; complex_print
  label_154: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2062]	GETUPVAL 	7 32	; delay_evaluate_x
  label_155: {
    Instruction i = 0x100001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2062]	MOVE     	8 2
  label_156: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2062]	CALL     	7 2 0
  label_157: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2062]	CALL     	6 0 2
  label_158: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2062]	CONCAT   	5 5 6
  label_159: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2062]	LOADK    	6 -5	; ")"
  label_160: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2062]	CONCAT   	5 5 6
  label_161: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2062]	RETURN   	5 2
  label_162: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2062]	JMP      	0 87	; to 251
  label_163: {
    Instruction i = 0x8015801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2063]	GETUPVAL 	5 33	; delay_builtin_func_p
  label_164: {
    Instruction i = 0x10800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2063]	MOVE     	6 2
  label_165: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2063]	CALL     	5 2 2
  label_166: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2063]	TEST     	5 0
  label_167: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_169;
    }
  }
  
  // [2063]	JMP      	0 21	; to 190
  label_168: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_190;
  }
  
  // [2064]	LOADK    	5 -14	; "%("
  label_169: {
    Instruction i = 0x00034141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2064]	GETUPVAL 	6 20	; complex_print
  label_170: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2064]	GETUPVAL 	7 34	; delay_builtin_func_f
  label_171: {
    Instruction i = 0x110001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2064]	MOVE     	8 2
  label_172: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2064]	CALL     	7 2 0
  label_173: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2064]	CALL     	6 0 2
  label_174: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2064]	CONCAT   	5 5 6
  label_175: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2064]	LOADK    	6 -4	; " "
  label_176: {
    Instruction i = 0x0000c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2064]	CONCAT   	5 5 6
  label_177: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2064]	GETUPVAL 	6 20	; complex_print
  label_178: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2064]	GETUPVAL 	7 35	; jsArray_to_list
  label_179: {
    Instruction i = 0x118001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2064]	GETUPVAL 	8 36	; delay_builtin_func_xs
  label_180: {
    Instruction i = 0x12000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2064]	MOVE     	9 2
  label_181: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2064]	CALL     	8 2 0
  label_182: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2064]	CALL     	7 0 0
  label_183: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2064]	CALL     	6 0 2
  label_184: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2064]	CONCAT   	5 5 6
  label_185: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2064]	LOADK    	6 -5	; ")"
  label_186: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2064]	CONCAT   	5 5 6
  label_187: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2064]	RETURN   	5 2
  label_188: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2064]	JMP      	0 61	; to 251
  label_189: {
    Instruction i = 0x800f001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2065]	GETUPVAL 	5 37	; delay_builtin_form_p
  label_190: {
    Instruction i = 0x12800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2065]	MOVE     	6 2
  label_191: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2065]	CALL     	5 2 2
  label_192: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2065]	TEST     	5 0
  label_193: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_195;
    }
  }
  
  // [2065]	JMP      	0 31	; to 226
  label_194: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_226;
  }
  
  // [2066]	LOADK    	5 -15	; "@("
  label_195: {
    Instruction i = 0x00038141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2066]	GETUPVAL 	6 20	; complex_print
  label_196: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	GETUPVAL 	7 30	; env2val
  label_197: {
    Instruction i = 0x0f0001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	GETUPVAL 	8 38	; delay_builtin_form_env
  label_198: {
    Instruction i = 0x13000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	MOVE     	9 2
  label_199: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2066]	CALL     	8 2 0
  label_200: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CALL     	7 0 0
  label_201: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CALL     	6 0 2
  label_202: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CONCAT   	5 5 6
  label_203: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2066]	LOADK    	6 -4	; " "
  label_204: {
    Instruction i = 0x0000c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2066]	CONCAT   	5 5 6
  label_205: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2066]	GETUPVAL 	6 20	; complex_print
  label_206: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	GETUPVAL 	7 39	; delay_builtin_form_f
  label_207: {
    Instruction i = 0x138001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	MOVE     	8 2
  label_208: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2066]	CALL     	7 2 0
  label_209: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CALL     	6 0 2
  label_210: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CONCAT   	5 5 6
  label_211: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2066]	LOADK    	6 -4	; " "
  label_212: {
    Instruction i = 0x0000c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2066]	CONCAT   	5 5 6
  label_213: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2066]	GETUPVAL 	6 20	; complex_print
  label_214: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	GETUPVAL 	7 35	; jsArray_to_list
  label_215: {
    Instruction i = 0x118001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	GETUPVAL 	8 40	; delay_builtin_form_xs
  label_216: {
    Instruction i = 0x14000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2066]	MOVE     	9 2
  label_217: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2066]	CALL     	8 2 0
  label_218: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CALL     	7 0 0
  label_219: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CALL     	6 0 2
  label_220: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2066]	CONCAT   	5 5 6
  label_221: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2066]	LOADK    	6 -5	; ")"
  label_222: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2066]	CONCAT   	5 5 6
  label_223: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2066]	RETURN   	5 2
  label_224: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2066]	JMP      	0 25	; to 251
  label_225: {
    Instruction i = 0x8006001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2067]	GETUPVAL 	5 41	; delay_apply_p
  label_226: {
    Instruction i = 0x14800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2067]	MOVE     	6 2
  label_227: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2067]	CALL     	5 2 2
  label_228: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2067]	TEST     	5 0
  label_229: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_231;
    }
  }
  
  // [2067]	JMP      	0 20	; to 251
  label_230: {
    Instruction i = 0x8004c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_251;
  }
  
  // [2068]	LOADK    	5 -16	; "^("
  label_231: {
    Instruction i = 0x0003c141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2068]	GETUPVAL 	6 20	; complex_print
  label_232: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2068]	GETUPVAL 	7 42	; delay_apply_f
  label_233: {
    Instruction i = 0x150001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2068]	MOVE     	8 2
  label_234: {
    Instruction i = 0x01000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2068]	CALL     	7 2 0
  label_235: {
    Instruction i = 0x010001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2068]	CALL     	6 0 2
  label_236: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2068]	CONCAT   	5 5 6
  label_237: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2068]	LOADK    	6 -4	; " "
  label_238: {
    Instruction i = 0x0000c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2068]	CONCAT   	5 5 6
  label_239: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2068]	GETUPVAL 	6 20	; complex_print
  label_240: {
    Instruction i = 0x0a000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2068]	GETUPVAL 	7 35	; jsArray_to_list
  label_241: {
    Instruction i = 0x118001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2068]	GETUPVAL 	8 43	; delay_apply_xs
  label_242: {
    Instruction i = 0x15800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2068]	MOVE     	9 2
  label_243: {
    Instruction i = 0x01000240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2068]	CALL     	8 2 0
  label_244: {
    Instruction i = 0x01000224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2068]	CALL     	7 0 0
  label_245: {
    Instruction i = 0x000001e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2068]	CALL     	6 0 2
  label_246: {
    Instruction i = 0x000081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2068]	CONCAT   	5 5 6
  label_247: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2068]	LOADK    	6 -5	; ")"
  label_248: {
    Instruction i = 0x00010181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2068]	CONCAT   	5 5 6
  label_249: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2068]	RETURN   	5 2
  label_250: {
    Instruction i = 0x01000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2070]	GETUPVAL 	5 2	; ERROR
  label_251: {
    Instruction i = 0x01000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2070]	TAILCALL 	5 1 0
  label_252: {
    Instruction i = 0x00800165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2070]	RETURN   	5 0
  label_253: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2071]	RETURN   	0 1
  label_254: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1958
// lastlinedefined = 2029
// what = Lua
static int zz_magic_function_160 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1959]	GETUPVAL 	2 0	; symbol_p
  label_0: {
    Instruction i = 0x00000085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1959]	MOVE     	3 0
  label_1: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1959]	CALL     	2 2 2
  label_2: {
    Instruction i = 0x010080a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1959]	TEST     	2 0
  label_3: {
    Instruction i = 0x000000a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_5;
    }
  }
  
  // [1959]	JMP      	0 4	; to 9
  label_4: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_9;
  }
  
  // [1960]	GETUPVAL 	2 1	; un_symbol
  label_5: {
    Instruction i = 0x00800085;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1960]	MOVE     	3 0
  label_6: {
    Instruction i = 0x000000c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1960]	TAILCALL 	2 2 0
  label_7: {
    Instruction i = 0x010000a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1960]	RETURN   	2 0
  label_8: {
    Instruction i = 0x000000a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1962]	LOADNIL  	2 0
  label_9: {
    Instruction i = 0x00000084;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    do {
      setnilvalue(ra++);
    } while (b--);
  }
  
  // [1970]	CLOSURE  	3 0	; 0x559f637b6960
  label_10: {
    Instruction i = 0x000000ec;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Proto *p = cl->p->p[GETARG_Bx(i)];
    LClosure *ncl = luaV_getcached(p, cl->upvals, base);  /* cached closure*/
    if (ncl == NULL)  /* no match? */
      luaV_pushclosure(L, p, cl->upvals, base, ra);  /* create a new one */
    else
      setclLvalue(L, ra, ncl);  /* push cashed closure */
    checkGC(L, ra + 1);
  }
  
  // [1970]	MOVE     	2 3
  label_11: {
    Instruction i = 0x01800080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1971]	GETUPVAL 	3 3	; maybe_list_to_jsArray
  label_12: {
    Instruction i = 0x018000c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1971]	MOVE     	4 0
  label_13: {
    Instruction i = 0x00000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1971]	CALL     	3 2 2
  label_14: {
    Instruction i = 0x010080e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1972]	EQ       	1 3 -1	; - false
  label_15: {
    Instruction i = 0x01c0005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_17;
    }
  }
  
  // [1972]	JMP      	0 219	; to 236
  label_16: {
    Instruction i = 0x8036801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_236;
  }
  
  // [1972]	LEN      	4 3
  label_17: {
    Instruction i = 0x0180011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1972]	EQ       	0 4 -2	; - 3
  label_18: {
    Instruction i = 0x0240401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_20;
    }
  }
  
  // [1972]	JMP      	0 216	; to 236
  label_19: {
    Instruction i = 0x8035c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_236;
  }
  
  // [1972]	GETUPVAL 	4 4	; jsbool_no_force_equal_p
  label_20: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1972]	GETTABLE 	5 3 -3	; 1
  label_21: {
    Instruction i = 0x01c08147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1972]	GETUPVAL 	6 5	; typeAnnotation_symbol
  label_22: {
    Instruction i = 0x02800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1972]	CALL     	4 3 2
  label_23: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1972]	TEST     	4 0
  label_24: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_26;
    }
  }
  
  // [1972]	JMP      	0 210	; to 236
  label_25: {
    Instruction i = 0x8034401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_236;
  }
  
  // [1973]	GETUPVAL 	4 3	; maybe_list_to_jsArray
  label_26: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1973]	GETTABLE 	5 3 -4	; 2
  label_27: {
    Instruction i = 0x01c0c147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1973]	CALL     	4 2 2
  label_28: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1974]	EQ       	1 4 -1	; - false
  label_29: {
    Instruction i = 0x0240005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_31;
    }
  }
  
  // [1974]	JMP      	0 96	; to 127
  label_30: {
    Instruction i = 0x8017c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1974]	LEN      	5 4
  label_31: {
    Instruction i = 0x0200015c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1974]	EQ       	0 5 -2	; - 3
  label_32: {
    Instruction i = 0x02c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_34;
    }
  }
  
  // [1974]	JMP      	0 93	; to 127
  label_33: {
    Instruction i = 0x8017001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1974]	GETUPVAL 	5 4	; jsbool_no_force_equal_p
  label_34: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1974]	GETTABLE 	6 4 -3	; 1
  label_35: {
    Instruction i = 0x02408187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1974]	GETUPVAL 	7 6	; function_symbol
  label_36: {
    Instruction i = 0x030001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1974]	CALL     	5 3 2
  label_37: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1974]	TEST     	5 0
  label_38: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_40;
    }
  }
  
  // [1974]	JMP      	0 87	; to 127
  label_39: {
    Instruction i = 0x8015801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1975]	GETTABLE 	5 4 -4	; 2
  label_40: {
    Instruction i = 0x0240c147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1976]	GETUPVAL 	6 3	; maybe_list_to_jsArray
  label_41: {
    Instruction i = 0x01800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1976]	MOVE     	7 5
  label_42: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1976]	CALL     	6 2 2
  label_43: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1977]	EQ       	1 6 -1	; - false
  label_44: {
    Instruction i = 0x0340005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_46;
    }
  }
  
  // [1977]	JMP      	0 24	; to 70
  label_45: {
    Instruction i = 0x8005c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_70;
  }
  
  // [1977]	LEN      	7 6
  label_46: {
    Instruction i = 0x030001dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1977]	EQ       	0 7 -3	; - 1
  label_47: {
    Instruction i = 0x03c0801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_49;
    }
  }
  
  // [1977]	JMP      	0 21	; to 70
  label_48: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_70;
  }
  
  // [1977]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_49: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1977]	GETTABLE 	8 4 -2	; 3
  label_50: {
    Instruction i = 0x02404207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1977]	GETUPVAL 	9 7	; something_symbol
  label_51: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1977]	CALL     	7 3 2
  label_52: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1977]	TEST     	7 0
  label_53: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_55;
    }
  }
  
  // [1977]	JMP      	0 15	; to 70
  label_54: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_70;
  }
  
  // [1978]	MOVE     	7 2
  label_55: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1978]	GETUPVAL 	8 8	; print_sys_name
  label_56: {
    Instruction i = 0x04000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1978]	GETTABLE 	9 6 -3	; 1
  label_57: {
    Instruction i = 0x03408247;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1978]	LOADK    	10 -5	; "inner"
  label_58: {
    Instruction i = 0x00010281;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1978]	CALL     	8 3 2
  label_59: {
    Instruction i = 0x01808224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1978]	LOADK    	9 -6	; "."
  label_60: {
    Instruction i = 0x00014241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1978]	CONCAT   	8 8 9
  label_61: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1978]	GETUPVAL 	9 8	; print_sys_name
  label_62: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1978]	GETTABLE 	10 3 -2	; 3
  label_63: {
    Instruction i = 0x01c04287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1978]	LOADK    	11 -5	; "inner"
  label_64: {
    Instruction i = 0x000102c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1978]	CALL     	9 3 2
  label_65: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1978]	CONCAT   	8 8 9
  label_66: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1978]	TAILCALL 	7 2 0
  label_67: {
    Instruction i = 0x010001e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1978]	RETURN   	7 0
  label_68: {
    Instruction i = 0x000001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1978]	JMP      	0 57	; to 127
  label_69: {
    Instruction i = 0x800e001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1979]	GETUPVAL 	7 9	; construction_p
  label_70: {
    Instruction i = 0x048001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1979]	MOVE     	8 5
  label_71: {
    Instruction i = 0x02800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1979]	CALL     	7 2 2
  label_72: {
    Instruction i = 0x010081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1979]	TEST     	7 0
  label_73: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_75;
    }
  }
  
  // [1979]	JMP      	0 31	; to 106
  label_74: {
    Instruction i = 0x8007801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_106;
  }
  
  // [1979]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_75: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1979]	GETUPVAL 	8 10	; construction_tail
  label_76: {
    Instruction i = 0x05000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1979]	MOVE     	9 5
  label_77: {
    Instruction i = 0x02800240;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1979]	CALL     	8 2 2
  label_78: {
    Instruction i = 0x01008224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1979]	GETUPVAL 	9 7	; something_symbol
  label_79: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1979]	CALL     	7 3 2
  label_80: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1979]	TEST     	7 0
  label_81: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_83;
    }
  }
  
  // [1979]	JMP      	0 23	; to 106
  label_82: {
    Instruction i = 0x8005801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_106;
  }
  
  // [1979]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_83: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1979]	GETTABLE 	8 4 -2	; 3
  label_84: {
    Instruction i = 0x02404207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1979]	GETUPVAL 	9 7	; something_symbol
  label_85: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1979]	CALL     	7 3 2
  label_86: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1979]	TEST     	7 0
  label_87: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_89;
    }
  }
  
  // [1979]	JMP      	0 17	; to 106
  label_88: {
    Instruction i = 0x8004001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_106;
  }
  
  // [1980]	MOVE     	7 2
  label_89: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1980]	GETUPVAL 	8 8	; print_sys_name
  label_90: {
    Instruction i = 0x04000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1980]	GETUPVAL 	9 11	; construction_head
  label_91: {
    Instruction i = 0x05800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1980]	MOVE     	10 5
  label_92: {
    Instruction i = 0x02800280;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1980]	CALL     	9 2 2
  label_93: {
    Instruction i = 0x01008264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1980]	LOADK    	10 -5	; "inner"
  label_94: {
    Instruction i = 0x00010281;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1980]	CALL     	8 3 2
  label_95: {
    Instruction i = 0x01808224;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1980]	LOADK    	9 -7	; "@"
  label_96: {
    Instruction i = 0x00018241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1980]	CONCAT   	8 8 9
  label_97: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1980]	GETUPVAL 	9 8	; print_sys_name
  label_98: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1980]	GETTABLE 	10 3 -2	; 3
  label_99: {
    Instruction i = 0x01c04287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1980]	LOADK    	11 -5	; "inner"
  label_100: {
    Instruction i = 0x000102c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1980]	CALL     	9 3 2
  label_101: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1980]	CONCAT   	8 8 9
  label_102: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1980]	TAILCALL 	7 2 0
  label_103: {
    Instruction i = 0x010001e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1980]	RETURN   	7 0
  label_104: {
    Instruction i = 0x000001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1980]	JMP      	0 21	; to 127
  label_105: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1981]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_106: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1981]	MOVE     	8 5
  label_107: {
    Instruction i = 0x02800200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1981]	GETUPVAL 	9 7	; something_symbol
  label_108: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1981]	CALL     	7 3 2
  label_109: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1981]	TEST     	7 0
  label_110: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_112;
    }
  }
  
  // [1981]	JMP      	0 15	; to 127
  label_111: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1981]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_112: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1981]	GETTABLE 	8 3 -2	; 3
  label_113: {
    Instruction i = 0x01c04207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1981]	GETUPVAL 	9 12	; theThing_symbol
  label_114: {
    Instruction i = 0x06000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1981]	CALL     	7 3 2
  label_115: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1981]	TEST     	7 0
  label_116: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_118;
    }
  }
  
  // [1981]	JMP      	0 9	; to 127
  label_117: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_127;
  }
  
  // [1982]	MOVE     	7 2
  label_118: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1982]	LOADK    	8 -8	; ":>"
  label_119: {
    Instruction i = 0x0001c201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1982]	GETUPVAL 	9 8	; print_sys_name
  label_120: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1982]	GETTABLE 	10 4 -2	; 3
  label_121: {
    Instruction i = 0x02404287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1982]	LOADK    	11 -5	; "inner"
  label_122: {
    Instruction i = 0x000102c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1982]	CALL     	9 3 2
  label_123: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1982]	CONCAT   	8 8 9
  label_124: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1982]	TAILCALL 	7 2 0
  label_125: {
    Instruction i = 0x010001e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1982]	RETURN   	7 0
  label_126: {
    Instruction i = 0x000001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1985]	GETUPVAL 	5 3	; maybe_list_to_jsArray
  label_127: {
    Instruction i = 0x01800145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1985]	GETTABLE 	6 3 -2	; 3
  label_128: {
    Instruction i = 0x01c04187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1985]	CALL     	5 2 2
  label_129: {
    Instruction i = 0x01008164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1986]	GETUPVAL 	6 4	; jsbool_no_force_equal_p
  label_130: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1986]	GETTABLE 	7 3 -4	; 2
  label_131: {
    Instruction i = 0x01c0c1c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1986]	GETUPVAL 	8 6	; function_symbol
  label_132: {
    Instruction i = 0x03000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1986]	CALL     	6 3 2
  label_133: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1986]	TEST     	6 0
  label_134: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_136;
    }
  }
  
  // [1986]	JMP      	0 20	; to 156
  label_135: {
    Instruction i = 0x8004c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_156;
  }
  
  // [1986]	EQ       	1 5 -1	; - false
  label_136: {
    Instruction i = 0x02c0005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_138;
    }
  }
  
  // [1986]	JMP      	0 18	; to 156
  label_137: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_156;
  }
  
  // [1986]	LEN      	6 5
  label_138: {
    Instruction i = 0x0280019c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1986]	EQ       	0 6 -4	; - 2
  label_139: {
    Instruction i = 0x0340c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_141;
    }
  }
  
  // [1986]	JMP      	0 15	; to 156
  label_140: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_156;
  }
  
  // [1986]	GETUPVAL 	6 4	; jsbool_no_force_equal_p
  label_141: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1986]	GETTABLE 	7 5 -3	; 1
  label_142: {
    Instruction i = 0x02c081c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1986]	GETUPVAL 	8 13	; isOrNot_symbol
  label_143: {
    Instruction i = 0x06800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1986]	CALL     	6 3 2
  label_144: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1986]	TEST     	6 0
  label_145: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_147;
    }
  }
  
  // [1986]	JMP      	0 9	; to 156
  label_146: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_156;
  }
  
  // [1987]	MOVE     	6 2
  label_147: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1987]	GETUPVAL 	7 8	; print_sys_name
  label_148: {
    Instruction i = 0x040001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1987]	GETTABLE 	8 5 -4	; 2
  label_149: {
    Instruction i = 0x02c0c207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1987]	LOADK    	9 -5	; "inner"
  label_150: {
    Instruction i = 0x00010241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1987]	CALL     	7 3 2
  label_151: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1987]	LOADK    	8 -9	; "?"
  label_152: {
    Instruction i = 0x00020201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1987]	CONCAT   	7 7 8
  label_153: {
    Instruction i = 0x038201dd;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1987]	TAILCALL 	6 2 0
  label_154: {
    Instruction i = 0x010001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1987]	RETURN   	6 0
  label_155: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1989]	EQ       	1 4 -1	; - false
  label_156: {
    Instruction i = 0x0240005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_158;
    }
  }
  
  // [1989]	JMP      	0 44	; to 202
  label_157: {
    Instruction i = 0x800ac01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1989]	LEN      	6 4
  label_158: {
    Instruction i = 0x0200019c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1989]	EQ       	0 6 -4	; - 2
  label_159: {
    Instruction i = 0x0340c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_161;
    }
  }
  
  // [1989]	JMP      	0 41	; to 202
  label_160: {
    Instruction i = 0x800a001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1989]	GETUPVAL 	6 4	; jsbool_no_force_equal_p
  label_161: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1989]	GETTABLE 	7 3 -2	; 3
  label_162: {
    Instruction i = 0x01c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1989]	GETUPVAL 	8 12	; theThing_symbol
  label_163: {
    Instruction i = 0x06000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1989]	CALL     	6 3 2
  label_164: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1989]	TEST     	6 0
  label_165: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_167;
    }
  }
  
  // [1989]	JMP      	0 35	; to 202
  label_166: {
    Instruction i = 0x8008801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1989]	GETUPVAL 	6 4	; jsbool_no_force_equal_p
  label_167: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1989]	GETTABLE 	7 4 -3	; 1
  label_168: {
    Instruction i = 0x024081c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1989]	GETUPVAL 	8 14	; form_symbol
  label_169: {
    Instruction i = 0x07000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1989]	CALL     	6 3 2
  label_170: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1989]	TEST     	6 0
  label_171: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_173;
    }
  }
  
  // [1989]	JMP      	0 29	; to 202
  label_172: {
    Instruction i = 0x8007001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1990]	GETUPVAL 	6 3	; maybe_list_to_jsArray
  label_173: {
    Instruction i = 0x01800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1990]	GETTABLE 	7 4 -4	; 2
  label_174: {
    Instruction i = 0x0240c1c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1990]	CALL     	6 2 2
  label_175: {
    Instruction i = 0x010081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1991]	EQ       	1 6 -1	; - false
  label_176: {
    Instruction i = 0x0340005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_178;
    }
  }
  
  // [1991]	JMP      	0 24	; to 202
  label_177: {
    Instruction i = 0x8005c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1991]	LEN      	7 6
  label_178: {
    Instruction i = 0x030001dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1991]	EQ       	0 7 -2	; - 3
  label_179: {
    Instruction i = 0x03c0401f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_181;
    }
  }
  
  // [1991]	JMP      	0 21	; to 202
  label_180: {
    Instruction i = 0x8005001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1991]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_181: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1991]	GETTABLE 	8 6 -3	; 1
  label_182: {
    Instruction i = 0x03408207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1991]	GETUPVAL 	9 6	; function_symbol
  label_183: {
    Instruction i = 0x03000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1991]	CALL     	7 3 2
  label_184: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1991]	TEST     	7 0
  label_185: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_187;
    }
  }
  
  // [1991]	JMP      	0 15	; to 202
  label_186: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1991]	GETUPVAL 	7 4	; jsbool_no_force_equal_p
  label_187: {
    Instruction i = 0x020001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1991]	GETTABLE 	8 6 -4	; 2
  label_188: {
    Instruction i = 0x0340c207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1991]	GETUPVAL 	9 7	; something_symbol
  label_189: {
    Instruction i = 0x03800245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1991]	CALL     	7 3 2
  label_190: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1991]	TEST     	7 0
  label_191: {
    Instruction i = 0x000001e2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_193;
    }
  }
  
  // [1991]	JMP      	0 9	; to 202
  label_192: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_202;
  }
  
  // [1992]	MOVE     	7 2
  label_193: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1992]	LOADK    	8 -10	; ":&>"
  label_194: {
    Instruction i = 0x00024201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1992]	GETUPVAL 	9 8	; print_sys_name
  label_195: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1992]	GETTABLE 	10 6 -2	; 3
  label_196: {
    Instruction i = 0x03404287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1992]	LOADK    	11 -5	; "inner"
  label_197: {
    Instruction i = 0x000102c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1992]	CALL     	9 3 2
  label_198: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1992]	CONCAT   	8 8 9
  label_199: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1992]	TAILCALL 	7 2 0
  label_200: {
    Instruction i = 0x010001e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1992]	RETURN   	7 0
  label_201: {
    Instruction i = 0x000001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1995]	GETUPVAL 	6 4	; jsbool_no_force_equal_p
  label_202: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1995]	GETTABLE 	7 3 -2	; 3
  label_203: {
    Instruction i = 0x01c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1995]	GETUPVAL 	8 7	; something_symbol
  label_204: {
    Instruction i = 0x03800205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1995]	CALL     	6 3 2
  label_205: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1995]	TEST     	6 0
  label_206: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_208;
    }
  }
  
  // [1995]	JMP      	0 3	; to 211
  label_207: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_211;
  }
  
  // [1995]	LOADK    	6 -11	; "_"
  label_208: {
    Instruction i = 0x00028181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1995]	TEST     	6 1
  label_209: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_211;
    }
  }
  
  // [1995]	JMP      	0 13	; to 224
  label_210: {
    Instruction i = 0x8003001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_224;
  }
  
  // [1995]	GETUPVAL 	6 4	; jsbool_no_force_equal_p
  label_211: {
    Instruction i = 0x02000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1995]	GETTABLE 	7 3 -2	; 3
  label_212: {
    Instruction i = 0x01c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1995]	GETUPVAL 	8 12	; theThing_symbol
  label_213: {
    Instruction i = 0x06000205;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1995]	CALL     	6 3 2
  label_214: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1995]	TEST     	6 0
  label_215: {
    Instruction i = 0x000001a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_217;
    }
  }
  
  // [1995]	JMP      	0 3	; to 220
  label_216: {
    Instruction i = 0x8000801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_220;
  }
  
  // [1995]	LOADK    	6 -12	; ""
  label_217: {
    Instruction i = 0x0002c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1995]	TEST     	6 1
  label_218: {
    Instruction i = 0x000041a2;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_220;
    }
  }
  
  // [1995]	JMP      	0 4	; to 224
  label_219: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_224;
  }
  
  // [1995]	GETUPVAL 	6 8	; print_sys_name
  label_220: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1995]	GETTABLE 	7 3 -2	; 3
  label_221: {
    Instruction i = 0x01c041c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1995]	LOADK    	8 -5	; "inner"
  label_222: {
    Instruction i = 0x00010201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1995]	CALL     	6 3 2
  label_223: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1996]	MOVE     	7 2
  label_224: {
    Instruction i = 0x010001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1996]	MOVE     	8 6
  label_225: {
    Instruction i = 0x03000200;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1996]	LOADK    	9 -13	; ":"
  label_226: {
    Instruction i = 0x00030241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1996]	CONCAT   	8 8 9
  label_227: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1996]	GETUPVAL 	9 8	; print_sys_name
  label_228: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1996]	GETTABLE 	10 3 -4	; 2
  label_229: {
    Instruction i = 0x01c0c287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1996]	LOADK    	11 -5	; "inner"
  label_230: {
    Instruction i = 0x000102c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1996]	CALL     	9 3 2
  label_231: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1996]	CONCAT   	8 8 9
  label_232: {
    Instruction i = 0x0402421d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1996]	TAILCALL 	7 2 0
  label_233: {
    Instruction i = 0x010001e5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1996]	RETURN   	7 0
  label_234: {
    Instruction i = 0x000001e6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1996]	JMP      	0 112	; to 348
  label_235: {
    Instruction i = 0x801bc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [1997]	EQ       	1 3 -1	; - false
  label_236: {
    Instruction i = 0x01c0005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_238;
    }
  }
  
  // [1997]	JMP      	0 110	; to 348
  label_237: {
    Instruction i = 0x801b401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [1997]	LEN      	4 3
  label_238: {
    Instruction i = 0x0180011c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [1997]	EQ       	0 4 -4	; - 2
  label_239: {
    Instruction i = 0x0240c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_241;
    }
  }
  
  // [1997]	JMP      	0 107	; to 348
  label_240: {
    Instruction i = 0x801a801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [1998]	GETUPVAL 	4 4	; jsbool_no_force_equal_p
  label_241: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1998]	GETTABLE 	5 3 -3	; 1
  label_242: {
    Instruction i = 0x01c08147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1998]	GETUPVAL 	6 14	; form_symbol
  label_243: {
    Instruction i = 0x07000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1998]	CALL     	4 3 2
  label_244: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1998]	TEST     	4 0
  label_245: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_247;
    }
  }
  
  // [1998]	JMP      	0 33	; to 280
  label_246: {
    Instruction i = 0x8008001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_280;
  }
  
  // [1999]	GETUPVAL 	4 3	; maybe_list_to_jsArray
  label_247: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1999]	GETTABLE 	5 3 -4	; 2
  label_248: {
    Instruction i = 0x01c0c147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [1999]	CALL     	4 2 2
  label_249: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2000]	EQ       	1 4 -1	; - false
  label_250: {
    Instruction i = 0x0240005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_252;
    }
  }
  
  // [2000]	JMP      	0 18	; to 270
  label_251: {
    Instruction i = 0x8004401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_270;
  }
  
  // [2000]	LEN      	5 4
  label_252: {
    Instruction i = 0x0200015c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [2000]	EQ       	0 5 -4	; - 2
  label_253: {
    Instruction i = 0x02c0c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_255;
    }
  }
  
  // [2000]	JMP      	0 15	; to 270
  label_254: {
    Instruction i = 0x8003801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_270;
  }
  
  // [2000]	GETUPVAL 	5 4	; jsbool_no_force_equal_p
  label_255: {
    Instruction i = 0x02000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2000]	GETTABLE 	6 4 -3	; 1
  label_256: {
    Instruction i = 0x02408187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2000]	GETUPVAL 	7 15	; system_symbol
  label_257: {
    Instruction i = 0x078001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2000]	CALL     	5 3 2
  label_258: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2000]	TEST     	5 0
  label_259: {
    Instruction i = 0x00000162;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_261;
    }
  }
  
  // [2000]	JMP      	0 9	; to 270
  label_260: {
    Instruction i = 0x8002001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_270;
  }
  
  // [2001]	MOVE     	5 2
  label_261: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2001]	LOADK    	6 -14	; "&+"
  label_262: {
    Instruction i = 0x00034181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2001]	GETUPVAL 	7 8	; print_sys_name
  label_263: {
    Instruction i = 0x040001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2001]	GETTABLE 	8 4 -4	; 2
  label_264: {
    Instruction i = 0x0240c207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2001]	LOADK    	9 -5	; "inner"
  label_265: {
    Instruction i = 0x00010241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2001]	CALL     	7 3 2
  label_266: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2001]	CONCAT   	6 6 7
  label_267: {
    Instruction i = 0x0301c19d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2001]	TAILCALL 	5 2 0
  label_268: {
    Instruction i = 0x01000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2001]	RETURN   	5 0
  label_269: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2003]	MOVE     	5 2
  label_270: {
    Instruction i = 0x01000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2003]	LOADK    	6 -15	; "&"
  label_271: {
    Instruction i = 0x00038181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2003]	GETUPVAL 	7 8	; print_sys_name
  label_272: {
    Instruction i = 0x040001c5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2003]	GETTABLE 	8 3 -4	; 2
  label_273: {
    Instruction i = 0x01c0c207;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2003]	LOADK    	9 -5	; "inner"
  label_274: {
    Instruction i = 0x00010241;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2003]	CALL     	7 3 2
  label_275: {
    Instruction i = 0x018081e4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2003]	CONCAT   	6 6 7
  label_276: {
    Instruction i = 0x0301c19d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2003]	TAILCALL 	5 2 0
  label_277: {
    Instruction i = 0x01000165;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2003]	RETURN   	5 0
  label_278: {
    Instruction i = 0x00000166;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2003]	JMP      	0 68	; to 348
  label_279: {
    Instruction i = 0x8010c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [2004]	GETUPVAL 	4 4	; jsbool_no_force_equal_p
  label_280: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2004]	GETTABLE 	5 3 -3	; 1
  label_281: {
    Instruction i = 0x01c08147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2004]	GETUPVAL 	6 13	; isOrNot_symbol
  label_282: {
    Instruction i = 0x06800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2004]	CALL     	4 3 2
  label_283: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2004]	TEST     	4 0
  label_284: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_286;
    }
  }
  
  // [2004]	JMP      	0 10	; to 296
  label_285: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_296;
  }
  
  // [2005]	MOVE     	4 2
  label_286: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2005]	GETUPVAL 	5 8	; print_sys_name
  label_287: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2005]	GETTABLE 	6 3 -4	; 2
  label_288: {
    Instruction i = 0x01c0c187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2005]	LOADK    	7 -5	; "inner"
  label_289: {
    Instruction i = 0x000101c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2005]	CALL     	5 3 2
  label_290: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2005]	LOADK    	6 -16	; "~"
  label_291: {
    Instruction i = 0x0003c181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2005]	CONCAT   	5 5 6
  label_292: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2005]	TAILCALL 	4 2 0
  label_293: {
    Instruction i = 0x01000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2005]	RETURN   	4 0
  label_294: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2005]	JMP      	0 52	; to 348
  label_295: {
    Instruction i = 0x800cc01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [2006]	GETUPVAL 	4 4	; jsbool_no_force_equal_p
  label_296: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2006]	GETTABLE 	5 3 -3	; 1
  label_297: {
    Instruction i = 0x01c08147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2006]	GETUPVAL 	6 15	; system_symbol
  label_298: {
    Instruction i = 0x07800185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2006]	CALL     	4 3 2
  label_299: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2006]	TEST     	4 0
  label_300: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_302;
    }
  }
  
  // [2006]	JMP      	0 10	; to 312
  label_301: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_312;
  }
  
  // [2007]	MOVE     	4 2
  label_302: {
    Instruction i = 0x01000100;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2007]	LOADK    	5 -17	; "+"
  label_303: {
    Instruction i = 0x00040141;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2007]	GETUPVAL 	6 8	; print_sys_name
  label_304: {
    Instruction i = 0x04000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2007]	GETTABLE 	7 3 -4	; 2
  label_305: {
    Instruction i = 0x01c0c1c7;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2007]	LOADK    	8 -5	; "inner"
  label_306: {
    Instruction i = 0x00010201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2007]	CALL     	6 3 2
  label_307: {
    Instruction i = 0x018081a4;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2007]	CONCAT   	5 5 6
  label_308: {
    Instruction i = 0x0281815d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2007]	TAILCALL 	4 2 0
  label_309: {
    Instruction i = 0x01000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2007]	RETURN   	4 0
  label_310: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2007]	JMP      	0 36	; to 348
  label_311: {
    Instruction i = 0x8008c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [2008]	GETUPVAL 	4 4	; jsbool_no_force_equal_p
  label_312: {
    Instruction i = 0x02000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2008]	GETTABLE 	5 3 -3	; 1
  label_313: {
    Instruction i = 0x01c08147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2008]	GETUPVAL 	6 16	; sub_symbol
  label_314: {
    Instruction i = 0x08000185;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2008]	CALL     	4 3 2
  label_315: {
    Instruction i = 0x01808124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2008]	TEST     	4 0
  label_316: {
    Instruction i = 0x00000122;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    if (GETARG_C(i) ? l_isfalse(ra) : !l_isfalse(ra)) {
      ci->u.l.savedpc++;

      goto label_318;
    }
  }
  
  // [2008]	JMP      	0 30	; to 348
  label_317: {
    Instruction i = 0x8007401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [2009]	GETUPVAL 	4 3	; maybe_list_to_jsArray
  label_318: {
    Instruction i = 0x01800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2009]	GETTABLE 	5 3 -4	; 2
  label_319: {
    Instruction i = 0x01c0c147;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2009]	CALL     	4 2 2
  label_320: {
    Instruction i = 0x01008124;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2010]	EQ       	1 4 -1	; - false
  label_321: {
    Instruction i = 0x0240005f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_323;
    }
  }
  
  // [2010]	JMP      	0 25	; to 348
  label_322: {
    Instruction i = 0x8006001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [2010]	LEN      	5 4
  label_323: {
    Instruction i = 0x0200015c;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [2010]	LT       	0 -3 5	; 1 -
  label_324: {
    Instruction i = 0x81014020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_326;
    }
  }
  
  // [2010]	JMP      	0 22	; to 348
  label_325: {
    Instruction i = 0x8005401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_348;
  }
  
  // [2011]	GETUPVAL 	5 8	; print_sys_name
  label_326: {
    Instruction i = 0x04000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2011]	GETTABLE 	6 4 -3	; 1
  label_327: {
    Instruction i = 0x02408187;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2011]	LOADK    	7 -5	; "inner"
  label_328: {
    Instruction i = 0x000101c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2011]	CALL     	5 3 2
  label_329: {
    Instruction i = 0x01808164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2013]	LOADK    	6 -3	; 1
  label_330: {
    Instruction i = 0x00008181;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2014]	LEN      	7 4
  label_331: {
    Instruction i = 0x020001dc;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    Protect(luaV_objlen(L, ra, RB(i)));
  }
  
  // [2014]	LT       	0 6 7
  label_332: {
    Instruction i = 0x0301c020;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int cmp;
    Protect(cmp = luaV_lessthan(L, RKB(i), RKC(i)));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_334;
    }
  }
  
  // [2014]	JMP      	0 10	; to 344
  label_333: {
    Instruction i = 0x8002401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_344;
  }
  
  // [2015]	MOVE     	7 5
  label_334: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2015]	LOADK    	8 -18	; "/"
  label_335: {
    Instruction i = 0x00044201;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2015]	GETUPVAL 	9 8	; print_sys_name
  label_336: {
    Instruction i = 0x04000245;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2015]	ADD      	10 6 -3	; - 1
  label_337: {
    Instruction i = 0x0340828d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [2015]	GETTABLE 	10 4 10
  label_338: {
    Instruction i = 0x02028287;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    StkId rb = RB(i);
    TValue *rc = RKC(i);
    gettableProtected(L, rb, rc, ra);
  }
  
  // [2015]	LOADK    	11 -5	; "inner"
  label_339: {
    Instruction i = 0x000102c1;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [2015]	CALL     	9 3 2
  label_340: {
    Instruction i = 0x01808264;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2015]	CONCAT   	5 7 9
  label_341: {
    Instruction i = 0x0382415d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [2016]	ADD      	6 6 -3	; - 1
  label_342: {
    Instruction i = 0x0340818d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    lua_Number nb; lua_Number nc;
    if (ttisinteger(rb) && ttisinteger(rc)) {
      lua_Integer ib = ivalue(rb); lua_Integer ic = ivalue(rc);
      setivalue(ra, intop(+, ib, ic));
    }
    else if (tonumber(rb, &nb) && tonumber(rc, &nc)) {
      setfltvalue(ra, luai_numadd(L, nb, nc));
    }
    else { Protect(luaT_trybinTM(L, rb, rc, ra, TM_ADD)); }
  }
  
  // [2016]	JMP      	0 -13	; to 331
  label_343: {
    Instruction i = 0x7ffc801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_331;
  }
  
  // [2019]	MOVE     	6 2
  label_344: {
    Instruction i = 0x01000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2019]	MOVE     	7 5
  label_345: {
    Instruction i = 0x028001c0;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2019]	TAILCALL 	6 2 0
  label_346: {
    Instruction i = 0x010001a5;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2019]	RETURN   	6 0
  label_347: {
    Instruction i = 0x000001a6;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2023]	EQ       	0 1 -5	; - "inner"
  label_348: {
    Instruction i = 0x00c1001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_350;
    }
  }
  
  // [2023]	JMP      	0 5	; to 355
  label_349: {
    Instruction i = 0x8001001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_355;
  }
  
  // [2024]	GETUPVAL 	4 17	; simple_print
  label_350: {
    Instruction i = 0x08800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2024]	MOVE     	5 0
  label_351: {
    Instruction i = 0x00000140;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2024]	TAILCALL 	4 2 0
  label_352: {
    Instruction i = 0x01000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2024]	RETURN   	4 0
  label_353: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2024]	JMP      	0 8	; to 363
  label_354: {
    Instruction i = 0x8001c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_363;
  }
  
  // [2025]	EQ       	0 1 -19	; - "top"
  label_355: {
    Instruction i = 0x00c4801f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_357;
    }
  }
  
  // [2025]	JMP      	0 6	; to 363
  label_356: {
    Instruction i = 0x8001401e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_363;
  }
  
  // [2026]	GETUPVAL 	4 17	; simple_print
  label_357: {
    Instruction i = 0x08800105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2026]	GETUPVAL 	5 18	; systemName_make
  label_358: {
    Instruction i = 0x09000145;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2026]	MOVE     	6 0
  label_359: {
    Instruction i = 0x00000180;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [2026]	CALL     	5 2 0
  label_360: {
    Instruction i = 0x01000164;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int nresults = GETARG_C(i) - 1;
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    if (luaD_precall(L, ra, nresults)) {  /* C function? */
      if (nresults >= 0)
        L->top = ci->top;  /* adjust results */
      Protect((void)0);  /* update 'base' */
    } else {  /* Lua function */
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2026]	TAILCALL 	4 0 0
  label_361: {
    Instruction i = 0x00000125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2026]	RETURN   	4 0
  label_362: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2028]	GETUPVAL 	4 2	; ERROR
  label_363: {
    Instruction i = 0x01000105;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [2028]	TAILCALL 	4 1 0
  label_364: {
    Instruction i = 0x00800125;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [2028]	RETURN   	4 0
  label_365: {
    Instruction i = 0x00000126;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [2029]	RETURN   	0 1
  label_366: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

// source = @../lua/lang.lua
// linedefined = 1963
// lastlinedefined = 1970
// what = Lua
static int zz_magic_function_161 (lua_State *L, LClosure *cl)
{
  CallInfo *ci = L->ci;
  TValue *k = cl->p->k;
  StkId base = ci->u.l.base;
  
  // Avoid warnings if the function has few opcodes:
  (void) ci;
  (void) k;
  (void) base;
  
  // [1964]	GETUPVAL 	1 0	; where
  label_0: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1964]	EQ       	0 1 -1	; - "inner"
  label_1: {
    Instruction i = 0x00c0001f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_3;
    }
  }
  
  // [1964]	JMP      	0 7	; to 10
  label_2: {
    Instruction i = 0x8001801e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_10;
  }
  
  // [1965]	LOADK    	1 -2	; "["
  label_3: {
    Instruction i = 0x00004041;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1965]	MOVE     	2 0
  label_4: {
    Instruction i = 0x00000080;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    setobjs2s(L, ra, RB(i));
  }
  
  // [1965]	CONCAT   	1 1 2
  label_5: {
    Instruction i = 0x0080805d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1965]	LOADK    	2 -3	; "]"
  label_6: {
    Instruction i = 0x00008081;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    TValue *rb = k + GETARG_Bx(i);
    setobj2s(L, ra, rb);
  }
  
  // [1965]	CONCAT   	1 1 2
  label_7: {
    Instruction i = 0x0080805d;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    int c = GETARG_C(i);
    StkId rb;
    L->top = base + c + 1;  /* mark the end of concat operands */
    Protect(luaV_concat(L, c - b + 1));
    ra = RA(i);  /* 'luaV_concat' may invoke TMs and move the stack */
    rb = base + b;
    setobjs2s(L, ra, rb);
    checkGC(L, (ra >= rb ? ra + 1 : rb));
    L->top = ci->top;  /* restore top */
  }
  
  // [1965]	RETURN   	1 2
  label_8: {
    Instruction i = 0x01000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1965]	JMP      	0 4	; to 14
  label_9: {
    Instruction i = 0x8000c01e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_14;
  }
  
  // [1966]	GETUPVAL 	1 0	; where
  label_10: {
    Instruction i = 0x00000045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1966]	EQ       	0 1 -4	; - "top"
  label_11: {
    Instruction i = 0x00c0c01f;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    TValue *rb = RKB(i);
    TValue *rc = RKC(i);
    int cmp;
    Protect(cmp = luaV_equalobj(L, rb, rc));
    if (cmp != GETARG_A(i)) {
      ci->u.l.savedpc++;

      goto label_13;
    }
  }
  
  // [1966]	JMP      	0 1	; to 14
  label_12: {
    Instruction i = 0x8000001e;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    (void) ra;
    int a = GETARG_A(i);
    if (a != 0) luaF_close(L, ci->u.l.base + a - 1);
    ci->u.l.savedpc += GETARG_sBx(i);
    goto label_14;
  }
  
  // [1967]	RETURN   	0 2
  label_13: {
    Instruction i = 0x01000026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1969]	GETUPVAL 	1 1	; ERROR
  label_14: {
    Instruction i = 0x00800045;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    setobj2s(L, ra, cl->upvals[b]->v);
  }
  
  // [1969]	TAILCALL 	1 1 0
  label_15: {
    Instruction i = 0x00800065;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (b != 0) L->top = ra+b;  /* else previous instruction set top */
    lua_assert(GETARG_C(i) - 1 == LUA_MULTRET);
    if (luaD_precall(L, ra, LUA_MULTRET)) {  /* C function? */
      Protect((void)0);  /* update 'base' */
    }
    else {
      luaV_execute(L);
      Protect((void)0);  /* update 'base' */
    }
  }
  
  // [1969]	RETURN   	1 0
  label_16: {
    Instruction i = 0x00000066;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
  // [1970]	RETURN   	0 1
  label_17: {
    Instruction i = 0x00800026;
    ci->u.l.savedpc++;
    if (L->hookmask & (LUA_MASKLINE | LUA_MASKCOUNT))
      Protect(luaG_traceexec(L));
    StkId ra = RA(i); /* WARNING: any stack reallocation invalidates 'ra' */
    lua_assert(base == ci->u.l.base);
    lua_assert(base <= L->top && L->top < L->stack + L->stacksize);
    
    int b = GETARG_B(i);
    if (cl->p->sizep > 0) luaF_close(L, base);
    int ret = (b != 0 ? b - 1 : cast_int(L->top - ra));
    luaD_poscall(L, ci, ra, ret);
    return ret;
  }
  
}

ZZ_MAGIC_FUNC zz_magic_functions[162] = {
  zz_magic_function_0,
  zz_magic_function_1,
  zz_magic_function_2,
  zz_magic_function_3,
  zz_magic_function_4,
  zz_magic_function_5,
  zz_magic_function_6,
  zz_magic_function_7,
  zz_magic_function_8,
  zz_magic_function_9,
  zz_magic_function_10,
  zz_magic_function_11,
  zz_magic_function_12,
  zz_magic_function_13,
  zz_magic_function_14,
  zz_magic_function_15,
  zz_magic_function_16,
  zz_magic_function_17,
  zz_magic_function_18,
  zz_magic_function_19,
  zz_magic_function_20,
  zz_magic_function_21,
  zz_magic_function_22,
  zz_magic_function_23,
  zz_magic_function_24,
  zz_magic_function_25,
  zz_magic_function_26,
  zz_magic_function_27,
  zz_magic_function_28,
  zz_magic_function_29,
  zz_magic_function_30,
  zz_magic_function_31,
  zz_magic_function_32,
  zz_magic_function_33,
  zz_magic_function_34,
  zz_magic_function_35,
  zz_magic_function_36,
  zz_magic_function_37,
  zz_magic_function_38,
  zz_magic_function_39,
  zz_magic_function_40,
  zz_magic_function_41,
  zz_magic_function_42,
  zz_magic_function_43,
  zz_magic_function_44,
  zz_magic_function_45,
  zz_magic_function_46,
  zz_magic_function_47,
  zz_magic_function_48,
  zz_magic_function_49,
  zz_magic_function_50,
  zz_magic_function_51,
  zz_magic_function_52,
  zz_magic_function_53,
  zz_magic_function_54,
  zz_magic_function_55,
  zz_magic_function_56,
  zz_magic_function_57,
  zz_magic_function_58,
  zz_magic_function_59,
  zz_magic_function_60,
  zz_magic_function_61,
  zz_magic_function_62,
  zz_magic_function_63,
  zz_magic_function_64,
  zz_magic_function_65,
  zz_magic_function_66,
  zz_magic_function_67,
  zz_magic_function_68,
  zz_magic_function_69,
  zz_magic_function_70,
  zz_magic_function_71,
  zz_magic_function_72,
  zz_magic_function_73,
  zz_magic_function_74,
  zz_magic_function_75,
  zz_magic_function_76,
  zz_magic_function_77,
  zz_magic_function_78,
  zz_magic_function_79,
  zz_magic_function_80,
  zz_magic_function_81,
  zz_magic_function_82,
  zz_magic_function_83,
  zz_magic_function_84,
  zz_magic_function_85,
  zz_magic_function_86,
